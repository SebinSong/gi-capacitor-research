import {
  registerPlugin
} from "./chunk-IOJ2MM6V-cached.js";
import {
  THEME_DARK,
  THEME_LIGHT,
  create_group_default,
  join_group_default
} from "./chunk-NSN4VF4M-cached.js";
import {
  ListItem_default
} from "./chunk-LUECJCV2-cached.js";
import {
  GLOBAL_DASHBOARD_SETTINGS
} from "./chunk-KOCLL7EV-cached.js";
import {
  RenderMessageWithMarkdown_default
} from "./chunk-ZWWQHF5P-cached.js";
import {
  Badge_default,
  Toggle_default
} from "./chunk-EUGZI4EZ-cached.js";
import {
  colorsManipulation_default,
  colors_default
} from "./chunk-VV2DISHU-cached.js";
import {
  PAYMENT_COMPLETED,
  PAYMENT_NOT_RECEIVED
} from "./chunk-5AEIP7HX-cached.js";
import {
  adjustedDistribution,
  unadjustedDistribution
} from "./chunk-5JVY5DQX-cached.js";
import {
  CURVE25519XSALSA20POLY1305,
  EDWARDS25519SHA512BATCH,
  SPMessage,
  decrypt,
  deriveKeyFromPassword,
  encrypt,
  generateSalt,
  getSubscriptionId,
  keyId,
  keygen,
  require_nacl_fast,
  require_scrypt_async,
  serializeKey
} from "./chunk-NVV2HOKV-cached.js";
import {
  CONTRACTS_MODIFIED,
  CONTRACTS_MODIFIED_READY,
  CONTRACT_IS_SYNCING,
  CONTRACT_REGISTERED,
  EVENT_HANDLED,
  EVENT_HANDLED_READY
} from "./chunk-76TQH32R-cached.js";
import {
  INVITE_STATUS
} from "./chunk-F2DYOYGG-cached.js";
import {
  isFunction,
  objectOf,
  string
} from "./chunk-KTNZHYGC-cached.js";
import {
  imageDataURItoBlob
} from "./chunk-DXIHOQP2-cached.js";
import {
  DEVICE_SETTINGS,
  KV_KEYS,
  KV_LOAD_STATUS,
  MAX_LOG_ENTRIES
} from "./chunk-LOAVQ5PN-cached.js";
import {
  broken_link_default
} from "./chunk-WFO36DTC-cached.js";
import {
  LoginForm_default
} from "./chunk-XCZJSM7Z-cached.js";
import {
  NotificationList_default
} from "./chunk-DNBNZGVL-cached.js";
import "./chunk-HRFGMP2Q-cached.js";
import {
  createPaymentInfo,
  paymentHashesFromPaymentPeriod
} from "./chunk-A3KNU2XZ-cached.js";
import {
  DAYS_MILLIS,
  HOURS_MILLIS,
  MINS_MILLIS,
  MONTHS_MILLIS,
  addTimeToDate,
  compareISOTimestamps,
  dateFromPeriodStamp,
  dateIsWithinPeriod,
  dateToPeriodStamp,
  periodStampsForDate
} from "./chunk-V3SQGGAF-cached.js";
import {
  SignupForm_default
} from "./chunk-OLAVKC5T-cached.js";
import {
  setupNativeNotificationsListeners
} from "./chunk-IR2UUO3D-cached.js";
import {
  allowedUrls_default
} from "./chunk-W7XZLSPA-cached.js";
import {
  require_buffer
} from "./chunk-SC5GGDZP-cached.js";
import {
  init_vue_esm,
  vue_esm_default
} from "./chunk-K33NK7LD-cached.js";
import "./chunk-GLMIE3CJ-cached.js";
import {
  ProfileCard_default
} from "./chunk-GDHKI2YN-cached.js";
import "./chunk-3T5W4UPP-cached.js";
import {
  Tooltip_default
} from "./chunk-DMA6TQP3-cached.js";
import {
  DESKTOP
} from "./chunk-532VGDFI-cached.js";
import {
  AvatarUser_default
} from "./chunk-K4WYPR2K-cached.js";
import {
  ModalBaseTemplate_default
} from "./chunk-PVWMN5B2-cached.js";
import {
  Avatar_default
} from "./chunk-OZHDGR4V-cached.js";
import {
  fetchNews,
  ignoreWhenNavigationCancelled,
  showNavMixin,
  vue_router_esm_default,
  withGroupCurrency
} from "./chunk-OBUPKMDO-cached.js";
import {
  currencies_default
} from "./chunk-AS6YVRB6-cached.js";
import {
  Secret,
  deserializer,
  serializer
} from "./chunk-DRW7AMFK-cached.js";
import {
  ButtonSubmit_default
} from "./chunk-YUM5UY76-cached.js";
import "./chunk-LO4V4OP4-cached.js";
import {
  BannerSimple_default,
  TransitionExpand_default
} from "./chunk-VVR7NWXN-cached.js";
import {
  cloneDeep,
  debounce,
  merge,
  omit,
  randomFromArray,
  randomIntFromRange,
  union
} from "./chunk-MTWMQLQH-cached.js";
import "./chunk-AMO3YQCO-cached.js";
import {
  CHATROOM_PRIVACY_LEVEL,
  INVITE_INITIAL_CREATOR,
  MAX_GROUP_MEMBER_COUNT,
  MAX_SAVED_PERIODS,
  MESSAGE_NOTIFY_SETTINGS,
  MESSAGE_RECEIVE,
  MESSAGE_SEND,
  PROFILE_STATUS,
  PROPOSAL_GENERIC
} from "./chunk-UYGYRQRQ-cached.js";
import "./chunk-YH4VCTQW-cached.js";
import "./chunk-OQLS3DKT-cached.js";
import {
  ModalTemplate_default
} from "./chunk-JWCCTDIV-cached.js";
import "./chunk-WNIDE56S-cached.js";
import {
  ModalClose_default
} from "./chunk-LLQHPKRZ-cached.js";
import "./chunk-UHGLFGQW-cached.js";
import {
  ACCEPTED_GROUP,
  CAPTURED_LOGS,
  CLOSE_MODAL,
  CONTRACT_SYNCS_RESET,
  DELETED_CHATROOM,
  ERROR_GROUP_GENERAL_CHATROOM_DOES_NOT_EXIST,
  ERROR_JOINING_CHATROOM,
  JOINED_CHATROOM,
  JOINED_GROUP,
  KV_EVENT,
  LEFT_CHATROOM,
  LEFT_GROUP,
  LOGIN,
  LOGIN_COMPLETE,
  LOGIN_ERROR,
  LOGOUT,
  NAMESPACE_REGISTRATION,
  NEW_CHATROOM_NOTIFICATION_SETTINGS,
  NEW_CHATROOM_SCROLL_POSITION,
  NEW_KV_LOAD_STATUS,
  NEW_LAST_LOGGED_IN,
  NEW_PREFERENCES,
  NEW_UNREAD_MESSAGES,
  NOTIFICATION_EMITTED,
  NOTIFICATION_REMOVED,
  NOTIFICATION_STATUS_LOADED,
  OFFLINE,
  ONLINE,
  OPEN_MODAL,
  PWA_INSTALLABLE,
  RECONNECTING,
  RECONNECTION_FAILED,
  REPLACE_MODAL,
  SERIOUS_ERROR,
  SET_APP_LOGS_FILTER,
  SET_MODAL_QUERIES,
  SWITCH_GROUP,
  THEME_CHANGE
} from "./chunk-4UEGBI3X-cached.js";
import {
  import_vuex_common,
  mapGetters,
  mapMutations,
  mapState
} from "./chunk-J6S33KSG-cached.js";
import {
  ChelErrorResourceGone,
  ChelErrorUnexpectedHttpResponseCode,
  GIErrorUIRuntimeError,
  L,
  LError,
  LTags
} from "./chunk-5ORPBQD5-cached.js";
import {
  esm_default
} from "./chunk-5B2FVEZA-cached.js";
import {
  __commonJS,
  __toESM
} from "./chunk-MKFIN2WI-cached.js";

// node_modules/idle-vue/dist/build.js
var require_build = __commonJS({
  "node_modules/idle-vue/dist/build.js"(exports, module) {
    module.exports = function(t) {
      function e(i) {
        if (n[i]) return n[i].exports;
        var s = n[i] = { i, l: false, exports: {} };
        return t[i].call(s.exports, s, s.exports, e), s.l = true, s.exports;
      }
      var n = {};
      return e.m = t, e.c = n, e.i = function(t2) {
        return t2;
      }, e.d = function(t2, n2, i) {
        e.o(t2, n2) || Object.defineProperty(t2, n2, { configurable: false, enumerable: true, get: i });
      }, e.n = function(t2) {
        var n2 = t2 && t2.__esModule ? function() {
          return t2.default;
        } : function() {
          return t2;
        };
        return e.d(n2, "a", n2), n2;
      }, e.o = function(t2, e2) {
        return Object.prototype.hasOwnProperty.call(t2, e2);
      }, e.p = "/", e(e.s = 5);
    }([function(t, e) {
      function n(t2, e2) {
        var n2 = t2[1] || "", s = t2[3];
        if (!s) return n2;
        if (e2 && "function" == typeof btoa) {
          var r2 = i(s);
          return [n2].concat(s.sources.map(function(t3) {
            return "/*# sourceURL=" + s.sourceRoot + t3 + " */";
          })).concat([r2]).join("\n");
        }
        return [n2].join("\n");
      }
      function i(t2) {
        return "/*# sourceMappingURL=data:application/json;charset=utf-8;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(t2)))) + " */";
      }
      t.exports = function(t2) {
        var e2 = [];
        return e2.toString = function() {
          return this.map(function(e3) {
            var i2 = n(e3, t2);
            return e3[2] ? "@media " + e3[2] + "{" + i2 + "}" : i2;
          }).join("");
        }, e2.i = function(t3, n2) {
          "string" == typeof t3 && (t3 = [[null, t3, ""]]);
          for (var i2 = {}, s = 0; s < this.length; s++) {
            var r2 = this[s][0];
            "number" == typeof r2 && (i2[r2] = true);
          }
          for (s = 0; s < t3.length; s++) {
            var o = t3[s];
            "number" == typeof o[0] && i2[o[0]] || (n2 && !o[2] ? o[2] = n2 : n2 && (o[2] = "(" + o[2] + ") and (" + n2 + ")"), e2.push(o));
          }
        }, e2;
      };
    }, function(t, e) {
      t.exports = function(t2, e2, n, i, s) {
        var r2, o = t2 = t2 || {}, a = typeof t2.default;
        "object" !== a && "function" !== a || (r2 = t2, o = t2.default);
        var u = "function" == typeof o ? o.options : o;
        e2 && (u.render = e2.render, u.staticRenderFns = e2.staticRenderFns), i && (u._scopeId = i);
        var c;
        if (s ? (c = function(t3) {
          t3 = t3 || this.$vnode && this.$vnode.ssrContext || this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext, t3 || "undefined" == typeof __VUE_SSR_CONTEXT__ || (t3 = __VUE_SSR_CONTEXT__), n && n.call(this, t3), t3 && t3._registeredComponents && t3._registeredComponents.add(s);
        }, u._ssrRegister = c) : n && (c = n), c) {
          var d = u.functional, l = d ? u.render : u.beforeCreate;
          d ? u.render = function(t3, e3) {
            return c.call(e3), l(t3, e3);
          } : u.beforeCreate = l ? [].concat(l, c) : [c];
        }
        return { esModule: r2, exports: o, options: u };
      };
    }, function(t, e, n) {
      function i(t2) {
        for (var e2 = 0; e2 < t2.length; e2++) {
          var n2 = t2[e2], i2 = d[n2.id];
          if (i2) {
            i2.refs++;
            for (var s2 = 0; s2 < i2.parts.length; s2++) i2.parts[s2](n2.parts[s2]);
            for (; s2 < n2.parts.length; s2++) i2.parts.push(r2(n2.parts[s2]));
            i2.parts.length > n2.parts.length && (i2.parts.length = n2.parts.length);
          } else {
            for (var o2 = [], s2 = 0; s2 < n2.parts.length; s2++) o2.push(r2(n2.parts[s2]));
            d[n2.id] = { id: n2.id, refs: 1, parts: o2 };
          }
        }
      }
      function s() {
        var t2 = document.createElement("style");
        return t2.type = "text/css", l.appendChild(t2), t2;
      }
      function r2(t2) {
        var e2, n2, i2 = document.querySelector('style[data-vue-ssr-id~="' + t2.id + '"]');
        if (i2) {
          if (h) return v;
          i2.parentNode.removeChild(i2);
        }
        if (A) {
          var r3 = f++;
          i2 = p || (p = s()), e2 = o.bind(null, i2, r3, false), n2 = o.bind(null, i2, r3, true);
        } else i2 = s(), e2 = a.bind(null, i2), n2 = function() {
          i2.parentNode.removeChild(i2);
        };
        return e2(t2), function(i3) {
          if (i3) {
            if (i3.css === t2.css && i3.media === t2.media && i3.sourceMap === t2.sourceMap) return;
            e2(t2 = i3);
          } else n2();
        };
      }
      function o(t2, e2, n2, i2) {
        var s2 = n2 ? "" : i2.css;
        if (t2.styleSheet) t2.styleSheet.cssText = m(e2, s2);
        else {
          var r3 = document.createTextNode(s2), o2 = t2.childNodes;
          o2[e2] && t2.removeChild(o2[e2]), o2.length ? t2.insertBefore(r3, o2[e2]) : t2.appendChild(r3);
        }
      }
      function a(t2, e2) {
        var n2 = e2.css, i2 = e2.media, s2 = e2.sourceMap;
        if (i2 && t2.setAttribute("media", i2), s2 && (n2 += "\n/*# sourceURL=" + s2.sources[0] + " */", n2 += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(s2)))) + " */"), t2.styleSheet) t2.styleSheet.cssText = n2;
        else {
          for (; t2.firstChild; ) t2.removeChild(t2.firstChild);
          t2.appendChild(document.createTextNode(n2));
        }
      }
      var u = "undefined" != typeof document;
      if ("undefined" != typeof DEBUG && DEBUG && !u) throw new Error("vue-style-loader cannot be used in a non-browser environment. Use { target: 'node' } in your Webpack config to indicate a server-rendering environment.");
      var c = n(16), d = {}, l = u && (document.head || document.getElementsByTagName("head")[0]), p = null, f = 0, h = false, v = function() {
      }, A = "undefined" != typeof navigator && /msie [6-9]\b/.test(navigator.userAgent.toLowerCase());
      t.exports = function(t2, e2, n2) {
        h = n2;
        var s2 = c(t2, e2);
        return i(s2), function(e3) {
          for (var n3 = [], r3 = 0; r3 < s2.length; r3++) {
            var o2 = s2[r3], a2 = d[o2.id];
            a2.refs--, n3.push(a2);
          }
          e3 ? (s2 = c(t2, e3), i(s2)) : s2 = [];
          for (var r3 = 0; r3 < n3.length; r3++) {
            var a2 = n3[r3];
            if (0 === a2.refs) {
              for (var u2 = 0; u2 < a2.parts.length; u2++) a2.parts[u2]();
              delete d[a2.id];
            }
          }
        };
      };
      var m = /* @__PURE__ */ function() {
        var t2 = [];
        return function(e2, n2) {
          return t2[e2] = n2, t2.filter(Boolean).join("\n");
        };
      }();
    }, function(t, e, n) {
      !function(e2, n2) {
        t.exports = n2();
      }(0, function() {
        return function(t2) {
          function e2(i) {
            if (n2[i]) return n2[i].exports;
            var s = n2[i] = { exports: {}, id: i, loaded: false };
            return t2[i].call(s.exports, s, s.exports, e2), s.loaded = true, s.exports;
          }
          var n2 = {};
          return e2.m = t2, e2.c = n2, e2.p = "", e2(0);
        }([function(t2, e2) {
          "use strict";
          function n2(t3, e3) {
            if (!(t3 instanceof e3)) throw new TypeError("Cannot call a class as a function");
          }
          var i = Object.assign || function(t3) {
            for (var e3 = 1; e3 < arguments.length; e3++) {
              var n3 = arguments[e3];
              for (var i2 in n3) Object.prototype.hasOwnProperty.call(n3, i2) && (t3[i2] = n3[i2]);
            }
            return t3;
          }, s = /* @__PURE__ */ function() {
            function t3(t4, e3) {
              for (var n3 = 0; n3 < e3.length; n3++) {
                var i2 = e3[n3];
                i2.enumerable = i2.enumerable || false, i2.configurable = true, "value" in i2 && (i2.writable = true), Object.defineProperty(t4, i2.key, i2);
              }
            }
            return function(e3, n3, i2) {
              return n3 && t3(e3.prototype, n3), i2 && t3(e3, i2), e3;
            };
          }(), r2 = function(t3, e3, n3) {
            e3.forEach(function(e4) {
              t3.addEventListener(e4, function(t4) {
                n3(t4);
              });
            });
          }, o = function(t3, e3) {
            e3.forEach(function(e4) {
              t3.removeEventListener(e4);
            });
          }, a = function() {
            function t3(e3) {
              n2(this, t3), this.defaults = { idle: 1e4, events: ["mousemove", "keydown", "mousedown", "touchstart"], onIdle: function() {
              }, onActive: function() {
              }, onHide: function() {
              }, onShow: function() {
              }, keepTracking: true, startAtIdle: false, recurIdleCall: false }, this.settings = i({}, this.defaults, e3), this.idle = this.settings.startAtIdle, this.visible = !this.settings.startAtIdle, this.visibilityEvents = ["visibilitychange", "webkitvisibilitychange", "mozvisibilitychange", "msvisibilitychange"], this.lastId = null;
            }
            return s(t3, [{ key: "resetTimeout", value: function(t4, e3) {
              if (this.idle && (e3.onActive.call(), this.idle = false), clearTimeout(t4), this.settings.keepTracking) return this.timeout(this.settings);
            } }, { key: "timeout", value: function(t4) {
              var e3 = this.settings.recurIdleCall ? setInterval : setTimeout;
              return e3(function() {
                this.idle = true, this.settings.onIdle.call();
              }.bind(this), this.settings.idle);
            } }, { key: "start", value: function() {
              window.addEventListener("idle:stop", function(t4) {
                o(window, this.settings.events), this.settings.keepTracking = false, this.resetTimeout(this.lastId, this.settings);
              }), this.lastId = this.timeout(this.settings), r2(window, this.settings.events, function(t4) {
                this.lastId = this.resetTimeout(this.lastId, this.settings);
              }.bind(this)), (this.settings.onShow || this.settings.onHide) && r2(document, this.visibilityEvents, function(t4) {
                document.hidden || document.webkitHidden || document.mozHidden || document.msHidden ? this.visible && (this.visible = false, this.settings.onHide.call()) : this.visible || (this.visible = true, this.settings.onShow.call());
              }.bind(this));
            } }]), t3;
          }();
          t2.exports = a;
        }]);
      });
    }, function(t, e, n) {
      function i(t2) {
        n(15);
      }
      var s = n(1)(n(6), n(13), i, null, null);
      t.exports = s.exports;
    }, function(t, e, n) {
      "use strict";
      function i(t2) {
        return t2 && t2.__esModule ? t2 : { default: t2 };
      }
      function s(t2, e2, n2) {
        return e2 in t2 ? Object.defineProperty(t2, e2, { value: n2, enumerable: true, configurable: true, writable: true }) : t2[e2] = n2, t2;
      }
      Object.defineProperty(e, "__esModule", { value: true });
      var r2 = n(3), o = i(r2), a = n(4), u = i(a);
      e.default = { IdleView: u.default, install: function(t2, e2) {
        var n2 = e2 || {}, i2 = n2.eventEmitter, r3 = n2.store, a2 = n2.moduleName, c = void 0 === a2 ? "idleVue" : a2, d = n2.idleTime, l = void 0 === d ? 6e4 : d, p = n2.events, f = void 0 === p ? ["mousemove", "keydown", "mousedown", "touchstart"] : p, h = n2.keepTracking, v = void 0 === h || h, A = n2.startAtIdle, m = void 0 === A || A;
        if (!i2 && !r3) throw Error("Either `eventEmitter` or `store` must be passed in options");
        r3 && r3.registerModule(c, { state: { isIdle: m }, mutations: s({}, c + "/IDLE_CHANGED", function(t3, e3) {
          t3.isIdle = e3;
        }) });
        var b = c + "_onIdle", g = c + "_onActive";
        new o.default({ idle: l, events: f, keepTracking: v, startAtIdle: m, onIdle: function() {
          i2 && i2.$emit(b), r3 && r3.commit(c + "/IDLE_CHANGED", true);
        }, onActive: function() {
          i2 && i2.$emit(g), r3 && r3.commit(c + "/IDLE_CHANGED", false);
        } }).start(), t2.component("IdleView", u.default), t2.mixin({ data: function() {
          var t3;
          return t3 = {}, s(t3, b, null), s(t3, g, null), t3;
        }, created: function() {
          i2 && this.$options.onIdle && (this[b] = this.$options.onIdle.bind(this), i2.$on(b, this[b])), i2 && this.$options.onActive && (this[g] = this.$options.onActive.bind(this), i2.$on(g, this[g]));
        }, destroyed: function() {
          i2 && this[b] && i2.$off(b, this[b]), i2 && this[g] && i2.$off(g, this[g]);
        }, computed: { isAppIdle: function() {
          return r3 && r3.state[c].isIdle;
        } } });
      } };
    }, function(t, e, n) {
      "use strict";
      Object.defineProperty(e, "__esModule", { value: true });
      var i = n(11), s = function(t2) {
        return t2 && t2.__esModule ? t2 : { default: t2 };
      }(i);
      e.default = { components: { Sprite: s.default }, onIdle: function() {
        this.$refs.sprite.play();
      }, onActive: function() {
        this.$refs.sprite.stop();
      } };
    }, function(t, e, n) {
      "use strict";
      Object.defineProperty(e, "__esModule", { value: true }), e.default = { name: "sprite", props: { spriteSrc: { type: String, default: "" }, spriteId: { type: String, default: "sprite" }, spriteW: { type: Number, default: 1 }, spriteH: { type: Number, default: 1 }, spriteSpeed: { type: Number, default: 1 } }, data: function() {
        return { visible: true, frameIndex: 0, tickCount: 0, frameLength: 0, ticksPerFrame: 0, numberOfFrames: 0, frameRate: 20, ctx: "", canvas: "", mySprite: "", animationFrameId: -1 };
      }, mounted: function() {
        var t2 = this;
        this.$nextTick(function() {
          t2.mySprite = new Image(), t2.mySprite.onload = function(e2) {
            t2.spriteInit(e2.target);
          }, t2.mySprite.src = t2.spriteSrc;
        });
      }, methods: { spriteInit: function(t2) {
        this.canvas = this.$el.querySelector("#" + this.spriteId), this.ctx = this.canvas.getContext("2d"), this.ticksPerFrame = this.spriteSpeed, this.frameLength = t2.width, this.numberOfFrames = t2.width / this.spriteW, this.spriteLoop();
      }, spriteUpdate: function() {
        ++this.tickCount > this.ticksPerFrame && (this.tickCount = 0, this.frameIndex < this.numberOfFrames - 1 ? this.frameIndex++ : this.frameIndex = 0);
      }, spriteRender: function() {
        this.ctx.clearRect(0, 0, this.spriteW, this.spriteH);
        var t2 = this.frameIndex * this.spriteW;
        this.ctx.drawImage(this.mySprite, t2, 0, this.spriteW, this.spriteH, 0, 0, this.spriteW, this.spriteH);
      }, spriteLoop: function() {
        this.animationFrameId = window.requestAnimationFrame(this.spriteLoop), this.spriteUpdate(), this.spriteRender();
      }, stop: function() {
        window.cancelAnimationFrame(this.animationFrameId);
      }, play: function() {
        this.spriteLoop();
      } } };
    }, function(t, e, n) {
      e = t.exports = n(0)(true), e.push([t.i, "", "", { version: 3, sources: [], names: [], mappings: "", file: "Sprite.vue", sourceRoot: "" }]);
    }, function(t, e, n) {
      e = t.exports = n(0)(true), e.push([t.i, ".idle-view{position:fixed;top:0;width:100vw;height:100vh;z-index:8888;pointer-events:none;display:none}.idle-view.isIdle{pointer-events:all;display:block}.idle-view .sprite{position:absolute;top:50%;left:50%;transform:translateX(-50%) translateY(-50%);height:10px;width:180px;z-index:9999;-webkit-transform:scale(.7)}.idle-view .overlay{width:100%;height:100%;position:absolute;z-index:8888;background:#000;opacity:0;-webkit-transition:opacity .8s cubic-bezier(.77,0,.175,1)}.idle-view.isIdle .overlay{opacity:.6}@-webkit-keyframes SlowMo{0%{background-position:0 50%}50%{background-position:100% 50%}to{background-position:0 50%}}", "", { version: 3, sources: ["/home/afonso/Dev/idle-vue/src/components/Idle.vue"], names: [], mappings: "AACA,WACE,eAAgB,AAChB,MAAO,AACP,YAAa,AACb,aAAc,AACd,aAAc,AAEd,oBAAqB,AACrB,YAAc,CACf,AACD,kBACE,mBAAoB,AACpB,aAAe,CAChB,AACD,mBACE,kBAAmB,AACnB,QAAS,AACT,SAAU,AACV,4CAA6C,AAE7C,YAAa,AACb,YAAa,AACb,aAAc,AACd,2BAA8B,CAC/B,AACD,oBACE,WAAY,AACZ,YAAa,AACb,kBAAmB,AACnB,aAAc,AAEd,gBAAiB,AACjB,UAAW,AAEX,yDAAkE,CACnE,AACD,2BACE,UAAa,CACd,AACD,0BACA,GAAG,yBAA0B,CAC5B,AACD,IAAI,4BAA4B,CAC/B,AACD,GAAK,yBAA0B,CAC9B,CACA", file: "Idle.vue", sourcesContent: ["\n.idle-view {\n  position: fixed;\n  top: 0;\n  width: 100vw;\n  height: 100vh;\n  z-index: 8888;\n\n  pointer-events: none;\n  display: none;\n}\n.idle-view.isIdle {\n  pointer-events: all;\n  display: block;\n}\n.idle-view .sprite {\n  position: absolute;\n  top: 50%;\n  left: 50%;\n  transform: translateX(-50%) translateY(-50%);\n\n  height: 10px;\n  width: 180px;\n  z-index: 9999;\n  -webkit-transform: scale(0.7);\n}\n.idle-view .overlay {\n  width: 100%;\n  height: 100%;\n  position: absolute;\n  z-index: 8888;\n\n  background: #000;\n  opacity: 0;\n  /*-webkit-animation: SlowMo 5s cubic-bezier(0.77, 0, 0.175, 1) infinite;*/\n  -webkit-transition: opacity 800ms cubic-bezier(0.77, 0, 0.175, 1);\n}\n.idle-view.isIdle .overlay {\n  opacity: 0.6;\n}\n@-webkit-keyframes SlowMo {\n0%{background-position:0% 50%\n}\n50%{background-position:100% 50%\n}\n100%{background-position:0% 50%\n}\n}\n"], sourceRoot: "" }]);
    }, function(t, e, n) {
      t.exports = n.p + "static/img/touch.ba3be66.png";
    }, function(t, e, n) {
      function i(t2) {
        n(14);
      }
      var s = n(1)(n(7), n(12), i, null, null);
      t.exports = s.exports;
    }, function(t, e) {
      t.exports = { render: function() {
        var t2 = this, e2 = t2.$createElement, n = t2._self._c || e2;
        return n("div", { staticClass: "sprite" }, [n("canvas", { attrs: { id: t2.spriteId, width: t2.spriteW, height: t2.spriteH } })]);
      }, staticRenderFns: [] };
    }, function(t, e, n) {
      t.exports = { render: function() {
        var t2 = this, e2 = t2.$createElement, i = t2._self._c || e2;
        return i("div", { staticClass: "idle-view", class: { isIdle: t2.isIdle } }, [i("div", { staticClass: "overlay" }), t2._v(" "), i("sprite", { ref: "sprite", attrs: { spriteId: "touch", spriteSrc: n(10), spriteW: 180, spriteH: 215 } })], 1);
      }, staticRenderFns: [] };
    }, function(t, e, n) {
      var i = n(8);
      "string" == typeof i && (i = [[t.i, i, ""]]), i.locals && (t.exports = i.locals);
      n(2)("b6696284", i, true);
    }, function(t, e, n) {
      var i = n(9);
      "string" == typeof i && (i = [[t.i, i, ""]]), i.locals && (t.exports = i.locals);
      n(2)("25b5ba8a", i, true);
    }, function(t, e) {
      t.exports = function(t2, e2) {
        for (var n = [], i = {}, s = 0; s < e2.length; s++) {
          var r2 = e2[s], o = r2[0], a = r2[1], u = r2[2], c = r2[3], d = { id: t2 + ":" + s, css: a, media: u, sourceMap: c };
          i[o] ? i[o].parts.push(d) : n.push(i[o] = { id: o, parts: [d] });
        }
        return n;
      };
    }]);
  }
});

// frontend/utils/blockies.js
var require_blockies = __commonJS({
  "frontend/utils/blockies.js"(exports, module) {
    !function() {
      function e(e2) {
        for (var o2 = 0; o2 < c.length; o2++) c[o2] = 0;
        for (var o2 = 0; o2 < e2.length; o2++) c[o2 % 4] = (c[o2 % 4] << 5) - c[o2 % 4] + e2.charCodeAt(o2);
      }
      function o() {
        var e2 = c[0] ^ c[0] << 11;
        return c[0] = c[1], c[1] = c[2], c[2] = c[3], c[3] = c[3] ^ c[3] >> 19 ^ e2 ^ e2 >> 8, (c[3] >>> 0) / (1 << 31 >>> 0);
      }
      function r2() {
        var e2 = Math.floor(360 * o()), r3 = 60 * o() + 40 + "%", t2 = 25 * (o() + o() + o() + o()) + "%", l2 = "hsl(" + e2 + "," + r3 + "," + t2 + ")";
        return l2;
      }
      function t(e2) {
        for (var r3 = e2, t2 = e2, l2 = Math.ceil(r3 / 2), n2 = r3 - l2, a2 = [], c2 = 0; t2 > c2; c2++) {
          for (var i2 = [], f = 0; l2 > f; f++) i2[f] = Math.floor(2.3 * o());
          var s = i2.slice(0, n2);
          s.reverse(), i2 = i2.concat(s);
          for (var h = 0; h < i2.length; h++) a2.push(i2[h]);
        }
        return a2;
      }
      function l(o2) {
        var t2 = {};
        return t2.seed = o2.seed || Math.floor(Math.random() * Math.pow(10, 16)).toString(16), e(t2.seed), t2.size = o2.size || 8, t2.scale = o2.scale || 4, t2.color = o2.color || r2(), t2.bgcolor = o2.bgcolor || r2(), t2.spotcolor = o2.spotcolor || r2(), t2;
      }
      function n(e2, o2) {
        var r3 = t(e2.size), l2 = Math.sqrt(r3.length);
        o2.width = o2.height = e2.size * e2.scale;
        var n2 = o2.getContext("2d");
        n2.fillStyle = e2.bgcolor, n2.fillRect(0, 0, o2.width, o2.height), n2.fillStyle = e2.color;
        for (var a2 = 0; a2 < r3.length; a2++) if (r3[a2]) {
          var c2 = Math.floor(a2 / l2), i2 = a2 % l2;
          n2.fillStyle = 1 == r3[a2] ? e2.color : e2.spotcolor, n2.fillRect(i2 * e2.scale, c2 * e2.scale, e2.scale, e2.scale);
        }
        return o2;
      }
      function a(e2) {
        var e2 = l(e2 || {}), o2 = document.createElement("canvas");
        return n(e2, o2), o2;
      }
      var c = new Array(4), i = { create: a, render: n };
      "undefined" != typeof module && (module.exports = i);
    }();
  }
});

// node_modules/dompurify/dist/purify.js
var require_purify = __commonJS({
  "node_modules/dompurify/dist/purify.js"(exports, module) {
    (function(global, factory) {
      typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory() : typeof define === "function" && define.amd ? define(factory) : (global = global || self, global.DOMPurify = factory());
    })(exports, function() {
      "use strict";
      function _toConsumableArray(arr) {
        if (Array.isArray(arr)) {
          for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) {
            arr2[i] = arr[i];
          }
          return arr2;
        } else {
          return Array.from(arr);
        }
      }
      var hasOwnProperty = Object.hasOwnProperty, setPrototypeOf = Object.setPrototypeOf, isFrozen = Object.isFrozen, getPrototypeOf = Object.getPrototypeOf, getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
      var freeze = Object.freeze, seal = Object.seal, create = Object.create;
      var _ref = typeof Reflect !== "undefined" && Reflect, apply = _ref.apply, construct = _ref.construct;
      if (!apply) {
        apply = function apply2(fun, thisValue, args) {
          return fun.apply(thisValue, args);
        };
      }
      if (!freeze) {
        freeze = function freeze2(x) {
          return x;
        };
      }
      if (!seal) {
        seal = function seal2(x) {
          return x;
        };
      }
      if (!construct) {
        construct = function construct2(Func, args) {
          return new (Function.prototype.bind.apply(Func, [null].concat(_toConsumableArray(args))))();
        };
      }
      var arrayForEach = unapply(Array.prototype.forEach);
      var arrayPop = unapply(Array.prototype.pop);
      var arrayPush = unapply(Array.prototype.push);
      var stringToLowerCase = unapply(String.prototype.toLowerCase);
      var stringMatch = unapply(String.prototype.match);
      var stringReplace = unapply(String.prototype.replace);
      var stringIndexOf = unapply(String.prototype.indexOf);
      var stringTrim = unapply(String.prototype.trim);
      var regExpTest = unapply(RegExp.prototype.test);
      var typeErrorCreate = unconstruct(TypeError);
      function unapply(func) {
        return function(thisArg) {
          for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
            args[_key - 1] = arguments[_key];
          }
          return apply(func, thisArg, args);
        };
      }
      function unconstruct(func) {
        return function() {
          for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
            args[_key2] = arguments[_key2];
          }
          return construct(func, args);
        };
      }
      function addToSet(set, array) {
        if (setPrototypeOf) {
          setPrototypeOf(set, null);
        }
        var l = array.length;
        while (l--) {
          var element = array[l];
          if (typeof element === "string") {
            var lcElement = stringToLowerCase(element);
            if (lcElement !== element) {
              if (!isFrozen(array)) {
                array[l] = lcElement;
              }
              element = lcElement;
            }
          }
          set[element] = true;
        }
        return set;
      }
      function clone(object) {
        var newObject = create(null);
        var property = void 0;
        for (property in object) {
          if (apply(hasOwnProperty, object, [property])) {
            newObject[property] = object[property];
          }
        }
        return newObject;
      }
      function lookupGetter(object, prop) {
        while (object !== null) {
          var desc = getOwnPropertyDescriptor(object, prop);
          if (desc) {
            if (desc.get) {
              return unapply(desc.get);
            }
            if (typeof desc.value === "function") {
              return unapply(desc.value);
            }
          }
          object = getPrototypeOf(object);
        }
        function fallbackValue(element) {
          console.warn("fallback value for", element);
          return null;
        }
        return fallbackValue;
      }
      var html = freeze(["a", "abbr", "acronym", "address", "area", "article", "aside", "audio", "b", "bdi", "bdo", "big", "blink", "blockquote", "body", "br", "button", "canvas", "caption", "center", "cite", "code", "col", "colgroup", "content", "data", "datalist", "dd", "decorator", "del", "details", "dfn", "dialog", "dir", "div", "dl", "dt", "element", "em", "fieldset", "figcaption", "figure", "font", "footer", "form", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hgroup", "hr", "html", "i", "img", "input", "ins", "kbd", "label", "legend", "li", "main", "map", "mark", "marquee", "menu", "menuitem", "meter", "nav", "nobr", "ol", "optgroup", "option", "output", "p", "picture", "pre", "progress", "q", "rp", "rt", "ruby", "s", "samp", "section", "select", "shadow", "small", "source", "spacer", "span", "strike", "strong", "style", "sub", "summary", "sup", "table", "tbody", "td", "template", "textarea", "tfoot", "th", "thead", "time", "tr", "track", "tt", "u", "ul", "var", "video", "wbr"]);
      var svg = freeze(["svg", "a", "altglyph", "altglyphdef", "altglyphitem", "animatecolor", "animatemotion", "animatetransform", "circle", "clippath", "defs", "desc", "ellipse", "filter", "font", "g", "glyph", "glyphref", "hkern", "image", "line", "lineargradient", "marker", "mask", "metadata", "mpath", "path", "pattern", "polygon", "polyline", "radialgradient", "rect", "stop", "style", "switch", "symbol", "text", "textpath", "title", "tref", "tspan", "view", "vkern"]);
      var svgFilters = freeze(["feBlend", "feColorMatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence"]);
      var svgDisallowed = freeze(["animate", "color-profile", "cursor", "discard", "fedropshadow", "feimage", "font-face", "font-face-format", "font-face-name", "font-face-src", "font-face-uri", "foreignobject", "hatch", "hatchpath", "mesh", "meshgradient", "meshpatch", "meshrow", "missing-glyph", "script", "set", "solidcolor", "unknown", "use"]);
      var mathMl = freeze(["math", "menclose", "merror", "mfenced", "mfrac", "mglyph", "mi", "mlabeledtr", "mmultiscripts", "mn", "mo", "mover", "mpadded", "mphantom", "mroot", "mrow", "ms", "mspace", "msqrt", "mstyle", "msub", "msup", "msubsup", "mtable", "mtd", "mtext", "mtr", "munder", "munderover"]);
      var mathMlDisallowed = freeze(["maction", "maligngroup", "malignmark", "mlongdiv", "mscarries", "mscarry", "msgroup", "mstack", "msline", "msrow", "semantics", "annotation", "annotation-xml", "mprescripts", "none"]);
      var text = freeze(["#text"]);
      var html$1 = freeze(["accept", "action", "align", "alt", "autocapitalize", "autocomplete", "autopictureinpicture", "autoplay", "background", "bgcolor", "border", "capture", "cellpadding", "cellspacing", "checked", "cite", "class", "clear", "color", "cols", "colspan", "controls", "controlslist", "coords", "crossorigin", "datetime", "decoding", "default", "dir", "disabled", "disablepictureinpicture", "disableremoteplayback", "download", "draggable", "enctype", "enterkeyhint", "face", "for", "headers", "height", "hidden", "high", "href", "hreflang", "id", "inputmode", "integrity", "ismap", "kind", "label", "lang", "list", "loading", "loop", "low", "max", "maxlength", "media", "method", "min", "minlength", "multiple", "muted", "name", "noshade", "novalidate", "nowrap", "open", "optimum", "pattern", "placeholder", "playsinline", "poster", "preload", "pubdate", "radiogroup", "readonly", "rel", "required", "rev", "reversed", "role", "rows", "rowspan", "spellcheck", "scope", "selected", "shape", "size", "sizes", "span", "srclang", "start", "src", "srcset", "step", "style", "summary", "tabindex", "title", "translate", "type", "usemap", "valign", "value", "width", "xmlns"]);
      var svg$1 = freeze(["accent-height", "accumulate", "additive", "alignment-baseline", "ascent", "attributename", "attributetype", "azimuth", "basefrequency", "baseline-shift", "begin", "bias", "by", "class", "clip", "clippathunits", "clip-path", "clip-rule", "color", "color-interpolation", "color-interpolation-filters", "color-profile", "color-rendering", "cx", "cy", "d", "dx", "dy", "diffuseconstant", "direction", "display", "divisor", "dur", "edgemode", "elevation", "end", "fill", "fill-opacity", "fill-rule", "filter", "filterunits", "flood-color", "flood-opacity", "font-family", "font-size", "font-size-adjust", "font-stretch", "font-style", "font-variant", "font-weight", "fx", "fy", "g1", "g2", "glyph-name", "glyphref", "gradientunits", "gradienttransform", "height", "href", "id", "image-rendering", "in", "in2", "k", "k1", "k2", "k3", "k4", "kerning", "keypoints", "keysplines", "keytimes", "lang", "lengthadjust", "letter-spacing", "kernelmatrix", "kernelunitlength", "lighting-color", "local", "marker-end", "marker-mid", "marker-start", "markerheight", "markerunits", "markerwidth", "maskcontentunits", "maskunits", "max", "mask", "media", "method", "mode", "min", "name", "numoctaves", "offset", "operator", "opacity", "order", "orient", "orientation", "origin", "overflow", "paint-order", "path", "pathlength", "patterncontentunits", "patterntransform", "patternunits", "points", "preservealpha", "preserveaspectratio", "primitiveunits", "r", "rx", "ry", "radius", "refx", "refy", "repeatcount", "repeatdur", "restart", "result", "rotate", "scale", "seed", "shape-rendering", "specularconstant", "specularexponent", "spreadmethod", "startoffset", "stddeviation", "stitchtiles", "stop-color", "stop-opacity", "stroke-dasharray", "stroke-dashoffset", "stroke-linecap", "stroke-linejoin", "stroke-miterlimit", "stroke-opacity", "stroke", "stroke-width", "style", "surfacescale", "systemlanguage", "tabindex", "targetx", "targety", "transform", "text-anchor", "text-decoration", "text-rendering", "textlength", "type", "u1", "u2", "unicode", "values", "viewbox", "visibility", "version", "vert-adv-y", "vert-origin-x", "vert-origin-y", "width", "word-spacing", "wrap", "writing-mode", "xchannelselector", "ychannelselector", "x", "x1", "x2", "xmlns", "y", "y1", "y2", "z", "zoomandpan"]);
      var mathMl$1 = freeze(["accent", "accentunder", "align", "bevelled", "close", "columnsalign", "columnlines", "columnspan", "denomalign", "depth", "dir", "display", "displaystyle", "encoding", "fence", "frame", "height", "href", "id", "largeop", "length", "linethickness", "lspace", "lquote", "mathbackground", "mathcolor", "mathsize", "mathvariant", "maxsize", "minsize", "movablelimits", "notation", "numalign", "open", "rowalign", "rowlines", "rowspacing", "rowspan", "rspace", "rquote", "scriptlevel", "scriptminsize", "scriptsizemultiplier", "selection", "separator", "separators", "stretchy", "subscriptshift", "supscriptshift", "symmetric", "voffset", "width", "xmlns"]);
      var xml = freeze(["xlink:href", "xml:id", "xlink:title", "xml:space", "xmlns:xlink"]);
      var MUSTACHE_EXPR = seal(/\{\{[\s\S]*|[\s\S]*\}\}/gm);
      var ERB_EXPR = seal(/<%[\s\S]*|[\s\S]*%>/gm);
      var DATA_ATTR = seal(/^data-[\-\w.\u00B7-\uFFFF]/);
      var ARIA_ATTR = seal(/^aria-[\-\w]+$/);
      var IS_ALLOWED_URI = seal(
        /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|cid|xmpp):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i
        // eslint-disable-line no-useless-escape
      );
      var IS_SCRIPT_OR_DATA = seal(/^(?:\w+script|data):/i);
      var ATTR_WHITESPACE = seal(
        /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g
        // eslint-disable-line no-control-regex
      );
      var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function(obj) {
        return typeof obj;
      } : function(obj) {
        return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
      };
      function _toConsumableArray$1(arr) {
        if (Array.isArray(arr)) {
          for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) {
            arr2[i] = arr[i];
          }
          return arr2;
        } else {
          return Array.from(arr);
        }
      }
      var getGlobal = function getGlobal2() {
        return typeof window === "undefined" ? null : window;
      };
      var _createTrustedTypesPolicy = function _createTrustedTypesPolicy2(trustedTypes, document2) {
        if ((typeof trustedTypes === "undefined" ? "undefined" : _typeof(trustedTypes)) !== "object" || typeof trustedTypes.createPolicy !== "function") {
          return null;
        }
        var suffix = null;
        var ATTR_NAME = "data-tt-policy-suffix";
        if (document2.currentScript && document2.currentScript.hasAttribute(ATTR_NAME)) {
          suffix = document2.currentScript.getAttribute(ATTR_NAME);
        }
        var policyName = "dompurify" + (suffix ? "#" + suffix : "");
        try {
          return trustedTypes.createPolicy(policyName, {
            createHTML: function createHTML(html$$1) {
              return html$$1;
            }
          });
        } catch (_) {
          console.warn("TrustedTypes policy " + policyName + " could not be created.");
          return null;
        }
      };
      function createDOMPurify() {
        var window2 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : getGlobal();
        var DOMPurify = function DOMPurify2(root) {
          return createDOMPurify(root);
        };
        DOMPurify.version = "2.2.7";
        DOMPurify.removed = [];
        if (!window2 || !window2.document || window2.document.nodeType !== 9) {
          DOMPurify.isSupported = false;
          return DOMPurify;
        }
        var originalDocument = window2.document;
        var document2 = window2.document;
        var DocumentFragment = window2.DocumentFragment, HTMLTemplateElement = window2.HTMLTemplateElement, Node2 = window2.Node, Element2 = window2.Element, NodeFilter = window2.NodeFilter, _window$NamedNodeMap = window2.NamedNodeMap, NamedNodeMap = _window$NamedNodeMap === void 0 ? window2.NamedNodeMap || window2.MozNamedAttrMap : _window$NamedNodeMap, Text = window2.Text, Comment = window2.Comment, DOMParser = window2.DOMParser, trustedTypes = window2.trustedTypes;
        var ElementPrototype = Element2.prototype;
        var cloneNode = lookupGetter(ElementPrototype, "cloneNode");
        var getNextSibling = lookupGetter(ElementPrototype, "nextSibling");
        var getChildNodes = lookupGetter(ElementPrototype, "childNodes");
        var getParentNode = lookupGetter(ElementPrototype, "parentNode");
        if (typeof HTMLTemplateElement === "function") {
          var template = document2.createElement("template");
          if (template.content && template.content.ownerDocument) {
            document2 = template.content.ownerDocument;
          }
        }
        var trustedTypesPolicy = _createTrustedTypesPolicy(trustedTypes, originalDocument);
        var emptyHTML = trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML("") : "";
        var _document = document2, implementation = _document.implementation, createNodeIterator = _document.createNodeIterator, getElementsByTagName = _document.getElementsByTagName, createDocumentFragment = _document.createDocumentFragment;
        var importNode = originalDocument.importNode;
        var documentMode = {};
        try {
          documentMode = clone(document2).documentMode ? document2.documentMode : {};
        } catch (_) {
        }
        var hooks = {};
        DOMPurify.isSupported = typeof getParentNode === "function" && implementation && typeof implementation.createHTMLDocument !== "undefined" && documentMode !== 9;
        var MUSTACHE_EXPR$$1 = MUSTACHE_EXPR, ERB_EXPR$$1 = ERB_EXPR, DATA_ATTR$$1 = DATA_ATTR, ARIA_ATTR$$1 = ARIA_ATTR, IS_SCRIPT_OR_DATA$$1 = IS_SCRIPT_OR_DATA, ATTR_WHITESPACE$$1 = ATTR_WHITESPACE;
        var IS_ALLOWED_URI$$1 = IS_ALLOWED_URI;
        var ALLOWED_TAGS = null;
        var DEFAULT_ALLOWED_TAGS = addToSet({}, [].concat(_toConsumableArray$1(html), _toConsumableArray$1(svg), _toConsumableArray$1(svgFilters), _toConsumableArray$1(mathMl), _toConsumableArray$1(text)));
        var ALLOWED_ATTR = null;
        var DEFAULT_ALLOWED_ATTR = addToSet({}, [].concat(_toConsumableArray$1(html$1), _toConsumableArray$1(svg$1), _toConsumableArray$1(mathMl$1), _toConsumableArray$1(xml)));
        var FORBID_TAGS = null;
        var FORBID_ATTR = null;
        var ALLOW_ARIA_ATTR = true;
        var ALLOW_DATA_ATTR = true;
        var ALLOW_UNKNOWN_PROTOCOLS = false;
        var SAFE_FOR_TEMPLATES = false;
        var WHOLE_DOCUMENT = false;
        var SET_CONFIG = false;
        var FORCE_BODY = false;
        var RETURN_DOM = false;
        var RETURN_DOM_FRAGMENT = false;
        var RETURN_DOM_IMPORT = true;
        var RETURN_TRUSTED_TYPE = false;
        var SANITIZE_DOM = true;
        var KEEP_CONTENT = true;
        var IN_PLACE = false;
        var USE_PROFILES = {};
        var FORBID_CONTENTS = addToSet({}, ["annotation-xml", "audio", "colgroup", "desc", "foreignobject", "head", "iframe", "math", "mi", "mn", "mo", "ms", "mtext", "noembed", "noframes", "noscript", "plaintext", "script", "style", "svg", "template", "thead", "title", "video", "xmp"]);
        var DATA_URI_TAGS = null;
        var DEFAULT_DATA_URI_TAGS = addToSet({}, ["audio", "video", "img", "source", "image", "track"]);
        var URI_SAFE_ATTRIBUTES = null;
        var DEFAULT_URI_SAFE_ATTRIBUTES = addToSet({}, ["alt", "class", "for", "id", "label", "name", "pattern", "placeholder", "summary", "title", "value", "style", "xmlns"]);
        var CONFIG = null;
        var formElement = document2.createElement("form");
        var _parseConfig = function _parseConfig2(cfg) {
          if (CONFIG && CONFIG === cfg) {
            return;
          }
          if (!cfg || (typeof cfg === "undefined" ? "undefined" : _typeof(cfg)) !== "object") {
            cfg = {};
          }
          cfg = clone(cfg);
          ALLOWED_TAGS = "ALLOWED_TAGS" in cfg ? addToSet({}, cfg.ALLOWED_TAGS) : DEFAULT_ALLOWED_TAGS;
          ALLOWED_ATTR = "ALLOWED_ATTR" in cfg ? addToSet({}, cfg.ALLOWED_ATTR) : DEFAULT_ALLOWED_ATTR;
          URI_SAFE_ATTRIBUTES = "ADD_URI_SAFE_ATTR" in cfg ? addToSet(clone(DEFAULT_URI_SAFE_ATTRIBUTES), cfg.ADD_URI_SAFE_ATTR) : DEFAULT_URI_SAFE_ATTRIBUTES;
          DATA_URI_TAGS = "ADD_DATA_URI_TAGS" in cfg ? addToSet(clone(DEFAULT_DATA_URI_TAGS), cfg.ADD_DATA_URI_TAGS) : DEFAULT_DATA_URI_TAGS;
          FORBID_TAGS = "FORBID_TAGS" in cfg ? addToSet({}, cfg.FORBID_TAGS) : {};
          FORBID_ATTR = "FORBID_ATTR" in cfg ? addToSet({}, cfg.FORBID_ATTR) : {};
          USE_PROFILES = "USE_PROFILES" in cfg ? cfg.USE_PROFILES : false;
          ALLOW_ARIA_ATTR = cfg.ALLOW_ARIA_ATTR !== false;
          ALLOW_DATA_ATTR = cfg.ALLOW_DATA_ATTR !== false;
          ALLOW_UNKNOWN_PROTOCOLS = cfg.ALLOW_UNKNOWN_PROTOCOLS || false;
          SAFE_FOR_TEMPLATES = cfg.SAFE_FOR_TEMPLATES || false;
          WHOLE_DOCUMENT = cfg.WHOLE_DOCUMENT || false;
          RETURN_DOM = cfg.RETURN_DOM || false;
          RETURN_DOM_FRAGMENT = cfg.RETURN_DOM_FRAGMENT || false;
          RETURN_DOM_IMPORT = cfg.RETURN_DOM_IMPORT !== false;
          RETURN_TRUSTED_TYPE = cfg.RETURN_TRUSTED_TYPE || false;
          FORCE_BODY = cfg.FORCE_BODY || false;
          SANITIZE_DOM = cfg.SANITIZE_DOM !== false;
          KEEP_CONTENT = cfg.KEEP_CONTENT !== false;
          IN_PLACE = cfg.IN_PLACE || false;
          IS_ALLOWED_URI$$1 = cfg.ALLOWED_URI_REGEXP || IS_ALLOWED_URI$$1;
          if (SAFE_FOR_TEMPLATES) {
            ALLOW_DATA_ATTR = false;
          }
          if (RETURN_DOM_FRAGMENT) {
            RETURN_DOM = true;
          }
          if (USE_PROFILES) {
            ALLOWED_TAGS = addToSet({}, [].concat(_toConsumableArray$1(text)));
            ALLOWED_ATTR = [];
            if (USE_PROFILES.html === true) {
              addToSet(ALLOWED_TAGS, html);
              addToSet(ALLOWED_ATTR, html$1);
            }
            if (USE_PROFILES.svg === true) {
              addToSet(ALLOWED_TAGS, svg);
              addToSet(ALLOWED_ATTR, svg$1);
              addToSet(ALLOWED_ATTR, xml);
            }
            if (USE_PROFILES.svgFilters === true) {
              addToSet(ALLOWED_TAGS, svgFilters);
              addToSet(ALLOWED_ATTR, svg$1);
              addToSet(ALLOWED_ATTR, xml);
            }
            if (USE_PROFILES.mathMl === true) {
              addToSet(ALLOWED_TAGS, mathMl);
              addToSet(ALLOWED_ATTR, mathMl$1);
              addToSet(ALLOWED_ATTR, xml);
            }
          }
          if (cfg.ADD_TAGS) {
            if (ALLOWED_TAGS === DEFAULT_ALLOWED_TAGS) {
              ALLOWED_TAGS = clone(ALLOWED_TAGS);
            }
            addToSet(ALLOWED_TAGS, cfg.ADD_TAGS);
          }
          if (cfg.ADD_ATTR) {
            if (ALLOWED_ATTR === DEFAULT_ALLOWED_ATTR) {
              ALLOWED_ATTR = clone(ALLOWED_ATTR);
            }
            addToSet(ALLOWED_ATTR, cfg.ADD_ATTR);
          }
          if (cfg.ADD_URI_SAFE_ATTR) {
            addToSet(URI_SAFE_ATTRIBUTES, cfg.ADD_URI_SAFE_ATTR);
          }
          if (KEEP_CONTENT) {
            ALLOWED_TAGS["#text"] = true;
          }
          if (WHOLE_DOCUMENT) {
            addToSet(ALLOWED_TAGS, ["html", "head", "body"]);
          }
          if (ALLOWED_TAGS.table) {
            addToSet(ALLOWED_TAGS, ["tbody"]);
            delete FORBID_TAGS.tbody;
          }
          if (freeze) {
            freeze(cfg);
          }
          CONFIG = cfg;
        };
        var MATHML_TEXT_INTEGRATION_POINTS = addToSet({}, ["mi", "mo", "mn", "ms", "mtext"]);
        var HTML_INTEGRATION_POINTS = addToSet({}, ["foreignobject", "desc", "title", "annotation-xml"]);
        var ALL_SVG_TAGS = addToSet({}, svg);
        addToSet(ALL_SVG_TAGS, svgFilters);
        addToSet(ALL_SVG_TAGS, svgDisallowed);
        var ALL_MATHML_TAGS = addToSet({}, mathMl);
        addToSet(ALL_MATHML_TAGS, mathMlDisallowed);
        var MATHML_NAMESPACE = "http://www.w3.org/1998/Math/MathML";
        var SVG_NAMESPACE = "http://www.w3.org/2000/svg";
        var HTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
        var _checkValidNamespace = function _checkValidNamespace2(element) {
          var parent = getParentNode(element);
          if (!parent || !parent.tagName) {
            parent = {
              namespaceURI: HTML_NAMESPACE,
              tagName: "template"
            };
          }
          var tagName = stringToLowerCase(element.tagName);
          var parentTagName = stringToLowerCase(parent.tagName);
          if (element.namespaceURI === SVG_NAMESPACE) {
            if (parent.namespaceURI === HTML_NAMESPACE) {
              return tagName === "svg";
            }
            if (parent.namespaceURI === MATHML_NAMESPACE) {
              return tagName === "svg" && (parentTagName === "annotation-xml" || MATHML_TEXT_INTEGRATION_POINTS[parentTagName]);
            }
            return Boolean(ALL_SVG_TAGS[tagName]);
          }
          if (element.namespaceURI === MATHML_NAMESPACE) {
            if (parent.namespaceURI === HTML_NAMESPACE) {
              return tagName === "math";
            }
            if (parent.namespaceURI === SVG_NAMESPACE) {
              return tagName === "math" && HTML_INTEGRATION_POINTS[parentTagName];
            }
            return Boolean(ALL_MATHML_TAGS[tagName]);
          }
          if (element.namespaceURI === HTML_NAMESPACE) {
            if (parent.namespaceURI === SVG_NAMESPACE && !HTML_INTEGRATION_POINTS[parentTagName]) {
              return false;
            }
            if (parent.namespaceURI === MATHML_NAMESPACE && !MATHML_TEXT_INTEGRATION_POINTS[parentTagName]) {
              return false;
            }
            var commonSvgAndHTMLElements = addToSet({}, ["title", "style", "font", "a", "script"]);
            return !ALL_MATHML_TAGS[tagName] && (commonSvgAndHTMLElements[tagName] || !ALL_SVG_TAGS[tagName]);
          }
          return false;
        };
        var _forceRemove = function _forceRemove2(node) {
          arrayPush(DOMPurify.removed, { element: node });
          try {
            node.parentNode.removeChild(node);
          } catch (_) {
            try {
              node.outerHTML = emptyHTML;
            } catch (_2) {
              node.remove();
            }
          }
        };
        var _removeAttribute = function _removeAttribute2(name, node) {
          try {
            arrayPush(DOMPurify.removed, {
              attribute: node.getAttributeNode(name),
              from: node
            });
          } catch (_) {
            arrayPush(DOMPurify.removed, {
              attribute: null,
              from: node
            });
          }
          node.removeAttribute(name);
          if (name === "is" && !ALLOWED_ATTR[name]) {
            if (RETURN_DOM || RETURN_DOM_FRAGMENT) {
              try {
                _forceRemove(node);
              } catch (_) {
              }
            } else {
              try {
                node.setAttribute(name, "");
              } catch (_) {
              }
            }
          }
        };
        var _initDocument = function _initDocument2(dirty) {
          var doc = void 0;
          var leadingWhitespace = void 0;
          if (FORCE_BODY) {
            dirty = "<remove></remove>" + dirty;
          } else {
            var matches = stringMatch(dirty, /^[\r\n\t ]+/);
            leadingWhitespace = matches && matches[0];
          }
          var dirtyPayload = trustedTypesPolicy ? trustedTypesPolicy.createHTML(dirty) : dirty;
          try {
            doc = new DOMParser().parseFromString(dirtyPayload, "text/html");
          } catch (_) {
          }
          if (!doc || !doc.documentElement) {
            doc = implementation.createHTMLDocument("");
            var _doc = doc, body = _doc.body;
            body.parentNode.removeChild(body.parentNode.firstElementChild);
            body.outerHTML = dirtyPayload;
          }
          if (dirty && leadingWhitespace) {
            doc.body.insertBefore(document2.createTextNode(leadingWhitespace), doc.body.childNodes[0] || null);
          }
          return getElementsByTagName.call(doc, WHOLE_DOCUMENT ? "html" : "body")[0];
        };
        var _createIterator = function _createIterator2(root) {
          return createNodeIterator.call(root.ownerDocument || root, root, NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_COMMENT | NodeFilter.SHOW_TEXT, function() {
            return NodeFilter.FILTER_ACCEPT;
          }, false);
        };
        var _isClobbered = function _isClobbered2(elm) {
          if (elm instanceof Text || elm instanceof Comment) {
            return false;
          }
          if (typeof elm.nodeName !== "string" || typeof elm.textContent !== "string" || typeof elm.removeChild !== "function" || !(elm.attributes instanceof NamedNodeMap) || typeof elm.removeAttribute !== "function" || typeof elm.setAttribute !== "function" || typeof elm.namespaceURI !== "string" || typeof elm.insertBefore !== "function") {
            return true;
          }
          return false;
        };
        var _isNode = function _isNode2(object) {
          return (typeof Node2 === "undefined" ? "undefined" : _typeof(Node2)) === "object" ? object instanceof Node2 : object && (typeof object === "undefined" ? "undefined" : _typeof(object)) === "object" && typeof object.nodeType === "number" && typeof object.nodeName === "string";
        };
        var _executeHook = function _executeHook2(entryPoint, currentNode, data) {
          if (!hooks[entryPoint]) {
            return;
          }
          arrayForEach(hooks[entryPoint], function(hook) {
            hook.call(DOMPurify, currentNode, data, CONFIG);
          });
        };
        var _sanitizeElements = function _sanitizeElements2(currentNode) {
          var content = void 0;
          _executeHook("beforeSanitizeElements", currentNode, null);
          if (_isClobbered(currentNode)) {
            _forceRemove(currentNode);
            return true;
          }
          if (stringMatch(currentNode.nodeName, /[\u0080-\uFFFF]/)) {
            _forceRemove(currentNode);
            return true;
          }
          var tagName = stringToLowerCase(currentNode.nodeName);
          _executeHook("uponSanitizeElement", currentNode, {
            tagName,
            allowedTags: ALLOWED_TAGS
          });
          if (!_isNode(currentNode.firstElementChild) && (!_isNode(currentNode.content) || !_isNode(currentNode.content.firstElementChild)) && regExpTest(/<[/\w]/g, currentNode.innerHTML) && regExpTest(/<[/\w]/g, currentNode.textContent)) {
            _forceRemove(currentNode);
            return true;
          }
          if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
            if (KEEP_CONTENT && !FORBID_CONTENTS[tagName]) {
              var parentNode = getParentNode(currentNode);
              var childNodes = getChildNodes(currentNode);
              if (childNodes && parentNode) {
                var childCount = childNodes.length;
                for (var i = childCount - 1; i >= 0; --i) {
                  parentNode.insertBefore(cloneNode(childNodes[i], true), getNextSibling(currentNode));
                }
              }
            }
            _forceRemove(currentNode);
            return true;
          }
          if (currentNode instanceof Element2 && !_checkValidNamespace(currentNode)) {
            _forceRemove(currentNode);
            return true;
          }
          if ((tagName === "noscript" || tagName === "noembed") && regExpTest(/<\/no(script|embed)/i, currentNode.innerHTML)) {
            _forceRemove(currentNode);
            return true;
          }
          if (SAFE_FOR_TEMPLATES && currentNode.nodeType === 3) {
            content = currentNode.textContent;
            content = stringReplace(content, MUSTACHE_EXPR$$1, " ");
            content = stringReplace(content, ERB_EXPR$$1, " ");
            if (currentNode.textContent !== content) {
              arrayPush(DOMPurify.removed, { element: currentNode.cloneNode() });
              currentNode.textContent = content;
            }
          }
          _executeHook("afterSanitizeElements", currentNode, null);
          return false;
        };
        var _isValidAttribute = function _isValidAttribute2(lcTag, lcName, value) {
          if (SANITIZE_DOM && (lcName === "id" || lcName === "name") && (value in document2 || value in formElement)) {
            return false;
          }
          if (ALLOW_DATA_ATTR && regExpTest(DATA_ATTR$$1, lcName)) ;
          else if (ALLOW_ARIA_ATTR && regExpTest(ARIA_ATTR$$1, lcName)) ;
          else if (!ALLOWED_ATTR[lcName] || FORBID_ATTR[lcName]) {
            return false;
          } else if (URI_SAFE_ATTRIBUTES[lcName]) ;
          else if (regExpTest(IS_ALLOWED_URI$$1, stringReplace(value, ATTR_WHITESPACE$$1, ""))) ;
          else if ((lcName === "src" || lcName === "xlink:href" || lcName === "href") && lcTag !== "script" && stringIndexOf(value, "data:") === 0 && DATA_URI_TAGS[lcTag]) ;
          else if (ALLOW_UNKNOWN_PROTOCOLS && !regExpTest(IS_SCRIPT_OR_DATA$$1, stringReplace(value, ATTR_WHITESPACE$$1, ""))) ;
          else if (!value) ;
          else {
            return false;
          }
          return true;
        };
        var _sanitizeAttributes = function _sanitizeAttributes2(currentNode) {
          var attr = void 0;
          var value = void 0;
          var lcName = void 0;
          var l = void 0;
          _executeHook("beforeSanitizeAttributes", currentNode, null);
          var attributes = currentNode.attributes;
          if (!attributes) {
            return;
          }
          var hookEvent = {
            attrName: "",
            attrValue: "",
            keepAttr: true,
            allowedAttributes: ALLOWED_ATTR
          };
          l = attributes.length;
          while (l--) {
            attr = attributes[l];
            var _attr = attr, name = _attr.name, namespaceURI = _attr.namespaceURI;
            value = stringTrim(attr.value);
            lcName = stringToLowerCase(name);
            hookEvent.attrName = lcName;
            hookEvent.attrValue = value;
            hookEvent.keepAttr = true;
            hookEvent.forceKeepAttr = void 0;
            _executeHook("uponSanitizeAttribute", currentNode, hookEvent);
            value = hookEvent.attrValue;
            if (hookEvent.forceKeepAttr) {
              continue;
            }
            _removeAttribute(name, currentNode);
            if (!hookEvent.keepAttr) {
              continue;
            }
            if (regExpTest(/\/>/i, value)) {
              _removeAttribute(name, currentNode);
              continue;
            }
            if (SAFE_FOR_TEMPLATES) {
              value = stringReplace(value, MUSTACHE_EXPR$$1, " ");
              value = stringReplace(value, ERB_EXPR$$1, " ");
            }
            var lcTag = currentNode.nodeName.toLowerCase();
            if (!_isValidAttribute(lcTag, lcName, value)) {
              continue;
            }
            try {
              if (namespaceURI) {
                currentNode.setAttributeNS(namespaceURI, name, value);
              } else {
                currentNode.setAttribute(name, value);
              }
              arrayPop(DOMPurify.removed);
            } catch (_) {
            }
          }
          _executeHook("afterSanitizeAttributes", currentNode, null);
        };
        var _sanitizeShadowDOM = function _sanitizeShadowDOM2(fragment) {
          var shadowNode = void 0;
          var shadowIterator = _createIterator(fragment);
          _executeHook("beforeSanitizeShadowDOM", fragment, null);
          while (shadowNode = shadowIterator.nextNode()) {
            _executeHook("uponSanitizeShadowNode", shadowNode, null);
            if (_sanitizeElements(shadowNode)) {
              continue;
            }
            if (shadowNode.content instanceof DocumentFragment) {
              _sanitizeShadowDOM2(shadowNode.content);
            }
            _sanitizeAttributes(shadowNode);
          }
          _executeHook("afterSanitizeShadowDOM", fragment, null);
        };
        DOMPurify.sanitize = function(dirty, cfg) {
          var body = void 0;
          var importedNode = void 0;
          var currentNode = void 0;
          var oldNode = void 0;
          var returnNode = void 0;
          if (!dirty) {
            dirty = "<!-->";
          }
          if (typeof dirty !== "string" && !_isNode(dirty)) {
            if (typeof dirty.toString !== "function") {
              throw typeErrorCreate("toString is not a function");
            } else {
              dirty = dirty.toString();
              if (typeof dirty !== "string") {
                throw typeErrorCreate("dirty is not a string, aborting");
              }
            }
          }
          if (!DOMPurify.isSupported) {
            if (_typeof(window2.toStaticHTML) === "object" || typeof window2.toStaticHTML === "function") {
              if (typeof dirty === "string") {
                return window2.toStaticHTML(dirty);
              }
              if (_isNode(dirty)) {
                return window2.toStaticHTML(dirty.outerHTML);
              }
            }
            return dirty;
          }
          if (!SET_CONFIG) {
            _parseConfig(cfg);
          }
          DOMPurify.removed = [];
          if (typeof dirty === "string") {
            IN_PLACE = false;
          }
          if (IN_PLACE) ;
          else if (dirty instanceof Node2) {
            body = _initDocument("<!---->");
            importedNode = body.ownerDocument.importNode(dirty, true);
            if (importedNode.nodeType === 1 && importedNode.nodeName === "BODY") {
              body = importedNode;
            } else if (importedNode.nodeName === "HTML") {
              body = importedNode;
            } else {
              body.appendChild(importedNode);
            }
          } else {
            if (!RETURN_DOM && !SAFE_FOR_TEMPLATES && !WHOLE_DOCUMENT && // eslint-disable-next-line unicorn/prefer-includes
            dirty.indexOf("<") === -1) {
              return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(dirty) : dirty;
            }
            body = _initDocument(dirty);
            if (!body) {
              return RETURN_DOM ? null : emptyHTML;
            }
          }
          if (body && FORCE_BODY) {
            _forceRemove(body.firstChild);
          }
          var nodeIterator = _createIterator(IN_PLACE ? dirty : body);
          while (currentNode = nodeIterator.nextNode()) {
            if (currentNode.nodeType === 3 && currentNode === oldNode) {
              continue;
            }
            if (_sanitizeElements(currentNode)) {
              continue;
            }
            if (currentNode.content instanceof DocumentFragment) {
              _sanitizeShadowDOM(currentNode.content);
            }
            _sanitizeAttributes(currentNode);
            oldNode = currentNode;
          }
          oldNode = null;
          if (IN_PLACE) {
            return dirty;
          }
          if (RETURN_DOM) {
            if (RETURN_DOM_FRAGMENT) {
              returnNode = createDocumentFragment.call(body.ownerDocument);
              while (body.firstChild) {
                returnNode.appendChild(body.firstChild);
              }
            } else {
              returnNode = body;
            }
            if (RETURN_DOM_IMPORT) {
              returnNode = importNode.call(originalDocument, returnNode, true);
            }
            return returnNode;
          }
          var serializedHTML = WHOLE_DOCUMENT ? body.outerHTML : body.innerHTML;
          if (SAFE_FOR_TEMPLATES) {
            serializedHTML = stringReplace(serializedHTML, MUSTACHE_EXPR$$1, " ");
            serializedHTML = stringReplace(serializedHTML, ERB_EXPR$$1, " ");
          }
          return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(serializedHTML) : serializedHTML;
        };
        DOMPurify.setConfig = function(cfg) {
          _parseConfig(cfg);
          SET_CONFIG = true;
        };
        DOMPurify.clearConfig = function() {
          CONFIG = null;
          SET_CONFIG = false;
        };
        DOMPurify.isValidAttribute = function(tag, attr, value) {
          if (!CONFIG) {
            _parseConfig({});
          }
          var lcTag = stringToLowerCase(tag);
          var lcName = stringToLowerCase(attr);
          return _isValidAttribute(lcTag, lcName, value);
        };
        DOMPurify.addHook = function(entryPoint, hookFunction) {
          if (typeof hookFunction !== "function") {
            return;
          }
          hooks[entryPoint] = hooks[entryPoint] || [];
          arrayPush(hooks[entryPoint], hookFunction);
        };
        DOMPurify.removeHook = function(entryPoint) {
          if (hooks[entryPoint]) {
            arrayPop(hooks[entryPoint]);
          }
        };
        DOMPurify.removeHooks = function(entryPoint) {
          if (hooks[entryPoint]) {
            hooks[entryPoint] = [];
          }
        };
        DOMPurify.removeAllHooks = function() {
          hooks = {};
        };
        return DOMPurify;
      }
      var purify = createDOMPurify();
      return purify;
    });
  }
});

// frontend/utils/CircularList.js
var CircularList = class {
  #buffer;
  #capacity = 0;
  #defaultValue = "";
  #isFull = false;
  #offset = 0;
  constructor(capacity, defaultValue = "") {
    this.#buffer = new Array(capacity).fill(defaultValue);
    this.#capacity = capacity;
    this.#defaultValue = defaultValue;
  }
  add(entry) {
    const capacity = this.#capacity;
    const offset = this.#offset;
    this.#buffer[offset] = entry;
    if (offset === capacity - 1) {
      this.#isFull = true;
    }
    this.#offset = (offset + 1) % capacity;
  }
  addAll(entries) {
    for (const entry of entries) {
      this.add(entry);
    }
  }
  clear() {
    this.#buffer.fill(this.#defaultValue);
    this.#isFull = false;
    this.#offset = 0;
  }
  toArray() {
    const buffer = this.#buffer;
    const offset = this.#offset;
    return this.#isFull ? [...buffer.slice(offset), ...buffer.slice(0, offset)] : buffer.slice(0, offset);
  }
};

// frontend/model/logger.js
var loggingLevels = ["debug", "error", "info", "log", "warn"];
var originalConsole = console;
var noop = (...args) => void 0;
async function createLogger(config2, { getItem: getItem2, removeItem: removeItem2, setItem: setItem2 }) {
  const entries = new CircularList(config2.maxEntries);
  const methods = Object.fromEntries(loggingLevels.map(
    (name) => [name, (...args) => {
      originalConsole[name](...args);
      captureLogEntry(logger2, name, config2.source, ...args);
    }]
  ));
  const appLogsFilter = [];
  const consoleProxy = new Proxy({ ...methods }, {
    get(o, p, r2) {
      return Reflect.has(o, p) ? Reflect.get(o, p, r2) : Reflect.get(originalConsole, p, r2);
    },
    has(o, p) {
      return Reflect.has(originalConsole, p);
    }
  });
  const logger2 = {
    get appLogsFilter() {
      return [...appLogsFilter];
    },
    console: consoleProxy,
    entries,
    async clear() {
      await removeItem2("entries");
      entries.clear();
    },
    async save() {
      try {
        await setItem2("entries", this.entries.toArray());
      } catch (error) {
        consoleProxy.error(error);
      }
    },
    setAppLogsFilter(filter) {
      appLogsFilter.splice(0, appLogsFilter.length, ...filter);
      for (const level of loggingLevels) {
        consoleProxy[level] = appLogsFilter.includes(level) ? methods[level] : noop;
      }
    }
  };
  const previousEntries = await (async () => {
    try {
      const stored = await getItem2("entries");
      return stored ? JSON.parse(stored) : [];
    } catch (error) {
      consoleProxy.error("Failed to parse stored entries:", error);
      return [];
    }
  })();
  if (config2.maxEntries < previousEntries.length) {
    previousEntries.splice(0, previousEntries.length - config2.maxEntries);
  }
  if (previousEntries.length) {
    logger2.entries.addAll(previousEntries);
  }
  return logger2;
}
function captureLogEntry(logger2, type, source, ...args) {
  const entry = {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    source,
    type,
    // Detect when arg is an Error and capture it properly.
    // ex: uncaught Vue errors or custom try/catch errors.
    msg: args.map((arg) => {
      try {
        const seen = /* @__PURE__ */ new WeakSet();
        return JSON.parse(
          JSON.stringify(arg, (_, v) => {
            if (v instanceof Error) {
              return {
                name: v.name,
                message: v.message,
                stack: v.stack
              };
            }
            if (typeof v === "object" && v !== null) {
              if (seen.has(v)) {
                return "[Circular Reference]";
              }
              seen.add(v);
            }
            return v;
          })
        );
      } catch (e) {
        return `[captureLogs failed to stringify argument of type '${typeof arg}'. Err: ${e.message}]`;
      }
    })
  };
  logger2.entries.add(entry);
  esm_default("sbp/selectors/fn", "okTurtles.events/emit")(CAPTURED_LOGS, entry);
}

// node_modules/@sbp/okturtles.data/dist/module.mjs
var _store = /* @__PURE__ */ new Map();
var module_default = esm_default("sbp/selectors/register", {
  "okTurtles.data/get": function(key) {
    return _store.get(key);
  },
  "okTurtles.data/set": function(key, data) {
    _store.set(key, data);
    return data;
  },
  "okTurtles.data/delete": function(key) {
    return _store.delete(key);
  },
  "okTurtles.data/add": function(key, data) {
    const array = _store.get(key);
    if (array) {
      array.push(data);
    } else {
      _store.set(key, [data]);
    }
  },
  "okTurtles.data/remove": function(key, data) {
    const array = _store.get(key);
    if (array) {
      const aLen = array.length;
      const filtered = array.filter((v) => v !== data);
      _store.set(key, filtered);
      return aLen - filtered.length;
    }
  },
  "okTurtles.data/apply": function(key, fn) {
    return fn(_store.get(key));
  }
});

// node_modules/@sbp/okturtles.events/dist/esm/index.js
var listenKey = (evt) => `events/${evt}/listeners`;
var esm_default2 = esm_default("sbp/selectors/register", {
  "okTurtles.events/_init": function() {
    this.errorHandler = (event, e) => {
      console.error(`[okTurtles.events] Error at handler for ${event}`, e);
    };
  },
  "okTurtles.events/on": function(event, handler) {
    esm_default("okTurtles.data/add", listenKey(event), handler);
    return () => esm_default("okTurtles.events/off", event, handler);
  },
  "okTurtles.events/once": function(event, handler) {
    const cbWithOff = (...args) => {
      handler(...args);
      esm_default("okTurtles.events/off", event, cbWithOff);
    };
    return esm_default("okTurtles.events/on", event, cbWithOff);
  },
  "okTurtles.events/emit": function(event, ...data) {
    var _a;
    for (const listener of esm_default("okTurtles.data/get", listenKey(event)) || []) {
      try {
        listener(...data);
      } catch (e) {
        (_a = this.errorHandler) === null || _a === void 0 ? void 0 : _a.call(this, event, e);
      }
    }
  },
  // almost identical to Vue.prototype.$off, except we require `event` argument
  "okTurtles.events/off": function(event, handler) {
    if (handler) {
      esm_default("okTurtles.data/remove", listenKey(event), handler);
    } else {
      esm_default("okTurtles.data/delete", listenKey(event));
    }
  },
  "okTurtles.events/setErrorHandler": function(errorHandler) {
    this.errorHandler = errorHandler;
  }
});

// frontend/model/logServer.js
var logServer_default = (console2) => {
  if (!self.location.href.startsWith("https://gi")) return;
  esm_default("okTurtles.events/on", CAPTURED_LOGS, ({ level, msg: stringifyMe }) => {
    if (level === "debug") return;
    const value = JSON.stringify(stringifyMe);
    const apiUrl = esm_default("sbp/selectors/fn", "okTurtles.data/get")("API_URL");
    fetch(`${apiUrl}/log`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ level, value })
    }).catch((e) => {
      console2.error(`[captureLogs] '${e.message}' attempting to log [${level}] to server:`, value);
    });
  });
};

// frontend/model/captureLogs.js
var config = {
  maxEntries: MAX_LOG_ENTRIES,
  source: "browser"
};
var originalConsole2 = self.console;
var logger = null;
var identityContractID = "";
var getItem = (key) => sessionStorage.getItem(`giConsole/${identityContractID}/${key}`);
var removeItem = (key) => sessionStorage.removeItem(`giConsole/${identityContractID}/${key}`);
var setItem = (key, value) => {
  sessionStorage.setItem(`giConsole/${identityContractID}/${key}`, typeof value === "string" ? value : JSON.stringify(value));
};
async function captureLogsStart(userLogged) {
  identityContractID = userLogged;
  logger = await createLogger(config, { getItem, removeItem, setItem });
  setItem("config", config);
  logger.setAppLogsFilter(esm_default("state/vuex/state").settings?.appLogsFilter ?? []);
  esm_default("okTurtles.events/on", SET_APP_LOGS_FILTER, logger.setAppLogsFilter);
  self.console = logger.console;
  const isNewSession = !sessionStorage.getItem("NEW_SESSION");
  if (isNewSession) {
    sessionStorage.setItem("NEW_SESSION", "1");
  }
  originalConsole2.log(isNewSession ? "NEW_SESSION" : "NEW_VISIT", "Starting to capture logs of type:", logger.appLogsFilter);
}
async function captureLogsPause({ wipeOut }) {
  if (wipeOut) {
    await clearLogs();
  }
  esm_default("okTurtles.events/off", SET_APP_LOGS_FILTER);
  console.log("captureLogs paused");
  self.console = originalConsole2;
}
async function clearLogs() {
  await logger?.clear();
}
window.addEventListener("visibilitychange", (event) => esm_default("appLogs/save").catch((e) => {
  console.error("Error saving logs during visibilitychange event handler", e);
}));
logServer_default(originalConsole2);
var captureLogs_default = esm_default("sbp/selectors/register", {
  "appLogs/get"() {
    return logger?.entries.toArray() ?? [];
  },
  async "appLogs/save"() {
    await logger?.save();
  },
  "appLogs/pauseCapture": captureLogsPause,
  "appLogs/startCapture": captureLogsStart,
  async "appLogs/clearLogs"(userID) {
    const savedID = identityContractID;
    identityContractID = userID;
    try {
      await clearLogs();
    } catch {
    }
    identityContractID = savedID;
  }
});

// node_modules/@sbp/okturtles.eventqueue/dist/esm/index.js
var isEventQueueSbpEvent = (e) => {
  return Object.prototype.hasOwnProperty.call(e, "sbpInvocation");
};
var esm_default3 = esm_default("sbp/selectors/register", {
  "okTurtles.eventQueue/_init": function() {
    this.eventQueues = /* @__PURE__ */ Object.create(null);
  },
  "okTurtles.eventQueue/isWaiting": function(name) {
    var _a;
    return !!((_a = this.eventQueues[name]) === null || _a === void 0 ? void 0 : _a.length);
  },
  "okTurtles.eventQueue/queuedInvocations": function(name) {
    var _a, _b;
    if (name == null) {
      return Object.fromEntries(Object.entries(this.eventQueues).map(([name2, events]) => [name2, events.map((event) => {
        if (isEventQueueSbpEvent(event)) {
          return event.sbpInvocation;
        } else {
          return event.fn;
        }
      })]));
    }
    return (_b = (_a = this.eventQueues[name]) === null || _a === void 0 ? void 0 : _a.map((event) => {
      if (isEventQueueSbpEvent(event)) {
        return event.sbpInvocation;
      } else {
        return event.fn;
      }
    })) !== null && _b !== void 0 ? _b : [];
  },
  "okTurtles.eventQueue/queueEvent": async function(name, invocation) {
    if (!Object.prototype.hasOwnProperty.call(this.eventQueues, name)) {
      this.eventQueues[name] = [];
    }
    const events = this.eventQueues[name];
    let accept;
    const promise = new Promise((resolve) => {
      accept = resolve;
    });
    const thisEvent = typeof invocation === "function" ? {
      fn: invocation,
      promise
    } : {
      sbpInvocation: invocation,
      promise
    };
    events.push(thisEvent);
    while (events.length > 0) {
      const event = events[0];
      if (event === thisEvent) {
        try {
          if (typeof invocation === "function") {
            return await invocation();
          } else {
            return await esm_default(...invocation);
          }
        } finally {
          accept();
          events.shift();
        }
      } else {
        await event.promise;
      }
    }
  }
});

// frontend/main.js
var import_idle_vue = __toESM(require_build());

// node_modules/wicg-inert/dist/inert.esm.js
var _createClass = /* @__PURE__ */ function() {
  function defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }
  return function(Constructor, protoProps, staticProps) {
    if (protoProps) defineProperties(Constructor.prototype, protoProps);
    if (staticProps) defineProperties(Constructor, staticProps);
    return Constructor;
  };
}();
function _classCallCheck(instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
}
(function() {
  if (typeof window === "undefined") {
    return;
  }
  var slice = Array.prototype.slice;
  var matches = Element.prototype.matches || Element.prototype.msMatchesSelector;
  var _focusableElementsString = ["a[href]", "area[href]", "input:not([disabled])", "select:not([disabled])", "textarea:not([disabled])", "button:not([disabled])", "details", "summary", "iframe", "object", "embed", "[contenteditable]"].join(",");
  var InertRoot = function() {
    function InertRoot2(rootElement, inertManager2) {
      _classCallCheck(this, InertRoot2);
      this._inertManager = inertManager2;
      this._rootElement = rootElement;
      this._managedNodes = /* @__PURE__ */ new Set();
      if (this._rootElement.hasAttribute("aria-hidden")) {
        this._savedAriaHidden = this._rootElement.getAttribute("aria-hidden");
      } else {
        this._savedAriaHidden = null;
      }
      this._rootElement.setAttribute("aria-hidden", "true");
      this._makeSubtreeUnfocusable(this._rootElement);
      this._observer = new MutationObserver(this._onMutation.bind(this));
      this._observer.observe(this._rootElement, { attributes: true, childList: true, subtree: true });
    }
    _createClass(InertRoot2, [{
      key: "destructor",
      value: function destructor() {
        this._observer.disconnect();
        if (this._rootElement) {
          if (this._savedAriaHidden !== null) {
            this._rootElement.setAttribute("aria-hidden", this._savedAriaHidden);
          } else {
            this._rootElement.removeAttribute("aria-hidden");
          }
        }
        this._managedNodes.forEach(function(inertNode) {
          this._unmanageNode(inertNode.node);
        }, this);
        this._observer = /** @type {?} */
        null;
        this._rootElement = /** @type {?} */
        null;
        this._managedNodes = /** @type {?} */
        null;
        this._inertManager = /** @type {?} */
        null;
      }
      /**
       * @return {!Set<!InertNode>} A copy of this InertRoot's managed nodes set.
       */
    }, {
      key: "_makeSubtreeUnfocusable",
      /**
       * @param {!Node} startNode
       */
      value: function _makeSubtreeUnfocusable(startNode) {
        var _this2 = this;
        composedTreeWalk(startNode, function(node2) {
          return _this2._visitNode(node2);
        });
        var activeElement = document.activeElement;
        if (!document.body.contains(startNode)) {
          var node = startNode;
          var root = void 0;
          while (node) {
            if (node.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
              root = /** @type {!ShadowRoot} */
              node;
              break;
            }
            node = node.parentNode;
          }
          if (root) {
            activeElement = root.activeElement;
          }
        }
        if (startNode.contains(activeElement)) {
          activeElement.blur();
          if (activeElement === document.activeElement) {
            document.body.focus();
          }
        }
      }
      /**
       * @param {!Node} node
       */
    }, {
      key: "_visitNode",
      value: function _visitNode(node) {
        if (node.nodeType !== Node.ELEMENT_NODE) {
          return;
        }
        var element = (
          /** @type {!Element} */
          node
        );
        if (element !== this._rootElement && element.hasAttribute("inert")) {
          this._adoptInertRoot(element);
        }
        if (matches.call(element, _focusableElementsString) || element.hasAttribute("tabindex")) {
          this._manageNode(element);
        }
      }
      /**
       * Register the given node with this InertRoot and with InertManager.
       * @param {!Node} node
       */
    }, {
      key: "_manageNode",
      value: function _manageNode(node) {
        var inertNode = this._inertManager.register(node, this);
        this._managedNodes.add(inertNode);
      }
      /**
       * Unregister the given node with this InertRoot and with InertManager.
       * @param {!Node} node
       */
    }, {
      key: "_unmanageNode",
      value: function _unmanageNode(node) {
        var inertNode = this._inertManager.deregister(node, this);
        if (inertNode) {
          this._managedNodes["delete"](inertNode);
        }
      }
      /**
       * Unregister the entire subtree starting at `startNode`.
       * @param {!Node} startNode
       */
    }, {
      key: "_unmanageSubtree",
      value: function _unmanageSubtree(startNode) {
        var _this3 = this;
        composedTreeWalk(startNode, function(node) {
          return _this3._unmanageNode(node);
        });
      }
      /**
       * If a descendant node is found with an `inert` attribute, adopt its managed nodes.
       * @param {!Element} node
       */
    }, {
      key: "_adoptInertRoot",
      value: function _adoptInertRoot(node) {
        var inertSubroot = this._inertManager.getInertRoot(node);
        if (!inertSubroot) {
          this._inertManager.setInert(node, true);
          inertSubroot = this._inertManager.getInertRoot(node);
        }
        inertSubroot.managedNodes.forEach(function(savedInertNode) {
          this._manageNode(savedInertNode.node);
        }, this);
      }
      /**
       * Callback used when mutation observer detects subtree additions, removals, or attribute changes.
       * @param {!Array<!MutationRecord>} records
       * @param {!MutationObserver} self
       */
    }, {
      key: "_onMutation",
      value: function _onMutation(records, self2) {
        records.forEach(function(record) {
          var target = (
            /** @type {!Element} */
            record.target
          );
          if (record.type === "childList") {
            slice.call(record.addedNodes).forEach(function(node) {
              this._makeSubtreeUnfocusable(node);
            }, this);
            slice.call(record.removedNodes).forEach(function(node) {
              this._unmanageSubtree(node);
            }, this);
          } else if (record.type === "attributes") {
            if (record.attributeName === "tabindex") {
              this._manageNode(target);
            } else if (target !== this._rootElement && record.attributeName === "inert" && target.hasAttribute("inert")) {
              this._adoptInertRoot(target);
              var inertSubroot = this._inertManager.getInertRoot(target);
              this._managedNodes.forEach(function(managedNode) {
                if (target.contains(managedNode.node)) {
                  inertSubroot._manageNode(managedNode.node);
                }
              });
            }
          }
        }, this);
      }
    }, {
      key: "managedNodes",
      get: function get() {
        return new Set(this._managedNodes);
      }
      /** @return {boolean} */
    }, {
      key: "hasSavedAriaHidden",
      get: function get() {
        return this._savedAriaHidden !== null;
      }
      /** @param {?string} ariaHidden */
    }, {
      key: "savedAriaHidden",
      set: function set(ariaHidden) {
        this._savedAriaHidden = ariaHidden;
      },
      get: function get() {
        return this._savedAriaHidden;
      }
    }]);
    return InertRoot2;
  }();
  var InertNode = function() {
    function InertNode2(node, inertRoot) {
      _classCallCheck(this, InertNode2);
      this._node = node;
      this._overrodeFocusMethod = false;
      this._inertRoots = /* @__PURE__ */ new Set([inertRoot]);
      this._savedTabIndex = null;
      this._destroyed = false;
      this.ensureUntabbable();
    }
    _createClass(InertNode2, [{
      key: "destructor",
      value: function destructor() {
        this._throwIfDestroyed();
        if (this._node && this._node.nodeType === Node.ELEMENT_NODE) {
          var element = (
            /** @type {!Element} */
            this._node
          );
          if (this._savedTabIndex !== null) {
            element.setAttribute("tabindex", this._savedTabIndex);
          } else {
            element.removeAttribute("tabindex");
          }
          if (this._overrodeFocusMethod) {
            delete element.focus;
          }
        }
        this._node = /** @type {?} */
        null;
        this._inertRoots = /** @type {?} */
        null;
        this._destroyed = true;
      }
      /**
       * @type {boolean} Whether this object is obsolete because the managed node is no longer inert.
       * If the object has been destroyed, any attempt to access it will cause an exception.
       */
    }, {
      key: "_throwIfDestroyed",
      /**
       * Throw if user tries to access destroyed InertNode.
       */
      value: function _throwIfDestroyed() {
        if (this.destroyed) {
          throw new Error("Trying to access destroyed InertNode");
        }
      }
      /** @return {boolean} */
    }, {
      key: "ensureUntabbable",
      /** Save the existing tabindex value and make the node untabbable and unfocusable */
      value: function ensureUntabbable() {
        if (this.node.nodeType !== Node.ELEMENT_NODE) {
          return;
        }
        var element = (
          /** @type {!Element} */
          this.node
        );
        if (matches.call(element, _focusableElementsString)) {
          if (
            /** @type {!HTMLElement} */
            element.tabIndex === -1 && this.hasSavedTabIndex
          ) {
            return;
          }
          if (element.hasAttribute("tabindex")) {
            this._savedTabIndex = /** @type {!HTMLElement} */
            element.tabIndex;
          }
          element.setAttribute("tabindex", "-1");
          if (element.nodeType === Node.ELEMENT_NODE) {
            element.focus = function() {
            };
            this._overrodeFocusMethod = true;
          }
        } else if (element.hasAttribute("tabindex")) {
          this._savedTabIndex = /** @type {!HTMLElement} */
          element.tabIndex;
          element.removeAttribute("tabindex");
        }
      }
      /**
       * Add another inert root to this inert node's set of managing inert roots.
       * @param {!InertRoot} inertRoot
       */
    }, {
      key: "addInertRoot",
      value: function addInertRoot(inertRoot) {
        this._throwIfDestroyed();
        this._inertRoots.add(inertRoot);
      }
      /**
       * Remove the given inert root from this inert node's set of managing inert roots.
       * If the set of managing inert roots becomes empty, this node is no longer inert,
       * so the object should be destroyed.
       * @param {!InertRoot} inertRoot
       */
    }, {
      key: "removeInertRoot",
      value: function removeInertRoot(inertRoot) {
        this._throwIfDestroyed();
        this._inertRoots["delete"](inertRoot);
        if (this._inertRoots.size === 0) {
          this.destructor();
        }
      }
    }, {
      key: "destroyed",
      get: function get() {
        return (
          /** @type {!InertNode} */
          this._destroyed
        );
      }
    }, {
      key: "hasSavedTabIndex",
      get: function get() {
        return this._savedTabIndex !== null;
      }
      /** @return {!Node} */
    }, {
      key: "node",
      get: function get() {
        this._throwIfDestroyed();
        return this._node;
      }
      /** @param {?number} tabIndex */
    }, {
      key: "savedTabIndex",
      set: function set(tabIndex) {
        this._throwIfDestroyed();
        this._savedTabIndex = tabIndex;
      },
      get: function get() {
        this._throwIfDestroyed();
        return this._savedTabIndex;
      }
    }]);
    return InertNode2;
  }();
  var InertManager = function() {
    function InertManager2(document2) {
      _classCallCheck(this, InertManager2);
      if (!document2) {
        throw new Error("Missing required argument; InertManager needs to wrap a document.");
      }
      this._document = document2;
      this._managedNodes = /* @__PURE__ */ new Map();
      this._inertRoots = /* @__PURE__ */ new Map();
      this._observer = new MutationObserver(this._watchForInert.bind(this));
      addInertStyle(document2.head || document2.body || document2.documentElement);
      if (document2.readyState === "loading") {
        document2.addEventListener("DOMContentLoaded", this._onDocumentLoaded.bind(this));
      } else {
        this._onDocumentLoaded();
      }
    }
    _createClass(InertManager2, [{
      key: "setInert",
      value: function setInert(root, inert) {
        if (inert) {
          if (this._inertRoots.has(root)) {
            return;
          }
          var inertRoot = new InertRoot(root, this);
          root.setAttribute("inert", "");
          this._inertRoots.set(root, inertRoot);
          if (!this._document.body.contains(root)) {
            var parent = root.parentNode;
            while (parent) {
              if (parent.nodeType === 11) {
                addInertStyle(parent);
              }
              parent = parent.parentNode;
            }
          }
        } else {
          if (!this._inertRoots.has(root)) {
            return;
          }
          var _inertRoot = this._inertRoots.get(root);
          _inertRoot.destructor();
          this._inertRoots["delete"](root);
          root.removeAttribute("inert");
        }
      }
      /**
       * Get the InertRoot object corresponding to the given inert root element, if any.
       * @param {!Node} element
       * @return {!InertRoot|undefined}
       */
    }, {
      key: "getInertRoot",
      value: function getInertRoot(element) {
        return this._inertRoots.get(element);
      }
      /**
       * Register the given InertRoot as managing the given node.
       * In the case where the node has a previously existing inert root, this inert root will
       * be added to its set of inert roots.
       * @param {!Node} node
       * @param {!InertRoot} inertRoot
       * @return {!InertNode} inertNode
       */
    }, {
      key: "register",
      value: function register(node, inertRoot) {
        var inertNode = this._managedNodes.get(node);
        if (inertNode !== void 0) {
          inertNode.addInertRoot(inertRoot);
        } else {
          inertNode = new InertNode(node, inertRoot);
        }
        this._managedNodes.set(node, inertNode);
        return inertNode;
      }
      /**
       * De-register the given InertRoot as managing the given inert node.
       * Removes the inert root from the InertNode's set of managing inert roots, and remove the inert
       * node from the InertManager's set of managed nodes if it is destroyed.
       * If the node is not currently managed, this is essentially a no-op.
       * @param {!Node} node
       * @param {!InertRoot} inertRoot
       * @return {?InertNode} The potentially destroyed InertNode associated with this node, if any.
       */
    }, {
      key: "deregister",
      value: function deregister(node, inertRoot) {
        var inertNode = this._managedNodes.get(node);
        if (!inertNode) {
          return null;
        }
        inertNode.removeInertRoot(inertRoot);
        if (inertNode.destroyed) {
          this._managedNodes["delete"](node);
        }
        return inertNode;
      }
      /**
       * Callback used when document has finished loading.
       */
    }, {
      key: "_onDocumentLoaded",
      value: function _onDocumentLoaded() {
        var inertElements = slice.call(this._document.querySelectorAll("[inert]"));
        inertElements.forEach(function(inertElement) {
          this.setInert(inertElement, true);
        }, this);
        this._observer.observe(this._document.body || this._document.documentElement, { attributes: true, subtree: true, childList: true });
      }
      /**
       * Callback used when mutation observer detects attribute changes.
       * @param {!Array<!MutationRecord>} records
       * @param {!MutationObserver} self
       */
    }, {
      key: "_watchForInert",
      value: function _watchForInert(records, self2) {
        var _this = this;
        records.forEach(function(record) {
          switch (record.type) {
            case "childList":
              slice.call(record.addedNodes).forEach(function(node) {
                if (node.nodeType !== Node.ELEMENT_NODE) {
                  return;
                }
                var inertElements = slice.call(node.querySelectorAll("[inert]"));
                if (matches.call(node, "[inert]")) {
                  inertElements.unshift(node);
                }
                inertElements.forEach(function(inertElement) {
                  this.setInert(inertElement, true);
                }, _this);
              }, _this);
              break;
            case "attributes":
              if (record.attributeName !== "inert") {
                return;
              }
              var target = (
                /** @type {!Element} */
                record.target
              );
              var inert = target.hasAttribute("inert");
              _this.setInert(target, inert);
              break;
          }
        }, this);
      }
    }]);
    return InertManager2;
  }();
  function composedTreeWalk(node, callback, shadowRootAncestor) {
    if (node.nodeType == Node.ELEMENT_NODE) {
      var element = (
        /** @type {!Element} */
        node
      );
      if (callback) {
        callback(element);
      }
      var shadowRoot = (
        /** @type {!HTMLElement} */
        element.shadowRoot
      );
      if (shadowRoot) {
        composedTreeWalk(shadowRoot, callback, shadowRoot);
        return;
      }
      if (element.localName == "content") {
        var content = (
          /** @type {!HTMLContentElement} */
          element
        );
        var distributedNodes = content.getDistributedNodes ? content.getDistributedNodes() : [];
        for (var i = 0; i < distributedNodes.length; i++) {
          composedTreeWalk(distributedNodes[i], callback, shadowRootAncestor);
        }
        return;
      }
      if (element.localName == "slot") {
        var slot = (
          /** @type {!HTMLSlotElement} */
          element
        );
        var _distributedNodes = slot.assignedNodes ? slot.assignedNodes({ flatten: true }) : [];
        for (var _i = 0; _i < _distributedNodes.length; _i++) {
          composedTreeWalk(_distributedNodes[_i], callback, shadowRootAncestor);
        }
        return;
      }
    }
    var child = node.firstChild;
    while (child != null) {
      composedTreeWalk(child, callback, shadowRootAncestor);
      child = child.nextSibling;
    }
  }
  function addInertStyle(node) {
    if (node.querySelector("style#inert-style, link#inert-style")) {
      return;
    }
    var style = document.createElement("style");
    style.setAttribute("id", "inert-style");
    style.textContent = "\n[inert] {\n  pointer-events: none;\n  cursor: default;\n}\n\n[inert], [inert] * {\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  -ms-user-select: none;\n  user-select: none;\n}\n";
    node.appendChild(style);
  }
  var inertManager = new InertManager(document);
  if (!Element.prototype.hasOwnProperty("inert")) {
    Object.defineProperty(Element.prototype, "inert", {
      enumerable: true,
      /** @this {!Element} */
      get: function get() {
        return this.hasAttribute("inert");
      },
      /** @this {!Element} */
      set: function set(inert) {
        inertManager.setInert(this, inert);
      }
    });
  }
})();

// node_modules/@chelonia/lib/dist/esm/local-selectors/index.mjs
var local_selectors_default = esm_default("sbp/selectors/register", {
  // This selector sets up event listeners on EVENT_HANDLED and CONTRACTS_MODIFIED
  // to keep Chelonia state in sync with some external state (e.g., Vuex).
  // This needs to be called from the context that owns this external state
  // (e.g., the tab in which the app is running) and because 'full' Chelonia may
  // be available in this context, we cannot use `chelonia/configure`.
  // _If there is no external state to be kept in sync with Chelonia, this selector doesn't need to be called_
  //
  // For example, **if Chelonia is running on a service worker**, the following
  // would be done.
  // 1. The service worker calls `chelonia/configure` and forwards EVENT_HANDLED
  //    and CONTRACTS_MODIFIED events to all clients (tabs)
  //    Note: `chelonia/configure` is called by the context running Chelonia
  // 2. Each tab uses `chelonia/*` to forward calls to Chelonia to the SW.
  //    Note: Except selectors defined in this file
  // 3. Each tab calls this selector once to set up event listeners on EVENT_HANDLED
  //    and CONTRACTS_MODIFIED, which will keep each tab's state updated every
  //    time Chelonia handles an event.
  "chelonia/externalStateSetup": function({ stateSelector, reactiveSet = Reflect.set.bind(Reflect), reactiveDel = Reflect.deleteProperty.bind(Reflect) }) {
    this.stateSelector = stateSelector;
    esm_default("okTurtles.events/on", EVENT_HANDLED, (contractID, message) => {
      esm_default("okTurtles.eventQueue/queueEvent", EVENT_HANDLED, async () => {
        const { contractState, cheloniaState } = await esm_default("chelonia/contract/fullState", contractID);
        const externalState = esm_default(stateSelector);
        if (cheloniaState) {
          if (!externalState.contracts) {
            reactiveSet(externalState, "contracts", /* @__PURE__ */ Object.create(null));
          }
          reactiveSet(externalState.contracts, contractID, cloneDeep(cheloniaState));
        } else if (externalState.contracts) {
          reactiveDel(externalState.contracts, contractID);
        }
        if (contractState) {
          reactiveSet(externalState, contractID, cloneDeep(contractState));
        } else {
          reactiveDel(externalState, contractID);
        }
        esm_default("okTurtles.events/emit", EVENT_HANDLED_READY, contractID, message);
      });
    });
    esm_default("okTurtles.events/on", CONTRACTS_MODIFIED, (subscriptionSet, { added, removed, permanent }) => {
      esm_default("okTurtles.eventQueue/queueEvent", EVENT_HANDLED, async () => {
        const states = added.length ? await esm_default("chelonia/contract/fullState", added) : {};
        const vuexState = esm_default("state/vuex/state");
        if (!vuexState.contracts) {
          reactiveSet(vuexState, "contracts", /* @__PURE__ */ Object.create(null));
        }
        removed.forEach((contractID) => {
          if (permanent) {
            reactiveSet(vuexState.contracts, contractID, null);
          } else {
            reactiveDel(vuexState.contracts, contractID);
          }
          reactiveDel(vuexState, contractID);
        });
        for (const contractID of added) {
          const { contractState, cheloniaState } = states[contractID];
          if (cheloniaState) {
            reactiveSet(vuexState.contracts, contractID, cloneDeep(cheloniaState));
          }
          if (contractState) {
            reactiveSet(vuexState, contractID, cloneDeep(contractState));
          }
        }
        esm_default("okTurtles.events/emit", CONTRACTS_MODIFIED_READY, subscriptionSet, {
          added,
          removed
        });
      });
    });
  },
  // This function is similar in purpose to `chelonia/contract/wait`, except
  // that it's also designed to take into account delays copying Chelonia state
  // to an external state (e.g., when using `chelonia/externalStateSetup`).
  "chelonia/externalStateWait": async function(contractID) {
    await esm_default("chelonia/contract/wait", contractID);
    const { cheloniaState } = await esm_default("chelonia/contract/fullState", contractID);
    const localState = esm_default(this.stateSelector);
    if (!cheloniaState || cheloniaState.height <= localState.contracts[contractID]?.height)
      return;
    return new Promise((resolve) => {
      const removeListener = esm_default("okTurtles.events/on", EVENT_HANDLED_READY, (cID) => {
        if (cID !== contractID)
          return;
        const localState2 = esm_default(this.stateSelector);
        if (cheloniaState.height <= localState2.contracts[contractID]?.height) {
          resolve();
          removeListener();
        }
      });
    });
  }
});

// frontend/controller/app/chatroom.js
var switchCurrentChatRoomHandler = ({ identityContractID: identityContractID2, groupContractID, chatRoomID }) => {
  const rootState = esm_default("state/vuex/state");
  if (identityContractID2 && rootState.loggedIn?.identityContractID !== identityContractID2) return;
  if (!rootState[groupContractID]) return;
  if (rootState.chatroom.currentChatRoomIDs[groupContractID] === chatRoomID) {
    esm_default("state/vuex/commit", "setCurrentChatRoomId", { groupID: groupContractID });
  }
};
esm_default("okTurtles.events/on", JOINED_CHATROOM, ({ identityContractID: identityContractID2, groupContractID, chatRoomID }) => {
  const rootState = esm_default("state/vuex/state");
  if (rootState.loggedIn?.identityContractID !== identityContractID2) return;
  if (!rootState.chatroom.currentChatRoomIDs[groupContractID] || rootState.chatroom.pendingChatRoomIDs[groupContractID] === chatRoomID) {
    let attemptCount = 0;
    const setCurrentChatRoomId = () => {
      const rootGetters = esm_default("state/vuex/getters");
      if (!rootGetters.isJoinedChatRoom(chatRoomID, identityContractID2)) {
        if (++attemptCount > 5) {
          console.warn("[JOINED_CHATROOM] Given up on setCurrentChatRoomId after 5 attempts", { identityContractID: identityContractID2, groupContractID, chatRoomID });
          return;
        }
        setTimeout(setCurrentChatRoomId, 5 * Math.pow(1.75, attemptCount));
      } else {
        esm_default("state/vuex/commit", "setCurrentChatRoomId", { groupID: groupContractID, chatRoomID });
      }
    };
    setCurrentChatRoomId();
  }
});
esm_default("okTurtles.events/on", LEFT_CHATROOM, switchCurrentChatRoomHandler);
esm_default("okTurtles.events/on", DELETED_CHATROOM, switchCurrentChatRoomHandler);
esm_default("okTurtles.events/on", NEW_CHATROOM_SCROLL_POSITION, ({ chatRoomID, messageHash }) => {
  if (messageHash) {
    esm_default("state/vuex/commit", "setChatRoomScrollPosition", { chatRoomID, messageHash });
  } else {
    esm_default("state/vuex/commit", "deleteChatRoomScrollPosition", { chatRoomID });
  }
});
esm_default("okTurtles.events/on", NEW_CHATROOM_NOTIFICATION_SETTINGS, ({ chatRoomID, settings }) => {
  esm_default("state/vuex/commit", "setChatroomNotificationSettings", {
    chatRoomID,
    settings
  });
});

// frontend/controller/app/group.js
esm_default("okTurtles.events/on", ERROR_GROUP_GENERAL_CHATROOM_DOES_NOT_EXIST, ({ identityContractID: identityContractID2, groupContractID }) => {
  if ("") {
    console.error("Error ERROR_GROUP_GENERAL_CHATROOM_DOES_NOT_EXIST", { identityContractID: identityContractID2, groupContractID });
    Promise.reject(new Error("ERROR_GROUP_GENERAL_CHATROOM_DOES_NOT_EXIST"));
  }
  const rootState = esm_default("state/vuex/state");
  if (rootState.loggedIn?.identityContractID !== identityContractID2) return;
  if (!rootState[groupContractID]) return;
  esm_default("chelonia/contract/wait", groupContractID).then(() => {
    const ourGroups = esm_default("state/vuex/getters").ourGroups;
    if (!ourGroups.includes(groupContractID)) return;
    const rootState2 = esm_default("state/vuex/state");
    if (!rootState2[groupContractID].generalChatRoomId) {
      esm_default("gi.ui/prompt", {
        heading: L("Error joining the #general chatroom"),
        question: L("There was an error joining the #general chatroom because it doesn't exist"),
        primaryButton: L("Close")
      });
    }
  });
});
esm_default("okTurtles.events/on", ERROR_JOINING_CHATROOM, ({ identityContractID: identityContractID2, groupContractID, chatRoomID }) => {
  if ("") {
    console.error("Error ERROR_JOINING_CHATROOM", { identityContractID: identityContractID2, groupContractID, chatRoomID });
    Promise.reject(new Error("ERROR_JOINING_CHATROOM"));
  }
  const rootState = esm_default("state/vuex/state");
  if (rootState.loggedIn?.identityContractID !== identityContractID2) return;
  if (!rootState[groupContractID]) return;
  esm_default("chelonia/contract/wait", groupContractID).then(() => {
    const ourGroups = esm_default("state/vuex/getters").ourGroups;
    if (!ourGroups.includes(groupContractID)) return;
    const rootState2 = esm_default("state/vuex/state");
    const rootGetters = esm_default("state/vuex/getters");
    if (rootState2[groupContractID].chatRooms[chatRoomID]?.members[identityContractID2]?.status === PROFILE_STATUS.ACTIVE && !rootGetters.isJoinedChatRoom(chatRoomID, identityContractID2)) {
      esm_default("gi.ui/prompt", {
        heading: L("Error joining chatroom"),
        question: L("There was an error joining the {chatRoomName} chatroom", {
          chatRoomName: rootState2[groupContractID].chatRooms[chatRoomID]?.name ? `#${rootState2[groupContractID].chatRooms[chatRoomID].name}` : L("(unknown)")
        }),
        primaryButton: L("Close")
      });
    }
  });
});
esm_default("okTurtles.events/on", JOINED_GROUP, ({ identityContractID: identityContractID2, groupContractID }) => {
  const rootState = esm_default("state/vuex/state");
  if (rootState.loggedIn?.identityContractID !== identityContractID2) return;
  if (!rootState[groupContractID]) return;
  if (!rootState.currentGroupId) {
    esm_default("state/vuex/commit", "setCurrentGroupId", { contractID: groupContractID });
    esm_default("state/vuex/commit", "setCurrentChatRoomId", {});
  }
});
esm_default("okTurtles.events/on", LEFT_GROUP, ({ identityContractID: identityContractID2, groupContractID }) => {
  const rootState = esm_default("state/vuex/state");
  if (rootState.loggedIn?.identityContractID !== identityContractID2) return;
  const state = rootState[identityContractID2];
  const currentGroupId = rootState.currentGroupId;
  if (!currentGroupId || currentGroupId === groupContractID) {
    const groupIdToSwitch = Object.entries(state.groups).map(([cID, { hasLeft }]) => !hasLeft && cID).filter(
      (cID) => cID && cID !== groupContractID
    ).sort(
      (cID) => (
        // prefer successfully joined groups
        esm_default("state/vuex/state")[cID]?.profiles?.[groupContractID] ? -1 : 1
      )
    )[0] || null;
    esm_default("state/vuex/commit", "setCurrentChatRoomId", {});
    esm_default("state/vuex/commit", "setCurrentGroupId", { contractID: groupIdToSwitch, forceRefresh: currentGroupId === groupContractID });
  }
});
esm_default("okTurtles.events/on", NEW_LAST_LOGGED_IN, ([contractID, data]) => {
  esm_default("state/vuex/commit", "setLastLoggedIn", [contractID, data]);
});
var group_default = esm_default("sbp/selectors/register", {
  "gi.app/group/createAndSwitch": async function(params) {
    const contractID = await esm_default("gi.actions/group/create", params);
    esm_default("gi.app/group/switch", contractID, true);
    return contractID;
  },
  "gi.app/group/switch": function(groupId, isNewlyCreated) {
    esm_default("okTurtles.events/emit", SWITCH_GROUP, { contractID: groupId, isNewlyCreated });
    esm_default("state/vuex/commit", "setCurrentGroupId", { contractID: groupId, isNewlyCreated });
  },
  "gi.app/group/joinAndSwitch": async function(params) {
    await esm_default("gi.actions/group/join", params);
    return esm_default("gi.app/group/switch", params.contractID, true);
  },
  "gi.app/group/joinWithInviteSecret": async function(groupId, secret) {
    const result = await esm_default("gi.actions/group/joinWithInviteSecret", groupId, secret);
    esm_default("gi.app/group/switch", groupId, true);
    return result;
  },
  "gi.app/group/addAndJoinChatRoom": async function(params) {
    const chatRoomID = await esm_default("gi.actions/group/addAndJoinChatRoom", params);
    esm_default("state/vuex/commit", "setPendingChatRoomId", { chatRoomID, groupID: params.contractID });
    return chatRoomID;
  },
  "gi.app/group/checkGroupSizeAndProposeMember": async function() {
    const enforceDunbar = true;
    const { groupMembersCount, currentGroupState } = esm_default("state/vuex/getters");
    const memberInvitesCount = Object.values(currentGroupState.invites || {}).filter((invite) => invite.creatorID !== INVITE_INITIAL_CREATOR).length;
    const isGroupSizeLarge = groupMembersCount + memberInvitesCount >= MAX_GROUP_MEMBER_COUNT;
    if (isGroupSizeLarge) {
      const translationArgs = {
        a_: `<a class='link' href='${allowedUrls_default.WIKIPEDIA_DUNBARS_NUMBER}' target='_blank'>`,
        _a: "</a>"
      };
      const promptConfig = enforceDunbar ? {
        heading: L("Large group size"),
        question: L("Group sizes are limited to {a_}Dunbar's Number{_a} to prevent fraud.", translationArgs),
        primaryButton: L("OK")
      } : {
        heading: L("Large group size"),
        question: L("Groups over 150 members are at significant risk for fraud, {a_}because it is difficult to verify everyone's identity.{_a} Are you sure that you want to add more members?", translationArgs),
        primaryButton: L("Yes"),
        secondaryButton: L("Cancel")
      };
      const primaryButtonSelected = await esm_default("gi.ui/prompt", promptConfig);
      if (!enforceDunbar && primaryButtonSelected) {
        esm_default("okTurtles.events/emit", REPLACE_MODAL, "AddMembers");
      } else return false;
    } else {
      esm_default("okTurtles.events/emit", OPEN_MODAL, "AddMembers");
    }
  },
  "gi.app/group/checkAndSeeProposal": function({ contractID, data }) {
    const rootGetters = esm_default("state/vuex/getters");
    const rootState = esm_default("state/vuex/state");
    if (rootState.currentGroupId !== contractID) {
      esm_default("state/vuex/commit", "setCurrentGroupId", { contractID });
    }
    const openProposalIds = Object.keys(rootGetters.currentGroupState.proposals || {});
    if (openProposalIds.includes(data.proposalHash)) {
      esm_default("controller/router").push({ path: "/dashboard#proposals" });
    } else {
      esm_default("okTurtles.events/emit", OPEN_MODAL, "PropositionsAllModal", { targetProposal: data.proposalHash });
    }
  },
  "gi.app/group/displayMincomeChangedPrompt": async function({ contractID, data }) {
    const rootState = esm_default("state/vuex/state");
    if (rootState.currentGroupId !== contractID) {
      esm_default("state/vuex/commit", "setCurrentGroupId", { contractID });
    }
    const promptOptions = data.increased ? {
      heading: L("Mincome changed"),
      question: L("Do you make at least {amount} per month?", { amount: withGroupCurrency(data.amount) }),
      primaryButton: data.memberType === "pledging" ? L("No") : L("Yes"),
      secondaryButton: data.memberType === "pledging" ? L("Yes") : L("No")
    } : {
      heading: L("Automatically switched to pledging {zero}", { zero: withGroupCurrency(0) }),
      question: L("You now make more than the mincome. Would you like to increase your pledge?"),
      primaryButton: L("Yes"),
      secondaryButton: L("No")
    };
    const primaryButtonSelected = await esm_default("gi.ui/prompt", promptOptions);
    if (primaryButtonSelected) {
      esm_default("okTurtles.events/emit", REPLACE_MODAL, "IncomeDetails");
    }
  }
});

// frontend/controller/app/identity.js
init_vue_esm();
var import_buffer2 = __toESM(require_buffer());

// node_modules/@chelonia/lib/dist/esm/zkpp.mjs
var import_buffer = __toESM(require_buffer(), 1);
var import_scrypt_async = __toESM(require_scrypt_async(), 1);
var import_tweetnacl = __toESM(require_nacl_fast(), 1);

// node_modules/@chelonia/lib/dist/esm/zkppConstants.mjs
var AUTHSALT = "AUTHSALT";
var CONTRACTSALT = "CONTRACTSALT";
var CS = "CS";
var SU = "SU";
var SALT_LENGTH_IN_OCTETS = 24;

// node_modules/@chelonia/lib/dist/esm/zkpp.mjs
var base64ToBase64url = (s) => s.replace(/\//g, "_").replace(/\+/g, "-").replace(/=*$/, "");
var base64urlToBase64 = (s) => s.replace(/_/g, "/").replace(/-/g, "+") + "=".repeat((4 - s.length % 4) % 4);
var hashStringArray = (...args) => {
  return import_tweetnacl.default.hash(import_buffer.Buffer.concat(args.map((s) => import_tweetnacl.default.hash(import_buffer.Buffer.from(s)))));
};
var hashRawStringArray = (...args) => {
  return import_tweetnacl.default.hash(import_buffer.Buffer.concat(args.map((s) => import_buffer.Buffer.from(s))));
};
var randomNonce = () => {
  return base64ToBase64url(import_buffer.Buffer.from(import_tweetnacl.default.randomBytes(12)).toString("base64"));
};
var hash = (v) => {
  return base64ToBase64url(import_buffer.Buffer.from(import_tweetnacl.default.hash(import_buffer.Buffer.from(v))).toString("base64"));
};
var computeCAndHc = (r2, s, h) => {
  const \u0127 = hashStringArray(r2, s);
  const c = hashStringArray(h, \u0127);
  const hc = import_tweetnacl.default.hash(c);
  return [c, hc];
};
var decryptContractSalt = (c, encryptedContractSaltBox) => {
  const encryptionKey = hashRawStringArray(CS, c).slice(0, import_tweetnacl.default.secretbox.keyLength);
  const encryptedContractSaltBoxBuf = import_buffer.Buffer.from(base64urlToBase64(encryptedContractSaltBox), "base64");
  const nonce = encryptedContractSaltBoxBuf.subarray(0, import_tweetnacl.default.secretbox.nonceLength);
  const encryptedContractSalt = encryptedContractSaltBoxBuf.subarray(import_tweetnacl.default.secretbox.nonceLength);
  const decrypted = import_tweetnacl.default.secretbox.open(encryptedContractSalt, nonce, encryptionKey);
  if (!decrypted)
    throw new Error("Failed to decrypt contract salt");
  return import_buffer.Buffer.from(decrypted).toString();
};
var hashPassword = (password, salt) => {
  return new Promise((resolve) => (0, import_scrypt_async.default)(password, salt, {
    N: 16384,
    r: 8,
    p: 1,
    dkLen: 32,
    encoding: "hex"
  }, resolve));
};
var boxKeyPair = () => {
  return import_tweetnacl.default.box.keyPair();
};
var saltAgreement = (publicKey, secretKey) => {
  const publicKeyBuf = import_buffer.Buffer.from(base64urlToBase64(publicKey), "base64");
  const dhKey = import_tweetnacl.default.box.before(publicKeyBuf, secretKey);
  if (!publicKeyBuf || publicKeyBuf.byteLength !== import_tweetnacl.default.box.publicKeyLength) {
    return false;
  }
  const authSalt = import_buffer.Buffer.from(hashStringArray(AUTHSALT, dhKey)).subarray(0, SALT_LENGTH_IN_OCTETS).toString("base64");
  const contractSalt = import_buffer.Buffer.from(hashStringArray(CONTRACTSALT, dhKey)).subarray(0, SALT_LENGTH_IN_OCTETS).toString("base64");
  return [authSalt, contractSalt];
};
var buildRegisterSaltRequest = async (publicKey, secretKey, password) => {
  const saltAgreementRes = saltAgreement(publicKey, secretKey);
  if (!saltAgreementRes) {
    throw new Error("Invalid public or secret key");
  }
  const [authSalt, contractSalt] = saltAgreementRes;
  const hashedPassword = await hashPassword(password, authSalt);
  const nonce = import_tweetnacl.default.randomBytes(import_tweetnacl.default.box.nonceLength);
  const encryptionKey = import_tweetnacl.default.hash(import_buffer.Buffer.from(authSalt + contractSalt)).slice(0, import_tweetnacl.default.secretbox.keyLength);
  const encryptedHashedPasswordBuf = import_tweetnacl.default.secretbox(import_buffer.Buffer.from(hashedPassword), nonce, encryptionKey);
  return [
    contractSalt,
    base64ToBase64url(import_buffer.Buffer.concat([nonce, encryptedHashedPasswordBuf]).toString("base64")),
    encryptionKey
  ];
};
var buildUpdateSaltRequestEc = async (password, c) => {
  const authSalt = import_buffer.Buffer.from(hashStringArray(AUTHSALT, c)).subarray(0, SALT_LENGTH_IN_OCTETS).toString("base64");
  const contractSalt = import_buffer.Buffer.from(hashStringArray(CONTRACTSALT, c)).subarray(0, SALT_LENGTH_IN_OCTETS).toString("base64");
  const encryptionKey = hashRawStringArray(SU, c).slice(0, import_tweetnacl.default.secretbox.keyLength);
  const nonce = import_tweetnacl.default.randomBytes(import_tweetnacl.default.secretbox.nonceLength);
  const hashedPassword = await hashPassword(password, authSalt);
  const encryptedArgsCiphertext = import_tweetnacl.default.secretbox(import_buffer.Buffer.from(hashedPassword), nonce, encryptionKey);
  const encryptedArgs = import_buffer.Buffer.concat([nonce, encryptedArgsCiphertext]);
  return [contractSalt, base64ToBase64url(encryptedArgs.toString("base64"))];
};

// frontend/model/database.js
var _instances = [];
var localforage = {
  async ready() {
    await Promise.all(_instances.map((lazyInitDb) => lazyInitDb()));
  },
  createInstance({ name, storeName }) {
    const lazyInitDb = /* @__PURE__ */ (() => {
      let promise;
      return () => {
        if (!promise) {
          promise = new Promise((resolve, reject) => {
            if (name.includes("-") || storeName.includes("-")) {
              reject(new Error("Unsupported characters in name: -"));
              return;
            }
            const openDB = (version) => {
              const request = self.indexedDB.open(name + "--" + storeName, version);
              request.onupgradeneeded = (event) => {
                const db = event.target.result;
                db.createObjectStore(storeName);
              };
              request.onsuccess = (event) => {
                const db = event.target.result;
                if (!db.objectStoreNames.contains(storeName)) {
                  return openDB(db.version + 1);
                }
                resolve(db);
              };
              request.onerror = (error) => {
                reject(error);
              };
              request.onblocked = (event) => {
                reject(new Error("DB is blocked"));
              };
            };
            openDB();
          });
        }
        return promise;
      };
    })();
    _instances.push(lazyInitDb);
    return {
      async clear() {
        const db = await lazyInitDb();
        const transaction = db.transaction([storeName], "readwrite");
        const objectStore = transaction.objectStore(storeName);
        const request = objectStore.clear();
        return new Promise((resolve, reject) => {
          request.onsuccess = () => {
            resolve();
          };
          request.onerror = (e) => {
            reject(e);
          };
        });
      },
      async getItem(key) {
        const db = await lazyInitDb();
        const transaction = db.transaction([storeName], "readonly");
        const objectStore = transaction.objectStore(storeName);
        const request = objectStore.get(key);
        return new Promise((resolve, reject) => {
          request.onsuccess = (event) => {
            resolve(event.target.result);
          };
          request.onerror = (e) => {
            reject(e);
          };
        });
      },
      async removeItem(key) {
        const db = await lazyInitDb();
        const transaction = db.transaction([storeName], "readwrite");
        const objectStore = transaction.objectStore(storeName);
        const request = objectStore.delete(key);
        return new Promise((resolve, reject) => {
          request.onsuccess = () => {
            resolve();
          };
          request.onerror = (e) => {
            reject(e.target.error);
          };
        });
      },
      async removeMany(keys) {
        const db = await lazyInitDb();
        const transaction = db.transaction([storeName], "readwrite");
        const objectStore = transaction.objectStore(storeName);
        for (const key of keys) {
          objectStore.delete(key);
        }
        return new Promise((resolve, reject) => {
          transaction.oncomplete = () => resolve();
          transaction.onerror = (e) => reject(e.target.error);
          transaction.onabort = (e) => reject(e.target.error);
        });
      },
      async setItem(key, value) {
        const db = await lazyInitDb();
        const transaction = db.transaction([storeName], "readwrite");
        const objectStore = transaction.objectStore(storeName);
        const request = objectStore.put(value, key);
        return new Promise((resolve, reject) => {
          request.onsuccess = () => {
            resolve();
          };
          request.onerror = (e) => {
            reject(e.target.error);
          };
        });
      }
    };
  }
};
var generateEncryptionParams = async (stateKeyEncryptionKeyFn) => {
  const stateEncryptionKey = keygen(CURVE25519XSALSA20POLY1305);
  const stateEncryptionKeyId = keyId(stateEncryptionKey);
  const stateEncryptionKeyS = serializeKey(stateEncryptionKey, true);
  const stateEncryptionKeyP = serializeKey(stateEncryptionKey, false);
  const salt = generateSalt();
  const stateKeyEncryptionKey = await stateKeyEncryptionKeyFn(stateEncryptionKeyId, salt);
  const encryptedStateEncryptionKey = encrypt(stateKeyEncryptionKey, stateEncryptionKeyS, stateEncryptionKeyId);
  return {
    encryptionParams: {
      stateEncryptionKeyId,
      salt,
      encryptedStateEncryptionKey
    },
    stateEncryptionKeyP
  };
};
if (false) {
  const log = localforage.createInstance({
    name: "Group Income",
    storeName: "Contracts"
  });
  esm_default("sbp/selectors/overwrite", {
    "chelonia.db/get": (key) => log.getItem(key),
    // TODO: handle QuotaExceededError
    "chelonia.db/set": (key, value) => log.setItem(key, value),
    "chelonia.db/delete": (key) => log.removeItem(key)
  });
  esm_default("sbp/selectors/lock", ["chelonia.db/get", "chelonia.db/set", "chelonia.db/delete"]);
}
var appSettings = localforage.createInstance({
  name: "Group Income",
  storeName: "Settings"
});
var EmptyValue = class extends Error {
};
var SETTING_CURRENT_USER = "@settings/currentUser";
var SETTING_CHELONIA_STATE = "CHELONIA_STATE";
esm_default("sbp/selectors/register", {
  "gi.db/ready": function() {
    return localforage.ready();
  },
  "gi.db/settings/save": function(key, value) {
    return appSettings.setItem("u" + key, value);
  },
  "gi.db/settings/load": function(key) {
    return appSettings.getItem("u" + key);
  },
  "gi.db/settings/delete": function(key) {
    return appSettings.removeItem("u" + key);
  },
  "gi.db/settings/saveEncrypted": async function(key, value, encryptionParams) {
    const {
      stateEncryptionKeyId,
      salt,
      encryptedStateEncryptionKey
    } = encryptionParams;
    const stateEncryptionKeyP = await appSettings.getItem("k" + stateEncryptionKeyId);
    if (!stateEncryptionKeyP) throw new Error(`Unable to retrieve the key corresponding to key ID ${stateEncryptionKeyId}`);
    const encryptedState = encrypt(stateEncryptionKeyP, JSON.stringify(value), key);
    return appSettings.setItem("e" + key, `${btoa(stateEncryptionKeyId)}.${btoa(salt)}.${btoa(encryptedStateEncryptionKey)}.${btoa(encryptedState)}`).finally(() => {
      esm_default("gi.db/settings/delete", key).catch((e) => {
        console.error("[gi.db/settings/saveEncrypted] Error deleting unencrypted data for key", key, e);
      });
    });
  },
  "gi.db/settings/loadEncrypted": function(key, stateKeyEncryptionKeyFn) {
    return appSettings.getItem("e" + key).then(async (encryptedValue) => {
      if (!encryptedValue || typeof encryptedValue !== "string") {
        throw new EmptyValue(`Unable to retrive state for ${key || ""}`);
      }
      const [stateEncryptionKeyId, salt, encryptedStateEncryptionKey, data] = encryptedValue.split(".").map((x) => atob(x));
      const stateKeyEncryptionKey = await stateKeyEncryptionKeyFn(stateEncryptionKeyId, salt);
      const stateEncryptionKeyS = decrypt(stateKeyEncryptionKey, encryptedStateEncryptionKey, stateEncryptionKeyId);
      const stateEncryptionKeyIdActual = keyId(stateEncryptionKeyS);
      if (stateEncryptionKeyIdActual !== stateEncryptionKeyId) {
        throw new Error(`Invalid state key ID: expected ${stateEncryptionKeyId} but got ${stateEncryptionKeyIdActual}`);
      }
      const value = JSON.parse(decrypt(stateEncryptionKeyS, data, key || ""));
      await appSettings.setItem("k" + stateEncryptionKeyId, stateEncryptionKeyS);
      return {
        encryptionParams: {
          stateEncryptionKeyId,
          salt,
          encryptedStateEncryptionKey
        },
        value
      };
    }).catch(async (e) => {
      if (!stateKeyEncryptionKeyFn) {
        throw e;
      }
      if (!(e instanceof EmptyValue)) {
        console.warn("Error while retrieving local state", e);
      }
      const { encryptionParams, stateEncryptionKeyP } = await generateEncryptionParams(stateKeyEncryptionKeyFn);
      await appSettings.setItem("k" + encryptionParams.stateEncryptionKeyId, stateEncryptionKeyP);
      return {
        encryptionParams,
        value: null
      };
    });
  },
  "gi.db/settings/deleteStateEncryptionKey": function({ stateEncryptionKeyId }) {
    return appSettings.removeItem("k" + stateEncryptionKeyId);
  },
  "gi.db/settings/deleteEncrypted": function(key) {
    return appSettings.removeItem("e" + key);
  }
});
var filesCache = localforage.createInstance({
  name: "Group Income",
  storeName: "Files Cache"
});
var maxFileEntries = 100;
esm_default("sbp/selectors/register", {
  "gi.db/filesCache/save": async function(cacheKey, blob) {
    if (cacheKey.startsWith("__")) throw new Error("Invalid key");
    const keys = await filesCache.getItem("keys") ?? [];
    return esm_default("okTurtles.eventQueue/queueEvent", "gi.db/files", () => {
      return filesCache.setItem(cacheKey, blob).then(async (v) => {
        console.log("successfully saved:", cacheKey);
        const idx = keys.indexOf(cacheKey);
        if (idx !== -1) {
          keys.splice(idx, 1);
        }
        keys.push(cacheKey);
        if (keys.length > maxFileEntries) {
          const last = keys.splice(0, keys.length - maxFileEntries);
          await Promise.all(last.map((e) => filesCache.removeItem(e)));
        }
        await filesCache.setItem("keys", keys);
      }).catch((e) => {
        console.error("error saving:", cacheKey, e);
      });
    });
  },
  "gi.db/filesCache/load": async function(cacheKey) {
    if (cacheKey.startsWith("__")) throw new Error("Invalid key");
    const file = await filesCache.getItem(cacheKey);
    if (file) {
      esm_default("okTurtles.eventQueue/queueEvent", "gi.db/files", async () => {
        const keys = await filesCache.getItem("keys") ?? [];
        const idx = keys.indexOf(cacheKey);
        if (idx !== -1) {
          keys.splice(idx, 1);
          keys.push(cacheKey);
          await filesCache.setItem("keys", keys);
        }
      }).catch((e) => {
        console.error("[gi.db/filesCache/load] Error updating keys");
      });
    }
    return file;
  },
  "gi.db/filesCache/delete": async function(cacheKey) {
    if (cacheKey.startsWith("__")) throw new Error("Invalid key");
    await filesCache.removeItem(cacheKey);
    esm_default("okTurtles.eventQueue/queueEvent", "gi.db/files", async () => {
      const keys = await filesCache.getItem("keys") ?? [];
      const idx = keys.indexOf(cacheKey);
      if (idx !== -1) {
        keys.splice(idx, 1);
        await filesCache.setItem("keys", keys);
      }
    }).catch((e) => {
      console.error("[gi.db/filesCache/delete] Error updating keys");
    });
  },
  "gi.db/filesCache/clear": async function() {
    await filesCache.clear();
  },
  "gi.db/filesCache/temporary/save": function(cacheKey, blob) {
    if (cacheKey.startsWith("__")) throw new Error("Invalid key");
    return esm_default("gi.db/filesCache/save", `temporary/${cacheKey}/`, blob);
  },
  "gi.db/filesCache/temporary/load": function(cacheKey) {
    if (cacheKey.startsWith("__")) throw new Error("Invalid key");
    return esm_default("gi.db/filesCache/load", `temporary/${cacheKey}/`);
  },
  "gi.db/filesCache/temporary/delete": function(cacheKey) {
    if (cacheKey.startsWith("__")) throw new Error("Invalid key");
    return esm_default("gi.db/filesCache/delete", `temporary/${cacheKey}/`);
  },
  "gi.db/filesCache/temporary/clear": function() {
    esm_default("okTurtles.eventQueue/queueEvent", "gi.db/files", async () => {
      const keys = await filesCache.getItem("keys") ?? [];
      const allTempKeys = keys.filter((k) => k.startsWith("temporary/"));
      await filesCache.removeMany(allTempKeys);
    }).catch((e) => {
      console.error("[gi.db/filesCache/temporary/clear] Error removing temporary keys", e);
    });
  }
});
var archive = localforage.createInstance({
  name: "Group Income",
  storeName: "Archive"
});
esm_default("sbp/selectors/register", {
  "gi.db/archive/save": function(key, value) {
    return archive.setItem(key, value);
  },
  "gi.db/archive/load": function(key) {
    return archive.getItem(key);
  },
  "gi.db/archive/delete": function(key) {
    return archive.removeItem(key);
  },
  "gi.db/archive/clear": function() {
    return archive.clear();
  }
});
var logs = localforage.createInstance({
  name: "Group Income",
  storeName: "Logs"
});
esm_default("sbp/selectors/register", {
  "gi.db/logs/save": function(key, value) {
    return logs.setItem(key, value);
  },
  "gi.db/logs/load": function(key) {
    return logs.getItem(key);
  },
  "gi.db/logs/delete": function(key) {
    return logs.removeItem(key);
  },
  "gi.db/logs/clear": function() {
    return logs.clear();
  }
});

// frontend/controller/utils/misc.js
function handleFetchResult(type) {
  return function(r2) {
    if (!r2.ok) {
      const msg = `${r2.status}: ${r2.statusText}`;
      if (r2.status === 404 || r2.status === 410) throw new ChelErrorResourceGone(msg, { cause: r2.status });
      throw new ChelErrorUnexpectedHttpResponseCode(msg);
    }
    return r2[type]();
  };
}

// frontend/controller/app/identity.js
var loadState = async (identityContractID2, password) => {
  if (password) {
    const stateKeyEncryptionKeyFn = (stateEncryptionKeyId, salt) => {
      return deriveKeyFromPassword(CURVE25519XSALSA20POLY1305, password, salt + stateEncryptionKeyId);
    };
    const { encryptionParams, value: state } = await esm_default("gi.db/settings/loadEncrypted", identityContractID2, stateKeyEncryptionKeyFn);
    if (state) {
      const cheloniaState = state.cheloniaState;
      delete state.cheloniaState;
      return { encryptionParams, state, cheloniaState };
    } else {
      return { encryptionParams, state: null, cheloniaState: null };
    }
  } else {
    const state = await esm_default("gi.db/settings/load", identityContractID2);
    return { encryptionParams: null, state, cheloniaState: null };
  }
};
esm_default("okTurtles.events/on", LOGIN, async ({ identityContractID: identityContractID2, encryptionParams, state }) => {
  await esm_default("okTurtles.eventQueue/queueEvent", EVENT_HANDLED, async () => {
    try {
      const vuexState = esm_default("state/vuex/state");
      if (vuexState.loggedIn && vuexState.loggedIn.identityContractID !== identityContractID2) {
        console.error("Received login event during active session", { receivedIdentityContractID: identityContractID2, existingIdentityContractID: vuexState.loggedIn.identityContractID });
        throw new Error("Received login event but there already is an active session");
      }
      const cheloniaState = cloneDeep(await esm_default("chelonia/rootState"));
      if (state) {
        if (state.contracts) {
          Object.keys(state.contracts).forEach((k) => {
            delete state[k];
          });
        }
        Object.keys(cheloniaState.contracts).forEach((k) => {
          if (cheloniaState[k]) {
            state[k] = cheloniaState[k];
          }
        });
        state.contracts = cheloniaState.contracts;
        if (cheloniaState.namespaceLookups) {
          state.namespaceLookups = cheloniaState.namespaceLookups;
        }
        esm_default("state/vuex/postUpgradeVerification", state);
        esm_default("state/vuex/replace", state);
      } else {
        const state2 = vuexState;
        if (state2.contracts) {
          Object.keys(state2.contracts).forEach((k) => {
            vue_esm_default.delete(state2, k);
          });
        }
        Object.keys(cheloniaState.contracts).forEach((k) => {
          if (cheloniaState[k]) {
            vue_esm_default.set(state2, k, cheloniaState[k]);
          }
        });
        vue_esm_default.set(state2, "contracts", cheloniaState.contracts);
        if (cheloniaState.namespaceLookups) {
          vue_esm_default.set(state2, "namespaceLookups", cheloniaState.namespaceLookups);
        }
        esm_default("state/vuex/postUpgradeVerification", state2);
      }
      if (encryptionParams) {
        esm_default("state/vuex/commit", "login", { identityContractID: identityContractID2, encryptionParams });
      }
      const currentState = esm_default("state/vuex/state");
      if (!currentState.currentGroupId) {
        const gId = Object.keys(currentState.contracts).find((cID) => currentState[identityContractID2].groups[cID] && !currentState[identityContractID2].groups[cID].hasLeft);
        if (gId) {
          esm_default("gi.app/group/switch", gId);
        }
      }
      esm_default("gi.db/settings/deleteEncrypted", identityContractID2).catch((e) => {
        console.error("Error deleting encrypted settings after login");
      });
      esm_default("okTurtles.events/emit", LOGIN_COMPLETE, { identityContractID: identityContractID2 });
    } catch (e) {
      esm_default("okTurtles.events/emit", LOGIN_ERROR, { identityContractID: identityContractID2, error: e });
    }
  });
});
esm_default("okTurtles.events/on", NEW_UNREAD_MESSAGES, (currentChatRoomUnreadMessages) => {
  esm_default("state/vuex/commit", "setUnreadMessages", currentChatRoomUnreadMessages);
});
esm_default("okTurtles.events/on", NEW_PREFERENCES, (preferences) => {
  esm_default("state/vuex/commit", "setPreferences", preferences);
});
esm_default("okTurtles.events/on", NEW_KV_LOAD_STATUS, (data) => {
  esm_default("state/vuex/commit", "setKvStoreStatus", data);
});
var identity_default = esm_default("sbp/selectors/register", {
  "gi.app/identity/retrieveSalt": async (identityContractID2, password) => {
    const r2 = randomNonce();
    const b = hash(r2);
    const authHash = await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/zkpp/${encodeURIComponent(identityContractID2)}/auth_hash?b=${encodeURIComponent(b)}`).then(handleFetchResult("json"));
    const { authSalt, s, sig } = authHash;
    const h = await hashPassword(password.valueOf(), authSalt);
    const [c, hc] = computeCAndHc(r2, s, h);
    const contractHash = await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/zkpp/${encodeURIComponent(identityContractID2)}/contract_hash?${new URLSearchParams({
      "r": r2,
      "s": s,
      "sig": sig,
      "hc": import_buffer2.Buffer.from(hc).toString("base64").replace(/\//g, "_").replace(/\+/g, "-").replace(/=*$/, "")
    }).toString()}`).then(handleFetchResult("text"));
    return JSON.parse(decryptContractSalt(c, contractHash));
  },
  "gi.app/identity/updateSaltRequest": async (identityContractID2, oldPassword, newPassword) => {
    const r2 = randomNonce();
    const b = hash(r2);
    const authHash = await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/zkpp/${encodeURIComponent(identityContractID2)}/auth_hash?b=${encodeURIComponent(b)}`).then(handleFetchResult("json"));
    const { authSalt, s, sig } = authHash;
    const h = await hashPassword(oldPassword.valueOf(), authSalt);
    const [c, hc] = computeCAndHc(r2, s, h);
    const [contractSalt, encryptedArgs] = await buildUpdateSaltRequestEc(newPassword.valueOf(), c);
    const response = await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/zkpp/${encodeURIComponent(identityContractID2)}/updatePasswordHash`, {
      method: "POST",
      headers: {
        "content-type": "application/x-www-form-urlencoded"
      },
      body: `${new URLSearchParams({
        "r": r2,
        "s": s,
        "sig": sig,
        "hc": import_buffer2.Buffer.from(hc).toString("base64").replace(/\//g, "_").replace(/\+/g, "-").replace(/=*$/, ""),
        "Ea": encryptedArgs
      }).toString()}`
    }).then(handleFetchResult("text"));
    const [oldContractSalt, updateToken] = JSON.parse(decryptContractSalt(c, response));
    return [contractSalt, oldContractSalt, updateToken];
  },
  "gi.app/identity/create": async function({
    data: { username, email, password, picture },
    publishOptions
  }) {
    password = password.valueOf();
    const keyPair = boxKeyPair();
    const r2 = import_buffer2.Buffer.from(keyPair.publicKey).toString("base64").replace(/\//g, "_").replace(/\+/g, "-");
    const b = hash(r2);
    const registrationRes = await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/zkpp/register/${encodeURIComponent(username)}`, {
      method: "POST",
      headers: {
        "content-type": "application/x-www-form-urlencoded"
      },
      body: `b=${encodeURIComponent(b)}`
    }).then(handleFetchResult("json"));
    const { p, s, sig } = registrationRes;
    const [contractSalt, Eh, encryptionKey] = await buildRegisterSaltRequest(p, keyPair.secretKey, password);
    const saltRegistrationTokenReq = await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/zkpp/register/${encodeURIComponent(username)}`, {
      method: "POST",
      headers: {
        "content-type": "application/x-www-form-urlencoded"
      },
      body: new URLSearchParams({
        "r": r2,
        "s": s,
        "sig": sig,
        "Eh": Eh
      })
    });
    if (!saltRegistrationTokenReq.ok) {
      throw new Error("Unable to register hash");
    }
    const encryptedToken = await saltRegistrationTokenReq.text();
    const token = decryptContractSalt(encryptionKey, encryptedToken);
    const IPK = await deriveKeyFromPassword(EDWARDS25519SHA512BATCH, password, contractSalt);
    const IEK = await deriveKeyFromPassword(CURVE25519XSALSA20POLY1305, password, contractSalt);
    try {
      const userID = await esm_default("gi.actions/identity/create", {
        IPK: new Secret(serializeKey(IPK, true)),
        IEK: new Secret(serializeKey(IEK, true)),
        publishOptions,
        username,
        email,
        picture,
        token: new Secret(token)
      });
      return userID;
    } catch (e) {
      console.error("gi.app/identity/create failed!", e);
      throw new GIErrorUIRuntimeError(L("Failed to create user identity: {reportError}", LError(e)));
    }
  },
  "gi.app/identity/signup": async function({ username, email, password }, publishOptions) {
    try {
      const randomAvatar = esm_default("gi.utils/avatar/create");
      const userID = await esm_default("gi.app/identity/create", {
        data: {
          username,
          email,
          password,
          picture: randomAvatar
        },
        publishOptions
      });
      return userID;
    } catch (e) {
      console.error("gi.app/identity/signup failed!", e);
      await esm_default("gi.app/identity/logout");
      const message = LError(e);
      if (e.name === "GIErrorUIRuntimeError") {
        message.reportError = e.message;
      }
      throw new GIErrorUIRuntimeError(L("Failed to signup: {reportError}", message));
    }
  },
  "gi.app/identity/login": function({ username, password: wpassword, identityContractID: identityContractID2 }) {
    return esm_default("okTurtles.eventQueue/queueEvent", "APP-LOGIN", async () => {
      console.debug("[gi.app/identity/login] Scheduled call starting", identityContractID2, username);
      if (username) {
        const nsIdentityContractID = await esm_default("namespace/lookup", username, { skipCache: true });
        if (!identityContractID2) {
          identityContractID2 = nsIdentityContractID;
        } else if (nsIdentityContractID !== identityContractID2) {
          console.error(new Error(`Identity contract ID mismatch during login: ${identityContractID2} != ${nsIdentityContractID}`));
          throw new GIErrorUIRuntimeError(L("Identity contract ID mismatch during login"));
        }
      }
      if (!identityContractID2) {
        throw new GIErrorUIRuntimeError(L("Incorrect username or password"));
      }
      const password = wpassword?.valueOf();
      const transientSecretKeys = [];
      let oldKeysAnchorCid;
      if (password) {
        try {
          const [salt, cid] = await esm_default("gi.app/identity/retrieveSalt", identityContractID2, wpassword);
          const IEK = await deriveKeyFromPassword(CURVE25519XSALSA20POLY1305, password, salt);
          transientSecretKeys.push(IEK);
          oldKeysAnchorCid = cid;
        } catch (e) {
          console.error("caught error calling retrieveSalt:", e);
          throw new GIErrorUIRuntimeError(L("Incorrect username or password"));
        }
      }
      try {
        await esm_default("appLogs/startCapture", identityContractID2);
        const { state, cheloniaState, encryptionParams } = await loadState(identityContractID2, password);
        let loginCompleteHandler, loginErrorHandler;
        try {
          const loginCompletePromise = new Promise((resolve, reject) => {
            const loginCompleteHandler2 = ({ identityContractID: id }) => {
              removeLoginErrorHandler();
              if (id === identityContractID2) {
                resolve(esm_default("state/vuex/save"));
              } else {
                reject(new Error(`Identity contract ID mismatch during login: ${identityContractID2} != ${id}`));
              }
            };
            const loginErrorHandler2 = ({ identityContractID: id, error }) => {
              removeLoginCompleteHandler();
              if (id === identityContractID2) {
                reject(error);
              } else {
                reject(new Error(`Identity contract ID mismatch during login (on error): ${identityContractID2} != ${id}`));
              }
            };
            const removeLoginCompleteHandler = esm_default("okTurtles.events/once", LOGIN_COMPLETE, loginCompleteHandler2);
            const removeLoginErrorHandler = esm_default("okTurtles.events/once", LOGIN_ERROR, loginErrorHandler2);
          });
          if (password) {
            await esm_default("gi.actions/identity/login", {
              identityContractID: identityContractID2,
              encryptionParams,
              cheloniaState,
              state,
              transientSecretKeys: new Secret(transientSecretKeys.map((k) => serializeKey(k, true))),
              oldKeysAnchorCid
            });
          } else {
            try {
              await esm_default("chelonia/contract/sync", identityContractID2);
            } catch (e) {
              loginCompletePromise.catch((e2) => {
                console.warn("[gi.app/identity/login] Error in login complete promise", e2);
              });
              if (true) {
                console.error("Error syncing identity contract, automatically logging out", identityContractID2, e);
                return esm_default("gi.app/identity/_private/logout", state);
              }
              throw e;
            }
            esm_default("okTurtles.events/emit", LOGIN, { identityContractID: identityContractID2, state });
          }
          await loginCompletePromise;
        } catch (e) {
          esm_default("okTurtles.events/off", LOGIN_COMPLETE, loginCompleteHandler);
          esm_default("okTurtles.events/off", LOGIN_ERROR, loginErrorHandler);
          const errMessage = e?.message || String(e);
          console.error("[gi.app/identity] Error during login contract sync", e);
          const forkedChain = e && (e.name === "ChelErrorForkedChain" || e.cause?.name === "ChelErrorForkedChain");
          const deleted = e && (e.name === "ChelErrorResourceGone" || e.cause?.name === "ChelErrorResourceGone");
          const promptOptions = {
            heading: L("Login error"),
            question: forkedChain ? L("The server's history for your identity contract has diverged from ours. This can happen in extremely rare circumstances due to either malicious activity or a bug. {br_}To fix this, the contract needs to be resynced, and some recent events may be missing. {br_}Would you like to log out and resync data on your next login? {br_}Error details: {err}.", { err: errMessage, ...LTags() }) : deleted ? L("Your account seems to have been deleted from the server. {br_}To fix this, you need to log out and create a new account. {br_}Error details: {err}.", { err: errMessage, ...LTags() }) : L("An error occurred while logging in. Please try logging in again. {br_}Error details: {err}.", { err: errMessage, ...LTags() }),
            primaryButton: L("Log out"),
            // secondaryButton: L('No'),
            primaryButtonStyle: "primary",
            // make primary button 'filled' style
            isContentCentered: !forkedChain && !deleted
          };
          const result = await esm_default("gi.ui/prompt", promptOptions);
          if (result) {
            esm_default("gi.ui/clearBanner");
            return esm_default("gi.app/identity/_private/logout", state, forkedChain);
          } else {
            esm_default("okTurtles.events/emit", LOGIN_ERROR, { username, identityContractID: identityContractID2, error: e });
            throw e;
          }
        }
        return identityContractID2;
      } catch (e) {
        console.error("gi.app/identity/login failed!", e);
        const humanErr = L("Failed to log in: {reportError}", LError(e));
        alert(humanErr);
        await esm_default("gi.app/identity/_private/logout").catch((e2) => {
          console.error("[gi.app/identity/login] Error calling logout (after failure to login)", e2);
        });
        throw new GIErrorUIRuntimeError(humanErr);
      }
    });
  },
  "gi.app/identity/signupAndLogin": async function({ username, email, password }) {
    const contractID = await esm_default("gi.app/identity/signup", { username, email, password });
    await esm_default("gi.app/identity/login", { username, password, identityContractID: contractID });
    return contractID;
  },
  // Unlike the login function, the wrapper for logging out is used using a
  // dedicated selector to allow it to be called from the login selector (if
  // error occurs)
  "gi.app/identity/_private/logout": async function(errorState, wipeOut) {
    try {
      const state = errorState || cloneDeep(esm_default("state/vuex/state"));
      if (!state.loggedIn) return;
      const cheloniaState = await esm_default("gi.actions/identity/logout");
      const { identityContractID: identityContractID2, encryptionParams } = state.loggedIn;
      if (encryptionParams) {
        if (!wipeOut) {
          state.cheloniaState = cheloniaState;
          Object.keys(cheloniaState.contracts || {}).forEach((contractID) => {
            delete state[contractID];
          });
          delete state.contracts;
          await esm_default("state/vuex/save", true, state);
        }
        await esm_default("gi.db/settings/deleteStateEncryptionKey", encryptionParams);
        await esm_default("appLogs/pauseCapture", { wipeOut: true });
      }
      if (wipeOut) {
        await Promise.all([
          esm_default("gi.db/settings/delete", identityContractID2),
          esm_default("gi.db/settings/deleteEncrypted", identityContractID2),
          esm_default("gi.db/settings/delete", SETTING_CHELONIA_STATE)
        ]);
      }
    } catch (e) {
      console.error(`${e.name} during logout: ${e.message}`, e);
    }
  },
  "gi.app/identity/logout": (...params) => {
    return esm_default("okTurtles.eventQueue/queueEvent", "APP-LOGIN", ["gi.app/identity/_private/logout", ...params]);
  },
  "gi.app/identity/changePassword": async (wOldPassword, wNewPassword) => {
    const state = esm_default("state/vuex/state");
    if (!state.loggedIn) return;
    const getters5 = esm_default("state/vuex/getters");
    const { identityContractID: identityContractID2 } = state.loggedIn;
    const username = getters5.usernameFromID(identityContractID2);
    const oldPassword = wOldPassword.valueOf();
    const newPassword = wNewPassword.valueOf();
    const [newContractSalt, oldContractSalt, updateToken] = await esm_default("gi.app/identity/updateSaltRequest", identityContractID2, wOldPassword, wNewPassword);
    const oldIPK = await deriveKeyFromPassword(EDWARDS25519SHA512BATCH, oldPassword, oldContractSalt);
    const oldIEK = await deriveKeyFromPassword(CURVE25519XSALSA20POLY1305, oldPassword, oldContractSalt);
    const newIPK = await deriveKeyFromPassword(EDWARDS25519SHA512BATCH, newPassword, newContractSalt);
    const newIEK = await deriveKeyFromPassword(CURVE25519XSALSA20POLY1305, newPassword, newContractSalt);
    return esm_default("gi.actions/identity/changePassword", {
      identityContractID: identityContractID2,
      username,
      oldIPK: new Secret(oldIPK),
      oldIEK: new Secret(oldIEK),
      newIPK: new Secret(newIPK),
      newIEK: new Secret(newIEK),
      updateToken: new Secret(updateToken)
    });
  },
  "gi.app/identity/delete": async (contractID, wPassword) => {
    const password = wPassword?.valueOf();
    const transientSecretKeys = [];
    let oldKeysAnchorCid;
    if (password) {
      try {
        const [salt, cid] = await esm_default("gi.app/identity/retrieveSalt", contractID, wPassword);
        const IEK = await deriveKeyFromPassword(CURVE25519XSALSA20POLY1305, password, salt);
        transientSecretKeys.push(IEK);
        oldKeysAnchorCid = cid;
      } catch (e) {
        console.error("caught error calling retrieveSalt:", e);
        throw new GIErrorUIRuntimeError(L("Incorrect username or password"));
      }
    }
    await esm_default("gi.actions/identity/delete", {
      contractID,
      transientSecretKeys: new Secret(transientSecretKeys.map((k) => serializeKey(k, true))),
      oldKeysAnchorCid
    });
  }
});

// node_modules/@chelonia/lib/node_modules/@sbp/okturtles.data/dist/esm/index.mjs
var _store2 = /* @__PURE__ */ new Map();
var esm_default4 = esm_default("sbp/selectors/register", {
  "okTurtles.data/get": function(key) {
    return _store2.get(key);
  },
  "okTurtles.data/set": function(key, data) {
    _store2.set(key, data);
    return data;
  },
  "okTurtles.data/delete": function(key) {
    return _store2.delete(key);
  },
  "okTurtles.data/add": function(key, data) {
    const array = _store2.get(key);
    if (array) {
      array.push(data);
    } else {
      _store2.set(key, [data]);
    }
  },
  "okTurtles.data/remove": function(key, data) {
    const array = _store2.get(key);
    if (array) {
      const aLen = array.length;
      const filtered = array.filter((v) => v !== data);
      _store2.set(key, filtered);
      return aLen - filtered.length;
    }
  },
  "okTurtles.data/iterKeys": function* () {
    yield* _store2.keys();
  },
  "okTurtles.data/keyCount": function() {
    return _store2.size;
  },
  "okTurtles.data/apply": function(key, fn) {
    return fn(_store2.get(key));
  }
});

// node_modules/@chelonia/lib/node_modules/@sbp/okturtles.events/dist/esm/index.mjs
var listenKey2 = (evt) => `events/${evt}/listeners`;
var esm_default5 = esm_default("sbp/selectors/register", {
  "okTurtles.events/_init": function() {
    this.errorHandler = (event, e) => {
      console.error(`[okTurtles.events] Error at handler for ${event}`, e);
    };
  },
  "okTurtles.events/on": function(event, handler) {
    esm_default("okTurtles.data/add", listenKey2(event), handler);
    return () => esm_default("okTurtles.events/off", event, handler);
  },
  "okTurtles.events/once": function(event, handler) {
    const cbWithOff = (...args) => {
      handler(...args);
      esm_default("okTurtles.events/off", event, cbWithOff);
    };
    return esm_default("okTurtles.events/on", event, cbWithOff);
  },
  "okTurtles.events/emit": function(event, ...data) {
    var _a;
    for (const listener of esm_default("okTurtles.data/get", listenKey2(event)) || []) {
      try {
        listener(...data);
      } catch (e) {
        (_a = this.errorHandler) === null || _a === void 0 ? void 0 : _a.call(this, event, e);
      }
    }
  },
  // almost identical to Vue.prototype.$off, except we require `event` argument
  "okTurtles.events/off": function(event, handler) {
    if (handler) {
      esm_default("okTurtles.data/remove", listenKey2(event), handler);
    } else {
      esm_default("okTurtles.data/delete", listenKey2(event));
    }
  },
  "okTurtles.events/setErrorHandler": function(errorHandler) {
    this.errorHandler = errorHandler;
  }
});

// node_modules/@chelonia/lib/dist/esm/pubsub/index.mjs
var NOTIFICATION_TYPE = Object.freeze({
  ENTRY: "entry",
  DELETION: "deletion",
  KV: "kv",
  KV_FILTER: "kv_filter",
  PING: "ping",
  PONG: "pong",
  PUB: "pub",
  SUB: "sub",
  UNSUB: "unsub",
  VERSION_INFO: "version_info"
});
var REQUEST_TYPE = Object.freeze({
  PUB: "pub",
  SUB: "sub",
  UNSUB: "unsub",
  PUSH_ACTION: "push_action",
  KV_FILTER: "kv_filter"
});
var RESPONSE_TYPE = Object.freeze({
  ERROR: "error",
  OK: "ok"
});
var PUSH_SERVER_ACTION_TYPE = Object.freeze({
  SEND_PUBLIC_KEY: "send-public-key",
  STORE_SUBSCRIPTION: "store-subscription",
  DELETE_SUBSCRIPTION: "delete-subscription",
  SEND_PUSH_NOTIFICATION: "send-push-notification"
});
var PUBSUB_ERROR = "pubsub-error";
var PUBSUB_RECONNECTION_FAILED = "pubsub-reconnection-failed";
var PUBSUB_RECONNECTION_SUCCEEDED = "pubsub-reconnection-succeeded";
var PUBSUB_SUBSCRIPTION_SUCCEEDED = "pubsub-subscription-succeeded";
var isKvFilterFresh = (ourKvFilter, theirKvFilter) => {
  if (!ourKvFilter !== !theirKvFilter) {
    return false;
  } else if (ourKvFilter && theirKvFilter) {
    if (ourKvFilter.length !== theirKvFilter.length) {
      return false;
    } else {
      const sortedA = [...ourKvFilter].sort();
      const sortedB = [...theirKvFilter].sort();
      for (let i = 0; i < sortedA.length; i++) {
        if (sortedA[i] !== sortedB[i]) {
          return false;
        }
      }
    }
  }
  return true;
};
var pubPayloadFactory = (client, channelID) => () => {
  const kvFilter = client.kvFilter.get(channelID);
  return kvFilter ? { kvFilter, channelID } : { channelID };
};
function runWithRetry(client, channelID, type, getPayload) {
  let attemptNo = 0;
  const { socket, options: options2 } = client;
  const instance = {};
  client.pendingOperations.tSet(type, channelID, instance);
  const send = () => {
    if (client.socket !== socket || socket?.readyState !== WebSocket.OPEN)
      return;
    const currentInstance = client.pendingOperations.tGet(type, channelID);
    if (currentInstance !== instance)
      return;
    if (attemptNo++ > options2.maxOpRetries) {
      console.warn(`[pubsub] Giving up ${type} for channel`, channelID);
      client.pendingOperations.tDelete(type, channelID);
      return;
    }
    const payload = getPayload();
    socket.send(createRequest(type, payload));
    const minDelay = (attemptNo - 1) * options2.opRetryInterval;
    const jitter = randomIntFromRange(0, options2.opRetryInterval);
    const delay = Math.min(200, minDelay) + jitter;
    setTimeout(send, delay);
  };
  send();
}
function createMessage(type, data, meta) {
  const message = { ...meta, type, data };
  let string2;
  const stringify = function() {
    if (!string2)
      string2 = JSON.stringify(this);
    return string2;
  };
  Object.defineProperties(message, {
    [Symbol.toPrimitive]: {
      value: stringify
    }
  });
  return message;
}
function createRequest(type, data) {
  return JSON.stringify(Object.assign({ type }, data));
}
var defaultClientEventHandlers = {
  // Emitted when the connection is closed.
  close(event) {
    const client = this;
    console.debug("[pubsub] Event: close", event.code, event.reason);
    client.failedConnectionAttempts++;
    if (client.socket) {
      for (const name of socketEventNames) {
        client.socket.removeEventListener(name, client.listeners[name]);
      }
    }
    client.socket = null;
    client.clearAllTimers();
    if (client.shouldReconnect) {
      const pendingSubscriptionMap = client.pendingOperations.get(REQUEST_TYPE.SUB);
      if (pendingSubscriptionMap) {
        for (const [channelID] of pendingSubscriptionMap) {
          pendingSubscriptionMap.set(channelID, {});
        }
      }
      client.subscriptionSet.forEach((channelID) => {
        if (!client.pendingOperations.tHas(REQUEST_TYPE.UNSUB, channelID)) {
          client.pendingOperations.tSet(REQUEST_TYPE.SUB, channelID, {});
        }
      });
    }
    client.subscriptionSet.clear();
    client.pendingOperations.tClear(REQUEST_TYPE.UNSUB);
    client.pendingOperations.tClear(REQUEST_TYPE.KV_FILTER);
    if (client.shouldReconnect && client.options.reconnectOnDisconnection) {
      if (client.failedConnectionAttempts > client.options.maxRetries) {
        esm_default("okTurtles.events/emit", PUBSUB_RECONNECTION_FAILED, client);
      } else {
        if (!isDefinetelyOffline() || client.isLocal) {
          client.scheduleConnectionAttempt();
        }
      }
    }
  },
  // Emitted when an error has occured.
  // The socket will be closed automatically by the engine if necessary.
  error(event) {
    const client = this;
    console.warn("[pubsub] Event: error", event);
    clearTimeout(client.pingTimeoutID);
  },
  // Emitted when a message is received.
  // The connection will be terminated if the message is malformed or has an
  // unexpected data type (e.g. binary instead of text).
  message(event) {
    const client = this;
    const { data } = event;
    if (typeof data !== "string") {
      esm_default("okTurtles.events/emit", PUBSUB_ERROR, client, {
        message: `Wrong data type: ${typeof data}`
      });
      return client.destroy();
    }
    let msg = { type: "" };
    try {
      msg = messageParser(data);
    } catch (error) {
      esm_default("okTurtles.events/emit", PUBSUB_ERROR, client, {
        message: `Malformed message: ${error?.message}`
      });
      return client.destroy();
    }
    const handler = client.messageHandlers[msg.type];
    if (handler) {
      handler.call(client, msg);
    } else {
      throw new Error(`Unhandled message type: ${msg.type}`);
    }
  },
  offline() {
    console.info("[pubsub] Event: offline");
    const client = this;
    client.clearAllTimers();
    client.failedConnectionAttempts = 0;
    client.socket?.close();
  },
  online() {
    console.info("[pubsub] Event: online");
    const client = this;
    if (client.options.reconnectOnOnline && client.shouldReconnect) {
      if (!client.socket) {
        client.failedConnectionAttempts = 0;
        client.scheduleConnectionAttempt();
      }
    }
  },
  // Emitted when the connection is established.
  open() {
    console.debug("[pubsub] Event: open");
    const client = this;
    const { options: options2 } = this;
    client.connectionTimeUsed = void 0;
    client.clearAllTimers();
    esm_default("okTurtles.events/emit", PUBSUB_RECONNECTION_SUCCEEDED, client);
    client.failedConnectionAttempts = -1;
    client.isNew = false;
    if (options2.pingTimeout > 0 && options2.pingTimeout < Infinity) {
      client.pingTimeoutID = setTimeout(() => {
        client.socket?.close();
      }, options2.pingTimeout);
    }
    for (const [channelID] of client.pendingOperations.get(REQUEST_TYPE.SUB) || []) {
      runWithRetry(client, channelID, REQUEST_TYPE.SUB, pubPayloadFactory(client, channelID));
    }
  },
  "reconnection-attempt"() {
    console.info("[pubsub] Trying to reconnect...");
  },
  "reconnection-succeeded"() {
    console.info("[pubsub] Connection re-established");
  },
  "reconnection-failed"() {
    console.warn("[pubsub] Reconnection failed");
    const client = this;
    client.destroy();
  },
  "reconnection-scheduled"(event) {
    const { delay, nth } = event.detail;
    console.info(`[pubsub] Scheduled connection attempt ${nth} in ~${delay} ms`);
  },
  "subscription-succeeded"(event) {
    const { channelID } = event.detail;
    console.debug(`[pubsub] Subscribed to channel ${channelID}`);
  }
};
var defaultMessageHandlers = {
  [NOTIFICATION_TYPE.ENTRY](msg) {
    console.debug("[pubsub] Received ENTRY:", msg);
  },
  [NOTIFICATION_TYPE.PING]({ data }) {
    const client = this;
    if (client.options.logPingMessages) {
      console.debug(`[pubsub] Ping received in ${Date.now() - Number(data)} ms`);
    }
    client.socket?.send(createMessage(NOTIFICATION_TYPE.PONG, data));
    clearTimeout(client.pingTimeoutID);
    client.pingTimeoutID = setTimeout(() => {
      client.socket?.close();
    }, client.options.pingTimeout);
  },
  [NOTIFICATION_TYPE.PUB]({ channelID, data }) {
    console.log(`[pubsub] Received data from channel ${channelID}:`, data);
  },
  [NOTIFICATION_TYPE.KV]({ channelID, key, data }) {
    console.log(`[pubsub] Received KV update from channel ${channelID} ${key}:`, data);
  },
  [NOTIFICATION_TYPE.SUB](msg) {
    console.debug(`[pubsub] Ignoring ${msg.type} message:`, msg.data);
  },
  [NOTIFICATION_TYPE.UNSUB](msg) {
    console.debug(`[pubsub] Ignoring ${msg.type} message:`, msg.data);
  },
  [RESPONSE_TYPE.ERROR]({ data }) {
    const { type, channelID, reason } = data;
    console.warn(`[pubsub] Received ERROR response for ${type} request to ${channelID}`);
    const client = this;
    switch (type) {
      case REQUEST_TYPE.SUB: {
        console.warn(`[pubsub] Could not subscribe to ${channelID}: ${reason}`);
        client.pendingOperations.tDelete(REQUEST_TYPE.SUB, channelID);
        break;
      }
      case REQUEST_TYPE.UNSUB: {
        console.warn(`[pubsub] Could not unsubscribe from ${channelID}: ${reason}`);
        client.pendingOperations.tDelete(REQUEST_TYPE.UNSUB, channelID);
        break;
      }
      case REQUEST_TYPE.PUSH_ACTION: {
        const { actionType, message } = data;
        console.warn(`[pubsub] Received ERROR for PUSH_ACTION request with the action type '${actionType}' and the following message: ${message}`);
        break;
      }
      case REQUEST_TYPE.KV_FILTER: {
        console.warn(`[pubsub] Could not set KV filter for ${channelID}: ${reason}`);
        client.pendingOperations.tDelete(REQUEST_TYPE.KV_FILTER, channelID);
        break;
      }
      default: {
        console.error(`[pubsub] Malformed response: invalid request type ${type}`);
      }
    }
  },
  [RESPONSE_TYPE.OK]({ data: { type, channelID, kvFilter } }) {
    const client = this;
    switch (type) {
      case REQUEST_TYPE.SUB: {
        if (client.pendingOperations.tHas(REQUEST_TYPE.SUB, channelID)) {
          client.pendingOperations.tDelete(REQUEST_TYPE.SUB, channelID);
          client.subscriptionSet.add(channelID);
          esm_default("okTurtles.events/emit", PUBSUB_SUBSCRIPTION_SUCCEEDED, client, { channelID });
          const ourKvFilter = client.kvFilter.get(channelID);
          if (!isKvFilterFresh(ourKvFilter, kvFilter)) {
            console.debug(`[pubsub] Subscribed to ${channelID}, need to set new KV filter`);
            this.setKvFilter(channelID, ourKvFilter);
          }
        } else {
          console.debug(`[pubsub] Received unexpected sub for ${channelID}`);
        }
        break;
      }
      case REQUEST_TYPE.UNSUB: {
        if (client.pendingOperations.tHas(REQUEST_TYPE.UNSUB, channelID)) {
          console.debug(`[pubsub] Unsubscribed from ${channelID}`);
          client.pendingOperations.tDelete(REQUEST_TYPE.UNSUB, channelID);
          client.subscriptionSet.delete(channelID);
        } else {
          console.debug(`[pubsub] Received unexpected unsub for ${channelID}`);
        }
        break;
      }
      case REQUEST_TYPE.KV_FILTER: {
        if (client.pendingOperations.tHas(REQUEST_TYPE.KV_FILTER, channelID)) {
          const ourKvFilter = client.kvFilter.get(channelID);
          if (isKvFilterFresh(ourKvFilter, kvFilter)) {
            console.debug(`[pubsub] Set KV filter for ${channelID}`, kvFilter);
            client.pendingOperations.tDelete(REQUEST_TYPE.KV_FILTER, channelID);
          } else {
            console.debug(`[pubsub] Received stale KV filter ack for ${channelID}`, kvFilter, ourKvFilter);
          }
        } else {
          console.debug(`[pubsub] Received unexpected kv-filter for ${channelID}`);
        }
        break;
      }
      default: {
        console.error(`[pubsub] Malformed response: invalid request type ${type}`);
      }
    }
  }
};
var globalEventNames = ["offline", "online"];
var socketEventNames = ["close", "error", "message", "open"];
var globalEventMap = /* @__PURE__ */ new Map();
if (typeof self === "object" && self instanceof EventTarget) {
  for (const name of globalEventNames) {
    const handler = (ev) => {
      const h = globalEventMap.get(name);
      return h?.(ev);
    };
    self.addEventListener(name, handler, false);
  }
}
var isDefinetelyOffline = () => typeof navigator === "object" && navigator.onLine === false;
var messageParser = (data) => {
  const msg = JSON.parse(data);
  if (typeof msg !== "object" || msg === null) {
    throw new TypeError("Message is null or not an object");
  }
  const { type } = msg;
  if (typeof type !== "string" || type === "") {
    throw new TypeError("Message type must be a non-empty string");
  }
  return msg;
};
for (const name of Object.keys(defaultClientEventHandlers)) {
  if (name === "error" || !socketEventNames.includes(name)) {
    esm_default("okTurtles.events/on", `pubsub-${name}`, (target, detail) => {
      const ev = new CustomEvent(name, { detail });
      target.listeners[name].call(target, ev);
    });
  }
}

// frontend/controller/instance-keys.js
var PUBSUB_INSTANCE = "@instance/pubsub";

// frontend/controller/backend.js
var languageFileMap = /* @__PURE__ */ new Map([
  ["fr", "french.json"],
  ["ru", "russian.json"],
  ["ko", "korean.json"],
  ["es", "spanish.json"],
  ["ja", "japanese.json"],
  ["uk", "ukrainian.json"],
  ["pt", "portuguese.json"],
  ["zh", "chinese.json"]
]);
esm_default("okTurtles.events/on", NOTIFICATION_TYPE.VERSION_INFO, (versionInfo) => {
  if (versionInfo.GI_VERSION === "2.5.1@2026-01-22T10:19:51.672Z") {
    sessionStorage.removeItem(NOTIFICATION_TYPE.VERSION_INFO);
    return;
  }
  console.info("New Group Income version available:", versionInfo);
  try {
    const existingSerialized = sessionStorage.getItem(NOTIFICATION_TYPE.VERSION_INFO);
    if (existingSerialized) {
      const existingVersionInfo = JSON.parse(existingSerialized);
      if (Array.isArray(existingVersionInfo) && !(Date.now() - existingVersionInfo[0] >= 2.5 * HOURS_MILLIS) && versionInfo.GI_VERSION === existingVersionInfo[1].GI_VERSION) {
        console.warn("[NOTIFICATION_TYPE.VERSION_INFO] A different Group Income version is available, but reloading has failed to address it", { existingVersionInfo, versionInfo });
        return;
      }
    }
    sessionStorage.setItem(NOTIFICATION_TYPE.VERSION_INFO, JSON.stringify([Date.now(), versionInfo]));
  } catch (e) {
    console.error("[NOTIFICATION_TYPE.VERSION_INFO] Error in handler", e);
  }
  esm_default("okTurtles.data/get", PUBSUB_INSTANCE)?.destroy();
  window.location.reload();
});
var backend_default = esm_default("sbp/selectors/register", {
  /**
   * Fetches a JSON object containing translation strings for a given language.
   *
   * @param language - A BPC-47 language tag like the value of `navigator.language`.
   *
   * @see The 'translations/init' SBP selector in `~view-utils/translations.js`.
   */
  async "backend/translations/get"(language) {
    const [languageCode] = language.toLowerCase().split("-");
    const languageFileName = languageFileMap.get(languageCode) || "";
    if (languageFileName !== "") {
      return await fetch(`${esm_default("okTurtles.data/get", "API_URL")}/assets/strings/${languageFileName}`).then(handleFetchResult("json"));
    }
  }
});

// frontend/controller/namespace.js
esm_default("sbp/selectors/register", {
  "namespace/lookupCached": (name) => {
    const cache = esm_default("state/vuex/state").namespaceLookups;
    return cache?.[name] ?? null;
  },
  "namespace/lookupReverseCached": (id) => {
    const cache = esm_default("state/vuex/state").reverseNamespaceLookups;
    return cache?.[id] ?? null;
  },
  "namespace/lookup": (name, { skipCache } = { skipCache: false }) => {
    if (!skipCache) {
      const cached = esm_default("namespace/lookupCached", name);
      if (cached) {
        return Promise.resolve(cached);
      }
    }
    return esm_default("sw-namespace/lookup", name, { skipCache });
  }
});

// frontend/model/state.js
init_vue_esm();

// frontend/model/notifications/storageConstants.js
var MAX_AGE_READ = 60 * DAYS_MILLIS;
var MAX_AGE_UNREAD = 180 * DAYS_MILLIS;
var MAX_COUNT = 30;
var MAX_COUNT_READ = 20;
var MAX_COUNT_UNREAD = 30;

// frontend/model/notifications/utils.js
var NEW_STATUS_DURATION = 2 * HOURS_MILLIS;
function age(notification) {
  return Date.now() - notification.timestamp;
}
function applyStorageRules(notifications, status = {}) {
  let items = notifications.filter((item) => !isExpired({ ...item, ...status[item.hash] }));
  if (items.length > MAX_COUNT) {
    items.sort(compareOnPriority);
    [
      [(item) => item.read, MAX_COUNT_READ],
      [(item) => !item.read, MAX_COUNT_UNREAD]
    ].forEach(([condition, associatedMaxCount]) => {
      if (associatedMaxCount !== -1 && associatedMaxCount < MAX_COUNT) {
        let count = 0;
        items = items.filter((item) => !condition(item) || (count++, count <= associatedMaxCount));
      }
    });
  }
  if (items.length > MAX_COUNT) {
    items.length = MAX_COUNT;
  }
  return items.sort(compareOnTimestamp);
}
function compareOnTimestamp(notificationA, notificationB) {
  return notificationB.timestamp - notificationA.timestamp;
}
function compareOnPriority(notificationA, notificationB) {
  const TWO_DAYS = 2 * DAYS_MILLIS;
  const FIFTEEN_DAYS = 15 * DAYS_MILLIS;
  const THREE_WEEKS = 21 * DAYS_MILLIS;
  const preferA = -1;
  const preferB = 1;
  if (notificationA.read === notificationB.read) {
    return notificationA.timestamp > notificationB.timestamp ? preferA : preferB;
  } else {
    const read = notificationA.read ? notificationA : notificationB;
    const unread = !notificationA.read ? notificationA : notificationB;
    let preferred = unread;
    const conditions = [
      (read2, unread2) => age(read2) < DAYS_MILLIS && age(unread2) > FIFTEEN_DAYS,
      (read2, unread2) => age(read2) < TWO_DAYS && age(unread2) > THREE_WEEKS
    ];
    if (conditions.some((condition) => condition(read, unread))) {
      preferred = read;
    }
    return preferred === notificationA ? preferA : preferB;
  }
}
function isExpired(notification) {
  return age(notification) > maxAge(notification);
}
function isNew(notification) {
  return age(notification) < NEW_STATUS_DURATION;
}
function isOlder(notification) {
  return !isNew(notification);
}
function maxAge(notification) {
  return notification.read ? MAX_AGE_READ : MAX_AGE_UNREAD;
}

// frontend/model/contracts/shared/getters/chatroom.js
var chatroom_default2 = {
  chatRoomSettings(state, getters5) {
    return getters5.currentChatRoomState.settings || {};
  },
  chatRoomAttributes(state, getters5) {
    return getters5.currentChatRoomState.attributes || {};
  },
  chatRoomMembers(state, getters5) {
    return getters5.currentChatRoomState.members || {};
  },
  chatRoomRecentMessages(state, getters5) {
    return getters5.currentChatRoomState.messages || [];
  },
  chatRoomPinnedMessages(state, getters5) {
    return (getters5.currentChatRoomState.pinnedMessages || []).sort((a, b) => a.height < b.height ? 1 : -1);
  },
  isJoinedChatRoomForChatRoom(state, getters5) {
    return (state2, memberID) => {
      if (!memberID) memberID = getters5.ourIdentityContractId;
      const members = state2?.members;
      return !!members?.[memberID] && !members[memberID].hasLeft;
    };
  },
  chatRoomActiveMemberIdsForChatRoom() {
    return (state) => {
      const members = state?.members;
      if (!members) return [];
      return Object.keys(members).filter((memberID) => !members[memberID].hasLeft);
    };
  }
};

// frontend/model/contracts/shared/getters/group.js
var group_default2 = {
  currentGroupOwnerID(state, getters5) {
    return getters5.currentGroupState.groupOwnerID;
  },
  groupSettingsForGroup(state, getters5) {
    return (state2) => state2.settings || {};
  },
  groupSettings(state, getters5) {
    return getters5.groupSettingsForGroup(getters5.currentGroupState);
  },
  profileActive(state, getters5) {
    return (member) => {
      const profiles = getters5.currentGroupState.profiles;
      return profiles?.[member]?.status === PROFILE_STATUS.ACTIVE;
    };
  },
  pendingAccept(state, getters5) {
    return (member) => {
      const profiles = getters5.currentGroupState.profiles;
      return profiles?.[member]?.status === PROFILE_STATUS.PENDING;
    };
  },
  groupProfileForGroup(state, getters5) {
    return (state2, member) => {
      const profiles = state2.profiles;
      return profiles && profiles[member] && {
        ...profiles[member],
        get lastLoggedIn() {
          return getters5.currentGroupLastLoggedIn[member] || this.joinedDate;
        }
      };
    };
  },
  groupProfile(state, getters5) {
    return (member) => getters5.groupProfileForGroup(getters5.currentGroupState, member);
  },
  groupProfilesForGroup(state, getters5) {
    return (state2) => {
      const profiles = {};
      for (const member in state2.profiles || {}) {
        const profile = getters5.groupProfileForGroup(state2, member);
        if (profile.status === PROFILE_STATUS.ACTIVE) {
          profiles[member] = profile;
        }
      }
      return profiles;
    };
  },
  groupProfiles(state, getters5) {
    return getters5.groupProfilesForGroup(getters5.currentGroupState);
  },
  groupCreatedDate(state, getters5) {
    return getters5.groupProfile(getters5.currentGroupOwnerID).joinedDate;
  },
  groupMincomeAmountForGroup(state, getters5) {
    return (state2) => getters5.groupSettingsForGroup(state2).mincomeAmount;
  },
  groupMincomeAmount(state, getters5) {
    return getters5.groupMincomeAmountForGroup(getters5.currentGroupState);
  },
  groupMincomeCurrencyForGroup(state, getters5) {
    return (state2) => {
      return getters5.groupSettingsForGroup(state2).mincomeCurrency;
    };
  },
  groupMincomeCurrency(state, getters5) {
    return getters5.groupMincomeCurrencyForGroup(getters5.currentGroupState);
  },
  // Oldest period key first.
  groupSortedPeriodKeysForGroup(state, getters5) {
    return (state2) => {
      const { distributionDate, distributionPeriodLength } = getters5.groupSettingsForGroup(state2);
      if (!distributionDate) return [];
      const keys = Object.keys(getters5.groupPeriodPaymentsForGroup(state2)).sort();
      if (!keys.length && MAX_SAVED_PERIODS > 0) {
        keys.push(dateToPeriodStamp(addTimeToDate(distributionDate, -distributionPeriodLength)));
      }
      if (keys[keys.length - 1] !== distributionDate) {
        keys.push(distributionDate);
      }
      return keys;
    };
  },
  groupSortedPeriodKeys(state, getters5) {
    return getters5.groupSortedPeriodKeysForGroup(getters5.currentGroupState);
  },
  // paymentTotalfromMembertoMemberID (state, getters) {
  // // this code was removed in https://github.com/okTurtles/group-income/pull/1691
  // // because it was unused. feel free to bring it back if needed.
  // },
  //
  // The following three getters return either a known period stamp for the given date,
  // or a predicted one according to the period length.
  // They may also return 'undefined', in which case the caller should check archived data.
  periodStampGivenDateForGroup(state, getters5) {
    return (state2, date, periods) => {
      return periodStampsForDate(date, {
        knownSortedStamps: periods || getters5.groupSortedPeriodKeysForGroup(state2),
        periodLength: getters5.groupSettingsForGroup(state2).distributionPeriodLength
      }).current;
    };
  },
  periodStampGivenDate(state, getters5) {
    return (date, periods) => {
      return getters5.periodStampGivenDateForGroup(getters5.currentGroupState, date, periods);
    };
  },
  periodBeforePeriodForGroup(state, getters5) {
    return (groupState, periodStamp, periods) => {
      return periodStampsForDate(periodStamp, {
        knownSortedStamps: periods || getters5.groupSortedPeriodKeysForGroup(groupState),
        periodLength: getters5.groupSettingsForGroup(groupState).distributionPeriodLength
      }).previous;
    };
  },
  periodBeforePeriod(state, getters5) {
    return (periodStamp, periods) => getters5.periodBeforePeriodForGroup(getters5.currentGroupState, periodStamp, periods);
  },
  periodAfterPeriodForGroup(state, getters5) {
    return (groupState, periodStamp, periods) => {
      return periodStampsForDate(periodStamp, {
        knownSortedStamps: periods || getters5.groupSortedPeriodKeysForGroup(groupState),
        periodLength: getters5.groupSettingsForGroup(groupState).distributionPeriodLength
      }).next;
    };
  },
  periodAfterPeriod(state, getters5) {
    return (periodStamp, periods) => getters5.periodAfterPeriodForGroup(getters5.currentGroupState, periodStamp, periods);
  },
  dueDateForPeriodForGroup(state, getters5) {
    return (state2, periodStamp, periods) => {
      return getters5.periodAfterPeriodForGroup(state2, periodStamp, periods);
    };
  },
  dueDateForPeriod(state, getters5) {
    return (periodStamp, periods) => {
      return getters5.dueDateForPeriodForGroup(getters5.currentGroupState, periodStamp, periods);
    };
  },
  paymentHashesForPeriodForGroup(state, getters5) {
    return (state2, periodStamp) => {
      const periodPayments = getters5.groupPeriodPaymentsForGroup(state2)[periodStamp];
      if (periodPayments) {
        return paymentHashesFromPaymentPeriod(periodPayments);
      }
    };
  },
  paymentHashesForPeriod(state, getters5) {
    return (periodStamp) => {
      return getters5.paymentHashesForPeriodForGroup(getters5.currentGroupState, periodStamp);
    };
  },
  groupMembersByContractID(state, getters5) {
    return Object.keys(getters5.groupProfiles);
  },
  groupMembersCount(state, getters5) {
    return getters5.groupMembersByContractID.length;
  },
  groupMembersPending(state, getters5) {
    const invites = getters5.currentGroupState.invites;
    const vmInvites = getters5.currentGroupState._vm.invites;
    const pendingMembers = /* @__PURE__ */ Object.create(null);
    for (const inviteKeyId in invites) {
      if (vmInvites[inviteKeyId].status === INVITE_STATUS.VALID && invites[inviteKeyId].creatorID !== INVITE_INITIAL_CREATOR) {
        pendingMembers[inviteKeyId] = {
          displayName: invites[inviteKeyId].invitee,
          invitedBy: invites[inviteKeyId].creatorID,
          expires: vmInvites[inviteKeyId].expires
        };
      }
    }
    return pendingMembers;
  },
  groupShouldPropose(state, getters5) {
    return getters5.groupMembersCount >= 3;
  },
  groupDistributionStarted(state, getters5) {
    return (currentDate) => currentDate >= getters5.groupSettings?.distributionDate;
  },
  groupProposalSettings(state, getters5) {
    return (proposalType = PROPOSAL_GENERIC) => {
      return getters5.groupSettings.proposals?.[proposalType];
    };
  },
  groupCurrencyForGroup(state, getters5) {
    return (state2) => {
      const mincomeCurrency = getters5.groupMincomeCurrencyForGroup(state2);
      return mincomeCurrency && currencies_default[mincomeCurrency];
    };
  },
  groupCurrency(state, getters5) {
    return getters5.groupCurrencyForGroup(getters5.currentGroupState);
  },
  groupMincomeSymbolWithCode(state, getters5) {
    return getters5.groupCurrency?.symbolWithCode;
  },
  groupPeriodPaymentsForGroup(state, getters5) {
    return (state2) => {
      return state2.paymentsByPeriod || {};
    };
  },
  groupPeriodPayments(state, getters5) {
    return getters5.groupPeriodPaymentsForGroup(getters5.currentGroupState);
  },
  groupThankYousFrom(state, getters5) {
    return getters5.currentGroupState.thankYousFrom || {};
  },
  groupStreaks(state, getters5) {
    return getters5.currentGroupState.streaks || {};
  },
  groupTotalPledgeAmount(state, getters5) {
    return getters5.currentGroupState.totalPledgeAmount || 0;
  },
  groupChatRooms(state, getters5) {
    return getters5.currentGroupState.chatRooms;
  },
  groupGeneralChatRoomId(state, getters5) {
    return getters5.currentGroupState.generalChatRoomId;
  },
  // getter is named haveNeedsForThisPeriod instead of haveNeedsForPeriod because it uses
  // getters.groupProfiles - and that is always based on the most recent values. we still
  // pass in the current period because it's used to set the "when" property
  haveNeedsForThisPeriodForGroup(state, getters5) {
    return (state2, currentPeriod) => {
      const groupProfiles = getters5.groupProfilesForGroup(state2);
      const haveNeeds = [];
      for (const memberID in groupProfiles) {
        const { incomeDetailsType, joinedDate } = groupProfiles[memberID];
        if (incomeDetailsType) {
          const amount = groupProfiles[memberID][incomeDetailsType];
          const haveNeed = incomeDetailsType === "incomeAmount" ? amount - getters5.groupMincomeAmountForGroup(state2) : amount;
          let when = dateFromPeriodStamp(currentPeriod).toISOString();
          if (dateIsWithinPeriod({
            date: joinedDate,
            periodStart: currentPeriod,
            periodLength: getters5.groupSettingsForGroup(state2).distributionPeriodLength
          })) {
            when = joinedDate;
          }
          haveNeeds.push({ memberID, haveNeed, when });
        }
      }
      return haveNeeds;
    };
  },
  haveNeedsForThisPeriod(state, getters5) {
    return (currentPeriod) => {
      return getters5.haveNeedsForThisPeriodForGroup(getters5.currentGroupState, currentPeriod);
    };
  },
  paymentsForPeriodForGroup(state, getters5) {
    return (state2, periodStamp) => {
      const hashes = getters5.paymentHashesForPeriodForGroup(state2, periodStamp);
      const events = [];
      if (hashes && hashes.length > 0) {
        const payments = state2.payments;
        for (const paymentHash of hashes) {
          const payment = payments[paymentHash];
          if (payment.data.status === PAYMENT_COMPLETED) {
            events.push(createPaymentInfo(paymentHash, payment));
          }
        }
      }
      return events;
    };
  },
  paymentsForPeriod(state, getters5) {
    return (periodStamp) => {
      return getters5.paymentsForPeriodForGroup(getters5.currentGroupState, periodStamp);
    };
  }
  // distributionEventsForMonth (state, getters) {
  //   return (monthstamp) => {
  //     // NOTE: if we ever switch back to the "real-time" adjusted distribution
  //     // algorithm, make sure that this function also handles userExitsGroupEvent
  //     const distributionEvents = getters.haveNeedEventsForMonth(monthstamp)
  //     const paymentEvents = getters.paymentEventsForMonth(monthstamp)
  //     distributionEvents.splice(distributionEvents.length, 0, paymentEvents)
  //     return distributionEvents.sort((a, b) => compareISOTimestamps(a.data.when, b.data.when))
  //   }
  // }
};

// frontend/model/contracts/shared/getters/identity.js
var identity_default2 = {
  loginState(state, getters5) {
    return getters5.currentIdentityState.loginState;
  },
  ourDirectMessages(state, getters5) {
    return getters5.currentIdentityState.chatRooms || {};
  }
};

// frontend/model/getters.js
var checkedUsername = (state, username, userID) => {
  if (username && state.namespaceLookups?.[username] === userID) {
    return username;
  }
};
var anyoneCanJoinInviteId = (invites, getters5) => Object.keys(invites).find(
  (invite) => (
    // First, we want 'anyone can join' invites
    invites[invite].creatorID === INVITE_INITIAL_CREATOR && // and that haven't been revoked
    getters5.currentGroupState._vm.invites[invite].status === INVITE_STATUS.VALID && // and that haven't expired (using negative logic because expires could be
    // undefined for non expiring-invites)
    !(getters5.currentGroupState._vm.invites[invite].expires < Date.now()) && // and that that haven't been entirely used up
    !(getters5.currentGroupState._vm.invites[invite].quantity <= 0)
  )
);
var getters = {
  // !!  IMPORTANT  !!
  //
  // We register pure Vuex getters here, but later on at the bottom of this file,
  // we will also import into Vuex the contract getters so that they can be reused
  // without having to be redefined. This is possible because Chelonia contract getters
  // are designed to be compatible with Vuex getters.
  //
  // We will use the getters 'currentGroupState', 'currentIdentityState', and
  // 'currentChatRoomState' as a "bridge" between the contract getters and Vuex.
  //
  // This makes it possible for the getters inside of contracts to refer to each
  // specific contractID instance, while the Vuex version of those getters that
  // are imported at the bottom of this file (in the listener for CONTRACT_REGISTERED
  // will reference the state for the specific contractID for either the current group,
  // the current user identity contract, or the current chatroom we're looking at.
  //
  // For getters that get data from only contract state, write them
  // under the 'getters' key of the object passed to 'chelonia/defineContract'.
  // See for example: frontend/model/contracts/group.js
  //
  // Again, for convenience, we've defined the same getter, `currentGroupState`,
  // twice, so that we can reuse the same getter definitions both here with Vuex,
  // and inside of the contracts (e.g. in group.js).
  //
  // The 'currentGroupState' here is based off the value of `state.currentGroupId`,
  // a user preference that does not exist in the group contract state.
  currentGroupState(state) {
    if (true) {
      if (typeof Window === "undefined") {
        const error = new Error("Tried to access currentGroupState from outside a browsing context");
        Promise.reject(error);
        throw error;
      }
    }
    return state[state.currentGroupId] || {};
  },
  currentIdentityState(state) {
    return state.loggedIn && state[state.loggedIn.identityContractID] || {};
  },
  ourUsername(state, getters5) {
    return state.loggedIn && getters5.usernameFromID(state.loggedIn.identityContractID);
  },
  ourPreferences(state) {
    return state.preferences;
  },
  ourProfileActive(state, getters5) {
    return getters5.profileActive(getters5.ourIdentityContractId);
  },
  ourPendingAccept(state, getters5) {
    return getters5.pendingAccept(getters5.ourIdentityContractId);
  },
  ourGroupProfileForGroup(state, getters5) {
    return (state2) => getters5.groupProfileForGroup(state2, getters5.ourIdentityContractId);
  },
  ourGroupProfile(state, getters5) {
    return getters5.ourGroupProfileForGroup(getters5.currentGroupState);
  },
  ourUserDisplayName(state, getters5) {
    const userContract = getters5.currentIdentityState || {};
    return userContract.attributes?.displayName || getters5.ourUsername || getters5.ourIdentityContractId;
  },
  ourIdentityContractId(state) {
    return state.loggedIn && state.loggedIn.identityContractID;
  },
  ourGroups(state, getters5) {
    const identityContractID2 = getters5.ourIdentityContractId;
    if (!identityContractID2) return [];
    return Object.keys(state[identityContractID2]?.groups || {}).filter(
      (gId) => !state[identityContractID2].groups[gId].hasLeft && state[gId]
    );
  },
  currentGroupLastLoggedIn(state) {
    return state.lastLoggedIn[state.currentGroupId] || {};
  },
  // NOTE: since this getter is written using `getters.ourUsername`, which is based
  //       on vuexState.loggedIn (a user preference), we cannot use this getter
  //       into group.js
  ourContributionSummary(state, getters5) {
    const groupProfiles = getters5.groupProfiles;
    const ourIdentityContractId = getters5.ourIdentityContractId;
    const ourGroupProfile = getters5.ourGroupProfile;
    if (!ourGroupProfile || !ourGroupProfile.incomeDetailsType) {
      return {};
    }
    const doWeNeedIncome = ourGroupProfile.incomeDetailsType === "incomeAmount";
    const distribution = getters5.groupIncomeDistribution;
    const nonMonetaryContributionsOf = (memberID) => groupProfiles[memberID].nonMonetaryContributions || [];
    return {
      givingMonetary: (() => {
        if (doWeNeedIncome) {
          return null;
        }
        const who = [];
        const whoIds = [];
        const total = distribution.filter((p) => p.fromMemberID === ourIdentityContractId).reduce((acc, payment) => {
          who.push(getters5.userDisplayNameFromID(payment.toMemberID));
          whoIds.push(payment.toMemberID);
          return acc + payment.amount;
        }, 0);
        return { who, whoIds, total, pledged: ourGroupProfile.pledgeAmount };
      })(),
      receivingMonetary: (() => {
        if (!doWeNeedIncome) {
          return null;
        }
        const needed = getters5.groupSettings.mincomeAmount - ourGroupProfile.incomeAmount;
        const who = [];
        const whoIds = [];
        const total = distribution.filter((p) => p.toMemberID === ourIdentityContractId).reduce((acc, payment) => {
          who.push(getters5.userDisplayNameFromID(payment.fromMemberID));
          whoIds.push(payment.fromMemberID);
          return acc + payment.amount;
        }, 0);
        return { who, whoIds, total, needed };
      })(),
      receivingNonMonetary: (() => {
        const listWho = Object.keys(groupProfiles).filter((memberID) => memberID !== ourIdentityContractId && nonMonetaryContributionsOf(memberID).length > 0);
        const listWhat = listWho.reduce((contr, memberID) => {
          const displayName = getters5.userDisplayNameFromID(memberID);
          const userContributions = nonMonetaryContributionsOf(memberID);
          userContributions.forEach((what) => {
            const contributionIndex = contr.findIndex((c) => c.what === what);
            if (contributionIndex >= 0) {
              contr[contributionIndex].who.push(displayName);
              contr[contributionIndex].whoIds.push(memberID);
            } else {
              contr.push({ who: [displayName], whoIds: [memberID], what });
            }
          });
          return contr;
        }, []);
        return listWho.length > 0 ? { what: listWhat, who: listWho } : null;
      })(),
      givingNonMonetary: (() => {
        const contributions = ourGroupProfile.nonMonetaryContributions;
        return contributions.length > 0 ? contributions : null;
      })()
    };
  },
  usernameFromID(state, getters5) {
    return (userID) => {
      const profile = getters5.ourContactProfilesById[userID];
      return profile?.username || state.reverseNamespaceLookups[userID] || userID;
    };
  },
  userDisplayNameFromID(state, getters5) {
    return (userID) => {
      if (userID === getters5.ourIdentityContractId) {
        return getters5.ourUserDisplayName;
      }
      const profile = getters5.ourContactProfilesById[userID];
      return profile?.displayName || profile?.username || state.reverseNamespaceLookups[userID] || userID;
    };
  },
  thisPeriodPaymentInfoForGroup(state, getters5) {
    return (state2) => {
      return getters5.groupPeriodPaymentsForGroup(state2)[getters5.currentPaymentPeriodForGroup(state2)];
    };
  },
  thisPeriodPaymentInfo(state, getters5) {
    return getters5.thisPeriodPaymentInfoForGroup(getters5.currentGroupState);
  },
  latePayments(state, getters5) {
    const periodPayments = getters5.groupPeriodPayments;
    if (Object.keys(periodPayments).length === 0) return;
    const ourIdentityContractId = getters5.ourIdentityContractId;
    const pPeriod = getters5.periodBeforePeriod(getters5.currentPaymentPeriod);
    const pPayments = periodPayments[pPeriod];
    if (pPayments) {
      return pPayments.lastAdjustedDistribution.filter((todo) => todo.fromMemberID === ourIdentityContractId);
    }
  },
  // used with graphs like those in the dashboard and in the income details modal
  groupIncomeDistribution(state, getters5) {
    return unadjustedDistribution({
      haveNeeds: getters5.haveNeedsForThisPeriod(getters5.currentPaymentPeriod),
      minimize: false
    });
  },
  // adjusted version of groupIncomeDistribution, used by the payments system
  groupIncomeAdjustedDistributionForGroup(state, getters5) {
    return (state2) => {
      const paymentInfo = getters5.thisPeriodPaymentInfoForGroup(state2);
      if (paymentInfo && paymentInfo.lastAdjustedDistribution) {
        return paymentInfo.lastAdjustedDistribution;
      } else {
        const period = getters5.currentPaymentPeriodForGroup(state2);
        return adjustedDistribution({
          distribution: unadjustedDistribution({
            haveNeeds: getters5.haveNeedsForThisPeriodForGroup(state2, period),
            minimize: getters5.groupSettingsForGroup(state2).minimizeDistribution
          }),
          payments: getters5.paymentsForPeriodForGroup(state2, period),
          dueOn: getters5.dueDateForPeriodForGroup(state2, period)
        });
      }
    };
  },
  groupIncomeAdjustedDistribution(state, getters5) {
    return getters5.groupIncomeAdjustedDistributionForGroup(getters5.currentGroupState);
  },
  ourPaymentsSentInPeriodForGroup(state, getters5) {
    return (state2, period) => {
      const periodPayments = getters5.groupPeriodPaymentsForGroup(state2);
      if (Object.keys(periodPayments).length === 0) return;
      const payments = [];
      const thisPeriodPayments = periodPayments[period];
      const paymentsFrom = thisPeriodPayments && thisPeriodPayments.paymentsFrom;
      if (paymentsFrom) {
        const ourIdentityContractId = getters5.ourIdentityContractId;
        const allPayments = state2.payments;
        for (const toMemberID in paymentsFrom[ourIdentityContractId]) {
          for (const paymentHash of paymentsFrom[ourIdentityContractId][toMemberID]) {
            const { data, meta, height } = allPayments[paymentHash];
            payments.push({ hash: paymentHash, height, data, meta, amount: data.amount, period });
          }
        }
      }
      return payments.sort((paymentA, paymentB) => paymentB.height - paymentA.height);
    };
  },
  ourPaymentsSentInPeriod(state, getters5) {
    return (period) => getters5.ourPaymentsSentInPeriodForGroup(getters5.currentGroupState, period);
  },
  ourPaymentsReceivedInPeriodForGroup(state, getters5) {
    return (state2, period) => {
      const periodPayments = getters5.groupPeriodPaymentsForGroup(state2);
      if (Object.keys(periodPayments).length === 0) return;
      const payments = [];
      const thisPeriodPayments = periodPayments[period];
      const paymentsFrom = thisPeriodPayments && thisPeriodPayments.paymentsFrom;
      if (paymentsFrom) {
        const ourIdentityContractId = getters5.ourIdentityContractId;
        const allPayments = state2.payments;
        for (const fromMemberID in paymentsFrom) {
          for (const toMemberID in paymentsFrom[fromMemberID]) {
            if (toMemberID === ourIdentityContractId) {
              for (const paymentHash of paymentsFrom[fromMemberID][toMemberID]) {
                const { data, meta, height } = allPayments[paymentHash];
                payments.push({ hash: paymentHash, height, data, meta, amount: data.amount });
              }
            }
          }
        }
      }
      return payments.sort((paymentA, paymentB) => paymentB.height - paymentA.height);
    };
  },
  ourPaymentsReceivedInPeriod(state, getters5) {
    return (period) => getters5.ourPaymentsReceivedInPeriodForGroup(getters5.currentGroupState, period);
  },
  ourPaymentsForGroup(state, getters5) {
    return (state2) => {
      const periodPayments = getters5.groupPeriodPaymentsForGroup(state2);
      if (Object.keys(periodPayments).length === 0) return;
      const ourIdentityContractId = getters5.ourIdentityContractId;
      const cPeriod = getters5.currentPaymentPeriodForGroup(state2);
      const pPeriod = getters5.periodBeforePeriodForGroup(state2, cPeriod);
      const currentSent = getters5.ourPaymentsSentInPeriodForGroup(state2, cPeriod);
      const previousSent = getters5.ourPaymentsSentInPeriodForGroup(state2, pPeriod);
      const currentReceived = getters5.ourPaymentsReceivedInPeriodForGroup(state2, cPeriod);
      const previousReceived = getters5.ourPaymentsReceivedInPeriodForGroup(state2, pPeriod);
      const todo = () => {
        return getters5.groupIncomeAdjustedDistributionForGroup(state2).filter((p) => p.fromMemberID === ourIdentityContractId);
      };
      return {
        sent: [...currentSent, ...previousSent],
        received: [...currentReceived, ...previousReceived],
        todo: todo()
      };
    };
  },
  ourPayments(state, getters5) {
    return getters5.ourPaymentsForGroup(getters5.currentGroupState);
  },
  ourPaymentsSummary(state, getters5) {
    const isNeeder = getters5.ourGroupProfile.incomeDetailsType === "incomeAmount";
    const ourIdentityContractId = getters5.ourIdentityContractId;
    const isOurPayment = (payment) => {
      return isNeeder ? payment.toMemberID === ourIdentityContractId : payment.fromMemberID === ourIdentityContractId;
    };
    const sumUpAmountReducer = (acc, payment) => acc + payment.amount;
    const cPeriod = getters5.currentPaymentPeriod;
    const ourAdjustedPayments = getters5.groupIncomeAdjustedDistribution.filter(isOurPayment);
    const receivedOrSent = isNeeder ? getters5.ourPaymentsReceivedInPeriod(cPeriod) : getters5.ourPaymentsSentInPeriod(cPeriod);
    const markedAsNotReceived = receivedOrSent.filter((payment) => payment.data.status === PAYMENT_NOT_RECEIVED);
    const markedAsNotReceivedTotal = markedAsNotReceived.reduce(sumUpAmountReducer, 0);
    const paymentsTotal = ourAdjustedPayments.length + receivedOrSent.length;
    const nonLateAdjusted = ourAdjustedPayments.filter((p) => !p.isLate);
    const paymentsDone = paymentsTotal - nonLateAdjusted.length - markedAsNotReceived.length;
    const hasPartials = ourAdjustedPayments.some((p) => p.partial);
    const amountDone = receivedOrSent.reduce(sumUpAmountReducer, 0) - markedAsNotReceivedTotal;
    const amountLeft = ourAdjustedPayments.reduce((acc, payment) => acc + payment.amount, 0) + markedAsNotReceivedTotal;
    const amountTotal = amountDone + amountLeft;
    return {
      paymentsDone,
      hasPartials,
      paymentsTotal,
      amountDone,
      amountTotal
    };
  },
  currentWelcomeInvite(state, getters5) {
    const invites = getters5.currentGroupState.invites;
    const inviteId = anyoneCanJoinInviteId(invites, getters5);
    const expires = getters5.currentGroupState._vm.invites[inviteId].expires;
    return { inviteId, expires };
  },
  // list of group names and contractIDs
  groupsByName(state, getters5) {
    const identityContractID2 = getters5.ourIdentityContractId;
    const groups = state[identityContractID2]?.groups;
    if (!groups) return [];
    return Object.entries(groups).filter(([, { hasLeft }]) => !hasLeft).map(([contractID]) => ({ groupName: state[contractID]?.settings?.groupName || L("Pending"), contractID, active: state[contractID]?.profiles?.[identityContractID2]?.status === PROFILE_STATUS.ACTIVE }));
  },
  profilesByGroup(state, getters5) {
    return (groupID) => {
      const profiles = {};
      if (state.contracts[groupID]?.type !== "gi.contracts/group") {
        return profiles;
      }
      const groupProfiles = state[groupID].profiles || {};
      for (const member in groupProfiles) {
        const profile = groupProfiles[member];
        if (profile.status === PROFILE_STATUS.ACTIVE) {
          profiles[member] = profile;
        }
      }
      return profiles;
    };
  },
  groupMembersSorted(state, getters5) {
    const profiles = getters5.currentGroupState.profiles;
    if (!profiles || !profiles[getters5.ourIdentityContractId]) return [];
    const weJoinedHeight = profiles[getters5.ourIdentityContractId].joinedHeight;
    const isNewMember = (memberID) => {
      if (memberID === getters5.ourIdentityContractId) {
        return false;
      }
      const memberProfile = profiles[memberID];
      if (!memberProfile) return false;
      const memberJoinedHeight = memberProfile.joinedHeight;
      const memberJoinedMs = new Date(memberProfile.joinedDate).getTime();
      const joinedAfterUs = weJoinedHeight < memberJoinedHeight;
      return joinedAfterUs && Date.now() - memberJoinedMs < 6048e5;
    };
    const groupMembersPending = getters5.groupMembersPending;
    return [groupMembersPending, getters5.groupProfiles].flatMap(Object.keys).filter((memberID) => getters5.groupProfiles[memberID] || !(getters5.groupMembersPending[memberID].expires < Date.now())).map((memberID) => {
      const { contractID, displayName, username } = getters5.globalProfile(memberID) || groupMembersPending[memberID] || (getters5.groupProfiles[memberID] ? { contractID: memberID } : {});
      return {
        id: memberID,
        // common unique ID: it can be either the contract ID or the invite key
        contractID,
        username,
        displayName: displayName || username || memberID,
        invitedBy: getters5.groupMembersPending[memberID],
        isNew: isNewMember(memberID)
      };
    }).sort((userA, userB) => {
      const nameA = userA.displayName.normalize().toUpperCase();
      const nameB = userB.displayName.normalize().toUpperCase();
      if (userA.invitedBy && !userB.invitedBy) {
        return -1;
      }
      if (!userA.invitedBy && userB.invitedBy) {
        return 1;
      }
      if (userA.isNew && !userB.isNew) {
        return -1;
      }
      if (!userA.isNew && userB.isNew) {
        return 1;
      }
      return nameA < nameB ? -1 : 1;
    });
  },
  groupProposals(state, getters5) {
    return (contractID) => state[contractID]?.proposals;
  },
  globalProfile(state, getters5) {
    return (memberID) => {
      return getters5.ourContactProfilesById[memberID];
    };
  },
  ourContactProfilesByUsername(state, getters5) {
    const profiles = {};
    Object.keys(state.contracts).filter((contractID) => state.contracts[contractID]?.type === "gi.contracts/identity").forEach((contractID) => {
      const attributes = state[contractID].attributes;
      if (attributes) {
        const username = checkedUsername(state, attributes.username, contractID);
        if (!username) return;
        profiles[username] = {
          ...attributes,
          username,
          contractID
        };
      }
    });
    return profiles;
  },
  ourContactProfilesById(state, getters5) {
    const profiles = {};
    Object.keys(state.contracts).filter((contractID) => state.contracts[contractID]?.type === "gi.contracts/identity").forEach((contractID) => {
      const attributes = state[contractID]?.attributes;
      if (attributes) {
        const username = checkedUsername(state, attributes.username, contractID);
        profiles[contractID] = {
          ...attributes,
          username,
          contractID
        };
      } else {
        profiles[contractID] = {
          contractID
        };
      }
    });
    Object.keys(state.reverseNamespaceLookups).forEach((contractID) => {
      if (profiles[contractID] || state.contracts[contractID] === null) return;
      profiles[contractID] = {
        username: state.reverseNamespaceLookups[contractID],
        contractID
      };
    });
    return profiles;
  },
  currentGroupContactProfilesById(state, getters5) {
    const currentGroupProfileIds = Object.keys(getters5.currentGroupState.profiles || {});
    const filtered = {};
    for (const identityContractID2 in getters5.ourContactProfilesById) {
      if (currentGroupProfileIds.includes(identityContractID2)) {
        filtered[identityContractID2] = getters5.ourContactProfilesById[identityContractID2];
      }
    }
    return filtered;
  },
  ourContactsById(state, getters5) {
    return Object.keys(getters5.ourContactProfilesById).sort((userIdA, userIdB) => {
      const nameA = getters5.ourContactProfilesById[userIdA].displayName || getters5.ourContactProfilesById[userIdA].username || userIdA;
      const nameB = getters5.ourContactProfilesById[userIdB].displayName || getters5.ourContactProfilesById[userIdB].username || userIdB;
      return nameA.normalize().toUpperCase() > nameB.normalize().toUpperCase() ? 1 : -1;
    });
  },
  ourContactsByUsername(state, getters5) {
    return Object.keys(getters5.ourContactProfilesByUsername).sort((usernameA, usernameB) => {
      const nameA = getters5.ourContactProfilesByUsername[usernameA].displayName || usernameA;
      const nameB = getters5.ourContactProfilesByUsername[usernameB].displayName || usernameB;
      return nameA.normalize().toUpperCase() > nameB.normalize().toUpperCase() ? 1 : -1;
    });
  },
  seenWelcomeScreen(state, getters5) {
    return getters5.currentIdentityState?.groups?.[state.currentGroupId]?.hasLeft || getters5.ourProfileActive && getters5.currentIdentityState?.groups?.[state.currentGroupId]?.seenWelcomeScreen;
  },
  ...chatroom_default2,
  ...group_default2,
  ...identity_default2
};
var getters_default = getters;

// frontend/model/notifications/vuexModule.js
init_vue_esm();

// frontend/model/notifications/getters.js
var getters2 = {
  notifications(state, getters5, rootState) {
    return state.items.map((item) => {
      const notification = { ...item, ...state.status[item.hash] };
      if (age(notification) > MAX_AGE_UNREAD) {
        return null;
      } else if (!notification.read && age(notification) > MAX_AGE_READ) {
        notification.read = true;
      }
      return notification;
    }).filter(Boolean);
  },
  // Notifications relevant to the current group only.
  currentGroupNotifications(state, getters5, rootState) {
    return getters5.notifications.filter((item) => item.groupID === rootState.currentGroupId);
  },
  // Notifications relevant to a specific group.
  notificationsByGroup(state, getters5) {
    return (groupID) => getters5.notifications.filter((item) => item.groupID === groupID);
  },
  currentGroupUnreadNotificationCount(state, getters5) {
    return getters5.currentGroupUnreadNotifications.length;
  },
  // Unread notifications relevant to the current group only.
  currentGroupUnreadNotifications(state, getters5, rootState) {
    return getters5.currentGroupNotifications.filter((item) => !item.read);
  },
  currentNewNotifications(state, getters5) {
    return getters5.currentNotifications.filter(isNew);
  },
  currentNotificationCount(state, getters5) {
    return getters5.currentNotifications.length;
  },
  // Notifications relevant to the current group, plus notifications that don't belong to any group in particular.
  currentNotifications(state, getters5, rootState) {
    return getters5.notifications.filter((item) => !item.groupID || item.groupID === rootState.currentGroupId);
  },
  currentOlderNotifications(state, getters5) {
    return getters5.currentNotifications.filter(isOlder);
  },
  currentUnreadNotificationCount(state, getters5) {
    return getters5.currentNotifications.filter((item) => !item.read).length;
  },
  currentUnreadNotifications(state, getters5) {
    return getters5.currentNotifications.filter((item) => !item.read);
  },
  totalUnreadNotificationCount(state, getters5) {
    return getters5.notifications.filter((item) => !item.read).length;
  },
  // Finds what number to display on a group's avatar badge in the sidebar. Used in GroupsList.vue.
  unreadGroupNotificationCountFor(state, getters5) {
    return (groupID) => getters5.unreadGroupNotificationsFor(groupID).length;
  },
  unreadGroupNotificationsFor(state, getters5, rootState) {
    return (groupID) => groupID === rootState.currentGroupId ? getters5.currentGroupUnreadNotifications : getters5.notifications.filter((item) => !item.read && item.groupID === groupID);
  }
};
var getters_default2 = getters2;

// frontend/model/notifications/mutationKeys.js
var ADD_NOTIFICATION = "addNotification";
var REMOVE_ALL_NOTIFICATIONS = "removeAllNotifications";
var REMOVE_NOTIFICATION = "removeNotification";
var SET_NOTIFICATIONS = "setNotifications";

// frontend/model/notifications/vuexModule.js
esm_default("okTurtles.events/on", NOTIFICATION_EMITTED, (notification) => {
  esm_default("state/vuex/commit", ADD_NOTIFICATION, notification);
});
esm_default("okTurtles.events/on", NOTIFICATION_REMOVED, (hashes) => {
  hashes.forEach((hash2) => esm_default("state/vuex/commit", REMOVE_NOTIFICATION, hash2));
});
esm_default("okTurtles.events/on", NOTIFICATION_STATUS_LOADED, (status) => {
  esm_default("state/vuex/commit", "setNotificationStatus", status);
});
var defaultState = {
  items: [],
  status: {}
};
var mutations = {
  // Seems necessary because the red badge would not clear upon signing up a new user in Cypress via the bypassUI mechanism.
  logout(state) {
    Object.keys(state).forEach((key) => {
      vue_esm_default.delete(state, key);
    });
    Object.assign(state, cloneDeep(defaultState));
  },
  [ADD_NOTIFICATION](state, notification) {
    if (state.items.some((item) => item.hash === notification.hash)) {
      return console.error("[ADD_NOTIFICATION] This notification is already in the store.", notification.hash);
    }
    state.items.push(notification);
    state.items.sort(compareOnTimestamp);
  },
  [REMOVE_NOTIFICATION](state, hash2) {
    const index = state.items.findIndex((item) => item.hash === hash2);
    if (index > -1) {
      state.items.splice(index, 1);
    }
  },
  [REMOVE_ALL_NOTIFICATIONS](state) {
    state.items.splice(0, state.items.length);
  },
  // Used upon successful login, with notifications from local storage.
  [SET_NOTIFICATIONS](state, notifications) {
    state.items.splice(0, state.items.length, ...notifications);
  },
  setNotificationStatus(state, status) {
    state.status = status;
  }
};
var vuexModule_default = {
  state: () => cloneDeep(defaultState),
  getters: getters_default2,
  mutations
};

// frontend/model/settings/vuexModule.js
var checkSystemColor = () => {
  return window.matchMedia?.("(prefers-color-scheme: dark)").matches ? THEME_DARK : THEME_LIGHT;
};
var updateMetaThemeTag = (theme2) => {
  const metaTag = document.querySelector('meta[name="theme-color"]');
  if (metaTag) {
    metaTag.content = theme2 === THEME_DARK ? "#2E3032" : "#F5F5F5";
  }
};
var defaultTheme = "system";
var defaultColor = checkSystemColor();
var defaultSettings = {
  appLogsFilter: true ? ["error", "warn", "info", "debug", "log"] : ["error", "warn", "info"],
  fontSize: 16,
  increasedContrast: false,
  notificationEnabled: null,
  // 3 values: null (unset), true (user-enabled), false (user-disabled)
  notificationVolume: 1,
  reducedMotion: false,
  theme: defaultTheme,
  themeColor: defaultColor
};
var getters3 = {
  colors(state) {
    return colors_default[state.themeColor];
  },
  fontSize(state) {
    return state.fontSize;
  },
  isDarkTheme(state) {
    return state.themeColor === THEME_DARK;
  },
  isReducedMotionMode(state) {
    return state.reducedMotion === true;
  },
  notificationVolume(state) {
    return state.notificationVolume;
  },
  theme(state) {
    return state.theme;
  }
};
var mutations2 = {
  resetSettings(state) {
    Object.assign(state, cloneDeep(defaultSettings));
  },
  setAppLogsFilter(state, filter) {
    state.appLogsFilter = filter;
    esm_default("okTurtles.events/emit", SET_APP_LOGS_FILTER, filter);
  },
  setFontSize(state, fontSize) {
    state.fontSize = fontSize;
  },
  setIncreasedContrast(state, isChecked) {
    state.increasedContrast = isChecked;
  },
  setNotificationEnabled(state, enabled) {
    console.info("[setNotificationEnabled] set to:", enabled);
    state.notificationEnabled = enabled;
    esm_default("sw/deviceSettings/set", DEVICE_SETTINGS.DISABLE_NOTIFICATIONS, !enabled).catch((e) => {
      console.error(`Couldn't set DISABLE_NOTIFICATIONS (${!enabled}): ${e.message}`);
    });
    esm_default("service-worker/setup-push-subscription").catch((e) => {
      console.error(`[setNotificationEnabled] Error calling setup-push-subscription (enabled=${enabled})`, e);
    });
  },
  setNotificationVolume(state, value) {
    state.notificationVolume = value;
  },
  setReducedMotion(state, isChecked) {
    state.reducedMotion = isChecked;
  },
  setTemporaryReducedMotion(state) {
    const tempSettings = state.reducedMotion;
    state.reducedMotion = true;
    setTimeout(() => {
      state.reducedMotion = tempSettings;
    }, 300);
  },
  setTheme(state, theme2) {
    state.theme = theme2;
    const themeColor = theme2 === "system" ? checkSystemColor() : theme2;
    state.themeColor = themeColor;
    esm_default("okTurtles.events/emit", THEME_CHANGE, themeColor);
  }
};
esm_default("okTurtles.events/on", LOGOUT, () => esm_default("state/vuex/commit", "resetSettings"));
esm_default("okTurtles.events/on", THEME_CHANGE, updateMetaThemeTag);
var vuexModule_default2 = {
  state: () => cloneDeep(defaultSettings),
  getters: getters3,
  mutations: mutations2
};

// frontend/model/chatroom/getters.js
var getters4 = {
  currentChatRoomId(state, getters5, rootState) {
    return state.currentChatRoomIDs[rootState.currentGroupId] || null;
  },
  currentChatRoomState(state, getters5, rootState) {
    return rootState[getters5.currentChatRoomId] || {};
  },
  chatNotificationSettings(state) {
    return Object.assign({
      publicDefault: {
        messageNotification: MESSAGE_NOTIFY_SETTINGS.DIRECT_MESSAGES,
        messageSound: MESSAGE_NOTIFY_SETTINGS.DIRECT_MESSAGES
      },
      privateDefault: {
        messageNotification: MESSAGE_NOTIFY_SETTINGS.ALL_MESSAGES,
        messageSound: MESSAGE_NOTIFY_SETTINGS.ALL_MESSAGES
      }
    }, state.chatNotificationSettings || {});
  },
  ourUnreadMessages(state) {
    return state.unreadMessages || {};
  },
  directMessagesByGroup(state, getters5, rootState) {
    return (groupID) => {
      const currentGroupDirectMessages = {};
      if (!groupID) {
        return currentGroupDirectMessages;
      }
      for (const chatRoomID of Object.keys(getters5.ourDirectMessages)) {
        const chatRoomState = rootState[chatRoomID];
        const directMessageSettings = getters5.ourDirectMessages[chatRoomID];
        const myIdentityId = getters5.ourIdentityContractId;
        if (!getters5.isJoinedChatRoom(chatRoomID, myIdentityId)) {
          continue;
        }
        const members = Object.keys(chatRoomState.members);
        const isDMToMyself = members.length === 1 && members[0] === myIdentityId;
        const partners = members.filter((memberID) => memberID !== myIdentityId).sort((p1, p2) => {
          const p1JoinedDate = new Date(chatRoomState.members[p1].joinedDate).getTime();
          const p2JoinedDate = new Date(chatRoomState.members[p2].joinedDate).getTime();
          return p1JoinedDate - p2JoinedDate;
        });
        const hasActiveMember = partners.some((memberID) => !!rootState[groupID]?.profiles[memberID]);
        if (directMessageSettings.visible && (isDMToMyself || hasActiveMember)) {
          const lastJoinedPartner = isDMToMyself ? myIdentityId : partners[partners.length - 1];
          const lastMsgTimeStamp = chatRoomState.messages?.length > 0 ? new Date(chatRoomState.messages[chatRoomState.messages.length - 1].datetime).getTime() : 0;
          currentGroupDirectMessages[chatRoomID] = {
            ...directMessageSettings,
            members,
            partners: partners.map((memberID) => ({
              contractID: memberID,
              username: getters5.usernameFromID(memberID),
              displayName: getters5.userDisplayNameFromID(memberID)
            })),
            lastJoinedPartner,
            // TODO: The UI should display display names, usernames and (in the future)
            // identity contract IDs differently in some way (e.g., font, font size,
            // prefix (@), etc.) to make it impossible (or at least obvious) to impersonate
            // users (e.g., 'user1' changing their display name to 'user2')
            title: isDMToMyself ? getters5.userDisplayNameFromID(myIdentityId) : partners.map((cID) => getters5.userDisplayNameFromID(cID)).join(", "),
            lastMsgTimeStamp,
            picture: getters5.ourContactProfilesById[lastJoinedPartner]?.picture,
            isDMToMyself
            // Can be useful when certain things in UI are meant only for 'DM to myself'
          };
        }
      }
      return currentGroupDirectMessages;
    };
  },
  ourGroupDirectMessages(state, getters5, rootState) {
    return getters5.directMessagesByGroup(rootState.currentGroupId);
  },
  // NOTE: this getter is used to find the ID of the direct message in the current group
  //       with the name[s] of partner[s]. Normally it's more useful to find direct message
  //       by the partners instead of contractID
  ourGroupDirectMessageFromUserIds(state, getters5, rootState) {
    return (partners) => {
      if (typeof partners === "string") {
        partners = [partners];
      }
      const shouldFindDMToMyself = partners.length === 1 && partners[0] === rootState.loggedIn.identityContractID;
      const currentGroupDirectMessages = getters5.ourGroupDirectMessages;
      return Object.keys(currentGroupDirectMessages).find((chatRoomID) => {
        const chatRoomSettings = currentGroupDirectMessages[chatRoomID];
        if (shouldFindDMToMyself) return chatRoomSettings.isDMToMyself;
        else {
          const cPartners = chatRoomSettings.partners.map((partner) => partner.contractID);
          return cPartners.length === partners.length && union(cPartners, partners).length === partners.length;
        }
      });
    };
  },
  isGroupDirectMessage(state, getters5) {
    return (chatRoomID) => !!getters5.ourGroupDirectMessages[chatRoomID || getters5.currentChatRoomId];
  },
  isDirectMessage(state, getters5) {
    return (chatRoomID) => !!getters5.ourDirectMessages[chatRoomID || getters5.currentChatRoomId];
  },
  isGroupDirectMessageToMyself(state, getters5) {
    return (chatRoomID) => {
      const chatRoomSettings = getters5.ourGroupDirectMessages[chatRoomID || getters5.currentChatRoomId];
      return !!chatRoomSettings && chatRoomSettings?.isDMToMyself;
    };
  },
  isJoinedChatRoom(state, getters5, rootState) {
    return (chatRoomID, memberID) => {
      return getters5.isJoinedChatRoomForChatRoom(rootState[chatRoomID], memberID);
    };
  },
  chatRoomActiveMemberIds(state, getters5) {
    return getters5.chatRoomActiveMemberIdsForChatRoom(getters5.currentChatRoomState);
  },
  currentChatVm(state, getters5, rootState) {
    return rootState?.[getters5.currentChatRoomId]?._vm || null;
  },
  currentChatRoomScrollPosition(state, getters5) {
    return state.chatRoomScrollPosition?.[getters5.currentChatRoomId];
  },
  currentChatRoomReadUntil(state, getters5) {
    return getters5.ourUnreadMessages[getters5.currentChatRoomId]?.readUntil;
  },
  chatRoomUnreadMessages(state, getters5) {
    return (chatRoomID) => {
      return getters5.ourUnreadMessages[chatRoomID]?.unreadMessages || [];
    };
  },
  isChatRoomManuallyMarkedUnread(state, getters5) {
    return (chatroomID) => {
      return Boolean(getters5.ourUnreadMessages[chatroomID || getters5.currentChatRoomId]?.readUntil?.isManuallyMarked);
    };
  },
  groupUnreadMessages(state, getters5, rootState) {
    return (groupID) => {
      const isGroupDirectMessage = (cID) => Object.keys(getters5.directMessagesByGroup(groupID)).includes(cID);
      const isGroupChatroom = (cID) => Object.keys(rootState[groupID]?.chatRooms || {}).includes(cID);
      return Object.keys(getters5.ourUnreadMessages).filter((cID) => isGroupDirectMessage(cID) || isGroupChatroom(cID)).map((cID) => getters5.ourUnreadMessages[cID].unreadMessages.length).reduce((sum, n) => sum + n, 0);
    };
  },
  groupIdFromChatRoomId(state, getters5, rootState) {
    return (chatRoomID) => Object.keys(rootState.contracts).find((cId) => rootState.contracts[cId]?.type === "gi.contracts/group" && Object.keys(rootState[cId].chatRooms).includes(chatRoomID));
  },
  chatRoomsInDetail(state, getters5, rootState) {
    const chatRoomsInDetail = merge({}, getters5.groupChatRooms);
    const myIdentityId = rootState.loggedIn.identityContractID;
    for (const contractID in chatRoomsInDetail) {
      const chatRoom = rootState[contractID];
      if (chatRoom?.attributes && getters5.isJoinedChatRoom(contractID, myIdentityId)) {
        chatRoomsInDetail[contractID] = {
          ...chatRoom.attributes,
          id: contractID,
          unreadMessagesCount: getters5.chatRoomUnreadMessages(contractID).length,
          joined: true
        };
      } else {
        const { name, privacyLevel } = chatRoomsInDetail[contractID];
        chatRoomsInDetail[contractID] = { id: contractID, name, privacyLevel, joined: false };
      }
    }
    return chatRoomsInDetail;
  },
  mentionableChatroomsInDetails(state, getters5) {
    return Object.values(getters5.chatRoomsInDetail).filter(
      (details) => [CHATROOM_PRIVACY_LEVEL.GROUP, CHATROOM_PRIVACY_LEVEL.PUBLIC].includes(details.privacyLevel) || details.privacyLevel === CHATROOM_PRIVACY_LEVEL.PRIVATE && details.joined
    );
  },
  getChatroomNameById(state, getters5) {
    return (chatRoomID) => {
      const found = Object.values(getters5.chatRoomsInDetail).find((details) => details.id === chatRoomID);
      return found ? found.name : null;
    };
  },
  chatRoomMembersInSort(state, getters5) {
    return getters5.groupMembersSorted.map((member) => ({ contractID: member.contractID, username: member.username, displayName: member.displayName })).filter((member) => !!getters5.chatRoomMembers[member.contractID]) || [];
  }
};
var getters_default3 = getters4;

// frontend/model/chatroom/vuexModule.js
init_vue_esm();
var defaultState2 = {
  currentChatRoomIDs: {},
  // { [groupId]: currentChatRoomId }
  pendingChatRoomIDs: {},
  // { [groupId]: currentChatRoomId }
  chatRoomScrollPosition: {},
  // [chatRoomID]: messageHash
  unreadMessages: null,
  // [chatRoomID]: { readUntil: { messageHash, createdHeight, isManuallyMarked?: boolean }, unreadMessages: [{ messageHash, createdHeight }]}
  chatNotificationSettings: {}
  // { [chatRoomID]: { messageNotification: MESSAGE_NOTIFY_SETTINGS, messageSound: MESSAGE_NOTIFY_SETTINGS } }
};
var mutations3 = {
  setCurrentChatRoomId(state, { groupID, chatRoomID }) {
    const rootState = esm_default("state/vuex/state");
    if (groupID && rootState[groupID]) {
      if (chatRoomID) {
        vue_esm_default.set(state.currentChatRoomIDs, groupID, chatRoomID);
      } else {
        vue_esm_default.set(state.currentChatRoomIDs, groupID, rootState[groupID].generalChatRoomId || null);
      }
      vue_esm_default.delete(state.pendingChatRoomIDs, groupID);
    } else {
      if (chatRoomID) {
        vue_esm_default.set(state.currentChatRoomIDs, rootState.currentGroupId, chatRoomID);
      } else {
        vue_esm_default.set(state.currentChatRoomIDs, rootState.currentGroupId, null);
      }
      vue_esm_default.delete(state.pendingChatRoomIDs, rootState.currentGroupId);
    }
  },
  setPendingChatRoomId(state, { groupID, chatRoomID }) {
    const rootState = esm_default("state/vuex/state");
    const rootGetters = esm_default("state/vuex/getters");
    if (rootGetters.isJoinedChatRoom(chatRoomID)) {
      mutations3.setCurrentChatRoomId(state, { groupID, chatRoomID });
      return;
    }
    if (groupID && rootState[groupID]) {
      if (chatRoomID) {
        vue_esm_default.set(state.pendingChatRoomIDs, groupID, chatRoomID);
      } else {
        vue_esm_default.set(state.pendingChatRoomIDs, groupID, null);
      }
    }
  },
  setUnreadMessages(state, value) {
    vue_esm_default.set(state, "unreadMessages", value);
  },
  setChatRoomScrollPosition(state, { chatRoomID, messageHash }) {
    vue_esm_default.set(state.chatRoomScrollPosition, chatRoomID, messageHash);
  },
  deleteChatRoomScrollPosition(state, { chatRoomID }) {
    vue_esm_default.delete(state.chatRoomScrollPosition, chatRoomID);
  },
  setChatroomNotificationSettings(state, { chatRoomID, settings }) {
    if (chatRoomID) {
      if (!state.chatNotificationSettings[chatRoomID]) {
        vue_esm_default.set(state.chatNotificationSettings, chatRoomID, {});
      }
      for (const key in settings) {
        vue_esm_default.set(state.chatNotificationSettings[chatRoomID], key, settings[key]);
      }
    }
  }
};
var vuexModule_default3 = {
  state: () => cloneDeep(defaultState2),
  getters: getters_default3,
  mutations: mutations3
};

// frontend/model/state.js
vue_esm_default.use(import_vuex_common.default);
var initialState = {
  currentGroupId: null,
  contracts: {},
  // contractIDs => { type:string, HEAD:string, height:number } (for contracts we've successfully subscribed to)
  loggedIn: false,
  // false | { username: string, identityContractID: string }
  namespaceLookups: /* @__PURE__ */ Object.create(null),
  // { [username]: sbp('namespace/lookup') }
  reverseNamespaceLookups: /* @__PURE__ */ Object.create(null),
  // { [contractID]: username }
  contractSigningKeys: /* @__PURE__ */ Object.create(null),
  lastLoggedIn: {},
  // Group last logged in information
  preferences: {},
  // { hideDistributionBanner: { [groupContractID]: boolean }, lastSeenNewsDate: string }
  periodicNotificationAlreadyFiredMap: {
    alreadyFired: /* @__PURE__ */ Object.create(null),
    // { notificationKey: boolean },
    lastRun: /* @__PURE__ */ Object.create(null)
    // { notificationKey: number },
  },
  kvStoreStatus: {
    // Context: Various parts of the app are closely related to kv-store. (eg. chatroom's 'readUntil' property is managed
    //          via kv-store so it's shared across multiple devices of the same user.)
    //          and thus sometimes need to know whether kv-store data has been loaded from the server and
    //          'kvStoreStatus' attribute here can be used to check the loading statuses of them.
    //
    // { [name]: 'non-init' | 'loading' | 'loaded' }
    identity: KV_LOAD_STATUS.NON_INIT,
    group: KV_LOAD_STATUS.NON_INIT
  }
};
if (window.matchMedia) {
  window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", (e) => {
    if (esm_default("state/vuex/getters").theme === "system") {
      store.commit("setTheme", "system");
    }
  });
}
var reactiveDate = vue_esm_default.observable({ date: /* @__PURE__ */ new Date() });
setInterval(function() {
  const rememberedPeriodStamp = store.getters.periodStampGivenDate?.(reactiveDate.date);
  const currentPeriodStamp = store.getters.periodStampGivenDate?.(/* @__PURE__ */ new Date());
  if (rememberedPeriodStamp !== currentPeriodStamp) {
    reactiveDate.date = /* @__PURE__ */ new Date();
  }
}, 60 * 1e3);
esm_default("sbp/selectors/register", {
  // 'state' is the Vuex state object, and it can only store JSON-like data
  "state/vuex/state": () => store.state,
  "state/vuex/reset": () => {
    const state = cloneDeep(initialState);
    state.notifications = vuexModule_default.state();
    state.settings = vuexModule_default2.state();
    state.chatroom = vuexModule_default3.state();
    state.idleVue = { isIdle: false };
    store.replaceState(state);
  },
  "state/vuex/replace": (state) => store.replaceState(state),
  "state/vuex/commit": (id, payload) => store.commit(id, payload),
  "state/vuex/getters": () => store.getters,
  "state/vuex/settings": () => store.state.settings,
  "state/vuex/postUpgradeVerification": function(state) {
    if (!state.preferences) {
      state.preferences = {};
    }
    if (!state.kvStoreStatus) {
      state.kvStoreStatus = {
        identity: KV_LOAD_STATUS.NON_INIT,
        group: KV_LOAD_STATUS.NON_INIT
      };
    }
    if (state.periodicNotificationAlreadyFiredMap) {
      if (!state.periodicNotificationAlreadyFiredMap.alreadyFired) {
        state.periodicNotificationAlreadyFiredMap.alreadyFired = /* @__PURE__ */ Object.create(null);
      }
      if (!state.periodicNotificationAlreadyFiredMap.lastRun) {
        state.periodicNotificationAlreadyFiredMap.lastRun = /* @__PURE__ */ Object.create(null);
      }
    }
    Promise.resolve(esm_default("chelonia/rootState")).then((state2) => {
      if (state2.namespaceLookups) return;
      const identityContractIDs = Object.entries(state2.contracts).filter(([, { type }]) => type === "gi.contracts/identity").map(([id]) => id);
      console.info("Fixing missing lookup entries", identityContractIDs);
      return Promise.all(identityContractIDs.map((id) => {
        const username = state2[id].attributes?.username;
        if (!username) return void 0;
        return esm_default("namespace/lookup", username, { skipCache: true });
      })).catch((e) => {
        console.error("[state/vuex/postUpgradeVerification] Error during lookup", e);
      });
    });
    (() => {
      Object.entries(state.namespaceLookups).filter(([, value]) => !state.reverseNamespaceLookups[value]).forEach(([name, value]) => {
        state.reverseNamespaceLookups[value] = name;
      });
      Object.entries(state.reverseNamespaceLookups).filter(([, name]) => !state.namespaceLookups[name]).forEach(([value, name]) => {
        state.namespaceLookups[name] = value;
      });
    })();
    (async () => {
      let count = 0;
      const internal = async () => {
        if (!state.loggedIn?.identityContractID && count++ <= 3) {
          setTimeout(internal, 300);
          return;
        }
        const ourIdentityContractId = state.loggedIn.identityContractID;
        const dmkId = await esm_default("chelonia/contract/currentKeyIdByName", ourIdentityContractId, "dmk");
        if (dmkId) return;
        await esm_default("gi.actions/identity/addDmk", ourIdentityContractId).catch((e) => {
          console.error("[postUpgradeVerification] Error adding DMK", e);
        });
      };
      await internal();
    })();
  },
  "state/vuex/save": (encrypted, state) => {
    return esm_default("okTurtles.eventQueue/queueEvent", "state/vuex/save", async function() {
      state = state || store.state;
      if (!state.loggedIn) {
        return;
      }
      const { identityContractID: identityContractID2, encryptionParams } = state.loggedIn;
      state.notifications.items = applyStorageRules(state.notifications.items || [], state.notifications.status || {});
      if (encrypted) {
        await esm_default("gi.db/settings/saveEncrypted", identityContractID2, state, encryptionParams);
      } else {
        await esm_default("gi.db/settings/save", identityContractID2, state);
      }
    });
  }
});
var mutations4 = {
  login(state, user) {
    state.loggedIn = user;
  },
  // isNewlyCreated will force a redirect to /pending-approval
  // forceRefresh will force a redirect to / (this is useful for closing
  // the modal when leaving a group; usually, it's not needed and should be
  // false)
  setCurrentGroupId(state, { contractID: currentGroupId, forceRefresh, isNewlyCreated }) {
    vue_esm_default.set(state, "currentGroupId", currentGroupId);
    if (!currentGroupId || forceRefresh) {
      if (esm_default("controller/router").history.current.path === "/join") return;
      esm_default("controller/router").push({ path: "/" }).catch(() => {
      });
    } else if (isNewlyCreated) {
      esm_default("controller/router").push({ path: "/pending-approval" }).catch(() => {
      });
    }
  },
  setPreferences(state, value) {
    vue_esm_default.set(state, "preferences", value);
  },
  setLastLoggedIn(state, [groupID, value]) {
    vue_esm_default.set(state.lastLoggedIn, groupID, value);
  },
  setKvStoreStatus(state, { name, status }) {
    if (name && status) {
      if (!Object.values(KV_LOAD_STATUS).includes(status)) {
        return console.warn(`bad setKvStoreStatus for ${name}: ${status}`);
      }
      state.kvStoreStatus[name] = status;
    }
  },
  // Since Chelonia directly modifies contract state without using 'commit', we
  // need this hack to tell the vuex developer tool it needs to refresh the state
  noop() {
  }
};
var store = new import_vuex_common.default.Store({
  state: cloneDeep(initialState),
  mutations: mutations4,
  getters: {
    ...getters_default,
    // this getter gets recomputed automatically according to the setInterval on reactiveDate
    currentPaymentPeriodForGroup(state, getters5) {
      return (state2) => getters5.periodStampGivenDateForGroup(state2, reactiveDate.date);
    },
    currentPaymentPeriod(state, getters5) {
      return getters5.currentPaymentPeriodForGroup(getters5.currentGroupState);
    }
  },
  modules: {
    notifications: vuexModule_default,
    settings: vuexModule_default2,
    chatroom: vuexModule_default3
  },
  strict: false
  // we're intentionally modifying state outside of commits
});
store.watch(function(state, getters5) {
  return getters5.currentGroupState.settings?.distributionDate;
}, function() {
  reactiveDate.date = /* @__PURE__ */ new Date();
});
var logoutInProgress = false;
var debouncedSave = debounce(() => !logoutInProgress && esm_default("state/vuex/save"), 500);
store.subscribe((commit) => {
  if (commit.type !== "noop") {
    debouncedSave();
  }
});
esm_default("okTurtles.events/on", EVENT_HANDLED, debouncedSave);
esm_default("sbp/filters/selector/add", "gi.app/identity/logout", function() {
  logoutInProgress = true;
  debouncedSave.clear();
});
esm_default("okTurtles.events/on", LOGOUT, () => {
  logoutInProgress = false;
});
if (true) {
  esm_default("okTurtles.events/on", EVENT_HANDLED, debounce(() => {
    store.commit("noop");
  }, 500));
}
esm_default("okTurtles.events/on", CONTRACT_REGISTERED, async (contract) => {
  const { contracts: { manifests } } = await esm_default("chelonia/config");
  if (manifests[contract.name] === contract.manifest) {
    if (contract.name === "gi.contracts/group") {
      store.watch(
        (state, getters5) => getters5.currentPaymentPeriod,
        (newPeriod, oldPeriod) => {
          const distributionDateInSettings = store.getters.groupSettings.distributionDate;
          const distributionStarted = store.getters.groupDistributionStarted(reactiveDate.date);
          if (oldPeriod && newPeriod && distributionStarted && newPeriod !== distributionDateInSettings) {
            esm_default("gi.actions/group/updateDistributionDate", { contractID: store.state.currentGroupId }).catch((e) => {
              console.error("okTurtles.events/on CONTRACT_REGISTERED Error calling updateDistributionDate", e);
            });
          }
        }
      );
    }
  }
});
var state_default = store;

// frontend/views/pages/Home.vue
var __vue_script__ = {
  name: "Home",
  components: {
    SvgJoinGroup: join_group_default,
    SvgCreateGroup: create_group_default,
    BannerSimple: BannerSimple_default,
    ButtonSubmit: ButtonSubmit_default,
    RenderMessageWithMarkdown: RenderMessageWithMarkdown_default
  },
  computed: {
    ...mapGetters([
      "currentIdentityState",
      "seenWelcomeScreen"
    ]),
    ...mapState([
      "currentGroupId"
    ]),
    isLoggedIn() {
      return !!this.$store.state.loggedIn;
    }
  },
  data() {
    return {
      ephemeral: {
        serverMessages: [],
        seenWelcomeScreen: false,
        listener: ({ contractID }) => {
          if (contractID !== this.currentGroupId) return;
          this.ephemeral.seenWelcomeScreen = false;
        },
        showPwaPromo: false
      }
    };
  },
  beforeMount() {
    esm_default("okTurtles.events/on", ACCEPTED_GROUP, this.ephemeral.listener);
    this.checkPwaInstallability();
    fetch(`${esm_default("okTurtles.data/get", "API_URL")}/serverMessages`).then(async (r2) => {
      const data = await r2.json();
      this.ephemeral.serverMessages = data.filter((msg) => msg.type === "giHomeWarning").map((msg) => msg.message);
    }).catch((e) => {
      console.error("[Home.vue] Error fetching or processing server announcements", e);
    });
  },
  mounted() {
    this.ephemeral.seenWelcomeScreen = this.seenWelcomeScreen;
    if (this.isLoggedIn && this.currentGroupId) {
      this.navigateToGroupPage();
    } else if (this.$route.query.next) {
      this.openModal("LoginModal");
    }
  },
  destroyed() {
    esm_default("okTurtles.events/off", ACCEPTED_GROUP, this.ephemeral.listener);
  },
  methods: {
    openModal(mode) {
      esm_default("okTurtles.events/emit", OPEN_MODAL, mode);
    },
    navigateToGroupPage() {
      const path = this.$route.query.next ?? (this.ephemeral.seenWelcomeScreen ? "/dashboard" : "/pending-approval");
      this.$router.push({ path }).catch((e) => ignoreWhenNavigationCancelled(e, path));
    },
    checkPwaInstallability() {
      this.ephemeral.showPwaPromo = esm_default("service-worker/check-pwa-installability");
      esm_default("okTurtles.events/once", PWA_INSTALLABLE, () => {
        this.ephemeral.showPwaPromo = true;
      });
    },
    async onInstallClick() {
      await esm_default("service-worker/trigger-install-prompt");
    }
  },
  watch: {
    currentGroupId(to) {
      this.ephemeral.seenWelcomeScreen = this.seenWelcomeScreen;
      if (to && this.isLoggedIn) {
        this.navigateToGroupPage();
      }
    },
    isLoggedIn(to) {
      if (to && this.currentGroupId) {
        this.navigateToGroupPage();
      }
    }
  }
};
var __vue_render__ = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return !_vm.currentGroupId ? _c(
    "main",
    { staticClass: "c-splash", attrs: { "data-test": "homeLogo" } },
    [
      !_vm.isLoggedIn ? _c(
        "header",
        { key: "title-login" },
        [
          _c("img", {
            staticClass: "logo",
            attrs: {
              src: "/assets/images/group-income-icon-transparent.png"
            }
          }),
          _c(
            "i18n",
            {
              staticClass: "is-title-1",
              attrs: { tag: "h1", "data-test": "welcomeHome" }
            },
            [_vm._v("Welcome to Group Income")]
          )
        ],
        1
      ) : _c(
        "header",
        { key: "title-not-login" },
        [
          _c("img", {
            staticClass: "logo-2",
            attrs: { src: "/assets/images/logo-transparent.png" }
          }),
          _c(
            "i18n",
            { staticClass: "is-subtitle", attrs: { tag: "p" } },
            [_vm._v("Welcome to Group Income")]
          ),
          _c(
            "i18n",
            {
              staticClass: "is-title-1",
              attrs: { tag: "h1", "data-test": "welcomeHomeLoggedIn" }
            },
            [_vm._v("Let\u2019s get this party started")]
          )
        ],
        1
      ),
      !_vm.isLoggedIn ? _c(
        "div",
        { key: "body-loggin", staticClass: "buttons" },
        [
          _c(
            "i18n",
            {
              ref: "loginBtn",
              attrs: { tag: "button", "data-test": "loginBtn" },
              on: {
                click: function($event) {
                  return _vm.openModal("LoginModal");
                }
              }
            },
            [_vm._v("Login")]
          ),
          _c(
            "i18n",
            {
              directives: [{ name: "focus", rawName: "v-focus" }],
              ref: "signupBtn",
              attrs: { tag: "button", "data-test": "signupBtn" },
              on: {
                click: function($event) {
                  return _vm.openModal("SignupModal");
                },
                keyup: function($event) {
                  if (!$event.type.indexOf("key") && _vm._k(
                    $event.keyCode,
                    "enter",
                    13,
                    $event.key,
                    "Enter"
                  )) {
                    return null;
                  }
                  return _vm.openModal("SignupModal");
                }
              }
            },
            [_vm._v("Signup")]
          )
        ],
        1
      ) : _c("div", { key: "body-create", staticClass: "create-or-join" }, [
        _c(
          "div",
          { staticClass: "card" },
          [
            _c("svg-create-group"),
            _c(
              "i18n",
              { staticClass: "is-title-3", attrs: { tag: "h3" } },
              [_vm._v("Create")]
            ),
            _c("i18n", { attrs: { tag: "p" } }, [
              _vm._v("Create a new group and invite your friends.")
            ]),
            _c(
              "i18n",
              {
                attrs: {
                  tag: "button",
                  "data-test": "createGroup",
                  "aria-label": _vm.L("Add a group")
                },
                on: {
                  click: function($event) {
                    return _vm.openModal("GroupCreationModal");
                  }
                }
              },
              [_vm._v("Create Group")]
            )
          ],
          1
        ),
        _c(
          "div",
          { staticClass: "card" },
          [
            _c("svg-join-group"),
            _c(
              "i18n",
              { staticClass: "is-title-3", attrs: { tag: "h3" } },
              [_vm._v("Join")]
            ),
            _c("i18n", { attrs: { tag: "p" } }, [
              _vm._v("Enter an existing group using your username.")
            ]),
            _c(
              "i18n",
              {
                attrs: { tag: "button", "data-test": "joinGroup" },
                on: {
                  click: function($event) {
                    return _vm.openModal("GroupJoinModal");
                  }
                }
              },
              [_vm._v("Join a Group")]
            )
          ],
          1
        )
      ]),
      !_vm.isLoggedIn && _vm.ephemeral.showPwaPromo ? _c(
        "banner-simple",
        {
          staticClass: "hide-hoverable-device hide-tablet c-pwa-promo",
          attrs: { severity: "general" },
          scopedSlots: _vm._u(
            [
              {
                key: "header",
                fn: function() {
                  return [
                    _c("i18n", [
                      _vm._v(
                        "Install this web app on your device for better usability."
                      )
                    ])
                  ];
                },
                proxy: true
              }
            ],
            null,
            false,
            1382767684
          )
        },
        [
          _c(
            "button-submit",
            {
              staticClass: "is-success is-small c-install-btn",
              attrs: { type: "button" },
              on: { click: _vm.onInstallClick }
            },
            [_vm._v(_vm._s(_vm.L("Install")))]
          )
        ],
        1
      ) : _vm._e(),
      !_vm.isLoggedIn && _vm.ephemeral.serverMessages.length > 0 ? _c(
        "footer",
        { staticClass: "c-footer" },
        _vm._l(_vm.ephemeral.serverMessages, function(text) {
          return _c(
            "banner-simple",
            {
              staticClass: "c-demo-warning",
              attrs: { severity: "warning" }
            },
            [
              _c("render-message-with-markdown", {
                attrs: { text }
              })
            ],
            1
          );
        }),
        1
      ) : _vm._e()
    ],
    1
  ) : _vm._e();
};
var __vue_staticRenderFns__ = [];
__vue_render__._withStripped = true;
var __vue_inject_styles__ = function(inject) {
  if (!inject) return;
  inject("data-v-eb568d1c_0", { source: ".c-splash[data-v-eb568d1c] {\n  display: flex;\n  flex-direction: column;\n  text-align: center;\n  align-items: center;\n  min-height: 100%;\n}\n@media screen and (min-width: 769px), print {\n.c-splash[data-v-eb568d1c] {\n    justify-content: center;\n}\n}\n.logo[data-v-eb568d1c] {\n  width: 8rem;\n  margin: 2rem auto;\n}\n.logo-2[data-v-eb568d1c] {\n  width: 8rem;\n  margin: 0 auto 3rem auto;\n}\n@media screen and (min-width: 769px), print {\n.logo-2[data-v-eb568d1c] {\n    margin: 0;\n    position: absolute;\n    left: 1.5rem;\n    top: 1.5rem;\n}\n}\n@media screen and (min-width: 769px), print {\n.is-subtitle[data-v-eb568d1c] {\n    margin-top: 3rem;\n}\n}\n@media screen and (min-width: 1200px) {\n.is-subtitle[data-v-eb568d1c] {\n    margin-top: 0;\n}\n}\n.buttons[data-v-eb568d1c] {\n  min-width: 230px;\n}\n.create-or-join[data-v-eb568d1c] {\n  display: flex;\n  width: 100%;\n  flex-wrap: wrap;\n  margin-top: 0.5rem;\n  justify-content: center;\n  text-align: left;\n}\n@media screen and (min-width: 769px), print {\n.create-or-join[data-v-eb568d1c] {\n    text-align: center;\n}\n}\n.card[data-v-eb568d1c] {\n  margin: 1.5rem 1.5rem 0 1.5rem;\n  width: 100%;\n}\n@media screen and (min-width: 769px), print {\n.card[data-v-eb568d1c] {\n    width: 24.375rem;\n}\n}\n.card h3[data-v-eb568d1c] {\n  margin-bottom: 0.25rem;\n}\n.card svg[data-v-eb568d1c] {\n  width: 6rem;\n  margin-left: 1rem;\n  float: right;\n}\n@media screen and (min-width: 769px), print {\n.card svg[data-v-eb568d1c] {\n    float: none;\n    width: 9.75rem;\n    margin: 2.5rem auto 1.5rem auto;\n}\n}\n.card .button[data-v-eb568d1c] {\n  margin-top: 2rem;\n}\n@media screen and (min-width: 769px), print {\n.card .button[data-v-eb568d1c] {\n    margin-bottom: 1rem;\n}\n}\n.c-footer[data-v-eb568d1c] {\n  margin-top: 2.25rem;\n  padding: 0 1rem;\n  max-width: 31.25rem;\n}\n.c-demo-warning[data-v-eb568d1c] {\n  text-align: start;\n  margin-block-end: 1rem;\n}\n.c-pwa-promo[data-v-eb568d1c] {\n  position: fixed;\n  bottom: 1.875rem;\n  left: 50%;\n  transform: translateX(-50%);\n  z-index: 10;\n  text-align: left;\n  width: calc(100vw - 3rem);\n  max-width: 31.25rem;\n}\n.c-pwa-promo[data-v-eb568d1c]  .c-body {\n  margin-top: 1rem;\n  text-align: right;\n}\n.c-install-btn[data-v-eb568d1c] {\n  padding-left: 1.75rem;\n  padding-right: 1.75rem;\n}\n\n/*# sourceMappingURL=Home.vue.map */", map: { "version": 3, "sources": ["frontend/views/pages/Home.vue", "Home.vue"], "names": [], "mappings": "AAuLA;EACA,aAAA;EACA,sBAAA;EACA,kBAAA;EACA,mBAAA;EACA,gBAAA;ACtLA;AACA;ADgLA;IAQA,uBAAA;ACrLE;AACF;ADwLA;EACA,WAAA;EACA,iBAAA;ACrLA;ADwLA;EACA,WAAA;EACA,wBAAA;ACrLA;AACA;ADkLA;IAKA,SAAA;IACA,kBAAA;IACA,YAAA;IACA,WAAA;ACpLE;AACF;AAEA;ADqLA;IAEA,gBAAA;ACpLE;AACF;AACA;ADgLA;IAMA,aAAA;ACnLE;AACF;ADsLA;EACA,gBAAA;ACnLA;ADsLA;EACA,aAAA;EACA,WAAA;EACA,eAAA;EACA,kBAAA;EACA,uBAAA;EACA,gBAAA;ACnLA;AACA;AD4KA;IAQA,kBAAA;ACjLE;AACF;ADoLA;EACA,8BAAA;EACA,WAAA;ACjLA;AACA;AD8KA;IAKA,gBAAA;AChLE;AACF;ADkLA;EACA,sBAAA;AChLA;ADmLA;EACA,WAAA;EACA,iBAAA;EACA,YAAA;ACjLA;AACA;AD6KA;IAKA,WAAA;IACA,cAAA;IACA,+BAAA;AC/KE;AACF;ADkLA;EACA,gBAAA;AChLA;AACA;AD8KA;IAIA,mBAAA;AC/KE;AACF;ADmLA;EACA,mBAAA;EACA,eAAA;EACA,mBAAA;AChLA;ADmLA;EACA,iBAAA;EACA,sBAAA;AChLA;ADmLA;EACA,eAAA;EACA,gBAAA;EACA,SAAA;EACA,2BAAA;EACA,WAAA;EACA,gBAAA;EACA,yBAAA;EACA,mBAAA;AChLA;ADkLA;EACA,gBAAA;EACA,iBAAA;AChLA;ADoLA;EACA,qBAAA;EACA,sBAAA;ACjLA;;AAEA,mCAAmC", "file": "Home.vue", "sourcesContent": [`<template lang='pug'>
main.c-splash(data-test='homeLogo' v-if='!currentGroupId')
  //- TODO: split this into two files, one showing the login/signup buttons
  //-       and the other showing the create/join group buttons!
  header(v-if='!isLoggedIn' key='title-login')
    img.logo(src='/assets/images/group-income-icon-transparent.png')
    i18n.is-title-1(tag='h1' data-test='welcomeHome') Welcome to Group Income

  header(v-else key='title-not-login')
    img.logo-2(src='/assets/images/logo-transparent.png')
    i18n.is-subtitle(tag='p') Welcome to Group Income
    i18n.is-title-1(tag='h1' data-test='welcomeHomeLoggedIn') Let\u2019s get this party started

  .buttons(v-if='!isLoggedIn' key='body-loggin')
    i18n(
      tag='button'
      ref='loginBtn'
      @click='openModal("LoginModal")'
      data-test='loginBtn'
    ) Login

    i18n(
      tag='button'
      ref='signupBtn'
      @click='openModal("SignupModal")'
      @keyup.enter='openModal("SignupModal")'
      data-test='signupBtn'
      v-focus=''
    ) Signup

  .create-or-join(v-else key='body-create')
    .card
      svg-create-group
      i18n.is-title-3(tag='h3') Create
      i18n(tag='p') Create a new group and invite your friends.

      i18n(
        tag='button'
        @click='openModal("GroupCreationModal")'
        data-test='createGroup'
        :aria-label='L("Add a group")'
      ) Create Group

    .card
      svg-join-group
      i18n.is-title-3(tag='h3') Join
      i18n(tag='p') Enter an existing group using your username.
      i18n(
        tag='button'
        @click='openModal("GroupJoinModal")'
        data-test='joinGroup'
      ) Join a Group

  banner-simple.hide-hoverable-device.hide-tablet.c-pwa-promo(
    v-if='!isLoggedIn && ephemeral.showPwaPromo'
    severity='general'
  )
    template(#header='')
      i18n Install this web app on your device for better usability.

    button-submit.is-success.is-small.c-install-btn(
      type='button'
      @click='onInstallClick'
    ) {{ L('Install') }}

  footer.c-footer(v-if='!isLoggedIn && ephemeral.serverMessages.length > 0')
    banner-simple.c-demo-warning(v-for='text in ephemeral.serverMessages' severity='warning')
      render-message-with-markdown(:text='text')
</template>

<script>
import sbp from '@sbp/sbp'
import { mapGetters, mapState } from 'vuex'
import { ACCEPTED_GROUP, OPEN_MODAL, PWA_INSTALLABLE } from '../../../frontend/utils/events.js'
import BannerSimple from '../../../frontend/views/components/banners/BannerSimple.vue'
import ButtonSubmit from '../../../frontend/views/components/ButtonSubmit.vue'
import RenderMessageWithMarkdown from '../../../frontend/views/containers/chatroom/chat-mentions/RenderMessageWithMarkdown.js'
import SvgCreateGroup from '../../../frontend/assets/svgs/create-group.svg'
import SvgJoinGroup from '../../../frontend/assets/svgs/join-group.svg'
import { ignoreWhenNavigationCancelled } from '../../../frontend/views/utils/misc.js'

export default ({
  name: 'Home',
  components: {
    SvgJoinGroup,
    SvgCreateGroup,
    BannerSimple,
    ButtonSubmit,
    RenderMessageWithMarkdown
  },
  computed: {
    ...mapGetters([
      'currentIdentityState',
      'seenWelcomeScreen'
    ]),
    ...mapState([
      'currentGroupId'
    ]),
    isLoggedIn () {
      return !!this.$store.state.loggedIn
    }
  },
  data () {
    return {
      ephemeral: {
        serverMessages: [],
        seenWelcomeScreen: false,
        listener: ({ contractID }) => {
          if (contractID !== this.currentGroupId) return
          // For first time joins, force redirect to /pending-approval
          this.ephemeral.seenWelcomeScreen = false
        },
        showPwaPromo: false
      }
    }
  },
  beforeMount () {
    sbp('okTurtles.events/on', ACCEPTED_GROUP, this.ephemeral.listener)
    this.checkPwaInstallability()
    fetch(\`\${sbp('okTurtles.data/get', 'API_URL')}/serverMessages\`).then(async (r) => {
      const data = await r.json()
      this.ephemeral.serverMessages = data.filter((msg) => msg.type === 'giHomeWarning').map((msg) => msg.message)
    }).catch(e => {
      console.error('[Home.vue] Error fetching or processing server announcements', e)
    })
  },
  mounted () {
    this.ephemeral.seenWelcomeScreen = this.seenWelcomeScreen
    if (this.isLoggedIn && this.currentGroupId) {
      this.navigateToGroupPage()
    } else if (this.$route.query.next) {
      this.openModal('LoginModal')
    }
  },
  destroyed () {
    sbp('okTurtles.events/off', ACCEPTED_GROUP, this.ephemeral.listener)
  },
  methods: {
    openModal (mode) {
      sbp('okTurtles.events/emit', OPEN_MODAL, mode)
    },
    navigateToGroupPage () {
      // NOTE:
      // When browser refresh is triggered, there is an issue that Vue router prematurely decides ('loginGuard' there) that
      // the user is not signed in when in actual reality user-login is still being processed and then
      // takes user to 'Home.vue' with the '$route.query.next' being set to the initial url.
      // In this particular condition, the app needs to immediately redirect user to '$route.query.next'
      // so that the user stays in the same page after the browser refresh.
      // (Related GH issue: https://github.com/okTurtles/group-income/issues/1830)
      const path = this.$route.query.next ?? (this.ephemeral.seenWelcomeScreen ? '/dashboard' : '/pending-approval')
      this.$router.push({ path }).catch(e => ignoreWhenNavigationCancelled(e, path))
    },
    checkPwaInstallability () {
      this.ephemeral.showPwaPromo = sbp('service-worker/check-pwa-installability')
      sbp('okTurtles.events/once', PWA_INSTALLABLE, () => {
        this.ephemeral.showPwaPromo = true
      })
    },
    async onInstallClick () {
      await sbp('service-worker/trigger-install-prompt')
    }
  },
  watch: {
    currentGroupId (to) {
      // Redirect to \`/pending-approval\` if our profile isn't active or the
      // welcome screen hasn't been approved
      this.ephemeral.seenWelcomeScreen = this.seenWelcomeScreen
      if (to && this.isLoggedIn) {
        this.navigateToGroupPage()
      }
    },
    isLoggedIn (to) {
      if (to && this.currentGroupId) {
        this.navigateToGroupPage()
      }
    }
  }
}: Object)
<\/script>

<style scoped lang="scss">
@import "../../../frontend/assets/style/_variables.scss";

.c-splash {
  display: flex;
  flex-direction: column;
  text-align: center;
  align-items: center;
  min-height: 100%;

  @include tablet {
    justify-content: center;
  }
}

.logo {
  width: 8rem;
  margin: 2rem auto;
}

.logo-2 {
  width: 8rem;
  margin: 0 auto 3rem auto;

  @include tablet {
    margin: 0;
    position: absolute;
    left: 1.5rem;
    top: 1.5rem;
  }
}

.is-subtitle {
  @include tablet {
    margin-top: 3rem;
  }

  @include desktop {
    margin-top: 0;
  }
}

.buttons {
  min-width: 230px;
}

.create-or-join {
  display: flex;
  width: 100%;
  flex-wrap: wrap;
  margin-top: 0.5rem;
  justify-content: center;
  text-align: left;
  @include tablet {
    text-align: center;
  }
}

.card {
  margin: 1.5rem 1.5rem 0 1.5rem;
  width: 100%;

  @include tablet {
    width: 24.375rem;
  }

  h3 {
    margin-bottom: 0.25rem;
  }

  svg {
    width: 6rem;
    margin-left: 1rem;
    float: right;
    @include tablet {
      float: none;
      width: 9.75rem;
      margin: 2.5rem auto 1.5rem auto;
    }
  }

  .button {
    margin-top: 2rem;

    @include tablet {
      margin-bottom: 1rem;
    }
  }
}

.c-footer {
  margin-top: 2.25rem;
  padding: 0 1rem;
  max-width: 31.25rem;
}

.c-demo-warning {
  text-align: start;
  margin-block-end: 1rem;
}

.c-pwa-promo {
  position: fixed;
  bottom: 1.875rem;
  left: 50%;
  transform: translateX(-50%);
  z-index: 10; // should be less than modal, tooltip, prompt etc.
  text-align: left;
  width: calc(100vw - 3rem);
  max-width: 31.25rem;

  ::v-deep .c-body {
    margin-top: 1rem;
    text-align: right;
  }
}

.c-install-btn {
  padding-left: 1.75rem;
  padding-right: 1.75rem;
}
</style>
`, ".c-splash {\n  display: flex;\n  flex-direction: column;\n  text-align: center;\n  align-items: center;\n  min-height: 100%;\n}\n@media screen and (min-width: 769px), print {\n  .c-splash {\n    justify-content: center;\n  }\n}\n\n.logo {\n  width: 8rem;\n  margin: 2rem auto;\n}\n\n.logo-2 {\n  width: 8rem;\n  margin: 0 auto 3rem auto;\n}\n@media screen and (min-width: 769px), print {\n  .logo-2 {\n    margin: 0;\n    position: absolute;\n    left: 1.5rem;\n    top: 1.5rem;\n  }\n}\n\n@media screen and (min-width: 769px), print {\n  .is-subtitle {\n    margin-top: 3rem;\n  }\n}\n@media screen and (min-width: 1200px) {\n  .is-subtitle {\n    margin-top: 0;\n  }\n}\n\n.buttons {\n  min-width: 230px;\n}\n\n.create-or-join {\n  display: flex;\n  width: 100%;\n  flex-wrap: wrap;\n  margin-top: 0.5rem;\n  justify-content: center;\n  text-align: left;\n}\n@media screen and (min-width: 769px), print {\n  .create-or-join {\n    text-align: center;\n  }\n}\n\n.card {\n  margin: 1.5rem 1.5rem 0 1.5rem;\n  width: 100%;\n}\n@media screen and (min-width: 769px), print {\n  .card {\n    width: 24.375rem;\n  }\n}\n.card h3 {\n  margin-bottom: 0.25rem;\n}\n.card svg {\n  width: 6rem;\n  margin-left: 1rem;\n  float: right;\n}\n@media screen and (min-width: 769px), print {\n  .card svg {\n    float: none;\n    width: 9.75rem;\n    margin: 2.5rem auto 1.5rem auto;\n  }\n}\n.card .button {\n  margin-top: 2rem;\n}\n@media screen and (min-width: 769px), print {\n  .card .button {\n    margin-bottom: 1rem;\n  }\n}\n\n.c-footer {\n  margin-top: 2.25rem;\n  padding: 0 1rem;\n  max-width: 31.25rem;\n}\n\n.c-demo-warning {\n  text-align: start;\n  margin-block-end: 1rem;\n}\n\n.c-pwa-promo {\n  position: fixed;\n  bottom: 1.875rem;\n  left: 50%;\n  transform: translateX(-50%);\n  z-index: 10;\n  text-align: left;\n  width: calc(100vw - 3rem);\n  max-width: 31.25rem;\n}\n.c-pwa-promo ::v-deep .c-body {\n  margin-top: 1rem;\n  text-align: right;\n}\n\n.c-install-btn {\n  padding-left: 1.75rem;\n  padding-right: 1.75rem;\n}\n\n/*# sourceMappingURL=Home.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__ = "data-v-eb568d1c";
var __vue_module_identifier__ = void 0;
var __vue_is_functional_template__ = false;
function __vue_normalize__(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
main.c-splash(data-test='homeLogo' v-if='!currentGroupId')
  //- TODO: split this into two files, one showing the login/signup buttons
  //-       and the other showing the create/join group buttons!
  header(v-if='!isLoggedIn' key='title-login')
    img.logo(src='/assets/images/group-income-icon-transparent.png')
    i18n.is-title-1(tag='h1' data-test='welcomeHome') Welcome to Group Income

  header(v-else key='title-not-login')
    img.logo-2(src='/assets/images/logo-transparent.png')
    i18n.is-subtitle(tag='p') Welcome to Group Income
    i18n.is-title-1(tag='h1' data-test='welcomeHomeLoggedIn') Let\u2019s get this party started

  .buttons(v-if='!isLoggedIn' key='body-loggin')
    i18n(
      tag='button'
      ref='loginBtn'
      @click='openModal("LoginModal")'
      data-test='loginBtn'
    ) Login

    i18n(
      tag='button'
      ref='signupBtn'
      @click='openModal("SignupModal")'
      @keyup.enter='openModal("SignupModal")'
      data-test='signupBtn'
      v-focus=''
    ) Signup

  .create-or-join(v-else key='body-create')
    .card
      svg-create-group
      i18n.is-title-3(tag='h3') Create
      i18n(tag='p') Create a new group and invite your friends.

      i18n(
        tag='button'
        @click='openModal("GroupCreationModal")'
        data-test='createGroup'
        :aria-label='L("Add a group")'
      ) Create Group

    .card
      svg-join-group
      i18n.is-title-3(tag='h3') Join
      i18n(tag='p') Enter an existing group using your username.
      i18n(
        tag='button'
        @click='openModal("GroupJoinModal")'
        data-test='joinGroup'
      ) Join a Group

  banner-simple.hide-hoverable-device.hide-tablet.c-pwa-promo(
    v-if='!isLoggedIn && ephemeral.showPwaPromo'
    severity='general'
  )
    template(#header='')
      i18n Install this web app on your device for better usability.

    button-submit.is-success.is-small.c-install-btn(
      type='button'
      @click='onInstallClick'
    ) {{ L('Install') }}

  footer.c-footer(v-if='!isLoggedIn && ephemeral.serverMessages.length > 0')
    banner-simple.c-demo-warning(v-for='text in ephemeral.serverMessages' severity='warning')
      render-message-with-markdown(:text='text')
</template>

<script>
import sbp from '@sbp/sbp'
import { mapGetters, mapState } from 'vuex'
import { ACCEPTED_GROUP, OPEN_MODAL, PWA_INSTALLABLE } from '../../../frontend/utils/events.js'
import BannerSimple from '../../../frontend/views/components/banners/BannerSimple.vue'
import ButtonSubmit from '../../../frontend/views/components/ButtonSubmit.vue'
import RenderMessageWithMarkdown from '../../../frontend/views/containers/chatroom/chat-mentions/RenderMessageWithMarkdown.js'
import SvgCreateGroup from '../../../frontend/assets/svgs/create-group.svg'
import SvgJoinGroup from '../../../frontend/assets/svgs/join-group.svg'
import { ignoreWhenNavigationCancelled } from '../../../frontend/views/utils/misc.js'

export default ({
  name: 'Home',
  components: {
    SvgJoinGroup,
    SvgCreateGroup,
    BannerSimple,
    ButtonSubmit,
    RenderMessageWithMarkdown
  },
  computed: {
    ...mapGetters([
      'currentIdentityState',
      'seenWelcomeScreen'
    ]),
    ...mapState([
      'currentGroupId'
    ]),
    isLoggedIn () {
      return !!this.$store.state.loggedIn
    }
  },
  data () {
    return {
      ephemeral: {
        serverMessages: [],
        seenWelcomeScreen: false,
        listener: ({ contractID }) => {
          if (contractID !== this.currentGroupId) return
          // For first time joins, force redirect to /pending-approval
          this.ephemeral.seenWelcomeScreen = false
        },
        showPwaPromo: false
      }
    }
  },
  beforeMount () {
    sbp('okTurtles.events/on', ACCEPTED_GROUP, this.ephemeral.listener)
    this.checkPwaInstallability()
    fetch(\`\${sbp('okTurtles.data/get', 'API_URL')}/serverMessages\`).then(async (r) => {
      const data = await r.json()
      this.ephemeral.serverMessages = data.filter((msg) => msg.type === 'giHomeWarning').map((msg) => msg.message)
    }).catch(e => {
      console.error('[Home.vue] Error fetching or processing server announcements', e)
    })
  },
  mounted () {
    this.ephemeral.seenWelcomeScreen = this.seenWelcomeScreen
    if (this.isLoggedIn && this.currentGroupId) {
      this.navigateToGroupPage()
    } else if (this.$route.query.next) {
      this.openModal('LoginModal')
    }
  },
  destroyed () {
    sbp('okTurtles.events/off', ACCEPTED_GROUP, this.ephemeral.listener)
  },
  methods: {
    openModal (mode) {
      sbp('okTurtles.events/emit', OPEN_MODAL, mode)
    },
    navigateToGroupPage () {
      // NOTE:
      // When browser refresh is triggered, there is an issue that Vue router prematurely decides ('loginGuard' there) that
      // the user is not signed in when in actual reality user-login is still being processed and then
      // takes user to 'Home.vue' with the '$route.query.next' being set to the initial url.
      // In this particular condition, the app needs to immediately redirect user to '$route.query.next'
      // so that the user stays in the same page after the browser refresh.
      // (Related GH issue: https://github.com/okTurtles/group-income/issues/1830)
      const path = this.$route.query.next ?? (this.ephemeral.seenWelcomeScreen ? '/dashboard' : '/pending-approval')
      this.$router.push({ path }).catch(e => ignoreWhenNavigationCancelled(e, path))
    },
    checkPwaInstallability () {
      this.ephemeral.showPwaPromo = sbp('service-worker/check-pwa-installability')
      sbp('okTurtles.events/once', PWA_INSTALLABLE, () => {
        this.ephemeral.showPwaPromo = true
      })
    },
    async onInstallClick () {
      await sbp('service-worker/trigger-install-prompt')
    }
  },
  watch: {
    currentGroupId (to) {
      // Redirect to \`/pending-approval\` if our profile isn't active or the
      // welcome screen hasn't been approved
      this.ephemeral.seenWelcomeScreen = this.seenWelcomeScreen
      if (to && this.isLoggedIn) {
        this.navigateToGroupPage()
      }
    },
    isLoggedIn (to) {
      if (to && this.currentGroupId) {
        this.navigateToGroupPage()
      }
    }
  }
}: Object)
<\/script>

<style scoped lang="scss">
@import "../../../frontend/assets/style/_variables.scss";

.c-splash {
  display: flex;
  flex-direction: column;
  text-align: center;
  align-items: center;
  min-height: 100%;

  @include tablet {
    justify-content: center;
  }
}

.logo {
  width: 8rem;
  margin: 2rem auto;
}

.logo-2 {
  width: 8rem;
  margin: 0 auto 3rem auto;

  @include tablet {
    margin: 0;
    position: absolute;
    left: 1.5rem;
    top: 1.5rem;
  }
}

.is-subtitle {
  @include tablet {
    margin-top: 3rem;
  }

  @include desktop {
    margin-top: 0;
  }
}

.buttons {
  min-width: 230px;
}

.create-or-join {
  display: flex;
  width: 100%;
  flex-wrap: wrap;
  margin-top: 0.5rem;
  justify-content: center;
  text-align: left;
  @include tablet {
    text-align: center;
  }
}

.card {
  margin: 1.5rem 1.5rem 0 1.5rem;
  width: 100%;

  @include tablet {
    width: 24.375rem;
  }

  h3 {
    margin-bottom: 0.25rem;
  }

  svg {
    width: 6rem;
    margin-left: 1rem;
    float: right;
    @include tablet {
      float: none;
      width: 9.75rem;
      margin: 2.5rem auto 1.5rem auto;
    }
  }

  .button {
    margin-top: 2rem;

    @include tablet {
      margin-bottom: 1rem;
    }
  }
}

.c-footer {
  margin-top: 2.25rem;
  padding: 0 1rem;
  max-width: 31.25rem;
}

.c-demo-warning {
  text-align: start;
  margin-block-end: 1rem;
}

.c-pwa-promo {
  position: fixed;
  bottom: 1.875rem;
  left: 50%;
  transform: translateX(-50%);
  z-index: 10; // should be less than modal, tooltip, prompt etc.
  text-align: left;
  width: calc(100vw - 3rem);
  max-width: 31.25rem;

  ::v-deep .c-body {
    margin-top: 1rem;
    text-align: right;
  }
}

.c-install-btn {
  padding-left: 1.75rem;
  padding-right: 1.75rem;
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__() {
  const styles = __vue_create_injector__.styles || (__vue_create_injector__.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__ = /* @__PURE__ */ __vue_normalize__(
  { render: __vue_render__, staticRenderFns: __vue_staticRenderFns__ },
  __vue_inject_styles__,
  __vue_script__,
  __vue_scope_id__,
  __vue_is_functional_template__,
  __vue_module_identifier__,
  false,
  __vue_create_injector__,
  void 0,
  void 0
);
var Home_default = __vue_component__;

// frontend/views/components/Loading.vue
var __vue_script__2 = {
  name: "Loading",
  props: {
    title: {
      default: L("Hang in there"),
      type: String
    },
    text: {
      default: L("Magic is happening..."),
      type: [String, Number]
    },
    theme: {
      validator(value) {
        return ["fullView", ""].indexOf(value) !== -1;
      }
    }
  }
};
var __vue_render__2 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "div",
    {
      staticClass: "c-loading has-text-centered",
      class: { "c-fullView": _vm.theme === "fullView" }
    },
    [
      _c("h1", { staticClass: "is-title-1" }, [_vm._v(_vm._s(_vm.title))]),
      _c("p", [_vm._v(_vm._s(_vm.text))])
    ]
  );
};
var __vue_staticRenderFns__2 = [];
__vue_render__2._withStripped = true;
var __vue_inject_styles__2 = function(inject) {
  if (!inject) return;
  inject("data-v-90277038_0", { source: ".c-loading[data-v-90277038] {\n  opacity: 0;\n  padding: 2rem 0;\n  animation: showLoading-data-v-90277038 200ms 500ms forwards;\n}\n.c-loading.c-fullView[data-v-90277038] {\n  min-height: 100%;\n  display: flex;\n  flex-direction: column;\n  justify-content: center;\n}\n@keyframes showLoading-data-v-90277038 {\nfrom {\n    opacity: 0;\n}\nto {\n    opacity: 1;\n}\n}\n\n/*# sourceMappingURL=Loading.vue.map */", map: { "version": 3, "sources": ["frontend/views/components/Loading.vue", "Loading.vue"], "names": [], "mappings": "AAiCA;EACA,UAAA;EACA,eAAA;EACA,2DAAA;AChCA;ADkCA;EACA,gBAAA;EACA,aAAA;EACA,sBAAA;EACA,uBAAA;AChCA;ADoCA;AACA;IAAA,UAAA;AChCE;ADiCF;IAAA,UAAA;AC9BE;AACF;;AAEA,sCAAsC", "file": "Loading.vue", "sourcesContent": [`<template lang='pug'>
.c-loading.has-text-centered(
  :class='{ "c-fullView": theme === "fullView" }'
)
  h1.is-title-1 {{ title }}
  p {{ text }}
</template>

<script>
import { L } from '../../../frontend/common/common.js'
export default ({
  name: 'Loading',
  props: {
    title: {
      default: L('Hang in there'),
      type: String
    },
    text: {
      default: L('Magic is happening...'),
      type: [String, Number]
    },
    theme: {
      validator (value) {
        return ['fullView', ''].indexOf(value) !== -1
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../frontend/assets/style/_variables.scss";

.c-loading {
  opacity: 0;
  padding: 2rem 0;
  animation: showLoading 200ms 500ms forwards;

  &.c-fullView {
    min-height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
}

@keyframes showLoading {
  from { opacity: 0; }
  to { opacity: 1; }
}
</style>
`, ".c-loading {\n  opacity: 0;\n  padding: 2rem 0;\n  animation: showLoading 200ms 500ms forwards;\n}\n.c-loading.c-fullView {\n  min-height: 100%;\n  display: flex;\n  flex-direction: column;\n  justify-content: center;\n}\n\n@keyframes showLoading {\n  from {\n    opacity: 0;\n  }\n  to {\n    opacity: 1;\n  }\n}\n\n/*# sourceMappingURL=Loading.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__2 = "data-v-90277038";
var __vue_module_identifier__2 = void 0;
var __vue_is_functional_template__2 = false;
function __vue_normalize__2(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
.c-loading.has-text-centered(
  :class='{ "c-fullView": theme === "fullView" }'
)
  h1.is-title-1 {{ title }}
  p {{ text }}
</template>

<script>
import { L } from '../../../frontend/common/common.js'
export default ({
  name: 'Loading',
  props: {
    title: {
      default: L('Hang in there'),
      type: String
    },
    text: {
      default: L('Magic is happening...'),
      type: [String, Number]
    },
    theme: {
      validator (value) {
        return ['fullView', ''].indexOf(value) !== -1
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../frontend/assets/style/_variables.scss";

.c-loading {
  opacity: 0;
  padding: 2rem 0;
  animation: showLoading 200ms 500ms forwards;

  &.c-fullView {
    min-height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
}

@keyframes showLoading {
  from { opacity: 0; }
  to { opacity: 1; }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__2() {
  const styles = __vue_create_injector__2.styles || (__vue_create_injector__2.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__2 = /* @__PURE__ */ __vue_normalize__2(
  { render: __vue_render__2, staticRenderFns: __vue_staticRenderFns__2 },
  __vue_inject_styles__2,
  __vue_script__2,
  __vue_scope_id__2,
  __vue_is_functional_template__2,
  __vue_module_identifier__2,
  false,
  __vue_create_injector__2,
  void 0,
  void 0
);
var Loading_default = __vue_component__2;

// frontend/views/pages/Join.vue
var syncFinished = false;
esm_default("okTurtles.events/once", LOGIN, () => {
  syncFinished = true;
});
var __vue_script__3 = {
  name: "Join",
  components: {
    Loading: Loading_default,
    LoginForm: LoginForm_default,
    SignupForm: SignupForm_default,
    Avatar: Avatar_default,
    SvgBrokenLink: broken_link_default,
    SvgCreateGroup: create_group_default
  },
  data() {
    return {
      ephemeral: {
        pageStatus: "LOADING",
        invitation: {},
        groupInfo: {},
        hash: null
      }
    };
  },
  computed: {
    ...mapGetters(["ourIdentityContractId"]),
    ...mapState(["currentGroupId"]),
    pageStatus: {
      get() {
        return this.ephemeral.pageStatus;
      },
      set(status) {
        const possibleStatus = ["LOADING", "SIGNING", "LOGGING", "INVALID", "EXPIRED", "JOINED"];
        if (!possibleStatus.includes(status)) {
          throw new Error(`Bad status: ${status}. Use one of the following: ${possibleStatus.join(", ")}`);
        }
        this.ephemeral.pageStatus = status;
      }
    }
  },
  mounted() {
    this.ephemeral.hash = new URLSearchParams(this.$route.hash.slice(1));
    if (syncFinished || !this.ourIdentityContractId) {
      this.initialize();
    } else {
      esm_default("okTurtles.events/once", LOGIN, () => this.initialize());
    }
  },
  methods: {
    async initialize() {
      try {
        const messageToAskAnother = L("You should ask for a new one. Sorry about that!");
        const groupId = this.ephemeral.hash.get("groupId");
        const secret = this.ephemeral.hash.get("secret");
        if (!groupId || !secret) {
          console.error("Invalid invite link: missing group ID or secret");
          this.ephemeral.errorMsg = messageToAskAnother;
          this.pageStatus = "INVALID";
          return;
        }
        const state = await esm_default("chelonia/latestContractState", groupId);
        const publicKeyId = keyId(secret);
        const invite = state._vm.invites[publicKeyId];
        if (invite?.expires < Date.now()) {
          console.log("Join.vue error: Link is already expired.");
          this.ephemeral.errorMsg = messageToAskAnother;
          this.pageStatus = "EXPIRED";
          return;
        } else if (invite?.initialQuantity > 0 && !invite.quantity) {
          console.log("Join.vue error: Link is already used.");
          this.ephemeral.errorMsg = messageToAskAnother;
          this.pageStatus = "INVALID";
          return;
        }
        if (invite?.status !== INVITE_STATUS.VALID) {
          console.error("Join.vue error: Link is not valid.");
          this.ephemeral.errorMsg = messageToAskAnother;
          this.pageStatus = "INVALID";
          return;
        }
        if (this.ourIdentityContractId) {
          const targetGroupId = this.ephemeral.hash?.get("groupId") || "";
          const targetGroupState = this.$store.state[targetGroupId] || {};
          if (this.currentGroupId && [PROFILE_STATUS.ACTIVE, PROFILE_STATUS.PENDING].includes(targetGroupState?.profiles?.[this.ourIdentityContractId])) {
            this.goToDashboard();
          } else if (this.checkAlreadyJoinedGroup(targetGroupId)) {
            this.ephemeral.groupInfo = {
              name: targetGroupState.settings?.groupName || "",
              id: targetGroupId
            };
            this.pageStatus = "JOINED";
            return;
          } else {
            await this.accept();
          }
          return;
        }
        const creatorID = this.ephemeral.hash.get("creatorID");
        const creatorUsername = this.ephemeral.hash.get("creatorUsername");
        const who = creatorUsername || creatorID;
        const message = who ? L("{who} invited you to join their group!", { who }) : L("You were invited to join");
        this.ephemeral.invitation = {
          groupName: this.ephemeral.hash.get("groupName") ?? L("(group name unavailable)"),
          creatorID,
          creatorUsername,
          message
        };
        this.pageStatus = "SIGNING";
      } catch (e) {
        console.error(e);
        this.ephemeral.errorMsg = `${L("Something went wrong. Please, try again.")} ${e.message}`;
        this.pageStatus = "INVALID";
      }
    },
    isStatus(status) {
      return this.pageStatus === status;
    },
    goHome() {
      this.$router.push({ path: "/" });
    },
    checkAlreadyJoinedGroup(targetGroupId) {
      if (this.ourIdentityContractId) {
        const myGroups = this.$store.state[this.ourIdentityContractId]?.groups || {};
        return myGroups[targetGroupId] && !myGroups[targetGroupId]?.hasLeft;
      } else return false;
    },
    goToDashboard(toGroupId) {
      if (toGroupId && this.currentGroupId !== toGroupId) {
        esm_default("gi.app/group/switch", toGroupId);
      }
      this.$router.push({ path: "/dashboard" });
    },
    async accept() {
      this.ephemeral.errorMsg = null;
      const groupId = this.ephemeral.hash.get("groupId");
      const secret = this.ephemeral.hash.get("secret");
      const profileStatus = this.$store.state.contracts[groupId]?.profiles?.[this.ourIdentityContractId]?.status;
      if ([PROFILE_STATUS.ACTIVE, PROFILE_STATUS.PENDING].includes(profileStatus)) {
        return this.goToDashboard();
      }
      try {
        await esm_default("gi.app/group/joinWithInviteSecret", groupId, secret);
      } catch (e) {
        console.error("Join.vue accept() error:", e);
        this.ephemeral.errorMsg = e.message;
        const alreadyJoinedErr = this.checkAlreadyJoinedGroup(groupId);
        this.pageStatus = alreadyJoinedErr ? "JOINED" : "INVALID";
        if (alreadyJoinedErr) {
          const targetGroupState = this.$store.state[groupId] || {};
          this.ephemeral.groupInfo = {
            name: targetGroupState.settings?.groupName || "",
            id: groupId
          };
        } else {
          esm_default("gi.ui/prompt", {
            heading: L("Failed to join the group"),
            question: L("Error details:{br_}{err}", { err: e.message, ...LTags() }),
            primaryButton: L("Close")
          });
        }
      }
    }
  }
};
var __vue_render__3 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "div",
    [
      _c("img", {
        staticClass: "c-logo",
        attrs: { src: "/assets/images/logo-transparent.png" }
      }),
      _vm.isStatus("LOADING") ? _c("loading", { attrs: { theme: "fullView" } }) : _c("div", { staticClass: "c-page" }, [
        _vm.isStatus("SIGNING") || _vm.isStatus("LOGGING") ? _c("div", [
          _c("div", { staticClass: "c-header" }, [
            _c(
              "div",
              { staticClass: "c-avatars" },
              [
                _vm.ephemeral.invitation.groupPicture ? _c("avatar", {
                  staticClass: "c-avatars-group",
                  attrs: {
                    src: _vm.ephemeral.invitation.groupPicture,
                    size: "lg"
                  }
                }) : _vm._e(),
                _vm.ephemeral.invitation.creatorPicture ? _c("avatar", {
                  staticClass: "c-avatars-creator",
                  attrs: {
                    src: _vm.ephemeral.invitation.creatorPicture
                  }
                }) : _vm._e()
              ],
              1
            ),
            _c(
              "h1",
              {
                staticClass: "is-title-1 c-title",
                attrs: { "data-test": "groupName" }
              },
              [_vm._v(_vm._s(_vm.ephemeral.invitation.groupName))]
            ),
            _c(
              "p",
              {
                staticClass: "has-text-1",
                attrs: { "data-test": "invitationMessage" }
              },
              [_vm._v(_vm._s(_vm.ephemeral.invitation.message))]
            )
          ]),
          _c(
            "div",
            { staticClass: "card" },
            [
              _vm.isStatus("SIGNING") ? _c("signup-form", {
                attrs: { postSubmit: _vm.accept }
              }) : _c("login-form", {
                attrs: { postSubmit: _vm.accept }
              })
            ],
            1
          ),
          _vm.isStatus("SIGNING") ? _c(
            "p",
            { staticClass: "c-switchEnter" },
            [
              _c("i18n", [_vm._v("Already have an account?")]),
              _vm._v("\xA0"),
              _c(
                "i18n",
                {
                  staticClass: "link",
                  attrs: { tag: "button" },
                  on: {
                    click: function($event) {
                      _vm.pageStatus = "LOGGING";
                    }
                  }
                },
                [_vm._v("Log in")]
              )
            ],
            1
          ) : _c(
            "p",
            { staticClass: "c-switchEnter" },
            [
              _c("i18n", [_vm._v("Not on Group Income yet?")]),
              _vm._v("\xA0"),
              _c(
                "i18n",
                {
                  staticClass: "link",
                  attrs: { tag: "button" },
                  on: {
                    click: function($event) {
                      _vm.pageStatus = "SIGNING";
                    }
                  }
                },
                [_vm._v("Create an account")]
              )
            ],
            1
          )
        ]) : _vm.isStatus("JOINED") ? _c(
          "div",
          { staticClass: "c-joined" },
          [
            _c("svg-create-group", { staticClass: "c-svg" }),
            _c(
              "i18n",
              {
                staticClass: "is-title-1",
                attrs: {
                  tag: "h1",
                  "data-test": "pageTitle",
                  args: { groupName: _vm.ephemeral.groupInfo.name }
                }
              },
              [_vm._v("You are already a member of '{groupName}'")]
            ),
            _c(
              "i18n",
              {
                staticClass: "has-text-1",
                attrs: { tag: "p", "data-test": "helperText" }
              },
              [_vm._v("You cannot join already joined group.")]
            ),
            _c(
              "i18n",
              {
                staticClass: "c-goHome",
                attrs: { tag: "button" },
                on: {
                  click: function($event) {
                    return _vm.goToDashboard(_vm.ephemeral.groupInfo.id);
                  }
                }
              },
              [_vm._v("Go to dashboard")]
            )
          ],
          1
        ) : _vm.isStatus("INVALID") ? _c(
          "div",
          { staticClass: "c-broken" },
          [
            _c("svg-broken-link", { staticClass: "c-svg" }),
            _c(
              "i18n",
              {
                staticClass: "is-title-1",
                attrs: {
                  tag: "h1",
                  "data-test": "pageTitle",
                  args: _vm.LTags()
                }
              },
              [_vm._v("Oh no! {br_}This invite is not valid")]
            ),
            _c(
              "p",
              {
                staticClass: "has-text-1",
                attrs: { "data-test": "helperText" }
              },
              [_vm._v(_vm._s(_vm.ephemeral.errorMsg))]
            ),
            _c(
              "i18n",
              {
                staticClass: "c-goHome",
                attrs: { tag: "button" },
                on: { click: _vm.goHome }
              },
              [_vm._v("Take me home")]
            )
          ],
          1
        ) : _vm.isStatus("EXPIRED") ? _c(
          "div",
          { staticClass: "c-broken" },
          [
            _c("svg-broken-link", { staticClass: "c-svg" }),
            _c(
              "i18n",
              {
                staticClass: "is-title-1",
                attrs: {
                  tag: "h1",
                  "data-test": "pageTitle",
                  args: _vm.LTags()
                }
              },
              [_vm._v("Oh no! {br_}This invite is already expired")]
            ),
            _c(
              "p",
              {
                staticClass: "has-text-1",
                attrs: { "data-test": "helperText" }
              },
              [_vm._v(_vm._s(_vm.ephemeral.errorMsg))]
            ),
            _c(
              "i18n",
              {
                staticClass: "c-goHome",
                attrs: { tag: "button" },
                on: { click: _vm.goHome }
              },
              [_vm._v("Take me home")]
            )
          ],
          1
        ) : _vm._e()
      ])
    ],
    1
  );
};
var __vue_staticRenderFns__3 = [];
__vue_render__3._withStripped = true;
var __vue_inject_styles__3 = function(inject) {
  if (!inject) return;
  inject("data-v-36959443_0", { source: ".c-logo[data-v-36959443] {\n  min-width: 8rem;\n  width: 8rem;\n  margin: auto;\n}\n@media screen and (min-width: 769px), print {\n.c-logo[data-v-36959443] {\n    position: absolute;\n    top: 1.5rem;\n    left: 1.5rem;\n}\n}\n.c-page[data-v-36959443] {\n  max-width: 33rem;\n  margin: auto;\n  height: 100%;\n}\n.c-welcome[data-v-36959443] {\n  margin-top: -1.5rem;\n}\n.c-header[data-v-36959443] {\n  margin-top: 1rem;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n}\n.c-header .c-title[data-v-36959443] {\n  order: 2;\n}\n@media screen and (min-width: 769px), print {\n.c-header[data-v-36959443] {\n    margin-top: 2.5rem;\n}\n}\n.c-avatars[data-v-36959443] {\n  position: relative;\n  margin-bottom: 1.5rem;\n}\n@media screen and (min-width: 769px), print {\n.c-avatars .c-avatars-group.is-lg[data-v-36959443] {\n    width: 8rem;\n    height: 8rem;\n}\n}\n.c-avatars .c-avatars-creator[data-v-36959443] {\n  position: absolute;\n  bottom: 0;\n  right: 0;\n  border: 2px solid #fff;\n}\n.card[data-v-36959443] {\n  padding: 1.5rem 1rem;\n  margin-top: 1.5rem;\n}\n@media screen and (min-width: 769px), print {\n.card[data-v-36959443] {\n    padding: 2.5rem;\n}\n}\n.c-switchEnter[data-v-36959443] {\n  margin-top: 1.5rem;\n  text-align: center;\n}\n.c-broken[data-v-36959443],\n.c-joined[data-v-36959443] {\n  margin-top: 20vh;\n  text-align: center;\n}\n.c-broken .is-title-1[data-v-36959443],\n.c-joined .is-title-1[data-v-36959443] {\n  margin-top: 3rem;\n  margin-bottom: 0.5rem;\n}\n.c-broken .c-goHome[data-v-36959443],\n.c-joined .c-goHome[data-v-36959443] {\n  margin-top: 2rem;\n}\n@media screen and (min-width: 769px), print {\n.c-debug[data-v-36959443] {\n    position: absolute;\n    bottom: 1rem;\n    right: 1rem;\n}\n}\n\n/*# sourceMappingURL=Join.vue.map */", map: { "version": 3, "sources": ["frontend/views/pages/Join.vue", "Join.vue"], "names": [], "mappings": "AAkPA;EACA,eAAA;EACA,WAAA;EACA,YAAA;ACjPA;AACA;AD6OA;IAMA,kBAAA;IACA,WAAA;IACA,YAAA;AChPE;AACF;ADmPA;EACA,gBAAA;EACA,YAAA;EACA,YAAA;AChPA;ADmPA;EACA,mBAAA;AChPA;ADmPA;EACA,gBAAA;EACA,aAAA;EACA,sBAAA;EACA,mBAAA;AChPA;ADkPA;EACA,QAAA;AChPA;AACA;ADwOA;IAWA,kBAAA;AChPE;AACF;ADmPA;EACA,kBAAA;EACA,qBAAA;AChPA;AACA;ADiPA;IAEA,WAAA;IACA,YAAA;AChPE;AACF;ADmPA;EACA,kBAAA;EACA,SAAA;EACA,QAAA;EACA,sBAAA;ACjPA;ADqPA;EACA,oBAAA;EACA,kBAAA;AClPA;AACA;AD+OA;IAKA,eAAA;ACjPE;AACF;ADoPA;EACA,kBAAA;EACA,kBAAA;ACjPA;ADoPA;;EAEA,gBAAA;EACA,kBAAA;ACjPA;ADmPA;;EACA,gBAAA;EACA,qBAAA;AChPA;ADmPA;;EACA,gBAAA;AChPA;AAEA;ADmPA;IACA,kBAAA;IACA,YAAA;IACA,WAAA;ACjPE;AACF;;AAEA,mCAAmC", "file": "Join.vue", "sourcesContent": [`<template lang='pug'>
div
  img.c-logo(src='/assets/images/logo-transparent.png')

  loading(theme='fullView' v-if='isStatus("LOADING")')
  .c-page(v-else)
    div(v-if='isStatus("SIGNING") || isStatus("LOGGING")')
      .c-header
        .c-avatars
          avatar.c-avatars-group(
            v-if='ephemeral.invitation.groupPicture'
            :src='ephemeral.invitation.groupPicture'
            size='lg'
          )
          avatar.c-avatars-creator(
            v-if='ephemeral.invitation.creatorPicture'
            :src='ephemeral.invitation.creatorPicture'
          )
        h1.is-title-1.c-title(data-test='groupName') {{ ephemeral.invitation.groupName }}
        p.has-text-1(data-test='invitationMessage') {{ ephemeral.invitation.message }}
      .card
        signup-form(v-if='isStatus("SIGNING")' :postSubmit='accept')
        login-form(v-else :postSubmit='accept')

      p.c-switchEnter(v-if='isStatus("SIGNING")')
        i18n Already have an account?
        | &nbsp;
        i18n.link(tag='button' @click='pageStatus = "LOGGING"') Log in
      p.c-switchEnter(v-else)
        i18n Not on Group Income yet?
        | &nbsp;
        i18n.link(tag='button' @click='pageStatus = "SIGNING"') Create an account

    .c-joined(v-else-if='isStatus("JOINED")')
      svg-create-group.c-svg
      i18n.is-title-1(tag='h1' data-test='pageTitle' :args='{ groupName: ephemeral.groupInfo.name }') You are already a member of '{groupName}'
      i18n.has-text-1(tag='p' data-test='helperText') You cannot join already joined group.
      i18n.c-goHome(tag='button' @click='goToDashboard(ephemeral.groupInfo.id)') Go to dashboard
    .c-broken(v-else-if='isStatus("INVALID")')
      svg-broken-link.c-svg
      i18n.is-title-1(tag='h1' data-test='pageTitle' :args='LTags()') Oh no! {br_}This invite is not valid
      p.has-text-1(data-test='helperText') {{ ephemeral.errorMsg }}
      i18n.c-goHome(tag='button' @click='goHome') Take me home
    .c-broken(v-else-if='isStatus("EXPIRED")')
      svg-broken-link.c-svg
      i18n.is-title-1(tag='h1' data-test='pageTitle' :args='LTags()') Oh no! {br_}This invite is already expired
      p.has-text-1(data-test='helperText') {{ ephemeral.errorMsg }}
      i18n.c-goHome(tag='button' @click='goHome') Take me home
</template>

<script>
import { L, LTags } from '../../../frontend/common/common.js'
import Avatar from '../../../frontend/views/components/Avatar.vue'
import Loading from '../../../frontend/views/components/Loading.vue'
import LoginForm from '../../../frontend/views/containers/access/LoginForm.vue'
import SignupForm from '../../../frontend/views/containers/access/SignupForm.vue'
import sbp from '@sbp/sbp'
import SvgBrokenLink from '../../../frontend/assets/svgs/broken-link.svg'
import SvgCreateGroup from '../../../frontend/assets/svgs/create-group.svg'
import { LOGIN } from '../../../frontend/utils/events.js'
import { mapGetters, mapState } from 'vuex'
import { INVITE_STATUS } from '@chelonia/lib/constants'
import { PROFILE_STATUS } from '../../../frontend/model/contracts/shared/constants.js'
import { keyId } from '@chelonia/crypto'

let syncFinished = false
sbp('okTurtles.events/once', LOGIN, () => { syncFinished = true })

export default ({
  name: 'Join',
  components: {
    Loading,
    LoginForm,
    SignupForm,
    Avatar,
    SvgBrokenLink,
    SvgCreateGroup
  },
  data () {
    return {
      ephemeral: {
        pageStatus: 'LOADING',
        invitation: {},
        groupInfo: {},
        hash: null
      }
    }
  },
  computed: {
    ...mapGetters(['ourIdentityContractId']),
    ...mapState(['currentGroupId']),
    pageStatus: {
      get () { return this.ephemeral.pageStatus },
      set (status) {
        const possibleStatus = ['LOADING', 'SIGNING', 'LOGGING', 'INVALID', 'EXPIRED', 'JOINED']
        if (!possibleStatus.includes(status)) {
          throw new Error(\`Bad status: \${status}. Use one of the following: \${possibleStatus.join(', ')}\`)
        }
        this.ephemeral.pageStatus = status
      }
    }
  },
  mounted () {
    // For some reason in some Cypress tests it loses the route hash when initialized is called
    this.ephemeral.hash = new URLSearchParams(this.$route.hash.slice(1))
    if (syncFinished || !this.ourIdentityContractId) {
      this.initialize()
    } else {
      sbp('okTurtles.events/once', LOGIN, () => this.initialize())
    }
  },
  methods: {
    async initialize () {
      try {
        const messageToAskAnother = L('You should ask for a new one. Sorry about that!')
        const groupId = this.ephemeral.hash.get('groupId')
        const secret = this.ephemeral.hash.get('secret')
        if (!groupId || !secret) {
          console.error('Invalid invite link: missing group ID or secret')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'INVALID'
          return
        }
        const state = await sbp('chelonia/latestContractState', groupId)
        const publicKeyId = keyId(secret)
        const invite = state._vm.invites[publicKeyId]
        if (invite?.expires < Date.now()) {
          console.log('Join.vue error: Link is already expired.')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'EXPIRED'
          return
        } else if (invite?.initialQuantity > 0 && !invite.quantity) {
          console.log('Join.vue error: Link is already used.')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'INVALID'
          return
        } if (invite?.status !== INVITE_STATUS.VALID) {
          console.error('Join.vue error: Link is not valid.')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'INVALID'
          return
        }
        if (this.ourIdentityContractId) {
          const targetGroupId = this.ephemeral.hash?.get('groupId') || ''
          const targetGroupState = this.$store.state[targetGroupId] || {}

          if (this.currentGroupId && [PROFILE_STATUS.ACTIVE, PROFILE_STATUS.PENDING].includes(targetGroupState?.profiles?.[this.ourIdentityContractId])) {
            this.goToDashboard()
          } else if (this.checkAlreadyJoinedGroup(targetGroupId)) { // if the user is already part of the target group.
            this.ephemeral.groupInfo = {
              name: targetGroupState.settings?.groupName || '',
              id: targetGroupId
            }
            this.pageStatus = 'JOINED'

            return
          } else {
            await this.accept()
          }
          return
        }
        const creatorID = this.ephemeral.hash.get('creatorID')
        const creatorUsername = this.ephemeral.hash.get('creatorUsername')
        const who = creatorUsername || creatorID
        const message = who
          ? L('{who} invited you to join their group!', { who })
          : L('You were invited to join')

        this.ephemeral.invitation = {
          groupName: this.ephemeral.hash.get('groupName') ?? L('(group name unavailable)'),
          creatorID,
          creatorUsername,
          message
        }
        this.pageStatus = 'SIGNING'
      } catch (e) {
        console.error(e)
        this.ephemeral.errorMsg = \`\${L('Something went wrong. Please, try again.')} \${e.message}\`
        this.pageStatus = 'INVALID'
      }
    },
    isStatus (status) {
      return this.pageStatus === status
    },
    goHome () {
      this.$router.push({ path: '/' })
    },
    checkAlreadyJoinedGroup (targetGroupId) {
      if (this.ourIdentityContractId) {
        const myGroups = this.$store.state[this.ourIdentityContractId]?.groups || {}
        return myGroups[targetGroupId] && !myGroups[targetGroupId]?.hasLeft
      } else return false
    },
    goToDashboard (toGroupId) {
      if (toGroupId && this.currentGroupId !== toGroupId) {
        sbp('gi.app/group/switch', toGroupId)
      }

      this.$router.push({ path: '/dashboard' })
    },
    async accept () {
      this.ephemeral.errorMsg = null
      const groupId = this.ephemeral.hash.get('groupId')
      const secret = this.ephemeral.hash.get('secret')

      const profileStatus = this.$store.state.contracts[groupId]?.profiles?.[this.ourIdentityContractId]?.status
      if ([PROFILE_STATUS.ACTIVE, PROFILE_STATUS.PENDING].includes(profileStatus)) {
        return this.goToDashboard()
      }
      try {
        await sbp('gi.app/group/joinWithInviteSecret', groupId, secret)
        // this.pageStatus = 'WELCOME'
      } catch (e) {
        console.error('Join.vue accept() error:', e)
        this.ephemeral.errorMsg = e.message

        const alreadyJoinedErr = this.checkAlreadyJoinedGroup(groupId)
        this.pageStatus = alreadyJoinedErr ? 'JOINED' : 'INVALID'
        if (alreadyJoinedErr) {
          // if errored by attempting to join an already-joined group, show a different UI informing it.
          const targetGroupState = this.$store.state[groupId] || {}

          this.ephemeral.groupInfo = {
            name: targetGroupState.settings?.groupName || '',
            id: groupId
          }
        } else {
          sbp('gi.ui/prompt', {
            heading: L('Failed to join the group'),
            question: L('Error details:{br_}{err}', { err: e.message, ...LTags() }),
            primaryButton: L('Close')
          })
        }
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../frontend/assets/style/_variables.scss";

.c-logo {
  min-width: 8rem;
  width: 8rem;
  margin: auto;

  @include tablet {
    position: absolute;
    top: 1.5rem;
    left: 1.5rem;
  }
}

.c-page {
  max-width: 33rem;
  margin: auto;
  height: 100%;
}

.c-welcome {
  margin-top: -1.5rem; // makeup for the default l-page padding-top to respect 100vh
}

.c-header {
  margin-top: 1rem;
  display: flex;
  flex-direction: column;
  align-items: center;

  .c-title {
    order: 2;
  }

  @include tablet {
    margin-top: 2.5rem;
  }
}

.c-avatars {
  position: relative;
  margin-bottom: 1.5rem;

  .c-avatars-group.is-lg {
    @include tablet {
      width: 8rem;
      height: 8rem;
    }
  }

  .c-avatars-creator {
    position: absolute;
    bottom: 0;
    right: 0;
    border: 2px solid $white;
  }
}

.card {
  padding: 1.5rem 1rem;
  margin-top: 1.5rem;

  @include tablet {
    padding: 2.5rem;
  }
}

.c-switchEnter {
  margin-top: 1.5rem;
  text-align: center;
}

.c-broken,
.c-joined {
  margin-top: 20vh;
  text-align: center;

  .is-title-1 {
    margin-top: 3rem;
    margin-bottom: 0.5rem;
  }

  .c-goHome {
    margin-top: 2rem;
  }
}

@include tablet {
  .c-debug {
    position: absolute;
    bottom: 1rem;
    right: 1rem;
  }
}
</style>
`, ".c-logo {\n  min-width: 8rem;\n  width: 8rem;\n  margin: auto;\n}\n@media screen and (min-width: 769px), print {\n  .c-logo {\n    position: absolute;\n    top: 1.5rem;\n    left: 1.5rem;\n  }\n}\n\n.c-page {\n  max-width: 33rem;\n  margin: auto;\n  height: 100%;\n}\n\n.c-welcome {\n  margin-top: -1.5rem;\n}\n\n.c-header {\n  margin-top: 1rem;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n}\n.c-header .c-title {\n  order: 2;\n}\n@media screen and (min-width: 769px), print {\n  .c-header {\n    margin-top: 2.5rem;\n  }\n}\n\n.c-avatars {\n  position: relative;\n  margin-bottom: 1.5rem;\n}\n@media screen and (min-width: 769px), print {\n  .c-avatars .c-avatars-group.is-lg {\n    width: 8rem;\n    height: 8rem;\n  }\n}\n.c-avatars .c-avatars-creator {\n  position: absolute;\n  bottom: 0;\n  right: 0;\n  border: 2px solid #fff;\n}\n\n.card {\n  padding: 1.5rem 1rem;\n  margin-top: 1.5rem;\n}\n@media screen and (min-width: 769px), print {\n  .card {\n    padding: 2.5rem;\n  }\n}\n\n.c-switchEnter {\n  margin-top: 1.5rem;\n  text-align: center;\n}\n\n.c-broken,\n.c-joined {\n  margin-top: 20vh;\n  text-align: center;\n}\n.c-broken .is-title-1,\n.c-joined .is-title-1 {\n  margin-top: 3rem;\n  margin-bottom: 0.5rem;\n}\n.c-broken .c-goHome,\n.c-joined .c-goHome {\n  margin-top: 2rem;\n}\n\n@media screen and (min-width: 769px), print {\n  .c-debug {\n    position: absolute;\n    bottom: 1rem;\n    right: 1rem;\n  }\n}\n\n/*# sourceMappingURL=Join.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__3 = "data-v-36959443";
var __vue_module_identifier__3 = void 0;
var __vue_is_functional_template__3 = false;
function __vue_normalize__3(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
div
  img.c-logo(src='/assets/images/logo-transparent.png')

  loading(theme='fullView' v-if='isStatus("LOADING")')
  .c-page(v-else)
    div(v-if='isStatus("SIGNING") || isStatus("LOGGING")')
      .c-header
        .c-avatars
          avatar.c-avatars-group(
            v-if='ephemeral.invitation.groupPicture'
            :src='ephemeral.invitation.groupPicture'
            size='lg'
          )
          avatar.c-avatars-creator(
            v-if='ephemeral.invitation.creatorPicture'
            :src='ephemeral.invitation.creatorPicture'
          )
        h1.is-title-1.c-title(data-test='groupName') {{ ephemeral.invitation.groupName }}
        p.has-text-1(data-test='invitationMessage') {{ ephemeral.invitation.message }}
      .card
        signup-form(v-if='isStatus("SIGNING")' :postSubmit='accept')
        login-form(v-else :postSubmit='accept')

      p.c-switchEnter(v-if='isStatus("SIGNING")')
        i18n Already have an account?
        | &nbsp;
        i18n.link(tag='button' @click='pageStatus = "LOGGING"') Log in
      p.c-switchEnter(v-else)
        i18n Not on Group Income yet?
        | &nbsp;
        i18n.link(tag='button' @click='pageStatus = "SIGNING"') Create an account

    .c-joined(v-else-if='isStatus("JOINED")')
      svg-create-group.c-svg
      i18n.is-title-1(tag='h1' data-test='pageTitle' :args='{ groupName: ephemeral.groupInfo.name }') You are already a member of '{groupName}'
      i18n.has-text-1(tag='p' data-test='helperText') You cannot join already joined group.
      i18n.c-goHome(tag='button' @click='goToDashboard(ephemeral.groupInfo.id)') Go to dashboard
    .c-broken(v-else-if='isStatus("INVALID")')
      svg-broken-link.c-svg
      i18n.is-title-1(tag='h1' data-test='pageTitle' :args='LTags()') Oh no! {br_}This invite is not valid
      p.has-text-1(data-test='helperText') {{ ephemeral.errorMsg }}
      i18n.c-goHome(tag='button' @click='goHome') Take me home
    .c-broken(v-else-if='isStatus("EXPIRED")')
      svg-broken-link.c-svg
      i18n.is-title-1(tag='h1' data-test='pageTitle' :args='LTags()') Oh no! {br_}This invite is already expired
      p.has-text-1(data-test='helperText') {{ ephemeral.errorMsg }}
      i18n.c-goHome(tag='button' @click='goHome') Take me home
</template>

<script>
import { L, LTags } from '../../../frontend/common/common.js'
import Avatar from '../../../frontend/views/components/Avatar.vue'
import Loading from '../../../frontend/views/components/Loading.vue'
import LoginForm from '../../../frontend/views/containers/access/LoginForm.vue'
import SignupForm from '../../../frontend/views/containers/access/SignupForm.vue'
import sbp from '@sbp/sbp'
import SvgBrokenLink from '../../../frontend/assets/svgs/broken-link.svg'
import SvgCreateGroup from '../../../frontend/assets/svgs/create-group.svg'
import { LOGIN } from '../../../frontend/utils/events.js'
import { mapGetters, mapState } from 'vuex'
import { INVITE_STATUS } from '@chelonia/lib/constants'
import { PROFILE_STATUS } from '../../../frontend/model/contracts/shared/constants.js'
import { keyId } from '@chelonia/crypto'

let syncFinished = false
sbp('okTurtles.events/once', LOGIN, () => { syncFinished = true })

export default ({
  name: 'Join',
  components: {
    Loading,
    LoginForm,
    SignupForm,
    Avatar,
    SvgBrokenLink,
    SvgCreateGroup
  },
  data () {
    return {
      ephemeral: {
        pageStatus: 'LOADING',
        invitation: {},
        groupInfo: {},
        hash: null
      }
    }
  },
  computed: {
    ...mapGetters(['ourIdentityContractId']),
    ...mapState(['currentGroupId']),
    pageStatus: {
      get () { return this.ephemeral.pageStatus },
      set (status) {
        const possibleStatus = ['LOADING', 'SIGNING', 'LOGGING', 'INVALID', 'EXPIRED', 'JOINED']
        if (!possibleStatus.includes(status)) {
          throw new Error(\`Bad status: \${status}. Use one of the following: \${possibleStatus.join(', ')}\`)
        }
        this.ephemeral.pageStatus = status
      }
    }
  },
  mounted () {
    // For some reason in some Cypress tests it loses the route hash when initialized is called
    this.ephemeral.hash = new URLSearchParams(this.$route.hash.slice(1))
    if (syncFinished || !this.ourIdentityContractId) {
      this.initialize()
    } else {
      sbp('okTurtles.events/once', LOGIN, () => this.initialize())
    }
  },
  methods: {
    async initialize () {
      try {
        const messageToAskAnother = L('You should ask for a new one. Sorry about that!')
        const groupId = this.ephemeral.hash.get('groupId')
        const secret = this.ephemeral.hash.get('secret')
        if (!groupId || !secret) {
          console.error('Invalid invite link: missing group ID or secret')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'INVALID'
          return
        }
        const state = await sbp('chelonia/latestContractState', groupId)
        const publicKeyId = keyId(secret)
        const invite = state._vm.invites[publicKeyId]
        if (invite?.expires < Date.now()) {
          console.log('Join.vue error: Link is already expired.')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'EXPIRED'
          return
        } else if (invite?.initialQuantity > 0 && !invite.quantity) {
          console.log('Join.vue error: Link is already used.')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'INVALID'
          return
        } if (invite?.status !== INVITE_STATUS.VALID) {
          console.error('Join.vue error: Link is not valid.')
          this.ephemeral.errorMsg = messageToAskAnother
          this.pageStatus = 'INVALID'
          return
        }
        if (this.ourIdentityContractId) {
          const targetGroupId = this.ephemeral.hash?.get('groupId') || ''
          const targetGroupState = this.$store.state[targetGroupId] || {}

          if (this.currentGroupId && [PROFILE_STATUS.ACTIVE, PROFILE_STATUS.PENDING].includes(targetGroupState?.profiles?.[this.ourIdentityContractId])) {
            this.goToDashboard()
          } else if (this.checkAlreadyJoinedGroup(targetGroupId)) { // if the user is already part of the target group.
            this.ephemeral.groupInfo = {
              name: targetGroupState.settings?.groupName || '',
              id: targetGroupId
            }
            this.pageStatus = 'JOINED'

            return
          } else {
            await this.accept()
          }
          return
        }
        const creatorID = this.ephemeral.hash.get('creatorID')
        const creatorUsername = this.ephemeral.hash.get('creatorUsername')
        const who = creatorUsername || creatorID
        const message = who
          ? L('{who} invited you to join their group!', { who })
          : L('You were invited to join')

        this.ephemeral.invitation = {
          groupName: this.ephemeral.hash.get('groupName') ?? L('(group name unavailable)'),
          creatorID,
          creatorUsername,
          message
        }
        this.pageStatus = 'SIGNING'
      } catch (e) {
        console.error(e)
        this.ephemeral.errorMsg = \`\${L('Something went wrong. Please, try again.')} \${e.message}\`
        this.pageStatus = 'INVALID'
      }
    },
    isStatus (status) {
      return this.pageStatus === status
    },
    goHome () {
      this.$router.push({ path: '/' })
    },
    checkAlreadyJoinedGroup (targetGroupId) {
      if (this.ourIdentityContractId) {
        const myGroups = this.$store.state[this.ourIdentityContractId]?.groups || {}
        return myGroups[targetGroupId] && !myGroups[targetGroupId]?.hasLeft
      } else return false
    },
    goToDashboard (toGroupId) {
      if (toGroupId && this.currentGroupId !== toGroupId) {
        sbp('gi.app/group/switch', toGroupId)
      }

      this.$router.push({ path: '/dashboard' })
    },
    async accept () {
      this.ephemeral.errorMsg = null
      const groupId = this.ephemeral.hash.get('groupId')
      const secret = this.ephemeral.hash.get('secret')

      const profileStatus = this.$store.state.contracts[groupId]?.profiles?.[this.ourIdentityContractId]?.status
      if ([PROFILE_STATUS.ACTIVE, PROFILE_STATUS.PENDING].includes(profileStatus)) {
        return this.goToDashboard()
      }
      try {
        await sbp('gi.app/group/joinWithInviteSecret', groupId, secret)
        // this.pageStatus = 'WELCOME'
      } catch (e) {
        console.error('Join.vue accept() error:', e)
        this.ephemeral.errorMsg = e.message

        const alreadyJoinedErr = this.checkAlreadyJoinedGroup(groupId)
        this.pageStatus = alreadyJoinedErr ? 'JOINED' : 'INVALID'
        if (alreadyJoinedErr) {
          // if errored by attempting to join an already-joined group, show a different UI informing it.
          const targetGroupState = this.$store.state[groupId] || {}

          this.ephemeral.groupInfo = {
            name: targetGroupState.settings?.groupName || '',
            id: groupId
          }
        } else {
          sbp('gi.ui/prompt', {
            heading: L('Failed to join the group'),
            question: L('Error details:{br_}{err}', { err: e.message, ...LTags() }),
            primaryButton: L('Close')
          })
        }
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../frontend/assets/style/_variables.scss";

.c-logo {
  min-width: 8rem;
  width: 8rem;
  margin: auto;

  @include tablet {
    position: absolute;
    top: 1.5rem;
    left: 1.5rem;
  }
}

.c-page {
  max-width: 33rem;
  margin: auto;
  height: 100%;
}

.c-welcome {
  margin-top: -1.5rem; // makeup for the default l-page padding-top to respect 100vh
}

.c-header {
  margin-top: 1rem;
  display: flex;
  flex-direction: column;
  align-items: center;

  .c-title {
    order: 2;
  }

  @include tablet {
    margin-top: 2.5rem;
  }
}

.c-avatars {
  position: relative;
  margin-bottom: 1.5rem;

  .c-avatars-group.is-lg {
    @include tablet {
      width: 8rem;
      height: 8rem;
    }
  }

  .c-avatars-creator {
    position: absolute;
    bottom: 0;
    right: 0;
    border: 2px solid $white;
  }
}

.card {
  padding: 1.5rem 1rem;
  margin-top: 1.5rem;

  @include tablet {
    padding: 2.5rem;
  }
}

.c-switchEnter {
  margin-top: 1.5rem;
  text-align: center;
}

.c-broken,
.c-joined {
  margin-top: 20vh;
  text-align: center;

  .is-title-1 {
    margin-top: 3rem;
    margin-bottom: 0.5rem;
  }

  .c-goHome {
    margin-top: 2rem;
  }
}

@include tablet {
  .c-debug {
    position: absolute;
    bottom: 1rem;
    right: 1rem;
  }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__3() {
  const styles = __vue_create_injector__3.styles || (__vue_create_injector__3.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__3 = /* @__PURE__ */ __vue_normalize__3(
  { render: __vue_render__3, staticRenderFns: __vue_staticRenderFns__3 },
  __vue_inject_styles__3,
  __vue_script__3,
  __vue_scope_id__3,
  __vue_is_functional_template__3,
  __vue_module_identifier__3,
  false,
  __vue_create_injector__3,
  void 0,
  void 0
);
var Join_default = __vue_component__3;

// frontend/utils/lazyLoadedView.js
init_vue_esm();

// frontend/views/containers/loading-error/ErrorModal.vue
var __vue_script__4 = {
  name: "LoadingModal",
  components: {
    ModalTemplate: ModalTemplate_default
  },
  props: {
    type: {
      type: Number,
      default: 500
    }
  }
};
var __vue_render__4 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "modal-template",
    {
      ref: "modal",
      staticClass: "has-background",
      attrs: { a11yTitle: _vm.L("Loading") }
    },
    [
      _c(
        "template",
        { slot: "title" },
        [
          _vm.type === 500 ? _c(
            "i18n",
            { staticClass: "is-title-2 c-title", attrs: { tag: "h2" } },
            [_vm._v("500")]
          ) : _c(
            "i18n",
            { staticClass: "is-title-2 c-title", attrs: { tag: "h2" } },
            [_vm._v("404")]
          )
        ],
        1
      ),
      _vm.type === 500 ? _c(
        "div",
        { staticClass: "c-wrapper" },
        [
          _c(
            "i18n",
            { staticClass: "is-title-3 c-title", attrs: { tag: "h2" } },
            [_vm._v("Server error")]
          ),
          _c("i18n", { staticClass: "c-text", attrs: { tag: "p" } }, [
            _vm._v("We are having some trouble. Please try again later.")
          ])
        ],
        1
      ) : _c(
        "div",
        { staticClass: "c-wrapper" },
        [
          _c(
            "i18n",
            { staticClass: "is-title-3 c-title", attrs: { tag: "h2" } },
            [_vm._v("Page not found")]
          ),
          _c("i18n", { staticClass: "c-text", attrs: { tag: "p" } }, [
            _vm._v("We couldn\u2019t find what you are looking for.")
          ]),
          _c(
            "router-link",
            { staticClass: "button", attrs: { to: "/" } },
            [_c("i18n", [_vm._v("Take me home")])],
            1
          )
        ],
        1
      )
    ],
    2
  );
};
var __vue_staticRenderFns__4 = [];
__vue_render__4._withStripped = true;
var __vue_inject_styles__4 = function(inject) {
  if (!inject) return;
  inject("data-v-24e45edb_0", { source: ".c-title[data-v-24e45edb] {\n  padding: 1.5rem 0;\n}\n.c-wrapper[data-v-24e45edb] {\n  text-align: center;\n}\n\n/*# sourceMappingURL=ErrorModal.vue.map */", map: { "version": 3, "sources": ["frontend/views/containers/loading-error/ErrorModal.vue", "ErrorModal.vue"], "names": [], "mappings": "AAmCA;EACA,iBAAA;AClCA;ADqCA;EACA,kBAAA;AClCA;;AAEA,yCAAyC", "file": "ErrorModal.vue", "sourcesContent": [`<template lang='pug'>
  modal-template(class='has-background' ref='modal' :a11yTitle='L("Loading")')
    template(slot='title')
      i18n(tag='h2' class='is-title-2 c-title' v-if='type===500') 500
      i18n(tag='h2' class='is-title-2 c-title' v-else) 404

    .c-wrapper(v-if='type===500')
      i18n.is-title-3.c-title(tag='h2') Server error
      i18n.c-text(tag='p') We are having some trouble. Please try again later.

    .c-wrapper(v-else)
      i18n.is-title-3.c-title(tag='h2') Page not found
      i18n.c-text(tag='p') We couldn\u2019t find what you are looking for.
      router-link.button(to='/')
        i18n Take me home
</template>

<script>
import ModalTemplate from '../../../../frontend/views/components/modal/ModalTemplate.vue'

export default ({
  name: 'LoadingModal',
  components: {
    ModalTemplate
  },
  props: {
    type: {
      type: Number,
      default: 500
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
.c-title {
  padding: 1.5rem 0;
}

.c-wrapper {
  text-align: center;
}
</style>
`, ".c-title {\n  padding: 1.5rem 0;\n}\n\n.c-wrapper {\n  text-align: center;\n}\n\n/*# sourceMappingURL=ErrorModal.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__4 = "data-v-24e45edb";
var __vue_module_identifier__4 = void 0;
var __vue_is_functional_template__4 = false;
function __vue_normalize__4(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
  modal-template(class='has-background' ref='modal' :a11yTitle='L("Loading")')
    template(slot='title')
      i18n(tag='h2' class='is-title-2 c-title' v-if='type===500') 500
      i18n(tag='h2' class='is-title-2 c-title' v-else) 404

    .c-wrapper(v-if='type===500')
      i18n.is-title-3.c-title(tag='h2') Server error
      i18n.c-text(tag='p') We are having some trouble. Please try again later.

    .c-wrapper(v-else)
      i18n.is-title-3.c-title(tag='h2') Page not found
      i18n.c-text(tag='p') We couldn\u2019t find what you are looking for.
      router-link.button(to='/')
        i18n Take me home
</template>

<script>
import ModalTemplate from '../../../../frontend/views/components/modal/ModalTemplate.vue'

export default ({
  name: 'LoadingModal',
  components: {
    ModalTemplate
  },
  props: {
    type: {
      type: Number,
      default: 500
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
.c-title {
  padding: 1.5rem 0;
}

.c-wrapper {
  text-align: center;
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__4() {
  const styles = __vue_create_injector__4.styles || (__vue_create_injector__4.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__4 = /* @__PURE__ */ __vue_normalize__4(
  { render: __vue_render__4, staticRenderFns: __vue_staticRenderFns__4 },
  __vue_inject_styles__4,
  __vue_script__4,
  __vue_scope_id__4,
  __vue_is_functional_template__4,
  __vue_module_identifier__4,
  false,
  __vue_create_injector__4,
  void 0,
  void 0
);
var ErrorModal_default = __vue_component__4;

// frontend/views/containers/loading-error/ErrorPage.vue
var __vue_script__5 = {
  name: "ErrorPage",
  props: {
    type: {
      type: Number,
      default: 500
    }
  },
  data() {
    return {
      ephemeral: {
        isOnline: null
        // show 'Retry' button only when the browser is online
      }
    };
  },
  methods: {
    onlineHandler() {
      this.ephemeral.isOnline = true;
    },
    offlineHandler() {
      this.ephemeral.isOnline = false;
    },
    onRetryClick() {
      window.location.reload();
    }
  },
  mounted() {
    if (navigator.onLine === true) {
      this.onlineHandler();
    }
    window.addEventListener("online", this.onlineHandler);
    window.addEventListener("offline", this.offlineHandler);
  },
  beforeDestroy() {
    window.removeEventListener("online", this.onlineHandler);
    window.removeEventListener("offline", this.offlineHandler);
  }
};
var __vue_render__5 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _vm.type === 500 ? _c(
    "div",
    { staticClass: "p-wrapper" },
    [
      _c("h1", { staticClass: "is-title-1 p-title" }, [_vm._v("500")]),
      _c(
        "i18n",
        { staticClass: "is-title-3 c-title", attrs: { tag: "h2" } },
        [_vm._v("Server error")]
      ),
      _c("i18n", { staticClass: "c-text", attrs: { tag: "p" } }, [
        _vm._v("We are having some trouble. Please try again later.")
      ]),
      _vm.ephemeral.isOnline ? _c(
        "i18n",
        {
          staticClass: "is-outlined is-small c-retry-btn",
          attrs: { tag: "button" },
          on: { click: _vm.onRetryClick }
        },
        [_vm._v("Retry")]
      ) : _vm._e()
    ],
    1
  ) : _c(
    "div",
    { staticClass: "p-wrapper" },
    [
      _c("h1", { staticClass: "is-title-1 p-title" }, [_vm._v("404")]),
      _c(
        "i18n",
        { staticClass: "is-title-3 c-title", attrs: { tag: "h2" } },
        [_vm._v("Page not found")]
      ),
      _c("i18n", { staticClass: "c-text", attrs: { tag: "p" } }, [
        _vm._v("We couldn\u2019t find what you are looking for.")
      ]),
      _c(
        "router-link",
        { staticClass: "button", attrs: { to: "/" } },
        [_c("i18n", [_vm._v("Take me home")])],
        1
      )
    ],
    1
  );
};
var __vue_staticRenderFns__5 = [];
__vue_render__5._withStripped = true;
var __vue_inject_styles__5 = function(inject) {
  if (!inject) return;
  inject("data-v-8c10a702_0", { source: ".p-wrapper[data-v-8c10a702] {\n  display: flex;\n  flex-direction: column;\n  text-align: center;\n  justify-content: center;\n  align-items: center;\n}\n.c-title[data-v-8c10a702] {\n  margin-bottom: 0.5rem;\n}\n.c-text[data-v-8c10a702] {\n  color: var(--text_1);\n}\n.c-text + .button[data-v-8c10a702] {\n  align-self: center;\n  margin-top: 2rem;\n}\n.c-retry-btn[data-v-8c10a702] {\n  width: max-content;\n}\n\n/*# sourceMappingURL=ErrorPage.vue.map */", map: { "version": 3, "sources": ["frontend/views/containers/loading-error/ErrorPage.vue", "ErrorPage.vue"], "names": [], "mappings": "AAgEA;EACA,aAAA;EACA,sBAAA;EACA,kBAAA;EACA,uBAAA;EACA,mBAAA;AC/DA;ADkEA;EACA,qBAAA;AC/DA;ADkEA;EACA,oBAAA;AC/DA;ADiEA;EACA,kBAAA;EACA,gBAAA;AC/DA;ADmEA;EACA,kBAAA;AChEA;;AAEA,wCAAwC", "file": "ErrorPage.vue", "sourcesContent": [`<template lang='pug'>
.p-wrapper(v-if='type===500')
  h1.is-title-1.p-title 500
  i18n.is-title-3.c-title(tag='h2') Server error
  i18n.c-text(tag='p') We are having some trouble. Please try again later.
  i18n.is-outlined.is-small.c-retry-btn(
    v-if='ephemeral.isOnline'
    tag='button'
    @click='onRetryClick'
  ) Retry

.p-wrapper(v-else)
  h1.is-title-1.p-title 404
  i18n.is-title-3.c-title(tag='h2') Page not found
  i18n.c-text(tag='p') We couldn\u2019t find what you are looking for.
  router-link.button(to='/')
    i18n Take me home
</template>

<script>
export default ({
  name: 'ErrorPage',
  props: {
    type: {
      type: Number,
      default: 500
    }
  },
  data () {
    return {
      ephemeral: {
        isOnline: null // show 'Retry' button only when the browser is online
      }
    }
  },
  methods: {
    onlineHandler () {
      this.ephemeral.isOnline = true
    },
    offlineHandler () {
      this.ephemeral.isOnline = false
    },
    onRetryClick () {
      window.location.reload()
    }
  },
  mounted () {
    if (navigator.onLine === true) {
      this.onlineHandler()
    }

    window.addEventListener('online', this.onlineHandler)
    window.addEventListener('offline', this.offlineHandler)
  },
  beforeDestroy () {
    window.removeEventListener('online', this.onlineHandler)
    window.removeEventListener('offline', this.offlineHandler)
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.p-wrapper {
  display: flex;
  flex-direction: column;
  text-align: center;
  justify-content: center;
  align-items: center;
}

.c-title {
  margin-bottom: 0.5rem;
}

.c-text {
  color: $text_1;

  & + .button {
    align-self: center;
    margin-top: 2rem;
  }
}

.c-retry-btn {
  width: max-content;
}
</style>
`, ".p-wrapper {\n  display: flex;\n  flex-direction: column;\n  text-align: center;\n  justify-content: center;\n  align-items: center;\n}\n\n.c-title {\n  margin-bottom: 0.5rem;\n}\n\n.c-text {\n  color: var(--text_1);\n}\n.c-text + .button {\n  align-self: center;\n  margin-top: 2rem;\n}\n\n.c-retry-btn {\n  width: max-content;\n}\n\n/*# sourceMappingURL=ErrorPage.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__5 = "data-v-8c10a702";
var __vue_module_identifier__5 = void 0;
var __vue_is_functional_template__5 = false;
function __vue_normalize__5(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
.p-wrapper(v-if='type===500')
  h1.is-title-1.p-title 500
  i18n.is-title-3.c-title(tag='h2') Server error
  i18n.c-text(tag='p') We are having some trouble. Please try again later.
  i18n.is-outlined.is-small.c-retry-btn(
    v-if='ephemeral.isOnline'
    tag='button'
    @click='onRetryClick'
  ) Retry

.p-wrapper(v-else)
  h1.is-title-1.p-title 404
  i18n.is-title-3.c-title(tag='h2') Page not found
  i18n.c-text(tag='p') We couldn\u2019t find what you are looking for.
  router-link.button(to='/')
    i18n Take me home
</template>

<script>
export default ({
  name: 'ErrorPage',
  props: {
    type: {
      type: Number,
      default: 500
    }
  },
  data () {
    return {
      ephemeral: {
        isOnline: null // show 'Retry' button only when the browser is online
      }
    }
  },
  methods: {
    onlineHandler () {
      this.ephemeral.isOnline = true
    },
    offlineHandler () {
      this.ephemeral.isOnline = false
    },
    onRetryClick () {
      window.location.reload()
    }
  },
  mounted () {
    if (navigator.onLine === true) {
      this.onlineHandler()
    }

    window.addEventListener('online', this.onlineHandler)
    window.addEventListener('offline', this.offlineHandler)
  },
  beforeDestroy () {
    window.removeEventListener('online', this.onlineHandler)
    window.removeEventListener('offline', this.offlineHandler)
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.p-wrapper {
  display: flex;
  flex-direction: column;
  text-align: center;
  justify-content: center;
  align-items: center;
}

.c-title {
  margin-bottom: 0.5rem;
}

.c-text {
  color: $text_1;

  & + .button {
    align-self: center;
    margin-top: 2rem;
  }
}

.c-retry-btn {
  width: max-content;
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__5() {
  const styles = __vue_create_injector__5.styles || (__vue_create_injector__5.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__5 = /* @__PURE__ */ __vue_normalize__5(
  { render: __vue_render__5, staticRenderFns: __vue_staticRenderFns__5 },
  __vue_inject_styles__5,
  __vue_script__5,
  __vue_scope_id__5,
  __vue_is_functional_template__5,
  __vue_module_identifier__5,
  false,
  __vue_create_injector__5,
  void 0,
  void 0
);
var ErrorPage_default = __vue_component__5;

// frontend/views/containers/loading-error/LoadingModal.vue
var __vue_script__6 = {
  name: "LoadingModal",
  components: {
    ModalTemplate: ModalTemplate_default
  }
};
var __vue_render__6 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "modal-template",
    {
      ref: "modal",
      staticClass: "has-background",
      attrs: {
        a11yTitle: _vm.L("Loading"),
        loading: true,
        "data-test": "loadingModal"
      }
    },
    [
      _c(
        "template",
        { slot: "title" },
        [
          _c(
            "i18n",
            { staticClass: "is-title-2 c-title", attrs: { tag: "h2" } },
            [_vm._v("Loading...")]
          )
        ],
        1
      ),
      _c("div", { staticClass: "loading-box" })
    ],
    2
  );
};
var __vue_staticRenderFns__6 = [];
__vue_render__6._withStripped = true;
var __vue_inject_styles__6 = function(inject) {
  if (!inject) return;
  inject("data-v-a26f0a20_0", { source: ".c-title[data-v-a26f0a20] {\n  padding: 1.5rem 0;\n}\n\n/*# sourceMappingURL=LoadingModal.vue.map */", map: { "version": 3, "sources": ["frontend/views/containers/loading-error/LoadingModal.vue", "LoadingModal.vue"], "names": [], "mappings": "AAoBA;EACA,iBAAA;ACnBA;;AAEA,2CAA2C", "file": "LoadingModal.vue", "sourcesContent": [`<template lang='pug'>
  modal-template(class='has-background' ref='modal' :a11yTitle='L("Loading")' :loading='true' data-test='loadingModal')
    template(slot='title')
      i18n(tag='h2' class='is-title-2 c-title') Loading...

    .loading-box
</template>

<script>
import ModalTemplate from '../../../../frontend/views/components/modal/ModalTemplate.vue'

export default ({
  name: 'LoadingModal',
  components: {
    ModalTemplate
  }
}: Object)
<\/script>

<style lang="scss" scoped>
.c-title {
  padding: 1.5rem 0;
}
</style>
`, ".c-title {\n  padding: 1.5rem 0;\n}\n\n/*# sourceMappingURL=LoadingModal.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__6 = "data-v-a26f0a20";
var __vue_module_identifier__6 = void 0;
var __vue_is_functional_template__6 = false;
function __vue_normalize__6(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
  modal-template(class='has-background' ref='modal' :a11yTitle='L("Loading")' :loading='true' data-test='loadingModal')
    template(slot='title')
      i18n(tag='h2' class='is-title-2 c-title') Loading...

    .loading-box
</template>

<script>
import ModalTemplate from '../../../../frontend/views/components/modal/ModalTemplate.vue'

export default ({
  name: 'LoadingModal',
  components: {
    ModalTemplate
  }
}: Object)
<\/script>

<style lang="scss" scoped>
.c-title {
  padding: 1.5rem 0;
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__6() {
  const styles = __vue_create_injector__6.styles || (__vue_create_injector__6.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__6 = /* @__PURE__ */ __vue_normalize__6(
  { render: __vue_render__6, staticRenderFns: __vue_staticRenderFns__6 },
  __vue_inject_styles__6,
  __vue_script__6,
  __vue_scope_id__6,
  __vue_is_functional_template__6,
  __vue_module_identifier__6,
  false,
  __vue_create_injector__6,
  void 0,
  void 0
);
var LoadingModal_default = __vue_component__6;

// frontend/views/containers/loading-error/LoadingBaseModal.vue
var __vue_script__7 = {
  name: "LoadingModal",
  components: {
    ModalBaseTemplate: ModalBaseTemplate_default
  }
};
var __vue_render__7 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "modal-base-template",
    {
      ref: "modal",
      staticClass: "has-background",
      attrs: { a11yTitle: _vm.L("Loading"), loading: true }
    },
    [
      _c("div", { staticClass: "wrapper-container" }, [
        _c(
          "div",
          { staticClass: "container" },
          [
            _c(
              "i18n",
              { staticClass: "is-title-2 c-title", attrs: { tag: "h2" } },
              [_vm._v("Loading...")]
            ),
            _c("div", { staticClass: "loading-box" })
          ],
          1
        )
      ])
    ]
  );
};
var __vue_staticRenderFns__7 = [];
__vue_render__7._withStripped = true;
var __vue_inject_styles__7 = function(inject) {
  if (!inject) return;
  inject("data-v-4b15c081_0", { source: ".wrapper-container[data-v-4b15c081] {\n  height: 100%;\n  width: 100%;\n  opacity: 1;\n  background-color: var(--general_2);\n}\n.container[data-v-4b15c081] {\n  max-width: 66vw;\n  margin: 0 auto;\n}\n.c-title[data-v-4b15c081] {\n  padding: 2.5rem 0 1rem 0;\n}\n\n/*# sourceMappingURL=LoadingBaseModal.vue.map */", map: { "version": 3, "sources": ["frontend/views/containers/loading-error/LoadingBaseModal.vue", "LoadingBaseModal.vue"], "names": [], "mappings": "AAuBA;EACA,YAAA;EACA,WAAA;EACA,UAAA;EACA,kCAAA;ACtBA;ADyBA;EACA,eAAA;EACA,cAAA;ACtBA;ADyBA;EACA,wBAAA;ACtBA;;AAEA,+CAA+C", "file": "LoadingBaseModal.vue", "sourcesContent": [`<template lang='pug'>
  modal-base-template(class='has-background' ref='modal' :a11yTitle='L("Loading")' :loading='true')
    .wrapper-container
      .container
        i18n(tag='h2' class='is-title-2 c-title') Loading...

        .loading-box
</template>

<script>
import ModalBaseTemplate from '../../../../frontend/views/components/modal/ModalBaseTemplate.vue'

export default ({
  name: 'LoadingModal',
  components: {
    ModalBaseTemplate
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.wrapper-container {
  height: 100%;
  width: 100%;
  opacity: 1;
  background-color: $general_2;
}

.container {
  max-width: 66vw;
  margin: 0 auto;
}

.c-title {
  padding: 2.5rem 0 1rem 0;
}
</style>
`, ".wrapper-container {\n  height: 100%;\n  width: 100%;\n  opacity: 1;\n  background-color: var(--general_2);\n}\n\n.container {\n  max-width: 66vw;\n  margin: 0 auto;\n}\n\n.c-title {\n  padding: 2.5rem 0 1rem 0;\n}\n\n/*# sourceMappingURL=LoadingBaseModal.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__7 = "data-v-4b15c081";
var __vue_module_identifier__7 = void 0;
var __vue_is_functional_template__7 = false;
function __vue_normalize__7(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
  modal-base-template(class='has-background' ref='modal' :a11yTitle='L("Loading")' :loading='true')
    .wrapper-container
      .container
        i18n(tag='h2' class='is-title-2 c-title') Loading...

        .loading-box
</template>

<script>
import ModalBaseTemplate from '../../../../frontend/views/components/modal/ModalBaseTemplate.vue'

export default ({
  name: 'LoadingModal',
  components: {
    ModalBaseTemplate
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.wrapper-container {
  height: 100%;
  width: 100%;
  opacity: 1;
  background-color: $general_2;
}

.container {
  max-width: 66vw;
  margin: 0 auto;
}

.c-title {
  padding: 2.5rem 0 1rem 0;
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__7() {
  const styles = __vue_create_injector__7.styles || (__vue_create_injector__7.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__7 = /* @__PURE__ */ __vue_normalize__7(
  { render: __vue_render__7, staticRenderFns: __vue_staticRenderFns__7 },
  __vue_inject_styles__7,
  __vue_script__7,
  __vue_scope_id__7,
  __vue_is_functional_template__7,
  __vue_module_identifier__7,
  false,
  __vue_create_injector__7,
  void 0,
  void 0
);
var LoadingBaseModal_default = __vue_component__7;

// frontend/views/containers/loading-error/LoadingPage.vue
var __vue_script__8 = {
  name: "LoadingPage"
};
var __vue_render__8 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c("div", { staticClass: "p-wrapper" }, [
    _c("header", { staticClass: "p-header" }, [
      _c("h1", { staticClass: "is-title-2 p-title" }, [
        _c("img", {
          staticClass: "c-logo",
          attrs: {
            src: "/assets/images/group-income-icon-transparent.png",
            alt: ""
          }
        }),
        _vm._v(_vm._s(_vm.$route.meta.title))
      ])
    ]),
    _vm._m(0)
  ]);
};
var __vue_staticRenderFns__8 = [
  function() {
    var _vm = this;
    var _h = _vm.$createElement;
    var _c = _vm._self._c || _h;
    return _c("main", { staticClass: "p-main" }, [
      _c("div", { staticClass: "loading-box" }),
      _c("div", { staticClass: "loading-box" }),
      _c("div", { staticClass: "loading-box" })
    ]);
  }
];
__vue_render__8._withStripped = true;
var __vue_inject_styles__8 = function(inject) {
  if (!inject) return;
  inject("data-v-b4e9b596_0", { source: '.p-wrapper[data-v-b4e9b596] {\n  display: grid;\n  grid-template-areas: "p-header" "p-main";\n  grid-template-columns: 1fr;\n  grid-template-rows: auto minmax(0, 1fr);\n  height: 100%;\n  width: 100vw;\n}\n@media screen and (min-width: 1200px) {\n.p-wrapper[data-v-b4e9b596] {\n    width: auto;\n    grid-template-columns: minmax(0, 1fr) 16.5rem;\n    grid-template-areas: "p-header p-sidebar" "p-main p-sidebar";\n}\n}\n.p-main[data-v-b4e9b596] {\n  grid-area: p-main;\n  width: calc(100% - 2rem);\n  margin: 0 auto;\n  padding-top: 1.5rem;\n  max-width: 50rem;\n  display: flex;\n  flex-direction: column;\n  -webkit-overflow-scrolling: touch;\n}\n@media screen and (min-width: 769px), print {\n.p-main[data-v-b4e9b596] {\n    width: calc(100% - 4rem);\n}\n}\n@media screen and (min-width: 1200px) {\n.p-main[data-v-b4e9b596] {\n    width: auto;\n    padding-top: 0;\n    margin-right: 2rem;\n    margin-left: 5.5rem;\n}\n}\n.p-header[data-v-b4e9b596] {\n  grid-area: p-header;\n  transition: padding ease-out 300ms;\n  text-align: center;\n  min-height: 4rem;\n}\n@media screen and (max-width: 1199px) {\n.p-header[data-v-b4e9b596] {\n    display: flex;\n    justify-content: center;\n    align-items: center;\n    position: sticky;\n    top: 0;\n    background: var(--general_2);\n    z-index: 3;\n}\n}\n@media screen and (min-width: 1200px) {\n.p-header[data-v-b4e9b596] {\n    display: block;\n    padding-top: 1.125rem;\n    text-align: left;\n    min-height: 4.75rem;\n    padding-left: 5.5rem;\n}\n}\n.p-title[data-v-b4e9b596] {\n  display: flex;\n  align-items: center;\n}\n.c-logo[data-v-b4e9b596] {\n  width: 1.5rem;\n  height: 1.5rem;\n  margin-right: 1rem;\n}\n@media screen and (min-width: 1200px) {\n.c-logo[data-v-b4e9b596] {\n    display: none;\n}\n}\n\n/*# sourceMappingURL=LoadingPage.vue.map */', map: { "version": 3, "sources": ["frontend/views/containers/loading-error/LoadingPage.vue", "LoadingPage.vue"], "names": [], "mappings": "AA4BA;EACA,aAAA;EACA,wCAAA;EACA,0BAAA;EACA,uCAAA;EACA,YAAA;EACA,YAAA;AC3BA;ADuDA;AAlCA;IASA,WAAA;IACA,6CAAA;IACA,4DACA;AC3BE;AACF;AD+BA;EACA,iBAAA;EACA,wBAAA;EACA,cAAA;EACA,mBAAA;EACA,gBAAA;EACA,aAAA;EACA,sBAAA;EC5BE,iCAAiC;AACnC;ADyBA;AALA;IAWA,wBAAA;AC3BE;AACF;ADgCA;AAjBA;IAeA,WAAA;IACA,cAAA;IACA,kBAAA;IACA,mBArCA;ACWE;AACF;AD6BA;EACA,mBAAA;EACA,kCAAA;EACA,kBAAA;EACA,gBAAA;AC1BA;ADWA;AAWA;IAOA,aAAA;IACA,uBAAA;IACA,mBAAA;IACA,gBAAA;IACA,MAAA;IACA,4BAAA;IACA,UAAA;ACzBE;AACF;ADMA;AAKA;IAiBA,cAAA;IACA,qBAAA;IACA,gBAAA;IACA,mBAAA;IACA,oBA9DA;ACsCE;AACF;AD2BA;EACA,aAAA;EACA,mBAAA;ACxBA;AD2BA;EACA,aAAA;EACA,cAAA;EACA,kBAAA;ACxBA;ADdA;AAmCA;IAMA,aAAA;ACvBE;AACF;;AAEA,0CAA0C", "file": "LoadingPage.vue", "sourcesContent": [`<template lang='pug'>
.p-wrapper
  header.p-header
    h1.is-title-2.p-title
      img.c-logo(
        src='/assets/images/group-income-icon-transparent.png'
        alt=''
      )
      | {{$route.meta.title}}

  main.p-main
    .loading-box
    .loading-box
    .loading-box
</template>

<script>
export default ({
  name: 'LoadingPage'
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

$pagePadding: 1rem;
$pagePaddingDesktop: 5.5rem;

.p-wrapper {
  display: grid;
  grid-template-areas: "p-header" "p-main";
  grid-template-columns: 1fr;
  grid-template-rows: auto minmax(0, 1fr);
  height: 100%;
  width: 100vw;

  @include desktop {
    width: auto;
    grid-template-columns: minmax(0, 1fr) $rightSideWidth;
    grid-template-areas:
      "p-header p-sidebar"
      "p-main p-sidebar";
  }
}

.p-main {
  grid-area: p-main;
  width: calc(100% - 2rem);
  margin: 0 auto;
  padding-top: 1.5rem;
  max-width: 50rem;
  display: flex;
  flex-direction: column;
  @include overflow-touch;

  @include tablet {
    width: calc(100% - 4rem);
  }

  @include desktop {
    width: auto;
    padding-top: 0;
    margin-right: 2rem;
    margin-left: $pagePaddingDesktop;
  }
}

.p-header {
  grid-area: p-header;
  transition: padding ease-out 300ms;
  text-align: center;
  min-height: 4rem;

  @include touch {
    display: flex;
    justify-content: center;
    align-items: center;
    position: sticky;
    top: 0;
    background: $general_2;
    z-index: 3;
  }

  @include desktop {
    display: block;
    padding-top: 1.125rem;
    text-align: left;
    min-height: 4.75rem;
    padding-left: $pagePaddingDesktop;
  }
}

.p-title {
  display: flex;
  align-items: center;
}

.c-logo {
  width: 1.5rem;
  height: 1.5rem;
  margin-right: 1rem;

  @include desktop {
    display: none;
  }
}
</style>
`, '.p-wrapper {\n  display: grid;\n  grid-template-areas: "p-header" "p-main";\n  grid-template-columns: 1fr;\n  grid-template-rows: auto minmax(0, 1fr);\n  height: 100%;\n  width: 100vw;\n}\n@media screen and (min-width: 1200px) {\n  .p-wrapper {\n    width: auto;\n    grid-template-columns: minmax(0, 1fr) 16.5rem;\n    grid-template-areas: "p-header p-sidebar" "p-main p-sidebar";\n  }\n}\n\n.p-main {\n  grid-area: p-main;\n  width: calc(100% - 2rem);\n  margin: 0 auto;\n  padding-top: 1.5rem;\n  max-width: 50rem;\n  display: flex;\n  flex-direction: column;\n  -webkit-overflow-scrolling: touch;\n}\n@media screen and (min-width: 769px), print {\n  .p-main {\n    width: calc(100% - 4rem);\n  }\n}\n@media screen and (min-width: 1200px) {\n  .p-main {\n    width: auto;\n    padding-top: 0;\n    margin-right: 2rem;\n    margin-left: 5.5rem;\n  }\n}\n\n.p-header {\n  grid-area: p-header;\n  transition: padding ease-out 300ms;\n  text-align: center;\n  min-height: 4rem;\n}\n@media screen and (max-width: 1199px) {\n  .p-header {\n    display: flex;\n    justify-content: center;\n    align-items: center;\n    position: sticky;\n    top: 0;\n    background: var(--general_2);\n    z-index: 3;\n  }\n}\n@media screen and (min-width: 1200px) {\n  .p-header {\n    display: block;\n    padding-top: 1.125rem;\n    text-align: left;\n    min-height: 4.75rem;\n    padding-left: 5.5rem;\n  }\n}\n\n.p-title {\n  display: flex;\n  align-items: center;\n}\n\n.c-logo {\n  width: 1.5rem;\n  height: 1.5rem;\n  margin-right: 1rem;\n}\n@media screen and (min-width: 1200px) {\n  .c-logo {\n    display: none;\n  }\n}\n\n/*# sourceMappingURL=LoadingPage.vue.map */'] }, media: void 0 });
};
var __vue_scope_id__8 = "data-v-b4e9b596";
var __vue_module_identifier__8 = void 0;
var __vue_is_functional_template__8 = false;
function __vue_normalize__8(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
.p-wrapper
  header.p-header
    h1.is-title-2.p-title
      img.c-logo(
        src='/assets/images/group-income-icon-transparent.png'
        alt=''
      )
      | {{$route.meta.title}}

  main.p-main
    .loading-box
    .loading-box
    .loading-box
</template>

<script>
export default ({
  name: 'LoadingPage'
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

$pagePadding: 1rem;
$pagePaddingDesktop: 5.5rem;

.p-wrapper {
  display: grid;
  grid-template-areas: "p-header" "p-main";
  grid-template-columns: 1fr;
  grid-template-rows: auto minmax(0, 1fr);
  height: 100%;
  width: 100vw;

  @include desktop {
    width: auto;
    grid-template-columns: minmax(0, 1fr) $rightSideWidth;
    grid-template-areas:
      "p-header p-sidebar"
      "p-main p-sidebar";
  }
}

.p-main {
  grid-area: p-main;
  width: calc(100% - 2rem);
  margin: 0 auto;
  padding-top: 1.5rem;
  max-width: 50rem;
  display: flex;
  flex-direction: column;
  @include overflow-touch;

  @include tablet {
    width: calc(100% - 4rem);
  }

  @include desktop {
    width: auto;
    padding-top: 0;
    margin-right: 2rem;
    margin-left: $pagePaddingDesktop;
  }
}

.p-header {
  grid-area: p-header;
  transition: padding ease-out 300ms;
  text-align: center;
  min-height: 4rem;

  @include touch {
    display: flex;
    justify-content: center;
    align-items: center;
    position: sticky;
    top: 0;
    background: $general_2;
    z-index: 3;
  }

  @include desktop {
    display: block;
    padding-top: 1.125rem;
    text-align: left;
    min-height: 4.75rem;
    padding-left: $pagePaddingDesktop;
  }
}

.p-title {
  display: flex;
  align-items: center;
}

.c-logo {
  width: 1.5rem;
  height: 1.5rem;
  margin-right: 1rem;

  @include desktop {
    display: none;
  }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__8() {
  const styles = __vue_create_injector__8.styles || (__vue_create_injector__8.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__8 = /* @__PURE__ */ __vue_normalize__8(
  { render: __vue_render__8, staticRenderFns: __vue_staticRenderFns__8 },
  __vue_inject_styles__8,
  __vue_script__8,
  __vue_scope_id__8,
  __vue_is_functional_template__8,
  __vue_module_identifier__8,
  false,
  __vue_create_injector__8,
  void 0,
  void 0
);
var LoadingPage_default = __vue_component__8;

// frontend/utils/lazyLoadedView.js
function asyncHandler(lazyImport, { loading, error } = {}) {
  return () => ({
    // HACK: sometimes a bundler bug makes it necessary to use
    // `.then(m => m.default ?? m)` when importing a module with `import()`.
    component: lazyImport().then((m) => m.default ?? m),
    loading,
    error
  });
}
function lazyComponent(name, lazyImport, { loading, error } = {}) {
  vue_esm_default.component(name, asyncHandler(lazyImport, { loading, error }));
}
function lazyModal(name, lazyImport) {
  lazyComponent(name, lazyImport, {
    loading: LoadingModal_default,
    error: ErrorModal_default
  });
}
function lazyModalFullScreen(name, lazyImport) {
  lazyComponent(name, lazyImport, {
    loading: LoadingBaseModal_default,
    error: ErrorModal_default
  });
}
function lazyPage(lazyImport, { loading = LoadingPage_default, error = ErrorPage_default } = {}) {
  const handler = asyncHandler(lazyImport, { loading, error });
  return () => Promise.resolve({
    functional: true,
    render(h, { data, children }) {
      return h(handler, data, children);
    }
  });
}
lazyModal("AccountRemovalModal", () => import("./AccountRemovalModal-RELHM5YY-cached.js"));
lazyModal("ChatMembersAllModal", () => import("./ChatMembersAllModal-4S4XJB6N-cached.js"));
lazyModal("CreateNewChannelModal", () => import("./CreateNewChannelModal-JWI3X4DO-cached.js"));
lazyModal("DeleteChannelModal", () => import("./DeleteChannelModal-5SBSEIHI-cached.js"));
lazyModal("EditChannelDescriptionModal", () => import("./EditChannelDescriptionModal-YE42WKIK-cached.js"));
lazyModal("EditChannelNameModal", () => import("./EditChannelNameModal-PFMQNEEP-cached.js"));
lazyModal("ChatNotificationSettingsModal", () => import("./ChatNotificationSettingsModal-YYYCDKJ3-cached.js"));
lazyModal("GroupLeaveModal", () => import("./GroupLeaveModal-SVKYE7UP-cached.js"));
lazyModal("GroupDeletionModal", () => import("./GroupDeletionModal-BNOKJJOR-cached.js"));
lazyModal("InvitationLinkModal", () => import("./InvitationLinkModal-N2GOBLNM-cached.js"));
lazyModal("LeaveChannelModal", () => import("./LeaveChannelModal-TWI5LBH3-cached.js"));
lazyModal("LoginModal", () => import("./LoginModal-DXAWGRMD-cached.js"));
lazyModal("NotificationModal", () => import("./NotificationModal-3HPOXL6L-cached.js"));
lazyModal("PasswordModal", () => import("./PasswordModal-JJ4IURVR-cached.js"));
lazyModal("SignupModal", () => import("./SignupModal-QYFE5F6H-cached.js"));
lazyModal("Prompt", () => import("./Prompt-KIJUG4A6-cached.js"));
lazyModal("SendThankYouModal", () => import("./SendThankYouModal-JGX6MDIT-cached.js"));
lazyModal("ThankYouNoteModal", () => import("./ThankYouNoteModal-GB64LSZW-cached.js"));
lazyModal("AvatarEditorModal", () => import("./AvatarEditorModal-KOWJLGL5-cached.js"));
lazyModal("ChatFileAttachmentWarningModal", () => import("./ChatFileAttachmentWarningModal-QW6E3DAP-cached.js"));
lazyModal("ImageViewerModal", () => import("./ImageViewerModal-RMJ6EA25-cached.js"));
lazyModal("VideoViewerModal", () => import("./VideoViewerModal-QH44XZWP-cached.js"));
lazyModalFullScreen("GroupCreationModal", () => import("./GroupCreationModal-SC5AXRTR-cached.js"));
lazyModalFullScreen("GroupJoinModal", () => import("./GroupJoinModal-YXMTKCP4-cached.js"));
lazyModalFullScreen("GroupMembersAllModal", () => import("./GroupMembersAllModal-UCW7UHAT-cached.js"));
lazyModalFullScreen("IncomeDetails", () => import("./IncomeDetails-GELUCIM7-cached.js"));
lazyModalFullScreen("UserSettingsModal", () => import("./UserSettingsModal-VAIUHTIG-cached.js"));
lazyComponent("AddMembers", () => import("./AddMembers-2XGLAWXP-cached.js"));
lazyComponent("ChangeVotingRules", () => import("./ChangeVotingRules-PP3TKXTR-cached.js"));
lazyComponent("MincomeProposal", () => import("./Mincome-BSETJ2UE-cached.js"));
lazyComponent("DistributionDateProposal", () => import("./DistributionDate-AZ5JXASR-cached.js"));
lazyComponent("PaymentsHistoryModal", () => import("./PaymentsHistoryModal-LWQVKJOG-cached.js"));
lazyComponent("RemoveMember", () => import("./RemoveMember-YQTCRRUG-cached.js"));
lazyComponent("GenericProposal", () => import("./GenericProposal-7SJI3RBK-cached.js"));
lazyModalFullScreen("PropositionsAllModal", () => import("./PropositionsAllModal-A7WE3SXK-cached.js"));
lazyComponent("PaymentDetail", () => import("./PaymentDetail-JTJJG6LR-cached.js"));
lazyComponent("RecordPayment", () => import("./RecordPayment-NQKTBVNU-cached.js"));
lazyComponent("SendPaymentsViaLightning", () => import("./SendPaymentsViaLightning-TLSLZSHC-cached.js"));
lazyComponent("ExportPaymentsModal", () => import("./ExportPaymentsModal-TBMB6GOS-cached.js"));
lazyComponent("Appearence", () => import("./Appearence-JREAH6CT-cached.js"));
lazyComponent("NotificationSettings", () => import("./NotificationSettings-VOZ7WS2L-cached.js"));
lazyComponent("AppLogs", () => import("./AppLogs-HM7P5XS4-cached.js"));
lazyComponent("NewDirectMessageModal", () => import("./NewDirectMessageModal-URTQJBZW-cached.js"));
lazyComponent("Placeholder", () => import("./Placeholder-MVVNZVRZ-cached.js"));
lazyComponent("Troubleshooting", () => import("./Troubleshooting-7CSD3LYU-cached.js"));
lazyComponent("UserProfile", () => import("./UserProfile-HJYKS5PL-cached.js"));
lazyComponent("Acknowledgements", () => import("./Acknowledgements-BN2R2Y36-cached.js"));
lazyComponent("DSModalFullscreen", () => import("./DSModalFullscreen-7RXDWB2K-cached.js"));
lazyComponent("DSModalNested", () => import("./DSModalNested-WY73IW7Q-cached.js"));
lazyComponent("DSModalQuery", () => import("./DSModalQuery-WD34GR5Z-cached.js"));

// frontend/controller/router.js
init_vue_esm();
var lazyContributions = lazyPage(() => import("./Contributions-KH2AFUWH-cached.js"));
var lazyDesignSystem = lazyPage(() => import("./DesignSystem-BQ6BD5FZ-cached.js"));
var lazyGroupChat = lazyPage(() => import("./GroupChat-2LV2KHC7-cached.js"));
var lazyGroupDashboard = lazyPage(() => import("./GroupDashboard-GNSUSV5C-cached.js"));
var lazyGlobalDashboard = lazyPage(() => import("./GlobalDashboard-T5LDMAJX-cached.js"));
var lazyGroupSettings = lazyPage(() => import("./GroupSettings-UVY2S7VH-cached.js"));
var lazyPayments = lazyPage(() => import("./Payments-LFM2GSYZ-cached.js"));
var lazyPendingApproval = lazyPage(() => import("./PendingApproval-OW6HQKDJ-cached.js"));
vue_esm_default.use(vue_router_esm_default);
var homeGuard = {
  guard: (to, from) => !!state_default.state.currentGroupId,
  redirect: (to, from) => {
    return {
      path: (
        // If we haven't accepted the invite OR we haven't clicked 'Awesome' on
        // the welcome screen, redirect to the '/pending-approval' page
        state_default.getters.seenWelcomeScreen ? "/dashboard" : "/pending-approval"
      )
    };
  }
};
var loginGuard = {
  guard: (to, from) => !state_default.state.loggedIn,
  redirect: (to, from) => ({ path: "/", query: { next: to.fullPath } })
};
var inviteGuard = {
  guard: (to, from) => {
    return !(to.hash.includes("groupId=") && to.hash.includes("secret="));
  },
  redirect: (to, from) => ({ path: "/" })
};
var groupGuard = {
  guard: (to, from) => !state_default.state.currentGroupId,
  redirect: (to, from) => ({ path: "/" })
};
var pendingApprovalGuard = {
  guard: (to, from) => state_default.state.currentGroupId && !state_default.getters.ourProfileActive,
  redirect: (to, from) => ({ path: "/pending-approval" })
};
function createEnterGuards(...guards) {
  return function(to, from, next) {
    for (const current of guards) {
      if (current.guard(to, from)) {
        return next(current.redirect(to, from));
      }
    }
    next();
  };
}
var router = new vue_router_esm_default({
  mode: "history",
  base: "/app",
  scrollBehavior(to, from, savedPosition) {
    return { x: 0, y: 0 };
  },
  routes: [
    {
      path: "/",
      component: Home_default,
      name: "home",
      meta: { title: L("Group Income") },
      // page title. see issue #45
      beforeEnter: createEnterGuards(homeGuard)
    },
    {
      path: "/design-system",
      component: lazyDesignSystem,
      name: "DesignSystem",
      meta: { title: L("Design System") }
      // beforeEnter: createEnterGuards(designGuard)
    },
    {
      path: "/dashboard",
      component: lazyGroupDashboard,
      name: "GroupDashboard",
      meta: { title: L("Group Dashboard") },
      beforeEnter: createEnterGuards(loginGuard, groupGuard, pendingApprovalGuard)
    },
    {
      path: "/global-dashboard/:id",
      component: lazyGlobalDashboard,
      name: "GlobalDashboard",
      meta: { title: L("Global Dashboard") },
      beforeEnter: createEnterGuards(loginGuard, groupGuard)
    },
    {
      path: "/global-dashboard",
      redirect: "/global-dashboard/news-and-updates"
    },
    {
      path: "/contributions",
      component: lazyContributions,
      name: "Contributions",
      meta: { title: L("Contributions") },
      beforeEnter: createEnterGuards(loginGuard, groupGuard, pendingApprovalGuard)
    },
    {
      path: "/payments",
      component: lazyPayments,
      name: "Payments",
      meta: { title: L("Payments") },
      beforeEnter: createEnterGuards(loginGuard, groupGuard, pendingApprovalGuard)
    },
    {
      path: "/group-chat",
      component: lazyGroupChat,
      name: "GroupChat",
      meta: { title: L("Group Chat") },
      beforeEnter: createEnterGuards(loginGuard, groupGuard, pendingApprovalGuard)
    },
    {
      path: "/group-chat/:chatRoomID",
      component: lazyGroupChat,
      name: "GroupChatConversation",
      meta: { title: L("Loading") },
      /**
       * The weird title `Loading` is used as title until the page is loaded
       * Chatroom details could be retrieved from the backend using it's id(chatRoomID)
       * So until that time, chatroom name is unknown and could display `Loading`
       */
      beforeEnter: createEnterGuards(loginGuard, groupGuard, pendingApprovalGuard)
    },
    {
      path: "/group-settings",
      component: lazyGroupSettings,
      name: "GroupSettings",
      meta: { title: L("Group Settings") },
      beforeEnter: createEnterGuards(loginGuard, groupGuard, pendingApprovalGuard)
    },
    {
      path: "/join",
      name: Join_default.name,
      component: Join_default,
      meta: { title: L("Join a Group") },
      beforeEnter: createEnterGuards(inviteGuard)
    },
    {
      path: "/pending-approval",
      component: lazyPendingApproval,
      name: "PendingApproval",
      meta: { title: L("Pending Approval") },
      beforeEnter: createEnterGuards(loginGuard)
    },
    ...true ? [{
      path: "/error-testing",
      name: "ErrorTesting",
      component: () => import("./ErrorTesting-P3IXVFW2-cached.js"),
      meta: { title: L("Error Testing") }
    }] : [],
    {
      path: "*",
      redirect: "/"
    }
  ]
});
router.beforeEach((to, from, next) => {
  if (to.query.modal && !["Prompt", "SignupModal", "LoginModal"].includes(to.query.modal) && !state_default.state.loggedIn) {
    return next({ path: "/", query: { next: to.fullPath } });
  }
  document.title = to.meta.title;
  next();
});
esm_default("sbp/selectors/register", {
  "controller/router": () => router
});
var router_default = router;

// frontend/utils/isPwa.js
var isPwa_default = /* @__PURE__ */ (() => {
  let isPwa;
  return () => {
    if (isPwa == null) {
      isPwa = window.matchMedia("(display-mode: standalone) or (display-mode: window-controls-overlay)").matches || // $FlowFixMe[prop-missing]
      navigator.standalone;
    }
    return isPwa;
  };
})();

// frontend/controller/service-worker.js
var pwa = {
  deferredInstallPrompt: null,
  installed: false
};
deserializer.register(SPMessage);
deserializer.register(Secret);
window.addEventListener("beforeinstallprompt", (e) => {
  pwa.deferredInstallPrompt = e;
  esm_default("okTurtles.events/emit", PWA_INSTALLABLE);
});
var serviceWorkerMap = /* @__PURE__ */ new WeakMap();
var waitUntilSwReady = () => {
  const promise = new Promise((resolve, reject) => {
    const messageChannel = new MessageChannel();
    messageChannel.port1.onmessage = (event) => {
      if (event.data.type === "ready") {
        if (event.data.currentSyncs) {
          esm_default("okTurtles.events/emit", CONTRACT_SYNCS_RESET, event.data.currentSyncs);
        }
        resolve();
      } else {
        reject(event.data.error);
      }
      messageChannel.port1.close();
    };
    messageChannel.port1.onmessageerror = () => {
      reject(new Error("Message error"));
      messageChannel.port1.close();
    };
    const oncontrollerchange = () => {
      navigator.serviceWorker.removeEventListener("controllerchange", oncontrollerchange, false);
      resolve(waitUntilSwReady());
    };
    navigator.serviceWorker.addEventListener("controllerchange", oncontrollerchange, false);
    navigator.serviceWorker.ready.then((worker) => {
      if (serviceWorkerMap.has(worker.active)) {
        resolve(serviceWorkerMap.get(worker.active));
        return;
      }
      serviceWorkerMap.set(worker.active, promise);
      worker.active.postMessage({
        type: "ready",
        port: messageChannel.port2,
        GI_VERSION: "2.5.1@2026-01-22T10:19:51.672Z"
      }, [messageChannel.port2]);
    }).catch((e) => {
      reject(e);
      messageChannel.port1.close();
    });
  });
  return promise;
};
esm_default("sbp/selectors/register", {
  "service-worker/setup": async function() {
    if (!("serviceWorker" in navigator)) {
      throw new Error("Service worker APIs missing");
    }
    try {
      const params = new URLSearchParams([
        ["routerBase", esm_default("controller/router").options.base ?? ""],
        ["standalone", isPwa_default() ? "1" : "0"]
      ]);
      const swRegistration = await navigator.serviceWorker.register(`/assets/js/sw-primary.js?${params}`, { scope: "/" });
      if (swRegistration.active == null && swRegistration.installing == null && swRegistration.waiting == null) {
        throw new Error("No valid service worker found");
      }
      await swRegistration.update();
      setInterval(() => esm_default("service-worker/update"), HOURS_MILLIS);
      navigator.serviceWorker.addEventListener("message", (event) => {
        const data = event.data;
        const silentEmit = esm_default("sbp/selectors/fn", "okTurtles.events/emit");
        if (typeof data === "object" && data.type) {
          switch (data.type) {
            case "pong":
              break;
            case "event": {
              esm_default("okTurtles.events/emit", event.data.subtype, ...deserializer(event.data.data));
              break;
            }
            case "navigate": {
              if (data.groupID) {
                esm_default("state/vuex/commit", "setCurrentGroupId", { contractID: data.groupID });
              }
              esm_default("controller/router").push({ path: data.path }).catch(console.warn);
              break;
            }
            // `sbp` invocations from the SW to the app. Used by the
            // `notificationclick` handler for notifications that have an
            // `sbpInvocation` instead of a `linkTo` property.
            case "sbp": {
              esm_default(...deserializer(event.data.data));
              break;
            }
            case CAPTURED_LOGS: {
              silentEmit(CAPTURED_LOGS, ...deserializer(event.data.data));
              break;
            }
            default:
              console.error("[sw] Received unknown message type from the service worker:", data);
              break;
          }
        }
      });
      await waitUntilSwReady();
      if (typeof PeriodicSyncManager === "function") {
        navigator.permissions.query({
          name: "periodic-background-sync"
        }).then((status) => {
          if (status.state !== "granted") {
            console.error("[service-workers/setup] Periodic sync event permission denied");
            return;
          }
          return navigator.serviceWorker.ready.then(
            (registration) => registration.periodicSync.register("periodic-notifications", {
              // An interval of 12 hours
              minInterval: 12 * HOURS_MILLIS
            })
          );
        }).catch((e) => {
          console.error("[service-workers/setup] Error setting up periodic background sync events", e);
        });
      }
      swRegistration.addEventListener("updatefound", (e) => {
        const handler = (e2) => {
          if (["activated", "redundant"].includes(e2.target.state)) {
            e2.target.removeEventListener("statechange", handler);
            return;
          }
          if (e2.target.state === "installed") {
            e2.target.postMessage({ type: "skip-waiting" });
          }
        };
        e.target.installing.addEventListener("statechange", handler, false);
      });
      setInterval(() => navigator.serviceWorker.controller?.postMessage({ type: "ping" }), 5e3);
    } catch (e) {
      console.error("error setting up service worker:", e);
      throw e;
    }
  },
  // We call this when the notification permission changes, to create a push
  // subscription and report it to the server. We need to do this outside of the
  // service worker because this generally happens after requesting the
  // notifications permission, which requires user interaction and can't be
  // done form the SW itself.
  // In theory, the `PushManager` APIs used are available in the SW and we could
  // have this function there. However, most examples perform this outside of the
  // SW, and private testing showed that it's more reliable doing it here.
  "service-worker/setup-push-subscription": async function(attemptNumber = 1) {
    if ("") {
      throw new Error("Push disabled in CI mode");
    }
    let retryAttemptsPromise = null;
    await esm_default("okTurtles.eventQueue/queueEvent", "service-worker/setup-push-subscription", async () => {
      const { notificationEnabled } = esm_default("state/vuex/state").settings;
      const registration = await navigator.serviceWorker.ready;
      if (!registration) {
        throw new Error("No service-worker registration found!");
      }
      const permissionState = await registration.pushManager.permissionState({ userVisibleOnly: true });
      const granted = permissionState === "granted" || Notification.permission === "granted";
      console.info(`[service-worker/setup-push-subscription] setup: notifications (${notificationEnabled}) perms (${granted})`);
      try {
        let subscription = null;
        let subID = null;
        if (notificationEnabled && granted) {
          subscription = await registration.pushManager.getSubscription();
          let newSub = false;
          let endpoint = null;
          if (!subscription || subscription.expirationTime != null && subscription.expirationTime <= Date.now()) {
            const subscriptionOptions = await esm_default("push/getSubscriptionOptions");
            subscription = await registration.pushManager.subscribe(subscriptionOptions);
            newSub = true;
          }
          if (subscription?.endpoint) {
            endpoint = new URL(subscription.endpoint).host;
            subID = await getSubscriptionId(subscription.toJSON());
          }
          console.info(`[service-worker/setup-push-subscription] got ${newSub ? "new" : "existing"} subscription '${subID}':`, endpoint);
        }
        console.info("[service-worker/setup-push-subscription] calling push/reportExistingSubscription on:", subID);
        await esm_default("push/reportExistingSubscription", subscription?.toJSON(), subscription?.options.applicationServerKey);
      } catch (e) {
        console.error("[service-worker/setup-push-subscription] error getting a subscription:", e);
        if (attemptNumber >= 12) {
          console.error("[service-worker/setup-push-subscription] maxAttempts reached, giving up");
          esm_default("gi.ui/prompt", {
            heading: L("Error setting up push notifications"),
            question: L("Error setting up push notifications: {errMsg}{br_}{br_}Please make sure {a_}push services are enabled{_a} in your Browser settings, and then try reloading the app and toggling the push notifications toggle in the Notifications user settings.", {
              errMsg: e?.message,
              a_: '<a class="link" target="_blank" href="https://stackoverflow.com/a/69624651">',
              _a: "</a>",
              br_: "<br/>"
            }),
            primaryButton: L("Close")
          });
          throw e;
        }
        retryAttemptsPromise = new Promise((resolve, reject) => {
          setTimeout(() => {
            esm_default("service-worker/setup-push-subscription", attemptNumber + 1).then(resolve).catch(reject);
          }, 5e3);
        });
      }
    });
    if (retryAttemptsPromise) {
      await retryAttemptsPromise;
    }
  },
  "service-worker/update": async function() {
    if ("serviceWorker" in navigator) {
      try {
        const swRegistration = await navigator.serviceWorker.ready;
        if (swRegistration) {
          const newRegistration = await swRegistration.update();
          return newRegistration;
        } else {
          console.debug("[sw] No active service-worker was found while checking for the updates.");
          return;
        }
      } catch (err) {
        console.error(`[sw] Failed to update the service-worker! - ${err.message}`);
      }
    }
  },
  "service-worker/check-pwa-installability": function() {
    return Boolean(pwa.deferredInstallPrompt);
  },
  "service-worker/trigger-install-prompt": async function() {
    if (!pwa.deferredInstallPrompt) {
      return false;
    }
    const result = await pwa.deferredInstallPrompt.prompt();
    return result.outcome;
  }
});
[LOGIN_COMPLETE, SET_APP_LOGS_FILTER].forEach(
  (event) => esm_default("okTurtles.events/on", event, (...data) => {
    navigator.serviceWorker.controller?.postMessage({ type: "event", subtype: event, data });
  })
);
esm_default("okTurtles.events/on", NEW_CHATROOM_SCROLL_POSITION, (obj) => {
  if (obj.from === "sw") return;
  navigator.serviceWorker.controller?.postMessage({ type: "event", subtype: NEW_CHATROOM_SCROLL_POSITION, data: [obj] });
});
esm_default("okTurtles.events/on", NEW_CHATROOM_NOTIFICATION_SETTINGS, (obj) => {
  if (obj.from === "sw") return;
  navigator.serviceWorker.controller?.postMessage({ type: "event", subtype: NEW_CHATROOM_NOTIFICATION_SETTINGS, data: [obj] });
});
var swRpc = (() => {
  if (!navigator.serviceWorker) {
    throw new Error("Missing service worker object");
  }
  let controller = navigator.serviceWorker.controller;
  navigator.serviceWorker.addEventListener("controllerchange", (ev) => {
    controller = navigator.serviceWorker.controller;
  }, false);
  const fn = async (maxControllerChanges, ...args) => {
    if (!controller) {
      throw new Error("Service worker not ready");
    }
    await waitUntilSwReady();
    return new Promise((resolve, reject) => {
      const messageChannel = new MessageChannel();
      const onmessage = (event) => {
        if (event.data && Array.isArray(event.data)) {
          const r2 = deserializer(event.data[1]);
          if (event.data[0] === true) {
            resolve(r2);
          } else {
            reject(r2);
          }
          cleanup();
        }
      };
      const onmessageerror = (event) => {
        reject(event.data);
        cleanup();
      };
      const oncontrollerchange = (event) => {
        if (maxControllerChanges > 0) {
          resolve(fn(maxControllerChanges - 1, ...args));
        } else {
          reject(new Error("SW controller changed"));
        }
        cleanup();
      };
      const cleanup = () => {
        messageChannel.port1.removeEventListener("message", onmessage, false);
        messageChannel.port1.removeEventListener("messageerror", onmessageerror, false);
        navigator.serviceWorker.removeEventListener("controllerchange", oncontrollerchange, false);
        messageChannel.port1.close();
      };
      messageChannel.port1.addEventListener("message", onmessage, false);
      messageChannel.port1.addEventListener("messageerror", onmessageerror, false);
      navigator.serviceWorker.addEventListener("controllerchange", oncontrollerchange, false);
      messageChannel.port1.start();
      const { data, transferables } = serializer(args);
      controller.postMessage({
        type: "sbp",
        port: messageChannel.port2,
        data
      }, [messageChannel.port2, ...transferables]);
    });
  };
  return (...args) => fn(3, ...args);
})();
esm_default("sbp/selectors/register", {
  "gi.actions/*": swRpc,
  "chelonia/*": swRpc,
  "sw-namespace/*": (...args) => {
    return swRpc(args[0].slice(3), ...args.slice(1));
  },
  "gi.notifications/*": swRpc,
  "sw/*": swRpc,
  "swLogs/*": swRpc,
  "push/*": swRpc
});

// frontend/views/components/AppStyles.vue
var __vue_script__9 = {
  name: "AppStyles",
  mixins: [colorsManipulation_default],
  computed: {
    ...mapGetters([
      "fontSize",
      "colors"
    ]),
    softTone() {
      return this.colors.theme === "dark" ? 0.1 : -0.1;
    }
  }
};
var __vue_render__9 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c("v-style", [
    _vm._v(
      ":root {\n  --primary_0: " + _vm._s(_vm.colors.primary_0) + ";\n  --primary_0_1: " + _vm._s(_vm.lightenDarkenColor(_vm.colors.primary_0, _vm.softTone)) + ";\n  --primary_1: " + _vm._s(_vm.colors.primary_1) + ";\n  --primary_2: " + _vm._s(_vm.colors.primary_2) + ";\n  --success_0: " + _vm._s(_vm.colors.success_0) + ";\n  --success_0_1: " + _vm._s(_vm.lightenDarkenColor(_vm.colors.success_0, _vm.softTone)) + ";\n  --success_1: " + _vm._s(_vm.colors.success_1) + ";\n  --success_2: " + _vm._s(_vm.colors.success_2) + ";\n  --warning_0: " + _vm._s(_vm.colors.warning_0) + ";\n  --warning_0_1: " + _vm._s(_vm.lightenDarkenColor(_vm.colors.warning_0, _vm.softTone)) + ";\n  --warning_0_text: " + _vm._s(_vm.colors.warning_0_text) + ";\n  --warning_0_2: " + _vm._s(_vm.colors.warning_0_2) + ";\n  --warning_1: " + _vm._s(_vm.colors.warning_1) + ";\n  --warning_2: " + _vm._s(_vm.colors.warning_2) + ";\n  --danger_0: " + _vm._s(_vm.colors.danger_0) + ";\n  --danger_0_1: " + _vm._s(_vm.lightenDarkenColor(_vm.colors.danger_0, _vm.softTone)) + ";\n  --danger_1: " + _vm._s(_vm.colors.danger_1) + ";\n  --danger_2: " + _vm._s(_vm.colors.danger_2) + ";\n  --danger_3: " + _vm._s(_vm.colors.danger_3) + ";\n  --text_0: " + _vm._s(_vm.colors.text_0) + ";\n  --text_1: " + _vm._s(_vm.colors.text_1) + ";\n  --general_0: " + _vm._s(_vm.colors.general_0) + ";\n  --general_1: " + _vm._s(_vm.colors.general_1) + ";\n  --general_1_opacity_6: " + _vm._s(_vm.hexToRgbA(_vm.colors.general_1, 0.6)) + ";\n  --general_2: " + _vm._s(_vm.colors.general_2) + ";\n  --background_0: " + _vm._s(_vm.colors.background_0) + ";\n  --background_0_opacity_0: " + _vm._s(_vm.hexToRgbA(_vm.colors.background_0, 0)) + ";\n  --text-size: " + _vm._s(_vm.fontSize) + "px;\n}"
    )
  ]);
};
var __vue_staticRenderFns__9 = [];
__vue_render__9._withStripped = true;
var __vue_inject_styles__9 = void 0;
var __vue_scope_id__9 = void 0;
var __vue_module_identifier__9 = void 0;
var __vue_is_functional_template__9 = false;
function __vue_normalize__9(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = "<template lang='pug'>\nv-style.\n  :root {\n    --primary_0: {{colors.primary_0}};\n    --primary_0_1: {{lightenDarkenColor(colors.primary_0, softTone)}};\n    --primary_1: {{colors.primary_1}};\n    --primary_2: {{colors.primary_2}};\n    --success_0: {{colors.success_0}};\n    --success_0_1: {{lightenDarkenColor(colors.success_0, softTone)}};\n    --success_1: {{colors.success_1}};\n    --success_2: {{colors.success_2}};\n    --warning_0: {{colors.warning_0}};\n    --warning_0_1: {{lightenDarkenColor(colors.warning_0, softTone)}};\n    --warning_0_text: {{colors.warning_0_text}};\n    --warning_0_2: {{colors.warning_0_2}};\n    --warning_1: {{colors.warning_1}};\n    --warning_2: {{colors.warning_2}};\n    --danger_0: {{colors.danger_0}};\n    --danger_0_1: {{lightenDarkenColor(colors.danger_0, softTone)}};\n    --danger_1: {{colors.danger_1}};\n    --danger_2: {{colors.danger_2}};\n    --danger_3: {{colors.danger_3}};\n    --text_0: {{colors.text_0}};\n    --text_1: {{colors.text_1}};\n    --general_0: {{colors.general_0}};\n    --general_1: {{colors.general_1}};\n    --general_1_opacity_6: {{hexToRgbA(colors.general_1, 0.6)}};\n    --general_2: {{colors.general_2}};\n    --background_0: {{colors.background_0}};\n    --background_0_opacity_0: {{hexToRgbA(colors.background_0, 0)}};\n    --text-size: {{fontSize}}px;\n  }\n</template>\n\n<script>\nimport colorsMixins from '../../../frontend/views/utils/colorsManipulation.js'\n\n// importing this here (outside of the style tag) is important,\n// because this is what results in the global main.css file\n// being placed in dist/assets/css/main.css, which is included\n// by index.html\nimport '../../../frontend/assets/style/main.scss'\nimport { mapGetters } from 'vuex'\n\nexport default ({\n  name: 'AppStyles',\n\n  mixins: [colorsMixins],\n\n  computed: {\n    ...mapGetters([\n      'fontSize',\n      'colors'\n    ]),\n    softTone () {\n      return this.colors.theme === 'dark' ? 0.1 : -0.1\n    }\n  }\n}: Object)\n<\/script>\n";
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (false) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
var __vue_component__9 = /* @__PURE__ */ __vue_normalize__9(
  { render: __vue_render__9, staticRenderFns: __vue_staticRenderFns__9 },
  __vue_inject_styles__9,
  __vue_script__9,
  __vue_scope_id__9,
  __vue_is_functional_template__9,
  __vue_module_identifier__9,
  false,
  void 0,
  void 0,
  void 0
);
var AppStyles_default = __vue_component__9;

// frontend/views/components/banners/BannerGeneral.vue
var __vue_script__10 = {
  name: "BannerGeneral",
  components: {
    TransitionExpand: TransitionExpand_default
  },
  data: () => ({
    ephemeral: {
      message: null,
      icon: null,
      severity: null
    }
  }),
  methods: {
    // To be used by parent. Example:
    // this.$refs.bannerGeneral.show(L('Trying to reconnect...'), 'wifi')
    clean() {
      this.ephemeral.message = "";
      this.ephemeral.icon = "";
      this.ephemeral.severity = "";
    },
    show(message, icon) {
      this.ephemeral.message = message;
      this.ephemeral.icon = icon;
      this.ephemeral.severity = "neutral";
    },
    danger(message, icon) {
      this.ephemeral.message = message;
      this.ephemeral.icon = icon;
      this.ephemeral.severity = "danger";
    },
    severity() {
      return this.ephemeral.severity;
    },
    debouncedShow({ message, icon, seconds, clearWhen }) {
      let clearBannerTimer;
      return debounce((...args) => {
        const clearBanner = () => {
          if (clearWhen()) {
            if (this.severity() !== "danger") {
              this.clean();
            }
            clearBannerTimer = void 0;
          } else {
            setTimeout(clearBanner, 500);
          }
        };
        if (!clearBannerTimer && !clearWhen()) {
          this.show(message(...args), icon);
          clearBannerTimer = setTimeout(clearBanner, 1e3 * seconds);
        }
      }, 1e3 * seconds);
    }
  }
};
var __vue_render__10 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c("transition-expand", [
    _vm.ephemeral.message ? _c(
      "div",
      {
        staticClass: "l-banner",
        class: "is-" + _vm.ephemeral.severity,
        attrs: { "data-test": "bannerGeneral", "aria-live": "polite" }
      },
      [
        _c("i", { class: "icon-" + _vm.ephemeral.icon + " is-prefix" }),
        _c("div", {
          directives: [
            {
              name: "safe-html",
              rawName: "v-safe-html:a",
              value: _vm.ephemeral.message,
              expression: "ephemeral.message",
              arg: "a"
            }
          ],
          staticClass: "c-banner-content"
        })
      ]
    ) : _vm._e()
  ]);
};
var __vue_staticRenderFns__10 = [];
__vue_render__10._withStripped = true;
var __vue_inject_styles__10 = function(inject) {
  if (!inject) return;
  inject("data-v-7d84fbb2_0", { source: ".l-banner[data-v-7d84fbb2] {\n  display: flex;\n  justify-content: center;\n  align-items: flex-start;\n}\n.l-banner .c-banner-content[data-v-7d84fbb2] {\n  text-align: left;\n  word-break: break-word;\n}\n.l-banner[data-v-7d84fbb2]  .link {\n  color: inherit;\n  border-bottom-color: inherit;\n  font-weight: inherit;\n}\n.is-neutral[data-v-7d84fbb2] {\n  background-color: var(--text_0);\n  color: var(--background_0);\n}\n.is-danger[data-v-7d84fbb2] {\n  background-color: var(--danger_0);\n  color: var(--background_0);\n}\n\n/*# sourceMappingURL=BannerGeneral.vue.map */", map: { "version": 3, "sources": ["frontend/views/components/banners/BannerGeneral.vue", "BannerGeneral.vue"], "names": [], "mappings": "AA6EA;EACA,aAAA;EACA,uBAAA;EACA,uBAAA;AC5EA;AD8EA;EACA,gBAAA;EACA,sBAAA;AC5EA;ADgFA;EACA,cAAA;EACA,4BAAA;EACA,oBAAA;AC7EA;ADgFA;EACA,+BAAA;EACA,0BAAA;AC7EA;ADgFA;EACA,iCAAA;EACA,0BAAA;AC7EA;;AAEA,4CAA4C", "file": "BannerGeneral.vue", "sourcesContent": ["<template lang='pug'>\n  transition-expand\n    .l-banner(\n      v-if='ephemeral.message'\n      data-test='bannerGeneral'\n      :class='`is-${ephemeral.severity}`'\n      aria-live='polite'\n    )\n      i(:class='`icon-${ephemeral.icon} is-prefix`')\n      .c-banner-content(v-safe-html:a='ephemeral.message')\n</template>\n\n<script>\nimport TransitionExpand from '../../../../frontend/views/components/TransitionExpand.vue'\nimport { debounce } from 'turtledash'\n\nexport default ({\n  name: 'BannerGeneral',\n  components: {\n    TransitionExpand\n  },\n  data: () => ({\n    ephemeral: {\n      message: null,\n      icon: null,\n      severity: null\n    }\n  }),\n  methods: {\n    // To be used by parent. Example:\n    // this.$refs.bannerGeneral.show(L('Trying to reconnect...'), 'wifi')\n    clean () {\n      this.ephemeral.message = ''\n      this.ephemeral.icon = ''\n      this.ephemeral.severity = ''\n    },\n    show (message, icon) {\n      this.ephemeral.message = message\n      this.ephemeral.icon = icon\n      this.ephemeral.severity = 'neutral'\n    },\n    danger (message, icon) {\n      this.ephemeral.message = message\n      this.ephemeral.icon = icon\n      this.ephemeral.severity = 'danger'\n    },\n    severity () {\n      return this.ephemeral.severity\n    },\n    debouncedShow ({ message, icon, seconds, clearWhen }: {\n      message: Function, icon: string, seconds: number, clearWhen: Function\n    }) {\n      let clearBannerTimer\n      return debounce((...args) => {\n        const clearBanner = () => {\n          if (clearWhen()) {\n            if (this.severity() !== 'danger') {\n              this.clean()\n            }\n            clearBannerTimer = undefined\n          } else {\n            // after first n seconds, check every half a second for clearWhen to change\n            setTimeout(clearBanner, 500)\n          }\n        }\n        if (!clearBannerTimer && !clearWhen()) {\n          this.show(message(...args), icon)\n          clearBannerTimer = setTimeout(clearBanner, 1000 * seconds)\n        }\n      }, 1000 * seconds)\n    }\n  }\n}: Object)\n<\/script>\n<style lang=\"scss\" scoped>\n@import \"../../../../frontend/assets/style/_variables.scss\";\n\n.l-banner {\n  display: flex;\n  justify-content: center;\n  align-items: flex-start;\n\n  .c-banner-content {\n    text-align: left;\n    word-break: break-word;\n  }\n}\n\n.l-banner ::v-deep .link {\n  color: inherit;\n  border-bottom-color: inherit;\n  font-weight: inherit;\n}\n\n.is-neutral {\n  background-color: $text_0;\n  color: $background;\n}\n\n.is-danger {\n  background-color: $danger_0;\n  color: $background;\n}\n</style>\n", ".l-banner {\n  display: flex;\n  justify-content: center;\n  align-items: flex-start;\n}\n.l-banner .c-banner-content {\n  text-align: left;\n  word-break: break-word;\n}\n\n.l-banner ::v-deep .link {\n  color: inherit;\n  border-bottom-color: inherit;\n  font-weight: inherit;\n}\n\n.is-neutral {\n  background-color: var(--text_0);\n  color: var(--background_0);\n}\n\n.is-danger {\n  background-color: var(--danger_0);\n  color: var(--background_0);\n}\n\n/*# sourceMappingURL=BannerGeneral.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__10 = "data-v-7d84fbb2";
var __vue_module_identifier__10 = void 0;
var __vue_is_functional_template__10 = false;
function __vue_normalize__10(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = "<template lang='pug'>\n  transition-expand\n    .l-banner(\n      v-if='ephemeral.message'\n      data-test='bannerGeneral'\n      :class='`is-${ephemeral.severity}`'\n      aria-live='polite'\n    )\n      i(:class='`icon-${ephemeral.icon} is-prefix`')\n      .c-banner-content(v-safe-html:a='ephemeral.message')\n</template>\n\n<script>\nimport TransitionExpand from '../../../../frontend/views/components/TransitionExpand.vue'\nimport { debounce } from 'turtledash'\n\nexport default ({\n  name: 'BannerGeneral',\n  components: {\n    TransitionExpand\n  },\n  data: () => ({\n    ephemeral: {\n      message: null,\n      icon: null,\n      severity: null\n    }\n  }),\n  methods: {\n    // To be used by parent. Example:\n    // this.$refs.bannerGeneral.show(L('Trying to reconnect...'), 'wifi')\n    clean () {\n      this.ephemeral.message = ''\n      this.ephemeral.icon = ''\n      this.ephemeral.severity = ''\n    },\n    show (message, icon) {\n      this.ephemeral.message = message\n      this.ephemeral.icon = icon\n      this.ephemeral.severity = 'neutral'\n    },\n    danger (message, icon) {\n      this.ephemeral.message = message\n      this.ephemeral.icon = icon\n      this.ephemeral.severity = 'danger'\n    },\n    severity () {\n      return this.ephemeral.severity\n    },\n    debouncedShow ({ message, icon, seconds, clearWhen }: {\n      message: Function, icon: string, seconds: number, clearWhen: Function\n    }) {\n      let clearBannerTimer\n      return debounce((...args) => {\n        const clearBanner = () => {\n          if (clearWhen()) {\n            if (this.severity() !== 'danger') {\n              this.clean()\n            }\n            clearBannerTimer = undefined\n          } else {\n            // after first n seconds, check every half a second for clearWhen to change\n            setTimeout(clearBanner, 500)\n          }\n        }\n        if (!clearBannerTimer && !clearWhen()) {\n          this.show(message(...args), icon)\n          clearBannerTimer = setTimeout(clearBanner, 1000 * seconds)\n        }\n      }, 1000 * seconds)\n    }\n  }\n}: Object)\n<\/script>\n<style lang=\"scss\" scoped>\n@import \"../../../../frontend/assets/style/_variables.scss\";\n\n.l-banner {\n  display: flex;\n  justify-content: center;\n  align-items: flex-start;\n\n  .c-banner-content {\n    text-align: left;\n    word-break: break-word;\n  }\n}\n\n.l-banner ::v-deep .link {\n  color: inherit;\n  border-bottom-color: inherit;\n  font-weight: inherit;\n}\n\n.is-neutral {\n  background-color: $text_0;\n  color: $background;\n}\n\n.is-danger {\n  background-color: $danger_0;\n  color: $background;\n}\n</style>\n";
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__9() {
  const styles = __vue_create_injector__9.styles || (__vue_create_injector__9.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__10 = /* @__PURE__ */ __vue_normalize__10(
  { render: __vue_render__10, staticRenderFns: __vue_staticRenderFns__10 },
  __vue_inject_styles__10,
  __vue_script__10,
  __vue_scope_id__10,
  __vue_is_functional_template__10,
  __vue_module_identifier__10,
  false,
  __vue_create_injector__9,
  void 0,
  void 0
);
var BannerGeneral_default = __vue_component__10;

// frontend/views/components/modal/Modal.vue
var __vue_script__11 = {
  name: "Modal",
  data() {
    return {
      content: null,
      // Main modal
      subcontent: [],
      // Collection of modal on top of modals
      queries: {
        // Queries to be used by modals
        // [modalName]: { queryKey: queryValue }
      },
      replacementQueries: {},
      // queries to be used for REPLACE_MODAL
      childData: {},
      replacement: null,
      // Replace the modal once the first one is close without updating the url
      lastFocus: null
      // Record element that open the modal
    };
  },
  computed: {
    activeSubContent() {
      return this.subcontent.length ? this.subcontent[this.subcontent.length - 1] : null;
    }
  },
  created() {
    esm_default("okTurtles.events/on", OPEN_MODAL, this.openModal);
    esm_default("okTurtles.events/on", CLOSE_MODAL, this.unloadModal);
    esm_default("okTurtles.events/on", REPLACE_MODAL, this.replaceModal);
    esm_default("okTurtles.events/on", SET_MODAL_QUERIES, this.setModalQueries);
    window.addEventListener("keyup", this.handleKeyUp);
  },
  mounted() {
    const { modal, subcontent } = this.$route.query;
    if (modal) this.openModal(modal);
    if (subcontent) this.openModal(subcontent);
  },
  beforeDestroy() {
    esm_default("okTurtles.events/off", OPEN_MODAL);
    esm_default("okTurtles.events/off", CLOSE_MODAL);
    esm_default("okTurtles.events/off", REPLACE_MODAL);
    esm_default("okTurtles.events/off", SET_MODAL_QUERIES);
    window.removeEventListener("keyup", this.handleKeyUp);
  },
  watch: {
    "$route"(to, from) {
      const toModal = to.query.modal;
      if (toModal) {
        if (toModal !== this.content) {
          if (this.content) this.replaceModal(toModal, omit(to.query, "modal"));
          else this.content = toModal;
        }
        if (to.query.subcontent) {
          const subContentTo = to.query.subcontent.split("+").pop();
          if (subContentTo !== this.activeSubContent) {
            const i = this.subcontent.indexOf(subContentTo);
            if (i !== -1) {
              this.subcontent = this.subcontent.slice(0, i);
            } else {
              this.subcontent = [subContentTo];
            }
          }
        }
      } else {
        if (this.content) {
          this.unloadModal();
        }
      }
    }
  },
  methods: {
    handleKeyUp(e) {
      if (this.content && e.key === "Escape") {
        e.preventDefault();
        this.unloadModal();
      }
    },
    updateUrl() {
      const subContentLen = this.subcontent.length;
      if (this.content || subContentLen > 0) {
        const contentQueries = this.queries[this.content] || {};
        const subContentQueries = this.queries[this.subcontent[subContentLen - 1]] || {};
        this.$router.push({
          query: {
            ...this.$route.query,
            ...contentQueries,
            ...subContentQueries,
            modal: this.content,
            subcontent: subContentLen ? this.subcontent.join("+") : void 0
          }
        }).catch(console.error);
      } else if (this.$route.query.modal || this.$route.query.subcontent) {
        const rQueries = { ...this.$route.query };
        const queriesToDelete = {
          modal: true,
          subcontent: true,
          ...this.queries[rQueries.modal]
        };
        for (const mQuery in queriesToDelete) {
          delete rQueries[mQuery];
        }
        this.$router.push({ rQueries }).catch(console.error);
      }
    },
    openModal(componentName, queries = {}, childData = {}) {
      if (this.content === componentName) return;
      this.lastFocus = document.activeElement;
      if (this.content) {
        this.subcontent.push(componentName);
      } else {
        this.content = componentName;
      }
      this.queries[componentName] = queries;
      this.updateUrl();
      this.childData[componentName] = childData;
    },
    unloadModal(targetModal = "") {
      const clearChildData = (key) => {
        delete this.childData[key];
      };
      const hasSubContent = this.subcontent.length > 0;
      let unloadDone = false;
      const unloadFromContentAndReFocus = (targetModal2) => {
        clearChildData(targetModal2);
        this.content = null;
        unloadDone = true;
        if (this.lastFocus) this.lastFocus.focus();
      };
      if (targetModal) {
        if (hasSubContent && this.subcontent.indexOf(targetModal) >= 0) {
          clearChildData(targetModal);
          this.subcontent = this.subcontent.filter((modalName) => modalName !== targetModal);
          unloadDone = true;
        } else if (this.content === targetModal) {
          unloadFromContentAndReFocus(targetModal);
        }
      } else {
        if (hasSubContent) {
          clearChildData(this.activeSubContent);
          this.subcontent.pop();
          unloadDone = true;
        } else {
          unloadFromContentAndReFocus(this.content);
        }
      }
      if (!unloadDone) {
        return;
      }
      if (this.replacement) {
        this.openModal(this.replacement, this.replacementQueries[this.replacement]);
        delete this.replacementQueries[this.replacement];
        this.replacement = null;
      } else {
        this.updateUrl();
      }
    },
    replaceModal(componentName, queries = null) {
      this.replacement = componentName;
      if (queries) {
        this.replacementQueries[componentName] = queries;
      }
      this.$refs[this.activeSubContent ? "subcontent" : "content"].$children[0].close();
    },
    setModalQueries(componentName, queries) {
      this.queries[componentName] = queries;
    }
  }
};
var __vue_render__11 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "div",
    [
      _c(
        _vm.content,
        _vm._b(
          { ref: "content", tag: "component" },
          "component",
          _vm.childData[_vm.content],
          false
        )
      ),
      _vm.activeSubContent ? _c(
        _vm.activeSubContent,
        _vm._b(
          { ref: "subcontent", tag: "component" },
          "component",
          _vm.childData[_vm.activeSubContent],
          false
        )
      ) : _vm._e()
    ],
    1
  );
};
var __vue_staticRenderFns__11 = [];
__vue_render__11._withStripped = true;
var __vue_inject_styles__11 = void 0;
var __vue_scope_id__11 = void 0;
var __vue_module_identifier__11 = void 0;
var __vue_is_functional_template__11 = false;
function __vue_normalize__11(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = "<template lang='pug'>\n  div\n    component(:is='content' ref='content' v-bind='childData[content]')\n    component(v-if='activeSubContent' :is='activeSubContent' ref='subcontent' v-bind='childData[activeSubContent]')\n</template>\n<script>\nimport sbp from '@sbp/sbp'\nimport { OPEN_MODAL, REPLACE_MODAL, CLOSE_MODAL, SET_MODAL_QUERIES } from '../../../../frontend/utils/events.js'\nimport { omit } from 'turtledash'\n\nexport default ({\n  name: 'Modal',\n  data () {\n    return {\n      content: null, // Main modal\n      subcontent: [], // Collection of modal on top of modals\n      queries: { // Queries to be used by modals\n        // [modalName]: { queryKey: queryValue }\n      },\n      replacementQueries: {}, // queries to be used for REPLACE_MODAL\n      childData: {},\n      replacement: null, // Replace the modal once the first one is close without updating the url\n      lastFocus: null // Record element that open the modal\n    }\n  },\n  computed: {\n    activeSubContent () {\n      return this.subcontent.length ? this.subcontent[this.subcontent.length - 1] : null\n    }\n  },\n  created () {\n    sbp('okTurtles.events/on', OPEN_MODAL, this.openModal)\n    sbp('okTurtles.events/on', CLOSE_MODAL, this.unloadModal)\n    sbp('okTurtles.events/on', REPLACE_MODAL, this.replaceModal)\n    sbp('okTurtles.events/on', SET_MODAL_QUERIES, this.setModalQueries)\n    // When press escape it should close the modal\n    window.addEventListener('keyup', this.handleKeyUp)\n  },\n  mounted () {\n    const { modal, subcontent } = this.$route.query\n    if (modal) this.openModal(modal)\n    if (subcontent) this.openModal(subcontent)\n  },\n  beforeDestroy () {\n    sbp('okTurtles.events/off', OPEN_MODAL)\n    sbp('okTurtles.events/off', CLOSE_MODAL)\n    sbp('okTurtles.events/off', REPLACE_MODAL)\n    sbp('okTurtles.events/off', SET_MODAL_QUERIES)\n    window.removeEventListener('keyup', this.handleKeyUp)\n  },\n  watch: {\n    '$route' (to, from) {\n      const toModal = to.query.modal\n\n      if (toModal) {\n        // We reset the modals with no animation for simplicity\n        if (toModal !== this.content) {\n          if (this.content) this.replaceModal(toModal, omit(to.query, 'modal')) // if another modal is already open, replace it.\n          else this.content = toModal\n        }\n\n        // Sometimes, the query string for modal has both 'modal' and 'subcontent' eg.) '/app/?modal=SignupModal&subcontent=Prompt'\n        if (to.query.subcontent) {\n          const subContentTo = to.query.subcontent.split('+').pop()\n          if (subContentTo !== this.activeSubContent) {\n            // Try to find the target subcontent in the list of subcontent\n            const i = this.subcontent.indexOf(subContentTo)\n            if (i !== -1) {\n              this.subcontent = this.subcontent.slice(0, i)\n            } else {\n              this.subcontent = [subContentTo]\n            }\n          }\n        }\n      } else {\n        // Prevent a bug where we click to close a modal at the same time we\n        // redirect a page (ex: setting modal -> logout). Sometimes the logout\n        // and redirect would happen before the modal closes. At this moment\n        // from.query.modal doesn't exist anymore. But the modal should be closed,\n        // so we force the unloadModal.\n        if (this.content) {\n          this.unloadModal()\n        }\n      }\n    }\n  },\n  methods: {\n    handleKeyUp (e) {\n      if (this.content && e.key === 'Escape') {\n        e.preventDefault()\n        this.unloadModal()\n      }\n    },\n    updateUrl () {\n      const subContentLen = this.subcontent.length\n      if (this.content || subContentLen > 0) {\n        const contentQueries = this.queries[this.content] || {}\n        const subContentQueries = this.queries[this.subcontent[subContentLen - 1]] || {}\n        this.$router.push({\n          query: {\n            ...this.$route.query,\n            ...contentQueries,\n            ...subContentQueries,\n            modal: this.content,\n            subcontent: subContentLen ? this.subcontent.join('+') : undefined\n          }\n        }).catch(console.error)\n      } else if (this.$route.query.modal || this.$route.query.subcontent) {\n        const rQueries = { ...this.$route.query }\n        const queriesToDelete = {\n          modal: true,\n          subcontent: true,\n          ...this.queries[rQueries.modal]\n        }\n\n        for (const mQuery in queriesToDelete) {\n          delete rQueries[mQuery]\n        }\n\n        this.$router.push({ rQueries }).catch(console.error)\n      }\n    },\n    openModal (componentName, queries = {}, childData = {}) {\n      // Don't open the same kind of modal twice.\n      if (this.content === componentName) return\n\n      this.lastFocus = document.activeElement\n      if (this.content) {\n        this.subcontent.push(componentName)\n      } else {\n        this.content = componentName\n      }\n      this.queries[componentName] = queries\n      this.updateUrl()\n      this.childData[componentName] = childData\n    },\n    unloadModal (targetModal = '') {\n      const clearChildData = key => { delete this.childData[key] }\n      const hasSubContent = this.subcontent.length > 0\n      let unloadDone = false\n      const unloadFromContentAndReFocus = (targetModal) => {\n        clearChildData(targetModal)\n        this.content = null\n        unloadDone = true\n        // Refocus on the button that opened this modal, if any.\n        // TODO - find a way to support lastFocus when opened through profile|notifications card.\n        if (this.lastFocus) this.lastFocus.focus()\n      }\n\n      if (targetModal) {\n        // when it's queried to close a particular modal, check if it's being presented now and unload it if so.\n        if (hasSubContent && this.subcontent.indexOf(targetModal) >= 0) {\n          clearChildData(targetModal)\n          this.subcontent = this.subcontent.filter(modalName => modalName !== targetModal)\n          unloadDone = true\n        } else if (this.content === targetModal) {\n          unloadFromContentAndReFocus(targetModal)\n        }\n      } else {\n        // when there is no particular modal queried, unload the latest one first.\n        if (hasSubContent) {\n          clearChildData(this.activeSubContent)\n          this.subcontent.pop()\n          unloadDone = true\n        } else {\n          unloadFromContentAndReFocus(this.content)\n        }\n      }\n\n      if (!unloadDone) { return } // If nothing has been unloaded, no need to perform below actions.\n\n      if (this.replacement) {\n        this.openModal(this.replacement, this.replacementQueries[this.replacement])\n\n        delete this.replacementQueries[this.replacement]\n        this.replacement = null\n      } else {\n        this.updateUrl()\n      }\n    },\n    replaceModal (componentName, queries = null) {\n      this.replacement = componentName\n      // At the moment you can only replace a modal if it's the main one by design\n      // Use direct children instead of sbp to wait for animation out\n      if (queries) {\n        this.replacementQueries[componentName] = queries\n      }\n\n      this.$refs[this.activeSubContent ? 'subcontent' : 'content'].$children[0].close()\n    },\n    setModalQueries (componentName, queries) {\n      this.queries[componentName] = queries\n    }\n  }\n}: Object)\n<\/script>\n";
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (false) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
var __vue_component__11 = /* @__PURE__ */ __vue_normalize__11(
  { render: __vue_render__11, staticRenderFns: __vue_staticRenderFns__11 },
  __vue_inject_styles__11,
  __vue_script__11,
  __vue_scope_id__11,
  __vue_is_functional_template__11,
  __vue_module_identifier__11,
  false,
  void 0,
  void 0,
  void 0
);
var Modal_default = __vue_component__11;

// frontend/views/components/sounds/Background.vue
var __vue_script__12 = {
  name: "BackgroundSounds",
  created() {
    esm_default("okTurtles.events/on", MESSAGE_RECEIVE, this.playMessageReceive);
    esm_default("okTurtles.events/on", MESSAGE_SEND, this.playMessageSend);
  },
  computed: {
    volumeFromStore() {
      return this.$store.getters.notificationVolume ?? 1;
    },
    isAppIdle() {
      return this.$store.state.idleVue?.isIdle;
    }
  },
  methods: {
    shouldPlay(contractID) {
      return !isPwa_default() && !(this.$route.name === "GroupChatConversation" && this.$route.params.chatRoomID === contractID && !this.isAppIdle);
    },
    playMessageReceive({ contractID }) {
      if (this.shouldPlay(contractID)) {
        this.$refs.msgReceive.play();
      }
    },
    playMessageSend() {
      if (this.shouldPlay()) {
        this.$refs.msgSend.play();
      }
    },
    updateAudioVolumes() {
      this.$refs.msgReceive.volume = this.volumeFromStore;
      this.$refs.msgSend.volume = this.volumeFromStore;
    }
  },
  mounted() {
    this.updateAudioVolumes();
  },
  watch: {
    volumeFromStore() {
      this.updateAudioVolumes();
    }
  }
};
var __vue_render__12 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c("div", { staticClass: "c-bgsound-wrapper" }, [
    _c("audio", {
      ref: "msgReceive",
      attrs: { src: "/assets/audio/msg-received.mp3", type: "audio/mpeg" }
    }),
    _c("audio", {
      ref: "msgSend",
      attrs: { src: "/assets/audio/msg-sent.mp3", type: "audio/mpeg" }
    })
  ]);
};
var __vue_staticRenderFns__12 = [];
__vue_render__12._withStripped = true;
var __vue_inject_styles__12 = function(inject) {
  if (!inject) return;
  inject("data-v-4aca9cd6_0", { source: "\n\n/*# sourceMappingURL=Background.vue.map */", map: { "version": 3, "sources": ["Background.vue"], "names": [], "mappings": ";;AAEA,yCAAyC", "file": "Background.vue", "sourcesContent": ["\n\n/*# sourceMappingURL=Background.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__12 = "data-v-4aca9cd6";
var __vue_module_identifier__12 = void 0;
var __vue_is_functional_template__12 = false;
function __vue_normalize__12(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
.c-bgsound-wrapper
  audio(ref='msgReceive' src='/assets/audio/msg-received.mp3' type='audio/mpeg')
  audio(ref='msgSend' src='/assets/audio/msg-sent.mp3' type='audio/mpeg')
</template>

<script>
import sbp from '@sbp/sbp'
import { MESSAGE_RECEIVE, MESSAGE_SEND } from '../../../../frontend/model/contracts/shared/constants.js'
import isPwa from '../../../../frontend/utils/isPwa.js'

export default ({
  name: 'BackgroundSounds',
  created () {
    sbp('okTurtles.events/on', MESSAGE_RECEIVE, this.playMessageReceive)
    sbp('okTurtles.events/on', MESSAGE_SEND, this.playMessageSend)
  },
  computed: {
    volumeFromStore () {
      return this.$store.getters.notificationVolume ?? 1 // The volume value in the store can be 0 too and we use it if that's the case.
    },
    isAppIdle () {
      // NOTE: idle-vue plugin will provide this.isAppIdle
      //       but sometimes it returns undefined, so redefine here
      return this.$store.state.idleVue?.isIdle
    }
  },
  methods: {
    shouldPlay (contractID: string) {
      return !isPwa() && !((this.$route.name === 'GroupChatConversation' && this.$route.params.chatRoomID === contractID) && !this.isAppIdle)
    },
    playMessageReceive ({ contractID }: {
      contractID: string,
      messageHash: string,
      messageType: string
    }) {
      if (this.shouldPlay(contractID)) {
        this.$refs.msgReceive.play()
      }
    },
    playMessageSend () {
      if (this.shouldPlay()) {
        this.$refs.msgSend.play()
      }
    },
    updateAudioVolumes () {
      this.$refs.msgReceive.volume = this.volumeFromStore
      this.$refs.msgSend.volume = this.volumeFromStore
    }
  },
  mounted () {
    this.updateAudioVolumes()
  },
  watch: {
    volumeFromStore () {
      // Update the audio elements accordingly when the volume change in the store is detected.
      this.updateAudioVolumes()
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__10() {
  const styles = __vue_create_injector__10.styles || (__vue_create_injector__10.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__12 = /* @__PURE__ */ __vue_normalize__12(
  { render: __vue_render__12, staticRenderFns: __vue_staticRenderFns__12 },
  __vue_inject_styles__12,
  __vue_script__12,
  __vue_scope_id__12,
  __vue_is_functional_template__12,
  __vue_module_identifier__12,
  false,
  __vue_create_injector__10,
  void 0,
  void 0
);
var Background_default = __vue_component__12;

// frontend/views/containers/notifications/NotificationCard.vue
var __vue_script__13 = {
  name: "NotificationCard",
  components: {
    ModalClose: ModalClose_default,
    NotificationList: NotificationList_default,
    Tooltip: Tooltip_default
  },
  methods: {
    toggleTooltip() {
      this.$refs.tooltip.toggle();
    },
    handleSettingsClick() {
      esm_default("okTurtles.events/emit", OPEN_MODAL, "UserSettingsModal", {
        tab: "notifications"
      });
      this.toggleTooltip();
    },
    markAllNotificationsAsRead() {
      esm_default("gi.notifications/markAllAsRead", this.currentGroupId);
    }
  },
  computed: {
    ...mapState([
      "currentGroupId"
    ]),
    ...mapGetters([
      "currentNotificationCount"
    ])
  }
};
var __vue_render__13 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "tooltip",
    {
      ref: "tooltip",
      attrs: {
        direction: "right",
        manual: true,
        opacity: 1,
        "aria-label": _vm.L("Show notifications")
      }
    },
    [
      _vm._t("default"),
      _c("template", { slot: "tooltip" }, [
        _c(
          "div",
          {
            staticClass: "card c-card",
            attrs: {
              role: "dialog",
              "aria-label": _vm.L("Notifications"),
              "data-test": "notificationCard"
            }
          },
          [
            _c(
              "div",
              { staticClass: "c-header" },
              [
                _c(
                  "i18n",
                  { staticClass: "is-title-4", attrs: { tag: "h2" } },
                  [_vm._v("Notifications")]
                ),
                _c(
                  "button",
                  {
                    staticClass: "is-icon-small c-settings-btn",
                    on: { click: _vm.handleSettingsClick }
                  },
                  [_c("i", { staticClass: "icon-cog" })]
                )
              ],
              1
            ),
            _c(
              "div",
              { staticClass: "c-body" },
              [
                _c("notification-list", {
                  attrs: { variant: "compact" },
                  on: { select: _vm.toggleTooltip }
                })
              ],
              1
            ),
            _vm.currentNotificationCount ? _c(
              "div",
              { staticClass: "c-footer" },
              [
                _c(
                  "i18n",
                  {
                    staticClass: "link",
                    attrs: {
                      tag: "button",
                      "data-test": "markAllAsRead_In_Tooltip"
                    },
                    on: { click: _vm.markAllNotificationsAsRead }
                  },
                  [_vm._v("Mark all as read")]
                ),
                _c(
                  "router-link",
                  {
                    staticClass: "link",
                    attrs: {
                      to: { query: { modal: "NotificationModal" } }
                    },
                    nativeOn: {
                      click: function($event) {
                        return _vm.toggleTooltip($event);
                      }
                    }
                  },
                  [_c("i18n", [_vm._v("See all")])],
                  1
                )
              ],
              1
            ) : _vm._e(),
            _c("modal-close", {
              staticClass: "c-close",
              attrs: { "aria-label": _vm.L("Close profile") },
              on: { close: _vm.toggleTooltip }
            })
          ],
          1
        )
      ])
    ],
    2
  );
};
var __vue_staticRenderFns__13 = [];
__vue_render__13._withStripped = true;
var __vue_inject_styles__13 = function(inject) {
  if (!inject) return;
  inject("data-v-81f6011e_0", { source: '.c-card[data-v-81f6011e] {\n  position: relative;\n  padding: 0 0 4rem;\n  color: var(--text_0);\n  max-width: 100vw;\n  border-radius: 3px;\n  width: 100vw;\n  box-shadow: none;\n}\n.c-card[data-v-81f6011e]:last-child {\n  margin-bottom: 0;\n}\n.is-active .c-card[data-v-81f6011e] {\n  animation: zoom 100ms both cubic-bezier(0.165, 0.84, 0.44, 1);\n}\n@media screen and (max-width: 768px) {\n.is-active .c-card[data-v-81f6011e] {\n    animation-name: enterFromBottom;\n}\n}\n@media screen and (min-width: 769px), print {\n.c-card[data-v-81f6011e] {\n    margin-top: -0.5rem;\n    box-shadow: 0 0.5rem 1.25rem rgba(54, 54, 54, 0.3);\n    width: 25rem;\n    padding: 0;\n}\n.c-card[data-v-81f6011e]::before {\n    content: "";\n    position: absolute;\n    top: 1rem;\n    left: -0.5rem;\n    display: block;\n    width: 0;\n    z-index: 0;\n    border-top: 0.5rem solid transparent;\n    border-right: 0.5rem solid var(--background_0);\n    border-bottom: 0.5rem solid transparent;\n}\n}\n.c-close[data-v-81f6011e] {\n  left: calc(100vw - 4rem);\n  top: 1.5rem;\n}\n@media screen and (min-width: 769px), print {\n.c-close[data-v-81f6011e] {\n    left: auto;\n    right: 1rem;\n    opacity: 0;\n    pointer-events: none;\n}\n.c-close[data-v-81f6011e]:focus {\n    opacity: 1;\n}\n}\n.c-header[data-v-81f6011e],\n.c-footer[data-v-81f6011e] {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n}\n.c-header[data-v-81f6011e] {\n  padding: 1rem 1rem 0.5rem;\n}\n.c-footer[data-v-81f6011e] {\n  border-top: 1px solid var(--general_1);\n  padding: 0.75rem 1rem;\n}\n.c-body[data-v-81f6011e] {\n  height: 18rem;\n  overflow: auto;\n  overscroll-behavior-y: contain;\n}\n.c-settings-btn .icon-cog[data-v-81f6011e] {\n  transform-origin: 49% 48%;\n}\n.c-settings-btn:hover .icon-cog[data-v-81f6011e], .c-settings-btn:focus .icon-cog[data-v-81f6011e] {\n  animation: cog-data-v-81f6011e 400ms forwards;\n}\n@keyframes cog-data-v-81f6011e {\nto {\n    transform: rotate(180deg);\n}\n}\n\n/*# sourceMappingURL=NotificationCard.vue.map */', map: { "version": 3, "sources": ["frontend/views/containers/notifications/NotificationCard.vue", "NotificationCard.vue"], "names": [], "mappings": "AA2EA;EACA,kBAAA;EACA,iBAAA;EACA,oBAAA;EACA,gBAAA;EACA,kBAAA;EACA,YAAA;EACA,gBAAA;AC1EA;AD4EA;EACA,gBAAA;AC1EA;AD8EA;EACA,6DAAA;AC5EA;AACA;AD0EA;IAGA,+BAAA;AC1EE;AACF;AACA;ADuDA;IAsBA,mBAAA;IACA,kDAAA;IACA,YAAA;IACA,UAAA;AC1EE;AD6EF;IACA,WAAA;IACA,kBAAA;IACA,SAAA;IACA,aAAA;IACA,cAAA;IACA,QAAA;IACA,UAAA;IACA,oCAAA;IACA,8CAAA;IACA,uCAAA;AC3EE;AACF;AD+EA;EACA,wBAAA;EACA,WAAA;AC5EA;AACA;ADyEA;IAKA,UAAA;IACA,WAAA;IACA,UAAA;IACA,oBAAA;AC3EE;AD6EF;IACA,UAAA;AC3EE;AACF;AD+EA;;EAEA,aAAA;EACA,8BAAA;EACA,mBAAA;AC5EA;AD+EA;EACA,yBAAA;AC5EA;AD+EA;EACA,sCAAA;EACA,qBAAA;AC5EA;AD+EA;EACA,aAAA;EACA,cAAA;EACA,8BAAA;AC5EA;ADgFA;EACA,yBAAA;AC7EA;ADgFA;EAEA,6CAAA;AC/EA;ADmFA;AACA;IAAA,yBAAA;AC/EE;AACF;;AAEA,+CAA+C", "file": "NotificationCard.vue", "sourcesContent": [`<template lang='pug'>
tooltip(
  direction='right'
  :manual='true'
  ref='tooltip'
  :opacity='1'
  :aria-label='L("Show notifications")'
)
  slot

  template(slot='tooltip')
    .card.c-card(role='dialog' :aria-label='L("Notifications")' data-test='notificationCard')
      .c-header
        i18n.is-title-4(tag='h2') Notifications
        button.is-icon-small.c-settings-btn(@click='handleSettingsClick')
          i.icon-cog

      .c-body
        notification-list(variant='compact' @select='toggleTooltip')
      .c-footer(v-if='currentNotificationCount')
        i18n.link(tag='button'
          @click='markAllNotificationsAsRead'
          data-test='markAllAsRead_In_Tooltip'
        ) Mark all as read

        router-link.link(:to='{ query: { modal: "NotificationModal" }}' @click.native='toggleTooltip')
          i18n See all

      modal-close.c-close(:aria-label='L("Close profile")' @close='toggleTooltip')
</template>

<script>
import sbp from '@sbp/sbp'
import { mapState, mapGetters } from 'vuex'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import ModalClose from '../../../../frontend/views/components/modal/ModalClose.vue'
import Tooltip from '../../../../frontend/views/components/Tooltip.vue'
import NotificationList from './NotificationList.vue'

export default {
  name: 'NotificationCard',
  components: {
    ModalClose,
    NotificationList,
    Tooltip
  },
  methods: {
    toggleTooltip () {
      this.$refs.tooltip.toggle()
    },
    handleSettingsClick () {
      // BUG - Section notification does not open. Fixed at #946
      sbp('okTurtles.events/emit', OPEN_MODAL, 'UserSettingsModal', {
        tab: 'notifications'
      })
      this.toggleTooltip()
    },
    markAllNotificationsAsRead () {
      sbp('gi.notifications/markAllAsRead', this.currentGroupId)
    }
  },
  computed: {
    ...mapState([
      'currentGroupId'
    ]),
    ...mapGetters([
      'currentNotificationCount'
    ])
  }
}
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-card {
  position: relative;
  padding: 0 0 4rem;
  color: $text_0;
  max-width: 100vw;
  border-radius: 3px;
  width: 100vw;
  box-shadow: none;

  &:last-child {
    margin-bottom: 0;
  }

  // is-active is from tooltip
  .is-active & {
    animation: zoom 100ms both cubic-bezier(0.165, 0.84, 0.44, 1);
    @include phone {
      animation-name: enterFromBottom;
    }
  }

  @include tablet {
    margin-top: -0.5rem; // better aligned with bell icon
    box-shadow: 0 0.5rem 1.25rem rgba(54, 54, 54, 0.3);
    width: 25rem;
    padding: 0;

    // triangle corner
    &::before {
      content: "";
      position: absolute;
      top: 1rem;
      left: -0.5rem;
      display: block;
      width: 0;
      z-index: 0;
      border-top: 0.5rem solid transparent;
      border-right: 0.5rem solid $background;
      border-bottom: 0.5rem solid transparent;
    }
  }
}

.c-close {
  left: calc(100vw - 4rem);
  top: 1.5rem;

  @include tablet {
    left: auto;
    right: 1rem;
    opacity: 0;
    pointer-events: none;

    &:focus {
      opacity: 1;
    }
  }
}

.c-header,
.c-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.c-header {
  padding: 1rem 1rem 0.5rem;
}

.c-footer {
  border-top: 1px solid $general_1;
  padding: 0.75rem 1rem;
}

.c-body {
  height: 18rem;
  overflow: auto;
  overscroll-behavior-y: contain;
}

.c-settings-btn {
  .icon-cog {
    transform-origin: 49% 48%;
  }

  &:hover .icon-cog,
  &:focus .icon-cog {
    animation: cog 400ms forwards;
  }
}

@keyframes cog {
  to { transform: rotate(180deg); }
}
</style>
`, '.c-card {\n  position: relative;\n  padding: 0 0 4rem;\n  color: var(--text_0);\n  max-width: 100vw;\n  border-radius: 3px;\n  width: 100vw;\n  box-shadow: none;\n}\n.c-card:last-child {\n  margin-bottom: 0;\n}\n.is-active .c-card {\n  animation: zoom 100ms both cubic-bezier(0.165, 0.84, 0.44, 1);\n}\n@media screen and (max-width: 768px) {\n  .is-active .c-card {\n    animation-name: enterFromBottom;\n  }\n}\n@media screen and (min-width: 769px), print {\n  .c-card {\n    margin-top: -0.5rem;\n    box-shadow: 0 0.5rem 1.25rem rgba(54, 54, 54, 0.3);\n    width: 25rem;\n    padding: 0;\n  }\n  .c-card::before {\n    content: "";\n    position: absolute;\n    top: 1rem;\n    left: -0.5rem;\n    display: block;\n    width: 0;\n    z-index: 0;\n    border-top: 0.5rem solid transparent;\n    border-right: 0.5rem solid var(--background_0);\n    border-bottom: 0.5rem solid transparent;\n  }\n}\n\n.c-close {\n  left: calc(100vw - 4rem);\n  top: 1.5rem;\n}\n@media screen and (min-width: 769px), print {\n  .c-close {\n    left: auto;\n    right: 1rem;\n    opacity: 0;\n    pointer-events: none;\n  }\n  .c-close:focus {\n    opacity: 1;\n  }\n}\n\n.c-header,\n.c-footer {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n}\n\n.c-header {\n  padding: 1rem 1rem 0.5rem;\n}\n\n.c-footer {\n  border-top: 1px solid var(--general_1);\n  padding: 0.75rem 1rem;\n}\n\n.c-body {\n  height: 18rem;\n  overflow: auto;\n  overscroll-behavior-y: contain;\n}\n\n.c-settings-btn .icon-cog {\n  transform-origin: 49% 48%;\n}\n.c-settings-btn:hover .icon-cog, .c-settings-btn:focus .icon-cog {\n  animation: cog 400ms forwards;\n}\n\n@keyframes cog {\n  to {\n    transform: rotate(180deg);\n  }\n}\n\n/*# sourceMappingURL=NotificationCard.vue.map */'] }, media: void 0 });
};
var __vue_scope_id__13 = "data-v-81f6011e";
var __vue_module_identifier__13 = void 0;
var __vue_is_functional_template__13 = false;
function __vue_normalize__13(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
tooltip(
  direction='right'
  :manual='true'
  ref='tooltip'
  :opacity='1'
  :aria-label='L("Show notifications")'
)
  slot

  template(slot='tooltip')
    .card.c-card(role='dialog' :aria-label='L("Notifications")' data-test='notificationCard')
      .c-header
        i18n.is-title-4(tag='h2') Notifications
        button.is-icon-small.c-settings-btn(@click='handleSettingsClick')
          i.icon-cog

      .c-body
        notification-list(variant='compact' @select='toggleTooltip')
      .c-footer(v-if='currentNotificationCount')
        i18n.link(tag='button'
          @click='markAllNotificationsAsRead'
          data-test='markAllAsRead_In_Tooltip'
        ) Mark all as read

        router-link.link(:to='{ query: { modal: "NotificationModal" }}' @click.native='toggleTooltip')
          i18n See all

      modal-close.c-close(:aria-label='L("Close profile")' @close='toggleTooltip')
</template>

<script>
import sbp from '@sbp/sbp'
import { mapState, mapGetters } from 'vuex'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import ModalClose from '../../../../frontend/views/components/modal/ModalClose.vue'
import Tooltip from '../../../../frontend/views/components/Tooltip.vue'
import NotificationList from './NotificationList.vue'

export default {
  name: 'NotificationCard',
  components: {
    ModalClose,
    NotificationList,
    Tooltip
  },
  methods: {
    toggleTooltip () {
      this.$refs.tooltip.toggle()
    },
    handleSettingsClick () {
      // BUG - Section notification does not open. Fixed at #946
      sbp('okTurtles.events/emit', OPEN_MODAL, 'UserSettingsModal', {
        tab: 'notifications'
      })
      this.toggleTooltip()
    },
    markAllNotificationsAsRead () {
      sbp('gi.notifications/markAllAsRead', this.currentGroupId)
    }
  },
  computed: {
    ...mapState([
      'currentGroupId'
    ]),
    ...mapGetters([
      'currentNotificationCount'
    ])
  }
}
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-card {
  position: relative;
  padding: 0 0 4rem;
  color: $text_0;
  max-width: 100vw;
  border-radius: 3px;
  width: 100vw;
  box-shadow: none;

  &:last-child {
    margin-bottom: 0;
  }

  // is-active is from tooltip
  .is-active & {
    animation: zoom 100ms both cubic-bezier(0.165, 0.84, 0.44, 1);
    @include phone {
      animation-name: enterFromBottom;
    }
  }

  @include tablet {
    margin-top: -0.5rem; // better aligned with bell icon
    box-shadow: 0 0.5rem 1.25rem rgba(54, 54, 54, 0.3);
    width: 25rem;
    padding: 0;

    // triangle corner
    &::before {
      content: "";
      position: absolute;
      top: 1rem;
      left: -0.5rem;
      display: block;
      width: 0;
      z-index: 0;
      border-top: 0.5rem solid transparent;
      border-right: 0.5rem solid $background;
      border-bottom: 0.5rem solid transparent;
    }
  }
}

.c-close {
  left: calc(100vw - 4rem);
  top: 1.5rem;

  @include tablet {
    left: auto;
    right: 1rem;
    opacity: 0;
    pointer-events: none;

    &:focus {
      opacity: 1;
    }
  }
}

.c-header,
.c-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.c-header {
  padding: 1rem 1rem 0.5rem;
}

.c-footer {
  border-top: 1px solid $general_1;
  padding: 0.75rem 1rem;
}

.c-body {
  height: 18rem;
  overflow: auto;
  overscroll-behavior-y: contain;
}

.c-settings-btn {
  .icon-cog {
    transform-origin: 49% 48%;
  }

  &:hover .icon-cog,
  &:focus .icon-cog {
    animation: cog 400ms forwards;
  }
}

@keyframes cog {
  to { transform: rotate(180deg); }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__11() {
  const styles = __vue_create_injector__11.styles || (__vue_create_injector__11.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__13 = /* @__PURE__ */ __vue_normalize__13(
  { render: __vue_render__13, staticRenderFns: __vue_staticRenderFns__13 },
  __vue_inject_styles__13,
  __vue_script__13,
  __vue_scope_id__13,
  __vue_is_functional_template__13,
  __vue_module_identifier__13,
  false,
  __vue_create_injector__11,
  void 0,
  void 0
);
var NotificationCard_default = __vue_component__13;

// frontend/views/containers/notifications/NotificationBell.vue
var __vue_script__14 = {
  name: "NotificationBell",
  components: {
    Badge: Badge_default,
    NotificationCard: NotificationCard_default
  },
  computed: {
    ...mapGetters([
      "currentUnreadNotificationCount"
    ])
  }
};
var __vue_render__14 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "div",
    [
      _c(
        "router-link",
        {
          staticClass: "button is-icon-small c-btn hide-tablet",
          attrs: { to: { query: { modal: "NotificationModal" } } }
        },
        [
          _c("i", {
            staticClass: "icon-bell",
            class: { "is-active": _vm.currentUnreadNotificationCount }
          }),
          _vm.currentUnreadNotificationCount ? _c("badge", { attrs: { "data-test": "alertNotification" } }, [
            _vm._v(_vm._s(_vm.currentUnreadNotificationCount))
          ]) : _vm._e()
        ],
        1
      ),
      _c("notification-card", [
        _c(
          "button",
          { staticClass: "is-icon-small c-btn hide-phone" },
          [
            _c("i", {
              staticClass: "icon-bell",
              class: { "is-active": _vm.currentUnreadNotificationCount }
            }),
            _vm.currentUnreadNotificationCount ? _c("badge", { attrs: { "data-test": "alertNotification" } }, [
              _vm._v(_vm._s(_vm.currentUnreadNotificationCount))
            ]) : _vm._e()
          ],
          1
        )
      ])
    ],
    1
  );
};
var __vue_staticRenderFns__14 = [];
__vue_render__14._withStripped = true;
var __vue_inject_styles__14 = function(inject) {
  if (!inject) return;
  inject("data-v-0762b3b9_0", { source: ".c-badge[data-v-0762b3b9] {\n  top: -0.25rem;\n  right: -0.25rem;\n}\n.icon-bell[data-v-0762b3b9] {\n  font-size: 0.9rem;\n  transform-origin: center 2px;\n  margin-top: 2px;\n}\n.icon-bell.is-active[data-v-0762b3b9] {\n  font-weight: 900;\n}\n.c-btn:hover .icon-bell[data-v-0762b3b9],\n.c-btn:focus .icon-bell[data-v-0762b3b9] {\n  animation: bell-data-v-0762b3b9 750ms forwards;\n}\n@keyframes bell-data-v-0762b3b9 {\n10% {\n    transform: rotate(15deg);\n}\n20% {\n    transform: rotate(-15deg);\n}\n35% {\n    transform: rotate(15deg);\n}\n60% {\n    transform: rotate(-10deg);\n}\n80% {\n    transform: rotate(5deg);\n}\n}\n\n/*# sourceMappingURL=NotificationBell.vue.map */", map: { "version": 3, "sources": ["frontend/views/containers/notifications/NotificationBell.vue", "NotificationBell.vue"], "names": [], "mappings": "AAiCA;EACA,aAAA;EACA,eAAA;AChCA;ADmCA;EACA,iBAAA;EACA,4BAAA;EACA,eAAA;AChCA;ADkCA;EACA,gBAAA;AChCA;ADsCA;;EACA,8CAAA;AClCA;ADsCA;AACA;IAAA,wBAAA;AClCE;ADmCF;IAAA,yBAAA;AChCE;ADiCF;IAAA,wBAAA;AC9BE;AD+BF;IAAA,yBAAA;AC5BE;AD6BF;IAAA,uBAAA;AC1BE;AACF;;AAEA,+CAA+C", "file": "NotificationBell.vue", "sourcesContent": [`<template lang='pug'>
div
  router-link.button.is-icon-small.c-btn.hide-tablet(:to='{ query: { modal: "NotificationModal" }}')
    i.icon-bell(:class='{ "is-active": currentUnreadNotificationCount }')
    badge(v-if='currentUnreadNotificationCount' data-test='alertNotification') {{ currentUnreadNotificationCount }}
  notification-card
    button.is-icon-small.c-btn.hide-phone
      i.icon-bell(:class='{ "is-active": currentUnreadNotificationCount }')
      badge(v-if='currentUnreadNotificationCount' data-test='alertNotification') {{ currentUnreadNotificationCount }}
</template>

<script>
import { mapGetters } from 'vuex'
import Badge from '../../../../frontend/views/components/Badge.vue'
import NotificationCard from './NotificationCard.vue'

export default {
  name: 'NotificationBell',
  components: {
    Badge,
    NotificationCard
  },
  computed: {
    ...mapGetters([
      'currentUnreadNotificationCount'
    ])
  }
}
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-badge {
  top: -0.25rem;
  right: -0.25rem;
}

.icon-bell {
  font-size: 0.9rem;
  transform-origin: center 2px;
  margin-top: 2px;

  &.is-active {
    font-weight: 900;
  }
}

.c-btn:hover,
.c-btn:focus {
  .icon-bell {
    animation: bell 750ms forwards;
  }
}

@keyframes bell {
  10% { transform: rotate(15deg); }
  20% { transform: rotate(-15deg); }
  35% { transform: rotate(15deg); }
  60% { transform: rotate(-10deg); }
  80% { transform: rotate(5deg); }
}
</style>
`, ".c-badge {\n  top: -0.25rem;\n  right: -0.25rem;\n}\n\n.icon-bell {\n  font-size: 0.9rem;\n  transform-origin: center 2px;\n  margin-top: 2px;\n}\n.icon-bell.is-active {\n  font-weight: 900;\n}\n\n.c-btn:hover .icon-bell,\n.c-btn:focus .icon-bell {\n  animation: bell 750ms forwards;\n}\n\n@keyframes bell {\n  10% {\n    transform: rotate(15deg);\n  }\n  20% {\n    transform: rotate(-15deg);\n  }\n  35% {\n    transform: rotate(15deg);\n  }\n  60% {\n    transform: rotate(-10deg);\n  }\n  80% {\n    transform: rotate(5deg);\n  }\n}\n\n/*# sourceMappingURL=NotificationBell.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__14 = "data-v-0762b3b9";
var __vue_module_identifier__14 = void 0;
var __vue_is_functional_template__14 = false;
function __vue_normalize__14(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
div
  router-link.button.is-icon-small.c-btn.hide-tablet(:to='{ query: { modal: "NotificationModal" }}')
    i.icon-bell(:class='{ "is-active": currentUnreadNotificationCount }')
    badge(v-if='currentUnreadNotificationCount' data-test='alertNotification') {{ currentUnreadNotificationCount }}
  notification-card
    button.is-icon-small.c-btn.hide-phone
      i.icon-bell(:class='{ "is-active": currentUnreadNotificationCount }')
      badge(v-if='currentUnreadNotificationCount' data-test='alertNotification') {{ currentUnreadNotificationCount }}
</template>

<script>
import { mapGetters } from 'vuex'
import Badge from '../../../../frontend/views/components/Badge.vue'
import NotificationCard from './NotificationCard.vue'

export default {
  name: 'NotificationBell',
  components: {
    Badge,
    NotificationCard
  },
  computed: {
    ...mapGetters([
      'currentUnreadNotificationCount'
    ])
  }
}
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-badge {
  top: -0.25rem;
  right: -0.25rem;
}

.icon-bell {
  font-size: 0.9rem;
  transform-origin: center 2px;
  margin-top: 2px;

  &.is-active {
    font-weight: 900;
  }
}

.c-btn:hover,
.c-btn:focus {
  .icon-bell {
    animation: bell 750ms forwards;
  }
}

@keyframes bell {
  10% { transform: rotate(15deg); }
  20% { transform: rotate(-15deg); }
  35% { transform: rotate(15deg); }
  60% { transform: rotate(-10deg); }
  80% { transform: rotate(5deg); }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__12() {
  const styles = __vue_create_injector__12.styles || (__vue_create_injector__12.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__14 = /* @__PURE__ */ __vue_normalize__14(
  { render: __vue_render__14, staticRenderFns: __vue_staticRenderFns__14 },
  __vue_inject_styles__14,
  __vue_script__14,
  __vue_scope_id__14,
  __vue_is_functional_template__14,
  __vue_module_identifier__14,
  false,
  __vue_create_injector__12,
  void 0,
  void 0
);
var NotificationBell_default = __vue_component__14;

// frontend/views/containers/navigation/GroupsList.vue
var __vue_script__15 = {
  name: "GroupsList",
  components: {
    Avatar: Avatar_default,
    Badge: Badge_default,
    Tooltip: Tooltip_default
  },
  data() {
    return {
      ephemeral: {
        latestNewsDate: null
      }
    };
  },
  created() {
    this.checkForNewNews();
  },
  computed: {
    ...mapState([
      "currentGroupId"
    ]),
    ...mapGetters([
      "groupsByName",
      "groupUnreadMessages",
      "unreadGroupNotificationCountFor",
      "ourPreferences"
    ]),
    badgeVisiblePerGroup() {
      return Object.fromEntries(
        this.groupsByName.map((group) => [
          group.contractID,
          this.groupUnreadMessages(group.contractID) + this.unreadGroupNotificationCountFor(group.contractID) > 0
        ])
      );
    },
    isInGlobalDashboard() {
      return this.$route.path.startsWith("/global-dashboard");
    },
    hasNewNews() {
      if (this.$route.path === "/global-dashboard/news-and-updates" || !this.ourPreferences?.lastSeenNewsDate || !this.ephemeral.latestNewsDate) {
        return false;
      }
      return new Date(this.ephemeral.latestNewsDate) > new Date(this.ourPreferences.lastSeenNewsDate);
    }
  },
  methods: {
    openModal(mode) {
      esm_default("okTurtles.events/emit", OPEN_MODAL, mode);
    },
    handleMenuSelect(id) {
      id && this.changeGroup(id);
      if (this.isInGlobalDashboard) {
        this.$router.push("/dashboard");
      }
    },
    onGlobalDashboardClick() {
      this.$router.push({ path: "/global-dashboard" });
    },
    changeGroup(hash2) {
      const path = this.$route.path;
      const emptyGroupPicture = this.groupPictureForContract(hash2) === "";
      if (hash2 !== this.currentGroupId) {
        esm_default("gi.app/group/switch", hash2);
      }
      if (emptyGroupPicture && path !== "/pending-approval") {
        this.$router.push({ path: "/pending-approval" });
      } else if (!emptyGroupPicture && path === "/pending-approval") {
        this.$router.push({ path: "/dashboard" });
      }
    },
    groupPictureForContract(contractID) {
      return this.$store.state[contractID]?.settings?.groupPicture || "";
    },
    async checkForNewNews() {
      try {
        const data = await fetchNews();
        if (data.length > 0) {
          this.ephemeral.latestNewsDate = data[0].createdAt;
        }
      } catch (error) {
        console.error("Failed to check for new news:", error);
      }
    }
  }
};
var __vue_render__15 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _vm.groupsByName.length ? _c(
    "ul",
    { staticClass: "c-group-list", attrs: { "data-test": "groupsList" } },
    [
      _c(
        "li",
        {
          staticClass: "c-group-list-item group-badge",
          class: { "is-active": _vm.isInGlobalDashboard }
        },
        [
          _c(
            "tooltip",
            {
              attrs: {
                direction: "right",
                text: _vm.L("Global dashboard")
              }
            },
            [
              _c(
                "button",
                {
                  staticClass: "c-group-picture is-unstyled c-global-dashboard-logo",
                  on: { click: _vm.onGlobalDashboardClick }
                },
                [
                  _c("avatar", {
                    staticClass: "c-avatar",
                    attrs: {
                      src: "/assets/images/group-income-icon-transparent-circle.png",
                      alt: "Global dashboard logo"
                    }
                  })
                ],
                1
              ),
              _vm.hasNewNews ? _c("badge", {
                attrs: {
                  type: "compact",
                  "data-test": "globalDashboardBadge"
                }
              }) : _vm._e()
            ],
            1
          )
        ],
        1
      ),
      _vm._l(_vm.groupsByName, function(group, index) {
        return _c(
          "li",
          {
            key: "group-" + index,
            staticClass: "c-group-list-item group-badge",
            class: {
              "is-active": !_vm.isInGlobalDashboard && _vm.currentGroupId === group.contractID
            }
          },
          [
            _c(
              "tooltip",
              { attrs: { direction: "right", text: group.groupName } },
              [
                _c(
                  "button",
                  {
                    staticClass: "c-group-picture is-unstyled",
                    on: {
                      click: function($event) {
                        return _vm.handleMenuSelect(group.contractID);
                      }
                    }
                  },
                  [
                    _c("avatar", {
                      staticClass: "c-avatar",
                      attrs: {
                        src: _vm.groupPictureForContract(group.contractID)
                      }
                    })
                  ],
                  1
                ),
                _vm.badgeVisiblePerGroup[group.contractID] ? _c("badge", {
                  attrs: {
                    type: "compact",
                    "data-test": "groupBadge-" + group.groupName
                  }
                }) : _vm._e()
              ],
              1
            )
          ],
          1
        );
      }),
      _c(
        "li",
        { staticClass: "c-group-list-item" },
        [
          _c(
            "tooltip",
            {
              attrs: {
                direction: "right",
                text: _vm.L("Create a new group")
              }
            },
            [
              _c(
                "button",
                {
                  staticClass: "is-icon has-background",
                  attrs: {
                    "data-test": "createGroup",
                    "aria-label": _vm.L("Add a group")
                  },
                  on: {
                    click: function($event) {
                      return _vm.openModal("GroupCreationModal");
                    }
                  }
                },
                [_c("i", { staticClass: "icon-plus" })]
              )
            ]
          )
        ],
        1
      )
    ],
    2
  ) : _vm._e();
};
var __vue_staticRenderFns__15 = [];
__vue_render__15._withStripped = true;
var __vue_inject_styles__15 = function(inject) {
  if (!inject) return;
  inject("data-v-69cbfde9_0", { source: '.c-badge[data-v-69cbfde9] {\n  top: 0;\n  right: 1px;\n  z-index: 2;\n}\n.c-group-list[data-v-69cbfde9] {\n  width: 3.5rem;\n  padding-top: 0.7rem;\n  background-color: var(--general_1);\n}\n.c-group-list-item[data-v-69cbfde9] {\n  position: relative;\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  height: 3.2rem;\n  margin-bottom: 0.25rem;\n}\n.group-badge[data-v-69cbfde9]::before {\n  content: "";\n  position: absolute;\n  width: 3rem;\n  height: 3rem;\n  border: 1px solid transparent;\n  border-radius: 50%;\n  transform: scale(0.7);\n  transition: all 0.25s cubic-bezier(0.18, 0.89, 0.32, 1.38);\n}\n.group-badge.is-active[data-v-69cbfde9]::before {\n  transform: scale(1);\n  border-color: var(--text_0);\n  animation: spin-data-v-69cbfde9 0.5s ease-out;\n}\n.group-badge[data-v-69cbfde9]:not(.is-active) {\n  cursor: pointer;\n}\n.group-badge[data-v-69cbfde9]:not(.is-active):focus::before, .group-badge[data-v-69cbfde9]:not(.is-active):hover::before {\n  border-color: #fff;\n  border-width: 3px;\n  transform: scale(1.1);\n}\n@keyframes spin-data-v-69cbfde9 {\n0% {\n    transform: rotate(45deg);\n    filter: hue-rotate(0deg);\n    border-color: var(--primary_0) transparent transparent;\n}\n50% {\n    transform: rotate(315deg);\n    filter: hue-rotate(360deg);\n    border-color: var(--primary_0) transparent transparent;\n}\n100% {\n    transform: rotate(585deg);\n    border-color: #fff;\n}\n}\n.c-avatar[data-v-69cbfde9] {\n  position: relative;\n  z-index: 1;\n}\n.c-group-picture[data-v-69cbfde9] {\n  display: flex;\n}\n.c-group-picture.c-global-dashboard-logo[data-v-69cbfde9] {\n  background-color: var(--background_0);\n  border-radius: 50%;\n  width: 2.5rem;\n  height: 2.5rem;\n}\n.c-group-picture.c-global-dashboard-logo[data-v-69cbfde9]  img.c-avatar {\n  width: 1.75rem;\n  height: 1.75rem;\n}\n\n/*# sourceMappingURL=GroupsList.vue.map */', map: { "version": 3, "sources": ["frontend/views/containers/navigation/GroupsList.vue", "GroupsList.vue"], "names": [], "mappings": "AA0JA;EACA,MAAA;EACA,UAAA;EACA,UAAA;ACzJA;AD4JA;EACA,aAAA;EACA,mBAAA;EACA,kCAAA;ACzJA;AD4JA;EACA,kBAAA;EACA,aAAA;EACA,uBAAA;EACA,mBAAA;EACA,cAAA;EACA,sBAAA;ACzJA;AD6JA;EACA,WAAA;EACA,kBAAA;EACA,WAAA;EACA,YAAA;EACA,6BAAA;EACA,kBAAA;EACA,qBAAA;EACA,0DAAA;AC1JA;AD8JA;EACA,mBAAA;EACA,2BAAA;EACA,6CAAA;AC5JA;ADgKA;EACA,eAAA;AC9JA;ADgKA;EAEA,kBAAA;EACA,iBAAA;EACA,qBAAA;AC/JA;ADoKA;AACA;IACA,wBAAA;IACA,wBAAA;IACA,sDAAA;ACjKE;ADoKF;IACA,yBAAA;IACA,0BAAA;IACA,sDAAA;AClKE;ADqKF;IACA,yBAAA;IACA,kBAAA;ACnKE;AACF;ADsKA;EACA,kBAAA;EACA,UAAA;ACpKA;ADuKA;EACA,aAAA;ACpKA;ADsKA;EACA,qCAAA;EACA,kBAAA;EACA,aAAA;EACA,cAAA;ACpKA;ADsKA;EACA,cAAA;EACA,eAAA;ACpKA;;AAEA,yCAAyC", "file": "GroupsList.vue", "sourcesContent": [`<template lang='pug'>
ul.c-group-list(v-if='groupsByName.length' data-test='groupsList')
  li.c-group-list-item.group-badge(:class='{ "is-active": isInGlobalDashboard }')
    tooltip(
      direction='right'
      :text='L("Global dashboard")'
    )
      button.c-group-picture.is-unstyled.c-global-dashboard-logo(@click='onGlobalDashboardClick')
        avatar.c-avatar(
          src='/assets/images/group-income-icon-transparent-circle.png'
          alt='Global dashboard logo'
        )
      badge(
        v-if='hasNewNews'
        type='compact'
        data-test='globalDashboardBadge'
      )

  li.c-group-list-item.group-badge(
    v-for='(group, index) in groupsByName'
    :key='\`group-\${index}\`'
    :class='{ "is-active": !isInGlobalDashboard && currentGroupId === group.contractID}'
  )
    tooltip(
      direction='right'
      :text='group.groupName'
    )
      button.c-group-picture.is-unstyled(@click='handleMenuSelect(group.contractID)')
        avatar.c-avatar(:src='groupPictureForContract(group.contractID)')
      badge(
        v-if='badgeVisiblePerGroup[group.contractID]'
        type='compact'
        :data-test='\`groupBadge-\${group.groupName}\`'
      )

  li.c-group-list-item
    tooltip(
      direction='right'
      :text='L("Create a new group")'
    )
      button(
        class='is-icon has-background'
        @click='openModal("GroupCreationModal")'
        data-test='createGroup'
        :aria-label='L("Add a group")'
      )
        i.icon-plus
</template>

<script>
import sbp from '@sbp/sbp'
import { mapGetters, mapState } from 'vuex'
import Avatar from '../../../../frontend/views/components/Avatar.vue'
import Badge from '../../../../frontend/views/components/Badge.vue'
import Tooltip from '../../../../frontend/views/components/Tooltip.vue'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import { fetchNews } from '../../../../frontend/views/utils/misc.js'

export default ({
  name: 'GroupsList',
  components: {
    Avatar,
    Badge,
    Tooltip
  },
  data () {
    return {
      ephemeral: {
        latestNewsDate: null
      }
    }
  },
  created () {
    this.checkForNewNews()
  },
  computed: {
    ...mapState([
      'currentGroupId'
    ]),
    ...mapGetters([
      'groupsByName',
      'groupUnreadMessages',
      'unreadGroupNotificationCountFor',
      'ourPreferences'
    ]),
    badgeVisiblePerGroup () {
      return Object.fromEntries(
        this.groupsByName.map(group => ([
          group.contractID,
          this.groupUnreadMessages(group.contractID) + this.unreadGroupNotificationCountFor(group.contractID) > 0
        ]))
      )
    },
    isInGlobalDashboard () {
      return this.$route.path.startsWith('/global-dashboard')
    },
    hasNewNews () {
      // Don't show badge if we're currently on the news page
      if (this.$route.path === '/global-dashboard/news-and-updates' ||
        !this.ourPreferences?.lastSeenNewsDate ||
        !this.ephemeral.latestNewsDate
      ) {
        return false
      }

      return new Date(this.ephemeral.latestNewsDate) > new Date(this.ourPreferences.lastSeenNewsDate)
    }
  },
  methods: {
    openModal (mode) {
      sbp('okTurtles.events/emit', OPEN_MODAL, mode)
    },
    handleMenuSelect (id) {
      id && this.changeGroup(id)

      if (this.isInGlobalDashboard) {
        this.$router.push('/dashboard')
      }
    },
    onGlobalDashboardClick () {
      this.$router.push(({ path: '/global-dashboard' }))
    },
    changeGroup (hash) {
      const path = this.$route.path
      const emptyGroupPicture = this.groupPictureForContract(hash) === ''
      if (hash !== this.currentGroupId) {
        sbp('gi.app/group/switch', hash)
      }
      if (emptyGroupPicture && path !== '/pending-approval') {
        this.$router.push(({ path: '/pending-approval' }))
      } else if (!emptyGroupPicture && path === '/pending-approval') {
        this.$router.push(({ path: '/dashboard' }))
      }
    },
    groupPictureForContract (contractID) {
      return this.$store.state[contractID]?.settings?.groupPicture || ''
    },
    async checkForNewNews () {
      try {
        const data = await fetchNews()
        if (data.length > 0) {
          this.ephemeral.latestNewsDate = data[0].createdAt
        }
      } catch (error) {
        console.error('Failed to check for new news:', error)
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-badge {
  top: 0;
  right: 1px; // Prevent badges from touching the vertical edge.
  z-index: 2; // Make badges appear above group avatars.
}

.c-group-list {
  width: 3.5rem;
  padding-top: 0.7rem;
  background-color: $general_1;
}

.c-group-list-item {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 3.2rem;
  margin-bottom: 0.25rem;
}

.group-badge {
  &::before {
    content: "";
    position: absolute;
    width: 3rem;
    height: 3rem;
    border: 1px solid transparent;
    border-radius: 50%;
    transform: scale(0.7);
    transition: all 0.25s cubic-bezier(0.18, 0.89, 0.32, 1.38);
  }

  &.is-active {
    &::before {
      transform: scale(1);
      border-color: $text_0;
      animation: spin 0.5s ease-out;
    }
  }

  &:not(.is-active) {
    cursor: pointer;

    &:focus::before,
    &:hover::before {
      border-color: $white;
      border-width: 3px;
      transform: scale(1.1);
    }
  }
}

@keyframes spin {
  0% {
    transform: rotate(45deg);
    filter: hue-rotate(0deg);
    border-color: $primary_0 transparent transparent;
  }

  50% {
    transform: rotate(315deg);
    filter: hue-rotate(360deg);
    border-color: $primary_0 transparent transparent;
  }

  100% {
    transform: rotate(585deg);
    border-color: $white;
  }
}

.c-avatar {
  position: relative;
  z-index: 1;
}

.c-group-picture {
  display: flex;

  &.c-global-dashboard-logo {
    background-color: $background_0;
    border-radius: 50%;
    width: 2.5rem;
    height: 2.5rem;

    ::v-deep img.c-avatar {
      width: 1.75rem;
      height: 1.75rem;
    }
  }
}
</style>
`, '.c-badge {\n  top: 0;\n  right: 1px;\n  z-index: 2;\n}\n\n.c-group-list {\n  width: 3.5rem;\n  padding-top: 0.7rem;\n  background-color: var(--general_1);\n}\n\n.c-group-list-item {\n  position: relative;\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  height: 3.2rem;\n  margin-bottom: 0.25rem;\n}\n\n.group-badge::before {\n  content: "";\n  position: absolute;\n  width: 3rem;\n  height: 3rem;\n  border: 1px solid transparent;\n  border-radius: 50%;\n  transform: scale(0.7);\n  transition: all 0.25s cubic-bezier(0.18, 0.89, 0.32, 1.38);\n}\n.group-badge.is-active::before {\n  transform: scale(1);\n  border-color: var(--text_0);\n  animation: spin 0.5s ease-out;\n}\n.group-badge:not(.is-active) {\n  cursor: pointer;\n}\n.group-badge:not(.is-active):focus::before, .group-badge:not(.is-active):hover::before {\n  border-color: #fff;\n  border-width: 3px;\n  transform: scale(1.1);\n}\n\n@keyframes spin {\n  0% {\n    transform: rotate(45deg);\n    filter: hue-rotate(0deg);\n    border-color: var(--primary_0) transparent transparent;\n  }\n  50% {\n    transform: rotate(315deg);\n    filter: hue-rotate(360deg);\n    border-color: var(--primary_0) transparent transparent;\n  }\n  100% {\n    transform: rotate(585deg);\n    border-color: #fff;\n  }\n}\n.c-avatar {\n  position: relative;\n  z-index: 1;\n}\n\n.c-group-picture {\n  display: flex;\n}\n.c-group-picture.c-global-dashboard-logo {\n  background-color: var(--background_0);\n  border-radius: 50%;\n  width: 2.5rem;\n  height: 2.5rem;\n}\n.c-group-picture.c-global-dashboard-logo ::v-deep img.c-avatar {\n  width: 1.75rem;\n  height: 1.75rem;\n}\n\n/*# sourceMappingURL=GroupsList.vue.map */'] }, media: void 0 });
};
var __vue_scope_id__15 = "data-v-69cbfde9";
var __vue_module_identifier__15 = void 0;
var __vue_is_functional_template__15 = false;
function __vue_normalize__15(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
ul.c-group-list(v-if='groupsByName.length' data-test='groupsList')
  li.c-group-list-item.group-badge(:class='{ "is-active": isInGlobalDashboard }')
    tooltip(
      direction='right'
      :text='L("Global dashboard")'
    )
      button.c-group-picture.is-unstyled.c-global-dashboard-logo(@click='onGlobalDashboardClick')
        avatar.c-avatar(
          src='/assets/images/group-income-icon-transparent-circle.png'
          alt='Global dashboard logo'
        )
      badge(
        v-if='hasNewNews'
        type='compact'
        data-test='globalDashboardBadge'
      )

  li.c-group-list-item.group-badge(
    v-for='(group, index) in groupsByName'
    :key='\`group-\${index}\`'
    :class='{ "is-active": !isInGlobalDashboard && currentGroupId === group.contractID}'
  )
    tooltip(
      direction='right'
      :text='group.groupName'
    )
      button.c-group-picture.is-unstyled(@click='handleMenuSelect(group.contractID)')
        avatar.c-avatar(:src='groupPictureForContract(group.contractID)')
      badge(
        v-if='badgeVisiblePerGroup[group.contractID]'
        type='compact'
        :data-test='\`groupBadge-\${group.groupName}\`'
      )

  li.c-group-list-item
    tooltip(
      direction='right'
      :text='L("Create a new group")'
    )
      button(
        class='is-icon has-background'
        @click='openModal("GroupCreationModal")'
        data-test='createGroup'
        :aria-label='L("Add a group")'
      )
        i.icon-plus
</template>

<script>
import sbp from '@sbp/sbp'
import { mapGetters, mapState } from 'vuex'
import Avatar from '../../../../frontend/views/components/Avatar.vue'
import Badge from '../../../../frontend/views/components/Badge.vue'
import Tooltip from '../../../../frontend/views/components/Tooltip.vue'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import { fetchNews } from '../../../../frontend/views/utils/misc.js'

export default ({
  name: 'GroupsList',
  components: {
    Avatar,
    Badge,
    Tooltip
  },
  data () {
    return {
      ephemeral: {
        latestNewsDate: null
      }
    }
  },
  created () {
    this.checkForNewNews()
  },
  computed: {
    ...mapState([
      'currentGroupId'
    ]),
    ...mapGetters([
      'groupsByName',
      'groupUnreadMessages',
      'unreadGroupNotificationCountFor',
      'ourPreferences'
    ]),
    badgeVisiblePerGroup () {
      return Object.fromEntries(
        this.groupsByName.map(group => ([
          group.contractID,
          this.groupUnreadMessages(group.contractID) + this.unreadGroupNotificationCountFor(group.contractID) > 0
        ]))
      )
    },
    isInGlobalDashboard () {
      return this.$route.path.startsWith('/global-dashboard')
    },
    hasNewNews () {
      // Don't show badge if we're currently on the news page
      if (this.$route.path === '/global-dashboard/news-and-updates' ||
        !this.ourPreferences?.lastSeenNewsDate ||
        !this.ephemeral.latestNewsDate
      ) {
        return false
      }

      return new Date(this.ephemeral.latestNewsDate) > new Date(this.ourPreferences.lastSeenNewsDate)
    }
  },
  methods: {
    openModal (mode) {
      sbp('okTurtles.events/emit', OPEN_MODAL, mode)
    },
    handleMenuSelect (id) {
      id && this.changeGroup(id)

      if (this.isInGlobalDashboard) {
        this.$router.push('/dashboard')
      }
    },
    onGlobalDashboardClick () {
      this.$router.push(({ path: '/global-dashboard' }))
    },
    changeGroup (hash) {
      const path = this.$route.path
      const emptyGroupPicture = this.groupPictureForContract(hash) === ''
      if (hash !== this.currentGroupId) {
        sbp('gi.app/group/switch', hash)
      }
      if (emptyGroupPicture && path !== '/pending-approval') {
        this.$router.push(({ path: '/pending-approval' }))
      } else if (!emptyGroupPicture && path === '/pending-approval') {
        this.$router.push(({ path: '/dashboard' }))
      }
    },
    groupPictureForContract (contractID) {
      return this.$store.state[contractID]?.settings?.groupPicture || ''
    },
    async checkForNewNews () {
      try {
        const data = await fetchNews()
        if (data.length > 0) {
          this.ephemeral.latestNewsDate = data[0].createdAt
        }
      } catch (error) {
        console.error('Failed to check for new news:', error)
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-badge {
  top: 0;
  right: 1px; // Prevent badges from touching the vertical edge.
  z-index: 2; // Make badges appear above group avatars.
}

.c-group-list {
  width: 3.5rem;
  padding-top: 0.7rem;
  background-color: $general_1;
}

.c-group-list-item {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 3.2rem;
  margin-bottom: 0.25rem;
}

.group-badge {
  &::before {
    content: "";
    position: absolute;
    width: 3rem;
    height: 3rem;
    border: 1px solid transparent;
    border-radius: 50%;
    transform: scale(0.7);
    transition: all 0.25s cubic-bezier(0.18, 0.89, 0.32, 1.38);
  }

  &.is-active {
    &::before {
      transform: scale(1);
      border-color: $text_0;
      animation: spin 0.5s ease-out;
    }
  }

  &:not(.is-active) {
    cursor: pointer;

    &:focus::before,
    &:hover::before {
      border-color: $white;
      border-width: 3px;
      transform: scale(1.1);
    }
  }
}

@keyframes spin {
  0% {
    transform: rotate(45deg);
    filter: hue-rotate(0deg);
    border-color: $primary_0 transparent transparent;
  }

  50% {
    transform: rotate(315deg);
    filter: hue-rotate(360deg);
    border-color: $primary_0 transparent transparent;
  }

  100% {
    transform: rotate(585deg);
    border-color: $white;
  }
}

.c-avatar {
  position: relative;
  z-index: 1;
}

.c-group-picture {
  display: flex;

  &.c-global-dashboard-logo {
    background-color: $background_0;
    border-radius: 50%;
    width: 2.5rem;
    height: 2.5rem;

    ::v-deep img.c-avatar {
      width: 1.75rem;
      height: 1.75rem;
    }
  }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__13() {
  const styles = __vue_create_injector__13.styles || (__vue_create_injector__13.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__15 = /* @__PURE__ */ __vue_normalize__15(
  { render: __vue_render__15, staticRenderFns: __vue_staticRenderFns__15 },
  __vue_inject_styles__15,
  __vue_script__15,
  __vue_scope_id__15,
  __vue_is_functional_template__15,
  __vue_module_identifier__15,
  false,
  __vue_create_injector__13,
  void 0,
  void 0
);
var GroupsList_default = __vue_component__15;

// frontend/views/containers/navigation/Profile.vue
var __vue_script__16 = {
  name: "Profile",
  components: {
    AvatarUser: AvatarUser_default,
    ProfileCard: ProfileCard_default
  },
  computed: {
    ...mapGetters([
      "ourUsername",
      "ourIdentityContractId"
    ]),
    ourDisplayName() {
      const userContract = this.$store.getters.currentIdentityState;
      return userContract && userContract.attributes && userContract.attributes.displayName;
    }
  },
  methods: {
    openModalUserSettings() {
      esm_default("okTurtles.events/emit", OPEN_MODAL, "UserSettingsModal");
    }
  }
};
var __vue_render__16 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _vm.$store.state.loggedIn ? _c(
    "div",
    { staticClass: "c-profile", attrs: { "data-test": "userProfile" } },
    [
      _c(
        "profile-card",
        {
          attrs: {
            contractID: _vm.ourIdentityContractId,
            direction: "top-left"
          }
        },
        [
          _c(
            "button",
            {
              staticClass: "is-unstyled c-avatar-user",
              attrs: { "data-test": "openProfileCard" }
            },
            [
              _c("avatar-user", {
                attrs: {
                  contractID: _vm.ourIdentityContractId,
                  size: "sm"
                }
              }),
              _c("div", { staticClass: "c-user" }, [
                _c(
                  "strong",
                  {
                    staticClass: "has-ellipsis",
                    attrs: {
                      "data-test": _vm.ourDisplayName ? "profileDisplayName" : "profileName"
                    }
                  },
                  [
                    _vm._v(
                      _vm._s(
                        _vm.ourDisplayName || _vm.ourUsername || _vm.ourIdentityContractId
                      )
                    )
                  ]
                ),
                _vm.ourDisplayName ? _c(
                  "span",
                  {
                    staticClass: "has-ellipsis",
                    attrs: { "data-test": "profileName" }
                  },
                  [_vm._v("@" + _vm._s(_vm.ourUsername))]
                ) : _vm._e()
              ])
            ],
            1
          )
        ]
      ),
      _c(
        "button",
        {
          staticClass: "is-icon-small",
          attrs: { "data-test": "settingsBtn" },
          on: { click: _vm.openModalUserSettings }
        },
        [_c("i", { staticClass: "icon-cog" })]
      )
    ],
    1
  ) : _vm._e();
};
var __vue_staticRenderFns__16 = [];
__vue_render__16._withStripped = true;
var __vue_inject_styles__16 = function(inject) {
  if (!inject) return;
  inject("data-v-329698fa_0", { source: '.c-profile[data-v-329698fa] {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  height: 4rem;\n  padding: 0 1rem;\n}\n.c-avatar-user[data-v-329698fa] {\n  display: flex;\n  align-items: center;\n}\n.c-avatar-user:focus .c-user strong[data-v-329698fa], .c-avatar-user:hover .c-user strong[data-v-329698fa] {\n  border-bottom-color: var(--text_0);\n}\n.c-user[data-v-329698fa] {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  margin-left: 0.5rem;\n  max-width: 7rem;\n  white-space: nowrap;\n  line-height: 1.3;\n  margin-top: 1px;\n}\n.c-user strong[data-v-329698fa] {\n  border-bottom: 1px solid transparent;\n  width: 100%;\n}\n.c-user span[data-v-329698fa] {\n  font-family: "Lato";\n  color: var(--text_1);\n  width: 100%;\n}\nbutton .icon-cog[data-v-329698fa] {\n  font-size: 0.875rem;\n  transform-origin: 49% 48%;\n}\nbutton:hover .icon-cog[data-v-329698fa], button:focus .icon-cog[data-v-329698fa] {\n  animation: cog-data-v-329698fa 400ms forwards;\n}\n@keyframes cog-data-v-329698fa {\nto {\n    transform: rotate(180deg);\n}\n}\n\n/*# sourceMappingURL=Profile.vue.map */', map: { "version": 3, "sources": ["frontend/views/containers/navigation/Profile.vue", "Profile.vue"], "names": [], "mappings": "AA2DA;EACA,aAAA;EACA,8BAAA;EACA,mBAAA;EACA,YAAA;EACA,eAAA;AC1DA;AD6DA;EACA,aAAA;EACA,mBAAA;AC1DA;AD8DA;EACA,kCAAA;AC5DA;ADiEA;EACA,aAAA;EACA,sBAAA;EACA,uBAAA;EACA,mBAAA;EACA,eAAA;EACA,mBAAA;EACA,gBAAA;EACA,eAAA;AC9DA;ADgEA;EACA,oCAAA;EACA,WAAA;AC9DA;ADiEA;EACA,mBAAA;EACA,oBAAA;EACA,WAAA;AC/DA;ADoEA;EACA,mBAAA;EACA,yBAAA;ACjEA;ADoEA;EAEA,6CAAA;ACnEA;ADuEA;AACA;IAAA,yBAAA;ACnEE;AACF;;AAEA,sCAAsC", "file": "Profile.vue", "sourcesContent": [`<template lang='pug'>
.c-profile(
  v-if='$store.state.loggedIn'
  data-test='userProfile'
)
  profile-card(:contractID='ourIdentityContractId' direction='top-left')
    button.is-unstyled.c-avatar-user(data-test='openProfileCard')
      avatar-user(:contractID='ourIdentityContractId' size='sm')
      .c-user
        strong.has-ellipsis(
          :data-test='ourDisplayName ? "profileDisplayName" : "profileName"'
        ) {{ ourDisplayName || ourUsername || ourIdentityContractId }}

        span.has-ellipsis(
          data-test='profileName'
          v-if='ourDisplayName'
        ) @{{ ourUsername }}

  button.is-icon-small(
    data-test='settingsBtn'
    @click='openModalUserSettings'
  )
    i.icon-cog
</template>

<script>
import sbp from '@sbp/sbp'
import AvatarUser from '../../../../frontend/views/components/AvatarUser.vue'
import ProfileCard from '../../../../frontend/views/components/ProfileCard.vue'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import { mapGetters } from 'vuex'

export default ({
  name: 'Profile',
  components: {
    AvatarUser,
    ProfileCard
  },
  computed: {
    ...mapGetters([
      'ourUsername',
      'ourIdentityContractId'
    ]),
    ourDisplayName () {
      const userContract = this.$store.getters.currentIdentityState
      return userContract && userContract.attributes && userContract.attributes.displayName
    }
  },
  methods: {
    openModalUserSettings () {
      sbp('okTurtles.events/emit', OPEN_MODAL, 'UserSettingsModal')
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-profile {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 4rem;
  padding: 0 1rem;
}

.c-avatar-user {
  display: flex;
  align-items: center;

  &:focus,
  &:hover {
    .c-user strong {
      border-bottom-color: $text_0;
    }
  }
}

.c-user {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  margin-left: 0.5rem;
  max-width: 7rem;
  white-space: nowrap;
  line-height: 1.3;
  margin-top: 1px;

  strong {
    border-bottom: 1px solid transparent;
    width: 100%;
  }

  span {
    font-family: "Lato";
    color: $text_1;
    width: 100%;
  }
}

button {
  .icon-cog {
    font-size: $size_4;
    transform-origin: 49% 48%;
  }

  &:hover .icon-cog,
  &:focus .icon-cog {
    animation: cog 400ms forwards;
  }
}

@keyframes cog {
  to { transform: rotate(180deg); }
}
</style>
`, '.c-profile {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  height: 4rem;\n  padding: 0 1rem;\n}\n\n.c-avatar-user {\n  display: flex;\n  align-items: center;\n}\n.c-avatar-user:focus .c-user strong, .c-avatar-user:hover .c-user strong {\n  border-bottom-color: var(--text_0);\n}\n\n.c-user {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  margin-left: 0.5rem;\n  max-width: 7rem;\n  white-space: nowrap;\n  line-height: 1.3;\n  margin-top: 1px;\n}\n.c-user strong {\n  border-bottom: 1px solid transparent;\n  width: 100%;\n}\n.c-user span {\n  font-family: "Lato";\n  color: var(--text_1);\n  width: 100%;\n}\n\nbutton .icon-cog {\n  font-size: 0.875rem;\n  transform-origin: 49% 48%;\n}\nbutton:hover .icon-cog, button:focus .icon-cog {\n  animation: cog 400ms forwards;\n}\n\n@keyframes cog {\n  to {\n    transform: rotate(180deg);\n  }\n}\n\n/*# sourceMappingURL=Profile.vue.map */'] }, media: void 0 });
};
var __vue_scope_id__16 = "data-v-329698fa";
var __vue_module_identifier__16 = void 0;
var __vue_is_functional_template__16 = false;
function __vue_normalize__16(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
.c-profile(
  v-if='$store.state.loggedIn'
  data-test='userProfile'
)
  profile-card(:contractID='ourIdentityContractId' direction='top-left')
    button.is-unstyled.c-avatar-user(data-test='openProfileCard')
      avatar-user(:contractID='ourIdentityContractId' size='sm')
      .c-user
        strong.has-ellipsis(
          :data-test='ourDisplayName ? "profileDisplayName" : "profileName"'
        ) {{ ourDisplayName || ourUsername || ourIdentityContractId }}

        span.has-ellipsis(
          data-test='profileName'
          v-if='ourDisplayName'
        ) @{{ ourUsername }}

  button.is-icon-small(
    data-test='settingsBtn'
    @click='openModalUserSettings'
  )
    i.icon-cog
</template>

<script>
import sbp from '@sbp/sbp'
import AvatarUser from '../../../../frontend/views/components/AvatarUser.vue'
import ProfileCard from '../../../../frontend/views/components/ProfileCard.vue'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import { mapGetters } from 'vuex'

export default ({
  name: 'Profile',
  components: {
    AvatarUser,
    ProfileCard
  },
  computed: {
    ...mapGetters([
      'ourUsername',
      'ourIdentityContractId'
    ]),
    ourDisplayName () {
      const userContract = this.$store.getters.currentIdentityState
      return userContract && userContract.attributes && userContract.attributes.displayName
    }
  },
  methods: {
    openModalUserSettings () {
      sbp('okTurtles.events/emit', OPEN_MODAL, 'UserSettingsModal')
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-profile {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 4rem;
  padding: 0 1rem;
}

.c-avatar-user {
  display: flex;
  align-items: center;

  &:focus,
  &:hover {
    .c-user strong {
      border-bottom-color: $text_0;
    }
  }
}

.c-user {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  margin-left: 0.5rem;
  max-width: 7rem;
  white-space: nowrap;
  line-height: 1.3;
  margin-top: 1px;

  strong {
    border-bottom: 1px solid transparent;
    width: 100%;
  }

  span {
    font-family: "Lato";
    color: $text_1;
    width: 100%;
  }
}

button {
  .icon-cog {
    font-size: $size_4;
    transform-origin: 49% 48%;
  }

  &:hover .icon-cog,
  &:focus .icon-cog {
    animation: cog 400ms forwards;
  }
}

@keyframes cog {
  to { transform: rotate(180deg); }
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__14() {
  const styles = __vue_create_injector__14.styles || (__vue_create_injector__14.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__16 = /* @__PURE__ */ __vue_normalize__16(
  { render: __vue_render__16, staticRenderFns: __vue_staticRenderFns__16 },
  __vue_inject_styles__16,
  __vue_script__16,
  __vue_scope_id__16,
  __vue_is_functional_template__16,
  __vue_module_identifier__16,
  false,
  __vue_create_injector__14,
  void 0,
  void 0
);
var Profile_default = __vue_component__16;

// frontend/views/containers/navigation/Navigation.vue
var __vue_script__17 = {
  name: "Navigation",
  mixins: [showNavMixin],
  components: {
    Badge: Badge_default,
    Toggle: Toggle_default,
    NotificationBell: NotificationBell_default,
    GroupsList: GroupsList_default,
    Profile: Profile_default,
    ListItem: ListItem_default
  },
  data() {
    return {
      config: {
        debounceResize: debounce(this.checkIsTouch, 250),
        globalDashboard: Object.entries(GLOBAL_DASHBOARD_SETTINGS).map(([key, entry]) => ({ ...entry, id: key }))
      },
      ephemeral: {
        isActive: false,
        isTouch: null,
        latestNewsDate: null
      }
    };
  },
  created() {
    this.checkIsTouch();
    this.checkForNewNews();
  },
  mounted() {
    window.addEventListener("resize", this.config.debounceResize);
  },
  beforeDestroy() {
    window.removeEventListener("resize", this.config.debounceResize);
  },
  watch: {
    $route(to, from) {
      const isDifferentPage = from.path !== to.path;
      if (this.ephemeral.isActive && isDifferentPage) {
        this.ephemeral.isActive = false;
      }
    }
  },
  computed: {
    ...mapState(["currentGroupId", "loggedIn"]),
    ...mapGetters([
      "groupsByName",
      "colors",
      "totalUnreadNotificationCount",
      "groupUnreadMessages",
      "ourPreferences"
    ]),
    currentGroupUnreadMessagesCount() {
      return !this.currentGroupId ? 0 : this.groupUnreadMessages(this.currentGroupId);
    },
    logo() {
      const name = this.colors.theme === "dark" ? "-white" : "";
      return `/assets/images/logo-transparent${name}.png`;
    },
    isInert() {
      return !this.ephemeral.isActive && this.ephemeral.isTouch;
    },
    notApprovedToGroupYet() {
      return this.$route.path === "/pending-approval";
    },
    isInGlobalDashboard() {
      return this.$route.path.startsWith("/global-dashboard");
    },
    hasNewNews() {
      if (this.$route.path === "/global-dashboard/news-and-updates") {
        return false;
      }
      const lastSeenNewsDate = this.ourPreferences?.lastSeenNewsDate;
      if (!lastSeenNewsDate || !this.ephemeral.latestNewsDate) {
        return false;
      }
      const lastSeen = new Date(lastSeenNewsDate);
      const latest = new Date(this.ephemeral.latestNewsDate);
      return latest > lastSeen;
    }
  },
  methods: {
    toggleMenu() {
      this.ephemeral.isActive = !this.ephemeral.isActive;
    },
    openModal(mode) {
      esm_default("okTurtles.events/emit", OPEN_MODAL, mode);
    },
    checkIsTouch() {
      this.ephemeral.isTouch = window.innerWidth < DESKTOP;
    },
    onMenuItemsClick() {
      this.ephemeral.isActive = false;
    },
    async checkForNewNews() {
      try {
        const data = await fetchNews();
        if (data.length > 0) {
          this.ephemeral.latestNewsDate = data[0].createdAt;
        }
      } catch (error) {
        console.error("Failed to check for new news:", error);
      }
    }
  }
};
var __vue_render__17 = function() {
  var _vm = this;
  var _h = _vm.$createElement;
  var _c = _vm._self._c || _h;
  return _c(
    "nav",
    {
      staticClass: "c-navigation",
      class: { "is-active": _vm.ephemeral.isActive },
      attrs: { "aria-label": _vm.L("Main"), "data-test": "navMenu" }
    },
    [
      _c(
        "toggle",
        {
          attrs: {
            element: "navigation",
            "aria-expanded": _vm.ephemeral.isActive,
            "data-test": "NavigationToggleBtn"
          },
          on: { toggle: _vm.toggleMenu }
        },
        [
          _vm.totalUnreadNotificationCount ? _c(
            "badge",
            {
              staticClass: "c-toggle-badge",
              attrs: { "data-test": "dashboardBadge" }
            },
            [_vm._v(_vm._s(_vm.totalUnreadNotificationCount))]
          ) : _vm.currentGroupUnreadMessagesCount ? _c(
            "badge",
            {
              staticClass: "c-toggle-badge",
              attrs: { "data-test": "dashboardBadge" }
            },
            [_vm._v(_vm._s(_vm.currentGroupUnreadMessagesCount))]
          ) : _vm._e()
        ],
        1
      ),
      _vm.groupsByName.length >= 1 ? _c("groups-list", { attrs: { inert: _vm.isInert } }) : _vm._e(),
      _c(
        "div",
        { staticClass: "c-navigation-wrapper", attrs: { inert: _vm.isInert } },
        [
          _vm.isInGlobalDashboard ? [
            _c(
              "div",
              { staticClass: "c-navigation-header" },
              [
                _c(
                  "i18n",
                  { staticClass: "sr-only", attrs: { tag: "h1" } },
                  [_vm._v("Global Dashboard Menu")]
                ),
                _c("img", {
                  staticClass: "c-logo",
                  attrs: { src: _vm.logo, alt: "GroupIncome's logo" }
                })
              ],
              1
            ),
            _c("div", { staticClass: "c-navigation-body" }, [
              _c("div", { staticClass: "c-navigation-body-top" }, [
                _c(
                  "ul",
                  { staticClass: "c-menu-list" },
                  _vm._l(_vm.config.globalDashboard, function(entry) {
                    return _c(
                      "list-item",
                      {
                        key: entry.id,
                        attrs: {
                          tag: "router-link",
                          icon: entry.icon,
                          to: entry.routeTo,
                          badgeCount: entry.id === "news-and-updates" && _vm.hasNewNews ? 1 : 0,
                          "data-test": "global-dashboard_" + entry.id
                        }
                      },
                      [_vm._v(_vm._s(entry.title))]
                    );
                  }),
                  1
                )
              ])
            ])
          ] : [
            _c(
              "div",
              { staticClass: "c-navigation-header" },
              [
                _c(
                  "i18n",
                  { staticClass: "sr-only", attrs: { tag: "h1" } },
                  [_vm._v("Main Menu")]
                ),
                _c("router-link", { attrs: { to: "/home" } }, [
                  _c("img", {
                    staticClass: "c-logo",
                    attrs: { src: _vm.logo, alt: "GroupIncome's logo" }
                  })
                ]),
                !_vm.notApprovedToGroupYet ? _c("notification-bell", {
                  attrs: { "data-test": "notificationBell" }
                }) : _vm._e()
              ],
              1
            ),
            !_vm.notApprovedToGroupYet && _vm.groupsByName.length ? _c(
              "div",
              {
                staticClass: "c-navigation-body",
                on: {
                  click: function($event) {
                    $event.stopPropagation();
                    return _vm.onMenuItemsClick($event);
                  }
                }
              },
              [
                _c("div", { staticClass: "c-navigation-body-top" }, [
                  _c(
                    "ul",
                    { staticClass: "c-menu-list" },
                    [
                      _c(
                        "list-item",
                        {
                          attrs: {
                            tag: "router-link",
                            icon: "columns",
                            to: "/dashboard",
                            "data-test": "dashboard"
                          }
                        },
                        [_c("i18n", [_vm._v("Dashboard")])],
                        1
                      ),
                      _c(
                        "list-item",
                        {
                          attrs: {
                            tag: "router-link",
                            icon: "chart-pie",
                            to: "/contributions",
                            "data-test": "contributionsLink"
                          }
                        },
                        [_c("i18n", [_vm._v("Contributions")])],
                        1
                      ),
                      _c(
                        "list-item",
                        {
                          attrs: {
                            tag: "router-link",
                            icon: "tag",
                            to: "/payments",
                            "data-test": "paymentsLink"
                          }
                        },
                        [_c("i18n", [_vm._v("Payments")])],
                        1
                      ),
                      _c(
                        "list-item",
                        {
                          attrs: {
                            tag: "router-link",
                            icon: "comments",
                            to: "/group-chat",
                            badgeCount: _vm.currentGroupUnreadMessagesCount,
                            "data-test": "groupChatLink"
                          }
                        },
                        [_c("i18n", [_vm._v("Chat")])],
                        1
                      ),
                      _c(
                        "list-item",
                        {
                          attrs: {
                            tag: "router-link",
                            icon: "cog",
                            to: "/group-settings",
                            "data-test": "groupSettingsLink"
                          }
                        },
                        [_c("i18n", [_vm._v("Group Settings")])],
                        1
                      )
                    ],
                    1
                  ),
                  _vm.groupsByName.length < 2 ? _c(
                    "div",
                    { staticClass: "c-navigation-separator" },
                    [
                      _c(
                        "button",
                        {
                          staticClass: "is-small is-outlined",
                          attrs: {
                            "data-test": "createGroup",
                            "aria-label": _vm.L("Add a group")
                          },
                          on: {
                            click: function($event) {
                              return _vm.openModal(
                                "GroupCreationModal"
                              );
                            }
                          }
                        },
                        [
                          _c("i", {
                            staticClass: "icon-plus is-prefix"
                          }),
                          _c("i18n", [_vm._v("Add a group")])
                        ],
                        1
                      )
                    ]
                  ) : _vm._e()
                ]),
                _c("div", { staticClass: "c-navigation-bottom" }, [
                  _c(
                    "ul",
                    { staticClass: "c-menu-list-bottom" },
                    [
                      _c(
                        "i18n",
                        {
                          attrs: {
                            tag: "a",
                            href: _vm.ALLOWED_URLS.BLOG_PAGE,
                            target: "_blank",
                            rel: "noopener noreferrer"
                          }
                        },
                        [_vm._v("Blog")]
                      ),
                      _c(
                        "i18n",
                        {
                          attrs: {
                            tag: "a",
                            href: _vm.ALLOWED_URLS.COMMUNITY_PAGE,
                            target: "_blank",
                            rel: "noopener noreferrer"
                          }
                        },
                        [_vm._v("Help & Feedback")]
                      ),
                      _c(
                        "i18n",
                        {
                          attrs: {
                            tag: "a",
                            href: _vm.ALLOWED_URLS.DONATE_PAGE,
                            target: "_blank",
                            rel: "noopener noreferrer"
                          }
                        },
                        [_vm._v("Donate")]
                      )
                    ],
                    1
                  )
                ])
              ]
            ) : _vm._e()
          ],
          _vm.showNav ? _c(
            "div",
            { staticClass: "c-navigation-footer" },
            [_c("profile")],
            1
          ) : _vm._e()
        ],
        2
      )
    ],
    1
  );
};
var __vue_staticRenderFns__17 = [];
__vue_render__17._withStripped = true;
var __vue_inject_styles__17 = function(inject) {
  if (!inject) return;
  inject("data-v-7f7f735e_0", { source: ".c-navigation[data-v-7f7f735e] {\n  display: flex;\n  flex-direction: row;\n  font-weight: normal;\n  background: var(--general_2);\n}\n.c-navigation-wrapper[data-v-7f7f735e] {\n  display: flex;\n  flex-direction: column;\n  min-width: 14.375rem;\n  justify-content: space-between;\n  height: 100%;\n}\n.c-navigation-header[data-v-7f7f735e] {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  flex-shrink: 0;\n  padding: 0 1rem;\n  height: 4.6rem;\n  margin-bottom: -0.1rem;\n}\n.c-navigation-body[data-v-7f7f735e] {\n  display: flex;\n  overflow: auto;\n  -webkit-overflow-scrolling: touch;\n  flex-direction: column;\n  justify-content: space-between;\n  flex-grow: 1;\n}\n.c-navigation-separator[data-v-7f7f735e] {\n  text-align: center;\n  margin: 1.5rem 1.5rem 0 1.5rem;\n  border-top: 1px solid var(--general_0);\n  padding-top: 1.5rem;\n}\n.c-navigation-bottom[data-v-7f7f735e] {\n  padding-top: 2rem;\n  padding-bottom: 0.5rem;\n}\n.c-navigation-footer[data-v-7f7f735e] {\n  flex-shrink: 0;\n}\n.c-menu-list-bottom[data-v-7f7f735e] {\n  display: flex;\n  flex-direction: column;\n  margin-left: 1rem;\n  font-size: 0.75rem;\n}\n.c-menu-list-bottom a[data-v-7f7f735e] {\n  line-height: 1.65rem;\n  color: var(--text_1);\n}\n.c-menu-list-bottom a[data-v-7f7f735e]:hover, .c-menu-list-bottom a[data-v-7f7f735e]:focus {\n  color: var(--text_0);\n  outline: none;\n}\n.c-logo[data-v-7f7f735e] {\n  min-width: 8rem;\n  width: 8rem;\n}\n.c-toggle[data-v-7f7f735e] {\n  left: 100%;\n}\n\n/*# sourceMappingURL=Navigation.vue.map */", map: { "version": 3, "sources": ["frontend/views/containers/navigation/Navigation.vue", "Navigation.vue"], "names": [], "mappings": "AA4OA;EACA,aAAA;EACA,mBAAA;EACA,mBAAA;EACA,4BAAA;AC3OA;AD8OA;EACA,aAAA;EACA,sBAAA;EACA,oBAAA;EACA,8BAAA;EACA,YAAA;AC3OA;AD8OA;EACA,aAAA;EACA,8BAAA;EACA,mBAAA;EACA,cAAA;EACA,eAAA;EACA,cAAA;EACA,sBAAA;AC3OA;AD8OA;EACA,aAAA;EACA,cAAA;EACA,iCAAA;EACA,sBAAA;EACA,8BAAA;EACA,YAAA;AC3OA;AD8OA;EACA,kBAAA;EACA,8BAAA;EACA,sCAAA;EACA,mBAAA;AC3OA;AD8OA;EACA,iBAAA;EACA,sBAAA;AC3OA;AD8OA;EACA,cAAA;AC3OA;AD8OA;EACA,aAAA;EACA,sBAAA;EACA,iBAAA;EACA,kBAAA;AC3OA;AD6OA;EACA,oBAAA;EACA,oBAAA;AC3OA;AD6OA;EAEA,oBAAA;EACA,aAAA;AC5OA;ADiPA;EACA,eAAA;EACA,WAAA;AC9OA;ADiPA;EACA,UAAA;AC9OA;;AAEA,yCAAyC", "file": "Navigation.vue", "sourcesContent": [`<template lang='pug'>
nav.c-navigation(
  :aria-label='L("Main")'
  :class='{ "is-active": ephemeral.isActive }'
  data-test='navMenu'
)
  toggle(@toggle='toggleMenu' element='navigation' :aria-expanded='ephemeral.isActive' data-test='NavigationToggleBtn')
    badge.c-toggle-badge(v-if='totalUnreadNotificationCount' data-test='dashboardBadge') {{ totalUnreadNotificationCount }}
    badge.c-toggle-badge(v-else-if='currentGroupUnreadMessagesCount' data-test='dashboardBadge') {{ currentGroupUnreadMessagesCount }}
  groups-list(v-if='groupsByName.length >= 1' :inert='isInert')

  .c-navigation-wrapper(:inert='isInert')
    template(v-if='isInGlobalDashboard')
      .c-navigation-header
        i18n.sr-only(tag='h1') Global Dashboard Menu

        img.c-logo(:src='logo' alt='GroupIncome\\'s logo')

      .c-navigation-body
        .c-navigation-body-top
          ul.c-menu-list
            list-item(v-for='entry in config.globalDashboard'
              tag='router-link'
              :key='entry.id'
              :icon='entry.icon'
              :to='entry.routeTo'
              :badgeCount='entry.id === "news-and-updates" && hasNewNews ? 1 : 0'
              :data-test='"global-dashboard_" + entry.id'
            ) {{ entry.title }}

    template(v-else)
      .c-navigation-header
        i18n.sr-only(tag='h1') Main Menu

        router-link(to='/home')
          img.c-logo(:src='logo' alt='GroupIncome\\'s logo')

        notification-bell(v-if='!notApprovedToGroupYet' data-test='notificationBell')

      .c-navigation-body(
        @click.stop='onMenuItemsClick'
        v-if='!notApprovedToGroupYet && groupsByName.length'
      )
        .c-navigation-body-top
          ul.c-menu-list
            list-item(tag='router-link' icon='columns' to='/dashboard' data-test='dashboard')
              i18n Dashboard
            list-item(tag='router-link' icon='chart-pie' to='/contributions' data-test='contributionsLink')
              i18n Contributions
            list-item(tag='router-link' icon='tag' to='/payments' data-test='paymentsLink')
              i18n Payments
            list-item(
              tag='router-link'
              icon='comments'
              to='/group-chat'
              :badgeCount='currentGroupUnreadMessagesCount'
              data-test='groupChatLink'
            )
              i18n Chat
            list-item(tag='router-link' icon='cog' to='/group-settings' data-test='groupSettingsLink')
              i18n Group Settings

          .c-navigation-separator(v-if='groupsByName.length < 2')
            button(
              class='is-small is-outlined'
              @click='openModal("GroupCreationModal")'
              data-test='createGroup'
              :aria-label='L("Add a group")'
            )
              i.icon-plus.is-prefix
              i18n Add a group

        .c-navigation-bottom
          ul.c-menu-list-bottom
            i18n(
              tag='a'
              :href='ALLOWED_URLS.BLOG_PAGE'
              target='_blank'
              rel='noopener noreferrer'
            ) Blog

            i18n(
              tag='a'
              :href='ALLOWED_URLS.COMMUNITY_PAGE'
              target='_blank'
              rel='noopener noreferrer'
            ) Help &amp; Feedback

            i18n(
              tag='a'
              :href='ALLOWED_URLS.DONATE_PAGE'
              target='_blank'
              rel='noopener noreferrer'
            ) Donate

    .c-navigation-footer(v-if='showNav')
      profile
</template>

<script>
import sbp from '@sbp/sbp'
import Badge from '../../../../frontend/views/components/Badge.vue'
import NotificationBell from '../../../../frontend/views/containers/notifications/NotificationBell.vue'
import GroupsList from './GroupsList.vue'
import Profile from './Profile.vue'
import Toggle from '../../../../frontend/views/components/Toggle.vue'
import ListItem from '../../../../frontend/views/components/ListItem.vue'
import { mapState, mapGetters } from 'vuex'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import { DESKTOP } from '../../../../frontend/views/utils/breakpoints.js'
import { showNavMixin, fetchNews } from '../../../../frontend/views/utils/misc.js'
import { GLOBAL_DASHBOARD_SETTINGS } from '../../../../frontend/views/pages/GlobalDashboard.vue'
import { debounce } from 'turtledash'

export default ({
  name: 'Navigation',
  mixins: [showNavMixin],
  components: {
    Badge,
    Toggle,
    NotificationBell,
    GroupsList,
    Profile,
    ListItem
  },
  data () {
    return {
      config: {
        debounceResize: debounce(this.checkIsTouch, 250),
        globalDashboard: Object.entries(GLOBAL_DASHBOARD_SETTINGS)
          .map(([key, entry]) => ({ ...entry, id: key }))
      },
      ephemeral: {
        isActive: false,
        isTouch: null,
        latestNewsDate: null
      }
    }
  },
  created () {
    this.checkIsTouch()
    this.checkForNewNews()
  },
  mounted () {
    // TODO - Create a single resize listener to be reused on components
    window.addEventListener('resize', this.config.debounceResize)
  },
  beforeDestroy () {
    window.removeEventListener('resize', this.config.debounceResize)
  },
  watch: {
    $route (to, from) {
      const isDifferentPage = from.path !== to.path
      if (this.ephemeral.isActive && isDifferentPage) {
        this.ephemeral.isActive = false
      }
    }
  },
  computed: {
    ...mapState(['currentGroupId', 'loggedIn']),
    ...mapGetters([
      'groupsByName',
      'colors',
      'totalUnreadNotificationCount',
      'groupUnreadMessages',
      'ourPreferences'
    ]),
    currentGroupUnreadMessagesCount () {
      return !this.currentGroupId ? 0 : this.groupUnreadMessages(this.currentGroupId)
    },
    logo () {
      const name = this.colors.theme === 'dark' ? '-white' : ''
      return \`/assets/images/logo-transparent\${name}.png\`
    },
    isInert () {
      return !this.ephemeral.isActive && this.ephemeral.isTouch
    },
    notApprovedToGroupYet () {
      // TODO: once the relevant work is implemented on back-end, this check logic needs to be updated accordingly
      return this.$route.path === '/pending-approval'
    },
    isInGlobalDashboard () {
      return this.$route.path.startsWith('/global-dashboard')
    },
    hasNewNews () {
      // Don't show badge if we're currently on the news page
      if (this.$route.path === '/global-dashboard/news-and-updates') {
        return false
      }

      // Check if there's a stored last seen date
      const lastSeenNewsDate = this.ourPreferences?.lastSeenNewsDate

      // If no last seen date is stored, don't show badge
      if (!lastSeenNewsDate || !this.ephemeral.latestNewsDate) {
        return false
      }

      // Compare the latest news date with the last seen date
      const lastSeen = new Date(lastSeenNewsDate)
      const latest = new Date(this.ephemeral.latestNewsDate)

      return latest > lastSeen
    }
  },
  methods: {
    toggleMenu () {
      this.ephemeral.isActive = !this.ephemeral.isActive
    },
    openModal (mode) {
      sbp('okTurtles.events/emit', OPEN_MODAL, mode)
    },
    checkIsTouch () {
      this.ephemeral.isTouch = window.innerWidth < DESKTOP
    },
    onMenuItemsClick () {
      // Close the menu when a menu item is clicked.
      this.ephemeral.isActive = false
    },
    async checkForNewNews () {
      try {
        const data = await fetchNews()
        if (data.length > 0) {
          this.ephemeral.latestNewsDate = data[0].createdAt
        }
      } catch (error) {
        console.error('Failed to check for new news:', error)
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-navigation {
  display: flex;
  flex-direction: row;
  font-weight: normal;
  background: $general_2;
}

.c-navigation-wrapper {
  display: flex;
  flex-direction: column;
  min-width: 14.375rem;
  justify-content: space-between;
  height: 100%;
}

.c-navigation-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-shrink: 0;
  padding: 0 1rem;
  height: 4.6rem;
  margin-bottom: -0.1rem;
}

.c-navigation-body {
  display: flex;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
  flex-direction: column;
  justify-content: space-between;
  flex-grow: 1;
}

.c-navigation-separator {
  text-align: center;
  margin: 1.5rem 1.5rem 0 1.5rem;
  border-top: 1px solid $general_0;
  padding-top: 1.5rem;
}

.c-navigation-bottom {
  padding-top: 2rem;
  padding-bottom: 0.5rem;
}

.c-navigation-footer {
  flex-shrink: 0;
}

.c-menu-list-bottom {
  display: flex;
  flex-direction: column;
  margin-left: 1rem;
  font-size: $size_5;

  a {
    line-height: 1.65rem;
    color: $text_1;

    &:hover,
    &:focus {
      color: $text_0;
      outline: none;
    }
  }
}

.c-logo {
  min-width: 8rem;
  width: 8rem;
}

.c-toggle {
  left: 100%;
}
</style>
`, ".c-navigation {\n  display: flex;\n  flex-direction: row;\n  font-weight: normal;\n  background: var(--general_2);\n}\n\n.c-navigation-wrapper {\n  display: flex;\n  flex-direction: column;\n  min-width: 14.375rem;\n  justify-content: space-between;\n  height: 100%;\n}\n\n.c-navigation-header {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  flex-shrink: 0;\n  padding: 0 1rem;\n  height: 4.6rem;\n  margin-bottom: -0.1rem;\n}\n\n.c-navigation-body {\n  display: flex;\n  overflow: auto;\n  -webkit-overflow-scrolling: touch;\n  flex-direction: column;\n  justify-content: space-between;\n  flex-grow: 1;\n}\n\n.c-navigation-separator {\n  text-align: center;\n  margin: 1.5rem 1.5rem 0 1.5rem;\n  border-top: 1px solid var(--general_0);\n  padding-top: 1.5rem;\n}\n\n.c-navigation-bottom {\n  padding-top: 2rem;\n  padding-bottom: 0.5rem;\n}\n\n.c-navigation-footer {\n  flex-shrink: 0;\n}\n\n.c-menu-list-bottom {\n  display: flex;\n  flex-direction: column;\n  margin-left: 1rem;\n  font-size: 0.75rem;\n}\n.c-menu-list-bottom a {\n  line-height: 1.65rem;\n  color: var(--text_1);\n}\n.c-menu-list-bottom a:hover, .c-menu-list-bottom a:focus {\n  color: var(--text_0);\n  outline: none;\n}\n\n.c-logo {\n  min-width: 8rem;\n  width: 8rem;\n}\n\n.c-toggle {\n  left: 100%;\n}\n\n/*# sourceMappingURL=Navigation.vue.map */"] }, media: void 0 });
};
var __vue_scope_id__17 = "data-v-7f7f735e";
var __vue_module_identifier__17 = void 0;
var __vue_is_functional_template__17 = false;
function __vue_normalize__17(template, style, script, scope, functional, moduleIdentifier, shadowMode, createInjector, createInjectorSSR, createInjectorShadow) {
  const component = (typeof script === "function" ? script.options : script) || {};
  component.__file = `<template lang='pug'>
nav.c-navigation(
  :aria-label='L("Main")'
  :class='{ "is-active": ephemeral.isActive }'
  data-test='navMenu'
)
  toggle(@toggle='toggleMenu' element='navigation' :aria-expanded='ephemeral.isActive' data-test='NavigationToggleBtn')
    badge.c-toggle-badge(v-if='totalUnreadNotificationCount' data-test='dashboardBadge') {{ totalUnreadNotificationCount }}
    badge.c-toggle-badge(v-else-if='currentGroupUnreadMessagesCount' data-test='dashboardBadge') {{ currentGroupUnreadMessagesCount }}
  groups-list(v-if='groupsByName.length >= 1' :inert='isInert')

  .c-navigation-wrapper(:inert='isInert')
    template(v-if='isInGlobalDashboard')
      .c-navigation-header
        i18n.sr-only(tag='h1') Global Dashboard Menu

        img.c-logo(:src='logo' alt='GroupIncome\\'s logo')

      .c-navigation-body
        .c-navigation-body-top
          ul.c-menu-list
            list-item(v-for='entry in config.globalDashboard'
              tag='router-link'
              :key='entry.id'
              :icon='entry.icon'
              :to='entry.routeTo'
              :badgeCount='entry.id === "news-and-updates" && hasNewNews ? 1 : 0'
              :data-test='"global-dashboard_" + entry.id'
            ) {{ entry.title }}

    template(v-else)
      .c-navigation-header
        i18n.sr-only(tag='h1') Main Menu

        router-link(to='/home')
          img.c-logo(:src='logo' alt='GroupIncome\\'s logo')

        notification-bell(v-if='!notApprovedToGroupYet' data-test='notificationBell')

      .c-navigation-body(
        @click.stop='onMenuItemsClick'
        v-if='!notApprovedToGroupYet && groupsByName.length'
      )
        .c-navigation-body-top
          ul.c-menu-list
            list-item(tag='router-link' icon='columns' to='/dashboard' data-test='dashboard')
              i18n Dashboard
            list-item(tag='router-link' icon='chart-pie' to='/contributions' data-test='contributionsLink')
              i18n Contributions
            list-item(tag='router-link' icon='tag' to='/payments' data-test='paymentsLink')
              i18n Payments
            list-item(
              tag='router-link'
              icon='comments'
              to='/group-chat'
              :badgeCount='currentGroupUnreadMessagesCount'
              data-test='groupChatLink'
            )
              i18n Chat
            list-item(tag='router-link' icon='cog' to='/group-settings' data-test='groupSettingsLink')
              i18n Group Settings

          .c-navigation-separator(v-if='groupsByName.length < 2')
            button(
              class='is-small is-outlined'
              @click='openModal("GroupCreationModal")'
              data-test='createGroup'
              :aria-label='L("Add a group")'
            )
              i.icon-plus.is-prefix
              i18n Add a group

        .c-navigation-bottom
          ul.c-menu-list-bottom
            i18n(
              tag='a'
              :href='ALLOWED_URLS.BLOG_PAGE'
              target='_blank'
              rel='noopener noreferrer'
            ) Blog

            i18n(
              tag='a'
              :href='ALLOWED_URLS.COMMUNITY_PAGE'
              target='_blank'
              rel='noopener noreferrer'
            ) Help &amp; Feedback

            i18n(
              tag='a'
              :href='ALLOWED_URLS.DONATE_PAGE'
              target='_blank'
              rel='noopener noreferrer'
            ) Donate

    .c-navigation-footer(v-if='showNav')
      profile
</template>

<script>
import sbp from '@sbp/sbp'
import Badge from '../../../../frontend/views/components/Badge.vue'
import NotificationBell from '../../../../frontend/views/containers/notifications/NotificationBell.vue'
import GroupsList from './GroupsList.vue'
import Profile from './Profile.vue'
import Toggle from '../../../../frontend/views/components/Toggle.vue'
import ListItem from '../../../../frontend/views/components/ListItem.vue'
import { mapState, mapGetters } from 'vuex'
import { OPEN_MODAL } from '../../../../frontend/utils/events.js'
import { DESKTOP } from '../../../../frontend/views/utils/breakpoints.js'
import { showNavMixin, fetchNews } from '../../../../frontend/views/utils/misc.js'
import { GLOBAL_DASHBOARD_SETTINGS } from '../../../../frontend/views/pages/GlobalDashboard.vue'
import { debounce } from 'turtledash'

export default ({
  name: 'Navigation',
  mixins: [showNavMixin],
  components: {
    Badge,
    Toggle,
    NotificationBell,
    GroupsList,
    Profile,
    ListItem
  },
  data () {
    return {
      config: {
        debounceResize: debounce(this.checkIsTouch, 250),
        globalDashboard: Object.entries(GLOBAL_DASHBOARD_SETTINGS)
          .map(([key, entry]) => ({ ...entry, id: key }))
      },
      ephemeral: {
        isActive: false,
        isTouch: null,
        latestNewsDate: null
      }
    }
  },
  created () {
    this.checkIsTouch()
    this.checkForNewNews()
  },
  mounted () {
    // TODO - Create a single resize listener to be reused on components
    window.addEventListener('resize', this.config.debounceResize)
  },
  beforeDestroy () {
    window.removeEventListener('resize', this.config.debounceResize)
  },
  watch: {
    $route (to, from) {
      const isDifferentPage = from.path !== to.path
      if (this.ephemeral.isActive && isDifferentPage) {
        this.ephemeral.isActive = false
      }
    }
  },
  computed: {
    ...mapState(['currentGroupId', 'loggedIn']),
    ...mapGetters([
      'groupsByName',
      'colors',
      'totalUnreadNotificationCount',
      'groupUnreadMessages',
      'ourPreferences'
    ]),
    currentGroupUnreadMessagesCount () {
      return !this.currentGroupId ? 0 : this.groupUnreadMessages(this.currentGroupId)
    },
    logo () {
      const name = this.colors.theme === 'dark' ? '-white' : ''
      return \`/assets/images/logo-transparent\${name}.png\`
    },
    isInert () {
      return !this.ephemeral.isActive && this.ephemeral.isTouch
    },
    notApprovedToGroupYet () {
      // TODO: once the relevant work is implemented on back-end, this check logic needs to be updated accordingly
      return this.$route.path === '/pending-approval'
    },
    isInGlobalDashboard () {
      return this.$route.path.startsWith('/global-dashboard')
    },
    hasNewNews () {
      // Don't show badge if we're currently on the news page
      if (this.$route.path === '/global-dashboard/news-and-updates') {
        return false
      }

      // Check if there's a stored last seen date
      const lastSeenNewsDate = this.ourPreferences?.lastSeenNewsDate

      // If no last seen date is stored, don't show badge
      if (!lastSeenNewsDate || !this.ephemeral.latestNewsDate) {
        return false
      }

      // Compare the latest news date with the last seen date
      const lastSeen = new Date(lastSeenNewsDate)
      const latest = new Date(this.ephemeral.latestNewsDate)

      return latest > lastSeen
    }
  },
  methods: {
    toggleMenu () {
      this.ephemeral.isActive = !this.ephemeral.isActive
    },
    openModal (mode) {
      sbp('okTurtles.events/emit', OPEN_MODAL, mode)
    },
    checkIsTouch () {
      this.ephemeral.isTouch = window.innerWidth < DESKTOP
    },
    onMenuItemsClick () {
      // Close the menu when a menu item is clicked.
      this.ephemeral.isActive = false
    },
    async checkForNewNews () {
      try {
        const data = await fetchNews()
        if (data.length > 0) {
          this.ephemeral.latestNewsDate = data[0].createdAt
        }
      } catch (error) {
        console.error('Failed to check for new news:', error)
      }
    }
  }
}: Object)
<\/script>

<style lang="scss" scoped>
@import "../../../../frontend/assets/style/_variables.scss";

.c-navigation {
  display: flex;
  flex-direction: row;
  font-weight: normal;
  background: $general_2;
}

.c-navigation-wrapper {
  display: flex;
  flex-direction: column;
  min-width: 14.375rem;
  justify-content: space-between;
  height: 100%;
}

.c-navigation-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-shrink: 0;
  padding: 0 1rem;
  height: 4.6rem;
  margin-bottom: -0.1rem;
}

.c-navigation-body {
  display: flex;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
  flex-direction: column;
  justify-content: space-between;
  flex-grow: 1;
}

.c-navigation-separator {
  text-align: center;
  margin: 1.5rem 1.5rem 0 1.5rem;
  border-top: 1px solid $general_0;
  padding-top: 1.5rem;
}

.c-navigation-bottom {
  padding-top: 2rem;
  padding-bottom: 0.5rem;
}

.c-navigation-footer {
  flex-shrink: 0;
}

.c-menu-list-bottom {
  display: flex;
  flex-direction: column;
  margin-left: 1rem;
  font-size: $size_5;

  a {
    line-height: 1.65rem;
    color: $text_1;

    &:hover,
    &:focus {
      color: $text_0;
      outline: none;
    }
  }
}

.c-logo {
  min-width: 8rem;
  width: 8rem;
}

.c-toggle {
  left: 100%;
}
</style>
`;
  if (!component.render) {
    component.render = template.render;
    component.staticRenderFns = template.staticRenderFns;
    component._compiled = true;
    if (functional) component.functional = true;
  }
  component._scopeId = scope;
  if (true) {
    let hook;
    if (false) {
      hook = function(context) {
        context = context || // cached call
        this.$vnode && this.$vnode.ssrContext || // stateful
        this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext;
        if (!context && typeof __VUE_SSR_CONTEXT__ !== "undefined") {
          context = __VUE_SSR_CONTEXT__;
        }
        if (style) {
          style.call(this, createInjectorSSR(context));
        }
        if (context && context._registeredComponents) {
          context._registeredComponents.add(moduleIdentifier);
        }
      };
      component._ssrRegister = hook;
    } else if (style) {
      hook = shadowMode ? function(context) {
        style.call(this, createInjectorShadow(context, this.$root.$options.shadowRoot));
      } : function(context) {
        style.call(this, createInjector(context));
      };
    }
    if (hook !== void 0) {
      if (component.functional) {
        const originalRender = component.render;
        component.render = function renderWithStyleInjection(h, context) {
          hook.call(context);
          return originalRender(h, context);
        };
      } else {
        const existing = component.beforeCreate;
        component.beforeCreate = existing ? [].concat(existing, hook) : [hook];
      }
    }
  }
  return component;
}
function __vue_create_injector__15() {
  const styles = __vue_create_injector__15.styles || (__vue_create_injector__15.styles = {});
  const isOldIE = typeof navigator !== "undefined" && /msie [6-9]\\b/.test(navigator.userAgent.toLowerCase());
  return function addStyle(id, css) {
    if (document.querySelector('style[data-vue-ssr-id~="' + id + '"]')) return;
    const group = isOldIE ? css.media || "default" : id;
    const style = styles[group] || (styles[group] = { ids: [], parts: [], element: void 0 });
    if (!style.ids.includes(id)) {
      let code = css.source;
      let index = style.ids.length;
      style.ids.push(id);
      if (false) {
        code += "\n/*# sourceURL=" + css.map.sources[0] + " */";
        code += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(css.map)))) + " */";
      }
      if (isOldIE) {
        style.element = style.element || document.querySelector("style[data-group=" + group + "]");
      }
      if (!style.element) {
        const head = document.head || document.getElementsByTagName("head")[0];
        const el = style.element = document.createElement("style");
        el.type = "text/css";
        if (css.media) el.setAttribute("media", css.media);
        if (isOldIE) {
          el.setAttribute("data-group", group);
          el.setAttribute("data-next-index", "0");
        }
        head.appendChild(el);
      }
      if (isOldIE) {
        index = parseInt(style.element.getAttribute("data-next-index"));
        style.element.setAttribute("data-next-index", index + 1);
      }
      if (style.element.styleSheet) {
        style.parts.push(code);
        style.element.styleSheet.cssText = style.parts.filter(Boolean).join("\n");
      } else {
        const textNode = document.createTextNode(code);
        const nodes = style.element.childNodes;
        if (nodes[index]) style.element.removeChild(nodes[index]);
        if (nodes.length) style.element.insertBefore(textNode, nodes[index]);
        else style.element.appendChild(textNode);
      }
    }
  };
}
var __vue_component__17 = /* @__PURE__ */ __vue_normalize__17(
  { render: __vue_render__17, staticRenderFns: __vue_staticRenderFns__17 },
  __vue_inject_styles__17,
  __vue_script__17,
  __vue_scope_id__17,
  __vue_is_functional_template__17,
  __vue_module_identifier__17,
  false,
  __vue_create_injector__15,
  void 0,
  void 0
);
var Navigation_default = __vue_component__17;

// frontend/views/utils/avatar.js
var import_blockies = __toESM(require_blockies());
var colorOptions = ["primary", "warning", "danger", "success"];
var theme = colors_default.light;
esm_default("sbp/selectors/register", {
  "gi.utils/avatar/create": function() {
    let avatarBase64, avatarBlob;
    try {
      const palette = randomFromArray(colorOptions);
      avatarBase64 = import_blockies.default.create({
        bgcolor: theme[`${palette}_0`],
        // darkest
        color: theme[`${palette}_1`],
        // medium
        spotcolor: theme[`${palette}_2`],
        // lightest
        size: 6,
        scale: 12
      }).toDataURL("image/png");
      avatarBlob = imageDataURItoBlob(avatarBase64);
    } catch (e) {
      console.warn("utils/avatar.js Avatar generation process didn't go well.", e);
      return null;
    }
    return avatarBlob;
  }
});

// frontend/views/utils/i18n.js
var import_dompurify2 = __toESM(require_purify());
init_vue_esm();

// frontend/views/utils/vSafeHtml.js
var import_dompurify = __toESM(require_purify());
init_vue_esm();
var defaultConfig = {
  ALLOWED_ATTR: ["class"],
  ALLOWED_TAGS: ["b", "br", "em", "i", "p", "small", "span", "strong", "sub", "sup", "u", "s", "code", "ul", "li", "pre", "blockquote", "del"],
  // This option was in the original file.
  RETURN_DOM_FRAGMENT: true
};
var transform = (el, binding) => {
  if (binding.oldValue !== binding.value) {
    let config2 = defaultConfig;
    const tagAllowedAttrs = {
      "a": ["href", "target"],
      "button": ["type"]
    };
    if (Object.keys(tagAllowedAttrs).includes(binding.arg)) {
      config2 = cloneDeep(config2);
      config2.ALLOWED_TAGS.push(binding.arg);
      config2.ALLOWED_ATTR.push(...tagAllowedAttrs[binding.arg] || []);
    }
    el.textContent = "";
    el.appendChild(import_dompurify.default.sanitize(binding.value, config2));
  }
};
vue_esm_default.directive("safe-html", {
  bind: transform,
  update: transform
});

// frontend/views/utils/i18n.js
vue_esm_default.prototype.L = L;
vue_esm_default.prototype.LTags = LTags;
var dompurifyConfig = {
  ...defaultConfig,
  ALLOWED_ATTR: ["class", "href", "rel", "target"],
  ALLOWED_TAGS: ["a", "b", "br", "button", "em", "i", "p", "small", "span", "strong", "sub", "sup", "u"],
  RETURN_DOM_FRAGMENT: false
};
function sanitize(inputString) {
  return import_dompurify2.default.sanitize(inputString, dompurifyConfig);
}
vue_esm_default.component("i18n", {
  functional: true,
  props: {
    args: [Object, Array],
    tag: {
      type: String,
      default: "span"
    },
    compile: Boolean
  },
  render: function(h, context) {
    const text = context.children[0].text;
    const translation = L(text, context.props.args || {});
    if (!translation) {
      console.warn("The following i18n text was not translated correctly:", text);
      return h(context.props.tag, context.data, text);
    }
    if (context.props.tag === "a" && context.data.attrs.target === "_blank") {
      context.data.attrs.rel = "noopener noreferrer";
    }
    if (context.props.compile) {
      const result = vue_esm_default.compile("<wrap>" + sanitize(translation) + "</wrap>");
      return result.render.call({
        _c: (tag, ...args) => {
          if (tag === "wrap") {
            return h(context.props.tag, context.data, ...args);
          } else {
            return h(tag, ...args);
          }
        },
        _v: (x) => x
      });
    } else {
      if (!context.data.domProps) context.data.domProps = {};
      context.data.domProps.innerHTML = sanitize(translation);
      return h(context.props.tag, context.data);
    }
  }
});

// frontend/views/utils/vError.js
init_vue_esm();
vue_esm_default.directive("error", {
  inserted(el, binding, vnode) {
    if (!binding.arg) {
      throw new Error(`v-error: missing argument on ${el.outerHTML}`);
    }
    if (!vnode.context.$v.form[binding.arg]) {
      throw new Error(`v-error: vuelidate doesn't have validation for ${binding.arg}`);
    }
    const opts = binding.value || {};
    const pErr = document.createElement(opts.tag || "span");
    for (const attr in opts.attrs) {
      pErr.setAttribute(attr, opts.attrs[attr]);
    }
    pErr.classList.add("error", "is-hidden");
    el.insertAdjacentElement("afterend", pErr);
  },
  update(el, binding, vnode) {
    if (vnode.context.$v.form[binding.arg].$error) {
      for (const key in vnode.context.$v.form[binding.arg].$params) {
        if (!vnode.context.$v.form[binding.arg][key]) {
          el.nextElementSibling.innerText = key;
          break;
        }
      }
      el.nextElementSibling.classList.remove("is-hidden");
    } else {
      el.nextElementSibling.classList.add("is-hidden");
    }
  },
  unbind(el) {
    el.nextElementSibling.remove();
  }
});

// frontend/views/utils/vFocus.js
init_vue_esm();
vue_esm_default.directive("focus", {
  // When the bound element is inserted into the DOM...
  inserted: (el, args) => {
    if (!args || args.value !== false) el.focus();
  }
});

// node_modules/@capacitor/dialog/dist/esm/index.js
var Dialog = registerPlugin("Dialog", {
  web: () => import("./web-7F4COR6A-cached.js").then((m) => new m.DialogWeb())
});

// frontend/views/utils/capacitor/utils.js
var utils_default = esm_default("sbp/selectors/register", {
  "gi.capacitor/dialog/alert": async (params) => {
    await Dialog.alert({
      title: params.title,
      message: params.message
    });
  }
});

// frontend/model/featureCheck.js
var requiredFeatures = {
  fetch: typeof fetch === "function" && typeof Request === "function" && typeof Response === "function",
  streams: typeof ReadableStream === "function",
  webSocket: typeof WebSocket === "function",
  reflect: typeof Reflect === "object",
  serviceWorker: typeof Navigator === "function" && typeof navigator === "object" && navigator instanceof Navigator && typeof navigator === "object" && typeof ServiceWorkerContainer === "function" && navigator.serviceWorker instanceof ServiceWorkerContainer,
  // $FlowFixMe[cannot-resolve-name]
  indexedDB: typeof IDBFactory === "function" && typeof indexedDB === "object" && indexedDB instanceof IDBFactory,
  sessionStorage: typeof Storage === "function" && typeof sessionStorage === "object" && sessionStorage instanceof Storage
};
console.info("Feature check", requiredFeatures);
var hasAllRequiredFeatures = Object.values(requiredFeatures).every(Boolean);
var featureCheck_default = hasAllRequiredFeatures;

// frontend/main.js
init_vue_esm();

// frontend/model/notifications/periodicNotifications.js
var shortestDelay;
var PERIODIC_NOTIFICATION_TYPE = {
  MIN1: "1MIN",
  MIN5: "5MIN",
  MIN15: "15MIN",
  MIN30: "30MIN"
};
var ephemeralNotificationState = {
  notifications: [],
  partition: /* @__PURE__ */ Object.create(null)
};
var typeToDelayMap = {
  [PERIODIC_NOTIFICATION_TYPE.MIN1]: 1 * MINS_MILLIS,
  [PERIODIC_NOTIFICATION_TYPE.MIN5]: 5 * MINS_MILLIS,
  [PERIODIC_NOTIFICATION_TYPE.MIN15]: 15 * MINS_MILLIS,
  [PERIODIC_NOTIFICATION_TYPE.MIN30]: 30 * MINS_MILLIS
};
var validateNotificationData = objectOf({
  stateKey: (value) => {
    value = string(value);
    if (value.length === 0) {
      throw new TypeError("Empty notification data state key");
    }
    return value;
  },
  emitCondition: isFunction,
  emit: isFunction,
  shouldClearStateKey: isFunction
});
async function runNotificationListRecursive() {
  const rootState = esm_default("state/vuex/state");
  const rootGetters = esm_default("state/vuex/getters");
  const firedMap = rootState.periodicNotificationAlreadyFiredMap.alreadyFired;
  const lastRunMap = rootState.periodicNotificationAlreadyFiredMap.lastRun;
  const callWithStates = (func, stateKey) => func.call(ephemeralNotificationState.partition[stateKey], { rootState, rootGetters });
  if (ephemeralNotificationState.clearTimeout) return;
  let aborted = false;
  const abort = () => {
    aborted = true;
  };
  ephemeralNotificationState.clearTimeout = abort;
  await Promise.all(
    Object.entries(esm_default("okTurtles.eventQueue/queuedInvocations")).map(([queue, invocations]) => {
      return !!invocations.length && esm_default("okTurtles.eventQueue/queueEvent", queue, () => {
      }).catch(() => {
      });
    })
  );
  for (const entry of ephemeralNotificationState.notifications) {
    if (aborted) break;
    const stateKey = entry.stateKey;
    const lastRun = lastRunMap[stateKey] || 0;
    const delay = entry.delay;
    if (Date.now() - lastRun >= delay) {
      try {
        if (!firedMap[stateKey] && callWithStates(entry.emitCondition, stateKey)) {
          await callWithStates(entry.emit, stateKey);
          firedMap[stateKey] = true;
        }
        if (firedMap[stateKey] && callWithStates(entry.shouldClearStateKey, stateKey)) {
          delete firedMap[stateKey];
        }
      } catch (e) {
        console.error("runNotificationListRecursive: Error calling notification", stateKey, e);
      }
      lastRunMap[stateKey] = Date.now();
    }
  }
  if (ephemeralNotificationState.clearTimeout !== abort) return;
  ephemeralNotificationState.clearTimeout = (() => {
    const timeoutId = setTimeout(
      () => {
        delete ephemeralNotificationState.clearTimeout;
        runNotificationListRecursive();
      },
      shortestDelay
    );
    return () => clearTimeout(timeoutId);
  })();
}
function clearTimeoutObject() {
  if (ephemeralNotificationState.clearTimeout) {
    ephemeralNotificationState.clearTimeout();
    delete ephemeralNotificationState.clearTimeout;
  }
  ephemeralNotificationState.notifications.forEach(({ stateKey }) => {
    ephemeralNotificationState.partition[stateKey] = /* @__PURE__ */ Object.create(null);
  });
}
esm_default("sbp/selectors/register", {
  "gi.periodicNotifications/init": function() {
    return runNotificationListRecursive().catch((e) => {
      console.error("[gi.periodicNotifications/init] Error", e);
    });
  },
  "gi.periodicNotifications/clearStatesAndStopTimers": function() {
    const rootState = esm_default("state/vuex/state");
    rootState.periodicNotificationAlreadyFiredMap = {
      alreadyFired: /* @__PURE__ */ Object.create(null),
      // { notificationKey: boolean },
      lastRun: /* @__PURE__ */ Object.create(null)
      // { notificationKey: number },
    };
    clearTimeoutObject();
  },
  "gi.periodicNotifications/importNotifications": function(entries) {
    const keySet = /* @__PURE__ */ new Set();
    for (const { type, notificationData } of entries) {
      if (!type || !notificationData) throw new Error("A required field in a periodic notification entry is missing.");
      const delay = typeToDelayMap[type];
      if (!delay) {
        throw new RangeError("Invalid delay");
      }
      if (!(shortestDelay <= delay)) shortestDelay = delay;
      validateNotificationData(notificationData);
      if (keySet.has(notificationData.stateKey)) {
        throw new Error("Duplicate periodic notification state key: " + notificationData.stateKey);
      }
      keySet.add(notificationData.stateKey);
      ephemeralNotificationState.partition[notificationData.stateKey] = /* @__PURE__ */ Object.create(null);
      ephemeralNotificationState.notifications.push({
        ...notificationData,
        delay
      });
    }
  }
});

// frontend/model/notifications/mainNotificationsMixin.js
var myNotificationHas = (checkFunc, groupId = "") => {
  const myNotifications = groupId ? esm_default("state/vuex/getters").notificationsByGroup(groupId) : esm_default("state/vuex/getters").currentNotifications;
  return myNotifications.some((item) => checkFunc(item));
};
var oneTimeNotificationEntries = [
  {
    name: "INCOME_DETAILS_OLD",
    emitCondition({ rootGetters }) {
      const { incomeDetailsLastUpdatedDate } = rootGetters.ourGroupProfile || {};
      const now = dateToPeriodStamp(/* @__PURE__ */ new Date());
      return incomeDetailsLastUpdatedDate && compareISOTimestamps(now, incomeDetailsLastUpdatedDate) > 6 * MONTHS_MILLIS && !myNotificationHas((item) => item.type === "INCOME_DETAILS_OLD" && item.data.lastUpdatedDate === incomeDetailsLastUpdatedDate);
    },
    emit({ rootGetters }) {
      esm_default("gi.notifications/emit", "INCOME_DETAILS_OLD", {
        months: 6,
        lastUpdatedDate: rootGetters.ourGroupProfile.incomeDetailsLastUpdatedDate
      });
    }
  }
];
var periodicNotificationEntries = [
  // The following fixes a rare issue that we're not sure exactly why it happens.
  // Sometimes, the `namespace/lookup` call made as a side-effect in the identity
  // contract seems to fail. The result of this is that the corresponding cached
  // namespace lookup entry isn't populated and the username is missing from the
  // UI. To fix this, we check for users that are missing a username and
  // do this lookup manually.
  // See: <https://github.com/okTurtles/group-income/pull/2306#pullrequestreview-2305605028>
  {
    type: PERIODIC_NOTIFICATION_TYPE.MIN30,
    notificationData: {
      stateKey: "username-fetch",
      emitCondition: () => true,
      emit({ rootState, rootGetters }) {
        Object.values(rootGetters.ourContactProfilesById).filter(
          // $FlowFixMe[incompatible-use]
          ({ username, contractID }) => !username && !!rootState[contractID]?.attributes?.username
        ).forEach(({ contractID }) => {
          const username = rootState[contractID].attributes.username;
          esm_default("namespace/lookup", username, { skipCache: true }).then((cID) => {
            if (cID !== contractID) {
              console.error(`[periodic notification] Mismatched username. The lookup result was ${cID} instead of ${contractID}`);
            }
          }).catch((e) => {
            console.error("[periodic notification] Error looking up username", username, "for", contractID, e);
          });
        });
      },
      shouldClearStateKey: () => true
    }
  }
];
var notificationMixin = {
  methods: {
    initOrResetPeriodicNotifications() {
      esm_default("gi.periodicNotifications/clearStatesAndStopTimers");
      esm_default("gi.periodicNotifications/init");
    },
    async checkAndEmitOneTimeNotifications() {
      const arg = { rootState: esm_default("state/vuex/state"), rootGetters: esm_default("state/vuex/getters") };
      for (const entry of oneTimeNotificationEntries) {
        if (entry.emitCondition(arg)) {
          await entry.emit(arg);
        }
      }
    }
  },
  mounted() {
    esm_default("gi.periodicNotifications/importNotifications", periodicNotificationEntries);
  }
};
var mainNotificationsMixin_default = notificationMixin;

// frontend/utils/faviconBadge.js
var currentFavicon = null;
var originalFavicon = null;
var faviconImage;
var canvas = null;
var options = {};
var r = Math.ceil(window.devicePixelRatio) || 1;
var size = 16 * r;
var defaults = {
  width: 8,
  height: 8,
  background: "#ff0000",
  crossOrigin: true
};
var getFaviconTag = () => {
  const links = document.getElementsByTagName("link");
  for (let i = 0, len = links.length; i < len; i++) {
    if ((links[i].getAttribute("rel") || "").match(/\bicon\b/i)) {
      return links[i];
    }
  }
  return null;
};
var removeFaviconTag = () => {
  const links = document.getElementsByTagName("link");
  for (let i = 0, len = links.length; i < len; i++) {
    const exists = typeof links[i] !== "undefined";
    if (exists && (links[i].getAttribute("rel") || "").match(/\bicon\b/i)) {
      links[i].parentNode?.removeChild(links[i]);
    }
  }
};
var getCurrentFavicon = () => {
  if (!originalFavicon || !currentFavicon) {
    const tag = getFaviconTag();
    currentFavicon = tag?.getAttribute("href") || "/assets/images/group-income-icon-transparent.png";
    if (!originalFavicon) {
      originalFavicon = currentFavicon;
    }
  }
  return currentFavicon;
};
var getCanvas = () => {
  if (!canvas) {
    canvas = document.createElement("canvas");
    canvas.width = size;
    canvas.height = size;
  }
  return canvas;
};
var setFaviconTag = (url) => {
  if (url) {
    removeFaviconTag();
    const link = document.createElement("link");
    link.type = "image/x-icon";
    link.rel = "icon";
    link.href = url;
    document.getElementsByTagName("head")[0].appendChild(link);
  }
};
var drawFavicon = (bubble) => {
  const context = getCanvas().getContext("2d");
  const src = getCurrentFavicon();
  faviconImage = document.createElement("img");
  faviconImage.onload = function() {
    context.clearRect(0, 0, size, size);
    context.drawImage(faviconImage, 0, 0, faviconImage?.width, faviconImage?.height, 0, 0, size, size);
    if (bubble) drawBubble(context);
    refreshFavicon();
  };
  if (!src?.match(/^data/) && options.crossOrigin) {
    faviconImage.crossOrigin = "anonymous";
  }
  faviconImage.src = src;
};
var drawBubble = (context) => {
  const width = options.width * r + 6 * r;
  const height = options.height * r;
  const bottom = 16 * r;
  const right = 16 * r;
  const arcRadius = Math.min(width, height) / 2;
  context.fillStyle = options.background;
  context.strokeStyle = options.background;
  context.lineWidth = r;
  context.arc(right - arcRadius, bottom - arcRadius, arcRadius, 0, 2 * Math.PI);
  context.fill();
};
var refreshFavicon = () => {
  if (getCanvas() instanceof HTMLCanvasElement) {
    setFaviconTag(getCanvas().toDataURL());
  }
};
var FaviconBadge = {
  setOptions: function(custom) {
    options = {};
    for (const key in defaults) {
      options[key] = custom[key] || defaults[key];
    }
  },
  setImage: function(url) {
    currentFavicon = url;
    refreshFavicon();
  },
  setBubble: function(bubble) {
    drawFavicon(bubble);
  },
  reset: function() {
    currentFavicon = originalFavicon;
    setFaviconTag(originalFavicon);
  }
};
if (!Object.keys(options).length) {
  FaviconBadge.setOptions(defaults);
}
var faviconBadge_default = FaviconBadge;

// frontend/utils/init-vue-plugins.js
init_vue_esm();
if ("ontouchstart" in window || "msMaxTouchPoints" in navigator) {
  import("./vue2-touch-events-HO5ZLWF2-cached.js").then((Vue2TouchEvents) => vue_esm_default.use(Vue2TouchEvents.default));
} else vue_esm_default.directive("touch", {});

// frontend/views/utils/vStyle.js
init_vue_esm();
vue_esm_default.component("v-style", {
  render: function(createElement) {
    return createElement("style", this.$slots.default);
  }
});

// frontend/main.js
console.info("GI_VERSION:", "2.5.1@2026-01-22T10:19:51.672Z");
console.info("GI_GIT_VERSION:", "v2.5.1-27-g8b878ca6-dirty");
console.info("CONTRACTS_VERSION:", "2.5.0");
console.info("LIGHTWEIGHT_CLIENT:", "true");
console.info("NODE_ENV:", "development");
esm_default("okTurtles.data/set", "API_URL", self.location.origin);
if ("") {
  const originalFetch = self.fetch;
  self.fetch = (...args) => {
    return originalFetch.apply(self, args).catch((e) => {
      console.error("FETCH FAILED", args, new Error().stack, e);
      throw e;
    });
  };
}
vue_esm_default.config.errorHandler = function(err, vm, info) {
  console.error(`uncaught Vue error in ${info}:`, err);
  if ("") throw err;
};
async function startApp() {
  const debugParam = new URLSearchParams(window.location.search).get("debug");
  if (true) {
    const reducer = (o, v) => {
      o[v] = true;
      return o;
    };
    const domainBlacklist = [
      "sbp",
      "okTurtles.data"
    ].reduce(reducer, {});
    const selectorBlacklist = [
      "chelonia.db/get",
      "chelonia.db/set",
      "chelonia/rootState",
      "chelonia/haveSecretKey",
      "chelonia/private/enqueuePostSyncOps",
      "chelonia/private/invoke",
      "state/vuex/state",
      "state/vuex/getters",
      "state/vuex/settings",
      "gi.db/settings/save"
    ].reduce(reducer, {});
    esm_default("sbp/filters/global/add", (domain, selector, data) => {
      if (domainBlacklist[domain] || selectorBlacklist[selector]) return;
      console.debug(`[sbp] ${selector}`, data);
    });
    esm_default("sbp/filters/selector/add", "gi.db/settings/save", (domain, selector, data) => {
      console.debug("[sbp] 'gi.db/settings/save'", data[0]);
    });
  }
  esm_default("chelonia/externalStateSetup", { stateSelector: "state/vuex/state", reactiveSet: vue_esm_default.set, reactiveDel: vue_esm_default.delete });
  esm_default("okTurtles.events/on", NAMESPACE_REGISTRATION, ({ name, value, deletedValue }) => {
    const cache = esm_default("state/vuex/state").namespaceLookups;
    const reverseCache = esm_default("state/vuex/state").reverseNamespaceLookups;
    if (deletedValue) {
      vue_esm_default.delete(cache, name);
      if (reverseCache[deletedValue] === name) {
        vue_esm_default.delete(reverseCache, deletedValue);
      }
    } else {
      vue_esm_default.set(cache, name, value);
      vue_esm_default.set(reverseCache, value, name);
    }
  });
  esm_default("okTurtles.events/on", SERIOUS_ERROR, (error, { contractID }) => {
    console.error("Serious error", contractID, error);
    esm_default("gi.ui/seriousErrorBanner", error);
    if (error?.name === "ChelErrorForkedChain") {
      const rootState = esm_default("state/vuex/state");
      if (!rootState.contracts[contractID]) {
        console.error("Forked chain detected. However, there is no contract entry.", { contractID }, error);
        return;
      }
      const type = rootState.contracts[contractID].type || "(unknown)";
      console.error("Forked chain detected", { contractID, type }, error);
      const retry = confirm(L("The server's history for '{type}' has diverged from ours. This can happen in extremely rare circumstances due to either malicious activity or a bug.\n\nTo fix this, the contract needs to be resynced, and some recent events may be missing. Would you like to do so now?\n\n(If problems persist, please open the Troubleshooting page under the User Settings and resync all contracts.)", { type }));
      if (retry) {
        esm_default("gi.ui/clearBanner");
        (rootState.loggedIn?.identityContractID === contractID ? esm_default("gi.actions/identity/logout", null, true) : esm_default("chelonia/contract/sync", contractID, { resync: true })).catch((e) => {
          console.error("Error during re-sync", contractID, e);
          alert(L("There was a problem resyncing the contract: {errMsg}\n\nPlease see the Application Logs under User Settings for more details. The Troubleshooting page in User Settings may be another way to fix the problem.", { errMsg: e?.message || e }));
        });
      }
    }
    if ("") {
      Promise.reject(error);
    }
  });
  if (true) {
    window.sbp = esm_default;
  }
  const initialSyncs = { ephemeral: { debouncedSyncBanner() {
  }, syncs: [] } };
  const syncFn = function(contractID, isSyncing) {
    if (isSyncing) {
      this.ephemeral.syncs.push(contractID);
      this.ephemeral.debouncedSyncBanner();
    } else if (this.ephemeral.syncs.includes(contractID)) {
      this.ephemeral.syncs = this.ephemeral.syncs.filter((id) => id !== contractID);
    }
  };
  const initialSyncFn = syncFn.bind(initialSyncs);
  try {
    await esm_default("translations/init", navigator.language);
  } catch (e) {
    const errMsg = `Fatal error while initializing Group Income: ${e.name} - ${e.message}

Please report this bug here: ${allowedUrls_default.ISSUE_PAGE}`;
    console.error(errMsg, e);
    alert(errMsg);
    return;
  }
  featureCheck_default && await Promise.race(
    [
      esm_default("service-worker/setup"),
      new Promise((resolve, reject) => {
        setTimeout(() => {
          reject(new Error("Timed out setting up service worker"));
        }, 8e3);
      })
    ]
  ).catch((e) => {
    console.error("[main] Error setting up service worker", e);
    alert(L("Error while setting up service worker: {err}", { err: e.message }));
    window.location.reload();
    throw e;
  });
  esm_default("gi.db/filesCache/temporary/clear");
  new vue_esm_default({
    router: router_default,
    mixins: [mainNotificationsMixin_default, showNavMixin],
    components: {
      AppStyles: AppStyles_default,
      BackgroundSounds: Background_default,
      BannerGeneral: BannerGeneral_default,
      Navigation: Navigation_default,
      Modal: Modal_default
    },
    data() {
      return {
        ephemeral: {
          syncs: [],
          // TODO/REVIEW page can load with already loggedin. -> this.$store.state.loggedIn ? 'yes' : 'no'
          finishedLogin: "no",
          debouncedSyncBanner: null,
          isCorrupted: false,
          // TODO #761
          ready: false
        }
      };
    },
    created() {
      esm_default("okTurtles.events/on", CONTRACT_SYNCS_RESET, (currentSyncs) => {
        this.ephemeral.syncs = currentSyncs;
      });
    },
    mounted() {
      let oldIdentityContractID = null;
      const reducedMotionQuery = window.matchMedia("(prefers-reduced-motion: reduce)") || {};
      if (reducedMotionQuery.matches || this.isInCypress) {
        this.setReducedMotion(true);
      }
      const { bannerGeneral } = this.$refs;
      esm_default("okTurtles.data/set", "BANNER", bannerGeneral);
      if (!featureCheck_default) {
        this.removeLoadingAnimation();
        esm_default("gi.ui/prompt", {
          heading: L("Unsupported browser"),
          question: L("This browser doesn't support all features required to use Group Income. Please try a different browser.")
        });
        return;
      }
      this.ephemeral.debouncedSyncBanner = bannerGeneral.debouncedShow({
        // we can't actually show in the global banner what contract is syncing because doing
        // so would involve having to repeatedly call the message() function, and if there
        // were other danger banners that needed to take precedence they would get covered
        message: () => L("Loading events from server..."),
        icon: "wifi",
        seconds: 2,
        clearWhen: () => !this.ephemeral.syncs.length
      });
      this.ephemeral.syncs = initialSyncs.ephemeral.syncs;
      if (this.ephemeral.syncs.length) {
        this.ephemeral.debouncedSyncBanner();
      }
      esm_default("okTurtles.events/off", CONTRACT_IS_SYNCING, initialSyncFn);
      esm_default("okTurtles.events/on", CONTRACT_IS_SYNCING, syncFn.bind(this));
      esm_default("okTurtles.events/on", LOGIN_COMPLETE, () => {
        setupNativeNotificationsListeners();
        const state = esm_default("state/vuex/state");
        if (!state.loggedIn) {
          console.warn("Received LOGIN_COMPLETE event but there state.loggedIn is not an object");
          return;
        }
        this.ephemeral.finishedLogin = "yes";
        if (this.$store.state.currentGroupId) {
          this.initOrResetPeriodicNotifications();
          this.checkAndEmitOneTimeNotifications();
        }
        vue_esm_default.use(import_idle_vue.default, { store: state_default, idleTime: 2 * 60 * 1e3 });
      });
      esm_default("okTurtles.events/on", LOGOUT, () => {
        const state = esm_default("state/vuex/state");
        if (!state.loggedIn) return;
        this.ephemeral.finishedLogin = "no";
        esm_default("gi.periodicNotifications/clearStatesAndStopTimers");
        esm_default("gi.db/settings/delete", state.loggedIn.identityContractID).catch((e) => {
          console.error("Logout event: error deleting settings");
        });
        esm_default("state/vuex/reset");
        router_default.currentRoute.path !== "/" && router_default.push({ path: "/" }).catch(console.error);
      });
      esm_default("okTurtles.events/once", LOGIN_ERROR, () => {
        this.removeLoadingAnimation();
      });
      esm_default("okTurtles.events/on", SWITCH_GROUP, ({ contractID, isNewlyCreated }) => {
        this.initOrResetPeriodicNotifications();
        this.checkAndEmitOneTimeNotifications();
      });
      esm_default("okTurtles.events/on", ONLINE, () => {
        const state = esm_default("state/vuex/state");
        if (state.loggedIn) {
          esm_default("service-worker/setup-push-subscription").catch((e) => {
            console.error("came back online, tried to report push subscription, but got:", e);
          });
        }
        esm_default("gi.ui/clearBanner");
      });
      esm_default("okTurtles.events/on", OFFLINE, () => {
        esm_default("gi.ui/showBanner", L("Your device appears to be offline."), "wifi");
      });
      esm_default("okTurtles.events/on", RECONNECTING, () => {
        esm_default("gi.ui/showBanner", L("Trying to reconnect..."), "wifi");
      });
      esm_default("okTurtles.events/on", RECONNECTION_FAILED, () => {
        esm_default("gi.ui/showBanner", L("We could not connect to the server. Please refresh the page."), "wifi");
      });
      esm_default("okTurtles.events/on", KV_EVENT, ({ contractID, key, data }) => {
        switch (key) {
          case KV_KEYS.LAST_LOGGED_IN: {
            esm_default("state/vuex/commit", "setLastLoggedIn", [contractID, data]);
            break;
          }
          case KV_KEYS.UNREAD_MESSAGES:
            esm_default("state/vuex/commit", "setUnreadMessages", data);
            break;
          case KV_KEYS.PREFERENCES:
            esm_default("state/vuex/commit", "setPreferences", data);
            break;
          case KV_KEYS.NOTIFICATIONS:
            esm_default("state/vuex/commit", "setNotificationStatus", data);
            break;
        }
      });
      if (navigator.onLine === false) {
        esm_default("okTurtles.events/emit", OFFLINE);
      }
      if (this.ephemeral.isCorrupted) {
        esm_default(
          "gi.ui/dangerBanner",
          L("Your app seems to be corrupted. Please {a_}re-sync your app data.{_a}", {
            "a_": `<a class="link" href="${window.location.pathname}?modal=UserSettingsModal&tab=troubleshooting">`,
            "_a": "</a>"
          }),
          "times-circle"
        );
      }
      esm_default("okTurtles.events/emit", THEME_CHANGE, this.$store.state.settings.themeColor);
      this.setBadgeOnTab();
      (async () => {
        try {
          const identityContractID2 = await esm_default("gi.db/settings/load", SETTING_CURRENT_USER);
          oldIdentityContractID = identityContractID2;
          if (identityContractID2 && this.ephemeral.finishedLogin !== "yes") {
            const removeHandler = esm_default("okTurtles.events/once", OPEN_MODAL, () => {
              this.removeLoadingAnimation();
            });
            await esm_default("gi.app/identity/login", { identityContractID: identityContractID2 });
            removeHandler();
            await esm_default("chelonia/contract/wait", identityContractID2);
          }
          this.ephemeral.ready = true;
          this.removeLoadingAnimation();
        } catch (e) {
          this.removeLoadingAnimation();
          oldIdentityContractID && esm_default("appLogs/clearLogs", oldIdentityContractID).catch((e2) => {
            console.error("[main] Error clearing logs for old session", oldIdentityContractID, e2);
          });
          console.error(`[main] caught ${e?.name} while fetching settings or handling a login error: ${e?.message || e}`, e);
          await esm_default("gi.app/identity/logout");
          await esm_default("gi.ui/prompt", {
            heading: L("Failed to login"),
            question: L("Error details: {reportError}", LError(e)),
            primaryButton: L("Close")
          });
        }
      })();
    },
    computed: {
      ...mapGetters(["groupsByName", "ourUnreadMessages", "totalUnreadNotificationCount"]),
      ...mapState(["contracts"]),
      ourUnreadMessagesCount() {
        return Object.keys(this.ourUnreadMessages).map((cId) => this.ourUnreadMessages[cId].unreadMessages.length).reduce((a, b) => a + b, 0);
      },
      shouldSetBadge() {
        return this.ourUnreadMessagesCount + this.totalUnreadNotificationCount > 0;
      },
      appClasses() {
        return {
          "l-with-navigation": this.showNav,
          "l-no-navigation": !this.showNav,
          "js-reducedMotion": this.$store.state.settings.reducedMotion,
          "is-dark-theme": this.$store.getters.isDarkTheme
        };
      },
      isInCypress() {
        return !!window.Cypress;
      }
    },
    methods: {
      ...mapMutations([
        "setReducedMotion"
      ]),
      setBadgeOnTab() {
        faviconBadge_default.setBubble(this.shouldSetBadge);
      },
      removeLoadingAnimation() {
        const loadingScreenEl = document.querySelector("#main-loading-screen");
        loadingScreenEl && loadingScreenEl.remove();
      }
    },
    watch: {
      shouldSetBadge(to, from) {
        this.setBadgeOnTab();
      }
    },
    store: state_default
    // make this and all child components aware of the new store
  }).$mount("#app");
}
startApp();
/*!
 * FaviconBadge - A small library for manipulating the Favicon
 * Modified from Tinycon.js by Tom Moor
 * Original project: https://github.com/tommoor/tinycon
 * Copyright (c) 2023 Alex Jin
 * @license MIT Licensed
 */
/*! Bundled license information:

dompurify/dist/purify.js:
  (*! @license DOMPurify | (c) Cure53 and other contributors | Released under the Apache license 2.0 and Mozilla Public License 2.0 | github.com/cure53/DOMPurify/blob/2.2.2/LICENSE *)
*/
//# sourceMappingURL=main.js.map
